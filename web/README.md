# web/ — surfaces (Next.js)

The reader-facing surfaces (Home + the four Signals surfaces). Empty until **Phase 0** of
`docs/roadmap.md`: scaffold Next.js + TS, port `design-reference/signals/tokens.css`, and
build the shared component kit from `design-reference/**/atoms.jsx`. Treat
`design-reference/` as the look-and-behavior contract and `docs/design-system.md` /
`docs/surfaces.md` as the spec. Do **not** copy the Babel-in-the-browser prototype setup.
