# SYNTHETIC TEST DATA — fabricated content only. A deliberately SPARSE Case Spec: only
# `case` and `problem` are authored. Everything else must surface in Distillation.missing[]
# (disclosed, never fabricated). For tests only.
case: Replicator Queue Notes
problem: Galley crew keep replicator adjustment notes in personal padds nobody else reads.
