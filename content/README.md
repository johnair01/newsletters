# content/ — the Library

Git-backed Markdown/MDX for published surfaces. "Review = a pull request" lives here: a
draft surface opens as a PR against this directory, and merging publishes it (Phase 4).
Empty until then.
