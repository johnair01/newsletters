# Round 3 scope — Phase 3 (worked module-a) deep review

**Cycle:** context(this note) → research+execute(subagent) → validate(orchestrator gates)
→ ship(client-readable PR into integration).

**Subject:** Phase 3 in the totality of its history — config → modulesite builder →
rendered corpus → --corpus module gate.
**Deliverables:** 03-VERIFICATION.md (retroactive, provenance-cited, fresh re-runs),
03-VALIDATION.md (nyquist map, honest edges), 03-LEARNINGS.md (source-attributed) +
accepted-gap records for the absent 03-PATTERNS and Phase-3 UI-SPEC (recorded, never
retro-fabricated).
**Standing lenses:** delta-to-reality; semantic/ontological drift (incl. the abstraction
guard firing twice this phase — the plan's own default path, the CLI docstring); the
shared-ledger-path caveat; Draft-vacuity honesty.
