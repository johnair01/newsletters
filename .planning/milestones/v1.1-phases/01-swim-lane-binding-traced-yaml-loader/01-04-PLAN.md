---
phase: 01-swim-lane-binding-traced-yaml-loader
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/test_abstraction_guard.py
  - tests/test_ai_optional.py
autonomous: true
requirements: [LANE-03, LANE-04]
user_setup: []

must_haves:
  truths:
    - "The abstraction-guard test FAILS the suite if any fixture/org-specific name (lane, module, owner id) appears in src/newsletters/ (LANE-03)"
    - "A bare install (yaml blocked) imports newsletters + newsletters.swimlane with `import yaml` unreachable; the [config] extra lazy-loads (LANE-04)"
    - "_load_yaml() raises a teaching ImportError naming [config] when PyYAML is absent; safe_load is the only parse path"
    - "The [config] extra is declared and contains PyYAML only (not an AI package)"
  artifacts:
    - path: "tests/test_abstraction_guard.py"
      provides: "grep-style scan of src/newsletters/ failing on any config-specific token leak"
      contains: "src/newsletters"
    - path: "tests/test_ai_optional.py"
      provides: "the [config]/yaml lazy-boundary + swimlane-AI-free block (parallel to the [excel] block)"
      contains: "config"
  key_links:
    - from: "tests/test_ai_optional.py"
      to: "src/newsletters/_yaml_loader.py"
      via: "meta-path-finder block-then-subprocess idiom (yaml blocked)"
      pattern: "meta_path|_yaml_loader|yaml"
    - from: "tests/test_abstraction_guard.py"
      to: "src/newsletters/"
      via: "scans every .py, asserts no fixture-specific token present"
      pattern: "src/newsletters"
---

<objective>
Land the two enforcement tests that close LANE-03 and LANE-04: (1) the abstraction-guard test that
fails the suite the instant any fixture/org/config-specific name leaks into `src/newsletters/`
(ABSTRACT EVERYTHING — lane sets are config, proven generic), and (2) the `[config]`/yaml
lazy-boundary block in `tests/test_ai_optional.py` proving a bare `pip install .` runs the spine with
`import yaml` unreachable and the boundary lazy-loads with a teaching error.

Purpose: These are the invariants-in-code (not comments) that make the ABSTRACT-EVERYTHING principle
and the AI-optional/minimal-core packaging real. Because `tests/test_ai_optional.py` runs inside the
CI bare-install job already, extending it (rather than editing `ci.yml`) satisfies the milestone's
bare-install-yaml-unreachable gate without weakening any existing gate.

Output: `tests/test_abstraction_guard.py` (new) + the `[config]` block appended to
`tests/test_ai_optional.py`.

NON-GOALS: do NOT edit `ci.yml` (the bare-install job already executes `tests/test_ai_optional.py`;
extend the test, not the workflow). Do NOT touch `faithfulness.py`/`coverage.py`/`conftest.py` or any
existing gate test. Do NOT weaken the existing `[excel]`/`[pptx]`/AI blocks — only ADD a parallel
`[config]` block.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-swim-lane-binding-traced-yaml-loader/01-CONTEXT.md
@.planning/phases/01-swim-lane-binding-traced-yaml-loader/01-PATTERNS.md
@tests/test_ai_optional.py
@src/newsletters/_yaml_loader.py
@src/newsletters/swimlane.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Abstraction-guard test — no fixture/config name in src/newsletters/ (LANE-03)</name>
  <files>tests/test_abstraction_guard.py</files>
  <read_first>
    - tests/test_ai_optional.py lines 284-304 (the "read the module text, assert a forbidden pattern
      is absent" mechanic — same shape, different needle set)
    - .planning/phases/.../01-PATTERNS.md lines 245-250 (abstraction-guard precedents + intent)
    - .planning/research/PITFALLS.md Pitfall 8 (config specifics leak into src/ or test assertions)
    - tests/fixtures/swimlane/module-x.yml + module-trap.yml (the fabricated token set to forbid)
    - sample_team/*.yml (names that must also never appear hardcoded in src/)
  </read_first>
  <action>
    Create `tests/test_abstraction_guard.py`. Assemble a denylist of config-specific tokens: the
    fabricated ids used in the committed swimlane fixtures (`module-x`, `area-*`, `lane-a`/`lane-b`,
    `owner-1`, `eng-01`, `metric-1`, `toolset-1`, and the trap fixture's synthetic ids) PLUS the
    sample_team names (read them from `sample_team/*.yml`, or a curated set) — i.e. every lane/module/
    owner/metric-specific string that is CONFIG, not code. Write `test_no_config_specific_name_in_src`:
    walk every `*.py` under `src/newsletters/`, read its text, and assert NONE of the denylisted tokens
    appear (fail with a clear message naming the file + token on any hit). Keep the denylist defined IN
    the test (a module-level constant) so the guard is self-contained and cannot be silently regressed.
    Guard against self-invalidation: match tokens as words/quoted-literals, not as substrings of
    unrelated identifiers, and do NOT let the test's own denylist literal be scanned (scan `src/`, not
    `tests/`). This test must FAIL the suite if a future edit hardcodes a fixture name in source — that
    failing behavior is the deliverable (author it so a planted leak would go red).
  </action>
  <verify>
    <automated>python -m pytest tests/test_abstraction_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_no_config_specific_name_in_src` passes on the current clean `src/newsletters/`.
    - The denylist is a self-contained module constant covering the swimlane fixture ids + sample_team
      names; the test scans `src/`, not `tests/`.
    - A planted config-name leak in any `src/newsletters/*.py` would make the test go red (demonstrable
      by the token-matching logic).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: [config]/yaml lazy-boundary tests — bare-install yaml unreachable (LANE-04)</name>
  <files>tests/test_ai_optional.py</files>
  <read_first>
    - tests/test_ai_optional.py lines 261-394 (the ENTIRE [excel] block — the one-for-one template:
      test_excel_extra_declared, test_openpyxl_loader_has_no_toplevel_openpyxl_import,
      test_adapters_package_imports_without_openpyxl (the meta-path-finder block-then-subprocess
      idiom), test_loader_raises_teaching_error_without_openpyxl, test_loader_returns_module_when_
      openpyxl_present) and lines 234-258 (test_worksurface_import_loads_no_ai_module)
    - src/newsletters/_yaml_loader.py (MISSING_YAML_MESSAGE + _load_yaml + safe_load parse)
    - src/newsletters/swimlane.py (the new top-level module whose AI/yaml-freeness is asserted)
    - .planning/phases/.../01-PATTERNS.md lines 267-283 (the one-for-one port table)
  </read_first>
  <action>
    Append a parallel `[config]`/yaml block to `tests/test_ai_optional.py` (ADD ONLY — do not modify
    the existing [excel]/[pptx]/AI tests). Port the [excel] block one-for-one, swapping
    `openpyxl`→`yaml`, `_openpyxl_loader`→`_yaml_loader`, `[excel]`→`[config]`, and the loader path
    constant to point at `src/newsletters/_yaml_loader.py`: (a) `test_config_extra_declared` — assert
    the `config` extra exists, contains PyYAML only, and is not an AI package; (b) a no-top-level-yaml
    test — assert `_yaml_loader.py` has zero column-0 `import yaml`/`from yaml`; (c) a
    swimlane-imports-without-yaml test — using the `sys.meta_path` finder block-then-subprocess idiom
    (with `PYDANTIC_DISABLE_PLUGINS=true`), block `yaml`, then `import newsletters`, `import
    newsletters.swimlane`, `from newsletters import _yaml_loader`, and assert `'yaml' not in
    sys.modules`; (d) `test_yaml_loader_raises_teaching_error_without_yaml` — with yaml blocked,
    `_load_yaml()` raises an ImportError whose message contains `[config]` and `yaml`/`PyYAML` (assert
    against the imported `MISSING_YAML_MESSAGE` constant to avoid string drift); (e) a
    returns-module-when-present test that skips cleanly on a bare env; and (f) mirror
    `test_worksurface_import_loads_no_ai_module` as `test_swimlane_import_loads_no_ai_module` — a fresh
    subprocess `from newsletters.swimlane import <load fn>` loads ZERO AI module (reuse the AI_MODULES
    set + PYDANTIC_DISABLE_PLUGINS pattern). Reuse the file's existing helpers (`_load_pyproject`,
    `_req_name`, `AI_REQUIREMENT_NAMES`, `AI_MODULES`, `REPO_ROOT`) — do not redefine them.
  </action>
  <verify>
    <automated>python -m pytest tests/test_ai_optional.py -k "config or yaml or swimlane" -q</automated>
  </verify>
  <acceptance_criteria>
    - The new `[config]`/yaml tests pass; the existing [excel]/[pptx]/AI tests remain unchanged and green.
    - Bare-install simulation proves `import newsletters` + `import newsletters.swimlane` succeed with
      `yaml` unreachable and not in sys.modules (LANE-04 SC5).
    - `_load_yaml()` teaching-error test asserts against `MISSING_YAML_MESSAGE` (no string drift).
    - `test_swimlane_import_loads_no_ai_module` proves the new module is AI-free.
    - mypy/black/isort clean on the added test code (NEW code clean vs baseline).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/ source text → public repo | A hardcoded config/org name in source is both an abstraction breach and a confidentiality risk |
| bare install → runtime | The [config] extra must not leak yaml into the bare (no-extras) spine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | `src/newsletters/*.py` | mitigate | The abstraction-guard test scans all source and fails the suite on any config-specific/real name leak (LANE-03), enforced in code not comments. |
| T-04-02 | Tampering / Elevation | bare-install yaml reachability | mitigate | The extended `test_ai_optional.py` proves `import yaml` is unreachable from a bare install and `swimlane`/`_yaml_loader` stay AI-free; runs inside the CI bare-install job (no ci.yml edit). |
| T-04-03 | Tampering | yaml deserialization | mitigate | The teaching-error test asserts the boundary exists and (via Plan 01-01) uses safe_load only; the block does not relax any existing gate. |
| T-04-SC | Tampering (supply chain) | `[config]` PyYAML | mitigate | `test_config_extra_declared` asserts the extra contains PyYAML only and is non-AI — a pinned, verified, canonical package (see Plan 01-01 T-01-SC). |
</threat_model>

<verification>
Plan-level gates (re-run independently):
- `python -m pytest tests/test_abstraction_guard.py tests/test_ai_optional.py -q` is green.
- The bare-install-yaml-unreachable assertions pass (the milestone's LANE-04 gate), and no existing
  test in `tests/test_ai_optional.py` was modified or weakened.
- `ci.yml` is byte-unchanged (not in files_modified); `faithfulness.py`/`coverage.py`/`conftest.py`
  untouched.
- mypy/black/isort clean on the NEW file + added code.
</verification>

<success_criteria>
- Abstraction-guard test fails the suite on any fixture/config name in `src/newsletters/` (LANE-03).
- Bare install imports the spine with `import yaml` unreachable; `[config]` extra lazy-loads with a
  teaching ImportError; swimlane is AI-free (LANE-04).
</success_criteria>

<output>
Create `.planning/phases/01-swim-lane-binding-traced-yaml-loader/01-04-SUMMARY.md` when done.
</output>
