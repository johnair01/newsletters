---
phase: 01-swim-lane-binding-traced-yaml-loader
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - tests/fixtures/swimlane/module-x.yml
  - tests/fixtures/swimlane/module-trap.yml
  - tests/test_swimlane.py
autonomous: true
requirements: [LANE-01, LANE-02]
user_setup: []

must_haves:
  truths:
    - "A committed generic fixture (synthetic names only) drives the loader; N lanes in → N SectionBindings out, bound at dict level (LANE-01 proof)"
    - "On the trap fixture every scalar READ is either content-addressed or routed to unextracted[] — len(claims)+len(unextracted)==scalars walked, with the EXACT ordered reason strings pinned (LANE-02, no silent drops)"
    - "Every emitted trace is_addressed is True, and an adversarial un-addressed trace is CAUGHT by a test, not silently passed (LANE-02, Hole B closed upstream)"
    - "Two loads of the same file produce byte-identical Source + bindings (determinism / SITE-06)"
  artifacts:
    - path: "tests/fixtures/swimlane/module-x.yml"
      provides: "Small generic well-formed module config — synthetic names only"
    - path: "tests/fixtures/swimlane/module-trap.yml"
      provides: "Adversarial fixture: duplicates, quotes, coercion (yes/1.0/date), block scalars, anchors/aliases"
    - path: "tests/test_swimlane.py"
      provides: "Coverage-identity, faithful-span, adversarial Hole-B, and determinism tests"
      contains: "def test_"
  key_links:
    - from: "tests/test_swimlane.py"
      to: "src/newsletters/swimlane.py"
      via: "drives the live loader; asserts structure + invariants, not lane names"
      pattern: "swimlane"
---

<objective>
Prove the loader is honest and deterministic with a committed generic fixture, an adversarial trap
fixture, and `tests/test_swimlane.py`. Closes the LANE-02 verification surface: the read-anchored
coverage identity (zero silent drops on the trap fixture), the Hole-B adversarial guard (an
un-addressed trace is caught, not passed), the faithful-span check, and byte-stable determinism —
plus the LANE-01 proof that N configured lanes yield N dict-level SectionBindings.

Purpose: This is where the circuit-breaker invariants become executable. The tests are authored by
DRIVING the live loader (never assumed): the exact ordered `_R_*` reason strings per trap scalar are
read from the loader's actual output, then pinned.

Output: two committed fixtures under `tests/fixtures/swimlane/` + `tests/test_swimlane.py`.

NON-GOALS: no `module-a` worked example (Phase 3 — this phase commits only small generic fixtures);
no Surface/render assertions (Phase 2). Tests assert STRUCTURE and INVARIANTS, never a specific lane
name/value (Pitfall 8) — where a concrete value is needed, read it from the same fixture the loader
reads so the assertion tracks the config.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-swim-lane-binding-traced-yaml-loader/01-CONTEXT.md
@.planning/phases/01-swim-lane-binding-traced-yaml-loader/01-PATTERNS.md
@.planning/research/PITFALLS.md
@src/newsletters/swimlane.py
@tests/test_worksurface.py
@tests/test_excel_golden.py
@sample_team/functional_groups.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the generic + trap fixtures (synthetic names only)</name>
  <files>tests/fixtures/swimlane/module-x.yml, tests/fixtures/swimlane/module-trap.yml</files>
  <read_first>
    - sample_team/functional_groups.yml + sample_team/q2_okrs.yml (the config SHAPE to generalize:
      lanes/functional groups with owner+members, kpis with label+values, objective refs)
    - .planning/phases/.../01-PATTERNS.md lines 175-201 (fixture shape + naming discipline + the trap
      fixture's required adversarial content)
    - .planning/research/PITFALLS.md Pitfall 4 (the scalar-location traps: duplicates, quoting,
      coercion yes/1.0/date, block scalars, anchors/aliases) and lines 486-491 (synthetic-names rule)
  </read_first>
  <action>
    Create `tests/fixtures/swimlane/module-x.yml`: a small, well-formed module config exercising the
    real shape the loader must generalize over — a module id, an area id, a lane list where each lane
    has a heading/id, a functional-group binding, an owner id, member ids, KPI entries (label + period
    values), and objective refs. Use ONLY obviously-fabricated generic ids (`module-x`, `area-*`,
    `lane-a`/`lane-b`, `owner-1`, `eng-01`, `metric-1`, `toolset-1`) — NOTHING resembling a real
    org/tool/metric/site/program name (public repo; Pitfall 8). Add a top comment stating the file is
    synthetic test data. Then create `tests/fixtures/swimlane/module-trap.yml`: a deliberately
    adversarial fixture that packs each scalar-location trap so the loader's honest routing is
    provable — a DUPLICATE value repeated across KPIs (proves the forward-only cursor gives distinct
    offsets), QUOTED scalars (single and double), TYPE-COERCED forms (`yes`, `1.0`, a bare `007`, a
    `2026-07-02` date), a BLOCK scalar (`|` and/or `>`), and an ANCHOR/ALIAS pair (`&a ... *a`). Keep
    it small and hand-checkable — its exact scalar set is the executable contract. Synthetic names
    only; comment it as the trap fixture. Do NOT author either fixture AROUND a loader gap (RETRO: a
    proof corpus authored around a bug proves nothing) — the trap fixture must exercise the loader's
    real routing, whatever it turns out to be.
  </action>
  <verify>
    <automated>python -c "import pathlib; from newsletters._yaml_loader import load_config; [load_config(pathlib.Path(p).read_text()) for p in ['tests/fixtures/swimlane/module-x.yml','tests/fixtures/swimlane/module-trap.yml']]; t=open('tests/fixtures/swimlane/module-trap.yml').read(); assert '&' in t and '*' in t and ('|' in t or '>' in t), 'trap must contain anchor/alias + block scalar'; print('fixtures parse; trap has anchors+block')"</automated>
  </verify>
  <acceptance_criteria>
    - Both fixtures exist under `tests/fixtures/swimlane/`, parse via safe_load, and use only
      fabricated synthetic names.
    - `module-x.yml` exercises the full lane/KPI/objective shape; `module-trap.yml` contains
      duplicates, quotes, coerced forms, a block scalar, and an anchor/alias pair.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Coverage-identity + faithful-span + LANE-01 binding tests</name>
  <files>tests/test_swimlane.py</files>
  <read_first>
    - tests/test_excel_golden.py lines 8-90 (the read-anchored coverage identity model:
      total_units == n_claims + len(unextracted_reasons); the R_* reason constants + EXPECTED dict
      pinned by driving the live extractor; the faithful-span assertion claim.text == trace.span and
      re-slice transcript[start:end])
    - tests/test_worksurface.py lines 70-118 (transcript-verbatim + Trace round-trip + is_addressed
      + content_hash checks)
    - src/newsletters/swimlane.py (the live loader — DRIVE it to read the actual ordered reason
      strings; do not assume them)
  </read_first>
  <action>
    Create `tests/test_swimlane.py`. Write `test_no_yaml_scalar_is_read_but_undisclosed`: load the
    trap fixture, gather all claims (across bindings, including KpiItem values) and all unextracted
    entries, and assert the read-anchored identity `len(claims) + len(unextracted) == scalars walked`,
    with ZERO unaccounted scalars (mirror the excel golden's `total_units` identity). Pin the EXACT
    ordered list of `_R_*` reason strings the loader emits for the trap fixture — obtain them by
    DRIVING the live loader once and recording its output (never assumed), and assert against the
    loader's module-level `_R_*` constants (import them, no inline string duplication → no drift).
    Write `test_faithful_spans`: for every minted claim/KpiItem trace, assert `claim.text ==
    trace.span` AND re-slicing `source.transcript[trace.start:trace.end]` reproduces it, and
    `trace.content_hash == source.content_hash()`, `not trace.is_stale_against(source)`. Write
    `test_lanes_bind_at_dict_level` (LANE-01): load `module-x.yml`, assert N configured lanes → N
    SectionBindings in file order, each with its heading/kpi_items/claims populated — asserting on
    STRUCTURE and counts read from the same fixture, never on a hardcoded lane name; and assert the
    loader neither imported nor instantiated `models.FunctionalGroup`/`Kpi` (e.g. assert the binding
    objects are `SectionBinding`/`KpiItem`/`Claim`, not `models` types). All value assertions that need
    a concrete string read it from the parsed fixture so the test tracks the config.
  </action>
  <verify>
    <automated>python -m pytest tests/test_swimlane.py -k "undisclosed or faithful or dict_level" -q</automated>
  </verify>
  <acceptance_criteria>
    - The coverage-identity test passes and pins the exact ordered `_R_*` reasons via the loader's own
      constants (no inline duplication).
    - Faithful-span test proves claim.text == trace.span == transcript re-slice, content-addressed,
      non-stale.
    - LANE-01 binding test proves N lanes → N dict-level SectionBindings without models instantiation,
      asserting structure not lane names.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Adversarial Hole-B guard + determinism test</name>
  <files>tests/test_swimlane.py</files>
  <read_first>
    - tests/test_worksurface.py lines 111-118 (byte-identical double-load determinism via
      model_dump_json) and lines 373-427 (the adversarial "prove it FIRES" pattern — craft a case that
      WOULD violate the invariant and assert it is caught)
    - .planning/research/PITFALLS.md "Hole B" (lines ~27-33) + Pitfall 5 (determinism)
    - src/newsletters/semantic.py lines 172-184 (is_addressed / is_stale_against)
  </read_first>
  <action>
    Add two tests to `tests/test_swimlane.py`. `test_every_emitted_trace_is_addressed` (Hole B, the
    positive half): load both fixtures and assert `trace.is_addressed is True` for EVERY trace on every
    claim/KpiItem the loader emits (no un-addressed free-pass trace escapes the loader). Then the
    adversarial half — prove the guard actually FIRES, not just passes: construct a case that WOULD
    produce an un-addressed trace (e.g. hand-build a `Claim` carrying a bare `Trace()` /
    `FreeLocator()` with `content_hash=None`, mirroring the worksurface adversarial pattern) and assert
    a check catches it (the assertion that would guard the loader output rejects it) — so the test
    demonstrably distinguishes addressed from un-addressed, closing Hole B upstream rather than
    trusting the gate. `test_load_is_byte_stable`: load the same fixture twice (and, to guard ordering,
    a second call is sufficient since inputs are a single file) and assert the Sources and the
    SectionBinding lists are byte-identical via `model_dump_json()` (mirror
    test_worksurface.py:111-118) — proving deterministic timestamps (EPOCH_ZERO), file-order
    iteration, and no set()-induced reordering.
  </action>
  <verify>
    <automated>python -m pytest tests/test_swimlane.py -k "addressed or byte_stable" -q</automated>
  </verify>
  <acceptance_criteria>
    - Every trace the loader emits is_addressed True (Hole B positive guard).
    - The adversarial half proves an un-addressed trace is CAUGHT, not silently passed.
    - Double-load byte-equality passes (deterministic Source + bindings).
    - Full `pytest tests/test_swimlane.py` is green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → public repo | Fixture content is published; a real name here is a confidentiality breach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | `tests/fixtures/swimlane/*.yml` | mitigate | Fabricated synthetic names only (`module-x`, `lane-a`, `owner-1`, …); a top comment marks each file synthetic. Committed-content name discipline is enforced repo-wide by the abstraction-guard test (Plan 01-04). |
| T-03-02 | Tampering | trap-fixture reason strings | mitigate | The trap fixture's expected `_R_*` reasons are derived by driving the LIVE loader and asserted against the loader's own constants — a fixture authored around a loader gap is explicitly forbidden (RETRO). |
| T-03-SC | Tampering (supply chain) | n/a | accept | No package installs in this plan; tests run under the existing `.[config,test]` env. |
</threat_model>

<verification>
Plan-level gates (re-run independently):
- `python -m pytest tests/test_swimlane.py -q` is fully green.
- Fixtures use synthetic names only (also checked repo-wide by Plan 01-04's guard).
- The coverage-identity, faithful-span, Hole-B adversarial, and determinism invariants all pass.
- mypy/black/isort clean on the NEW file `tests/test_swimlane.py`.
</verification>

<success_criteria>
- Read-anchored coverage identity holds on the trap fixture with exact pinned reasons; zero silent
  drops (LANE-02).
- Every emitted trace is_addressed True; an un-addressed trace is caught adversarially (LANE-02, Hole B).
- N lanes → N dict-level SectionBindings proven (LANE-01); double-load byte-stable.
</success_criteria>

<output>
Create `.planning/phases/01-swim-lane-binding-traced-yaml-loader/01-03-SUMMARY.md` when done.
</output>
