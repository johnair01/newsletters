---
phase: 02-module-scope-report-composer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/newsletters/compose.py
autonomous: true
requirements: [COMP-04]
must_haves:
  truths:
    - "The composed surface carries a stable R-NNN from site.Ledger.ref_for against its own content/module/ids.json ledger — reuse-only, append-only, never a hardcoded ref (COMP-04)"
    - "Quote slot is sourced-or-omit: a QuoteBlock is rendered ONLY from a traced owner/manager quote claim (attr = owner id); if absent the block is omitted and a missing[] note added — never a fabricated quote"
    - "A FanoutBlock stub is present with declared kinds (article/newsletter/learning) and href=None"
    - "The connective/quote/fanout additions author no numerals or facts (COMP-03 upheld); the surface still ships Draft"
  artifacts:
    - path: "src/newsletters/compose.py"
      provides: "quote slot (sourced-or-omit), fanout stub, R-NNN via reused site.Ledger"
      contains: "Ledger"
  key_links:
    - from: "src/newsletters/compose.py compose_module_report"
      to: "site.Ledger.ref_for"
      via: "reuse (never fork) site.py; append-only ref against content/module/ids.json"
      pattern: "ref_for"
---

<objective>
Complete `compose_module_report` (same file as Plan 02-02, next wave) with the COMP-04 identity and
structure pieces: a sourced-or-omit owner/manager QuoteBlock, a FanoutBlock stub (href=None), and a
stable `R-NNN` ref obtained via the EXISTING `site.Ledger` against a NEW, dedicated
`content/module/ids.json` ledger (reuse-only, append-only, never hardcoded).

Purpose: give the module report its stable identity and its owner-voice + fan-out slots without ever
authoring a fact — the quote is rendered only if it was traced from config, the fan-out is a declared
stub, and the ref comes from the append-only ledger authority.
Output: the finished `compose.py` composer, still Draft-only, still deterministic.

LEDGER DECISION (LOCKED for this phase — 02-CONTEXT.md discretion, resolved with Phase 3 in mind):
`compose_module_report` takes an explicit `Ledger` instance and calls `ledger.ref_for(slug, "report")`
to READ/ASSIGN the ref in-memory (append-only, immutable on re-sight). It does NOT call `ledger.save()`
— persistence is the CALLER's job (Phase 3's `build_module_site` mirrors `build_work_site`:
`Ledger.load("content/module/ids.json") → compose → ledger.save()`). This matches how
`Site.from_surfaces` reads/extends the in-memory ledger while the caller owns `save()` (site.py), and
keeps compose free of disk writes so tests stay isolated. The default ledger path
`content/module/ids.json` is documented; tests pass a tmp ledger and NEVER touch the committed path.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-scope-report-composer/02-CONTEXT.md
@.planning/phases/02-module-scope-report-composer/02-PATTERNS.md

@src/newsletters/site.py
@src/newsletters/semantic.py

FORBIDDEN TO EDIT (02-CONTEXT.md / PITFALLS #11 — absolute): `semantic.py`, `models.py`,
`templates.py`, `render.py`, `site.py` (READ-ONLY reuse — do NOT fork the Ledger), `faithfulness.py`,
`coverage.py`, `conftest.py`, ANY existing test, `.importlinter`, `ci.yml`, `swimlane.py`. The ONLY
writable target is `src/newsletters/compose.py` (extending Plan 02-02's function).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sourced-or-omit QuoteBlock + FanoutBlock stub</name>
  <files>src/newsletters/compose.py</files>
  <read_first>
    - src/newsletters/semantic.py — `QuoteBlock(text, attr)`, `FanoutBlock(heading, links)`,
      `FanoutLink(kind, title, href=None)`.
    - src/newsletters/worksurface.py `build_work_surfaces` (~333-343) — the FanoutBlock stub shape
      (declared kinds, descriptive titles; here href stays None).
    - 02-CONTEXT.md "Identity & structure" (quote slot: sourced-or-omit; unowned lane honesty; fanout
      stub with declared kinds).
  </read_first>
  <behavior>
    - A load carrying a traced owner/manager quote claim -> one QuoteBlock(text=<claim text>, attr=<owner id>).
    - A load with NO traced quote claim -> NO QuoteBlock; a missing[] note is added instead.
    - An unowned lane -> attribution falls back to an "unassigned"-style honesty marker, no fabricated quote.
    - A FanoutBlock stub is always present: declared kinds (article/newsletter/learning), each href=None.
    - Adding these blocks introduces no numerals/facts into any non-ClaimsBlock text.
  </behavior>
  <action>
    Extend `compose_module_report` to add, after the per-lane sections:
    - A sourced-or-omit QuoteBlock. Decide how the owner/manager quote flows (02-CONTEXT.md
      discretion — via a SectionBinding extension is NOT permitted here since swimlane.py is frozen
      after Plan 02-01; prefer detecting a traced quote claim already present in the load, or a
      parallel structure you derive from the traced claims). If a traced quote claim exists, emit
      `QuoteBlock(text=<the traced claim's verbatim text>, attr=<owner id, a config value>)`. If none
      exists, DO NOT emit a QuoteBlock — append a `missing[]` note ("owner/manager quote not provided"
      or similar; wording is discretion) and, for an unowned lane, use an "unassigned"-style honesty
      marker rather than a name. NEVER fabricate quote text.
    - A FanoutBlock stub: `FanoutBlock(links=[FanoutLink(kind=..., title=..., href=None) for the
      declared kinds article/newsletter/learning])`. Titles are structural/descriptive labels with no
      numerals; `href=None` (the fan-out targets do not exist yet — never a dead link, never fabricated).
    Keep the surface Draft; keep prose numeral-free; keep determinism (no clock, file order).
  </action>
  <verify>
    <automated>python -c "
from newsletters.semantic import Source, Claim, KpiItem, Trace
from newsletters.swimlane import SwimlaneLoad, SectionBinding
from newsletters.compose import compose_module_report
from newsletters.adapters._timestamps import EPOCH_ZERO
src=Source(id='m', context='module-config:m', transcript='10 20 finding', timestamp=EPOCH_ZERO)
c0=Claim(text='10', evidence=[Trace.from_source(src,0,2)]); c1=Claim(text='20', evidence=[Trace.from_source(src,3,5)])
b=SectionBinding(heading='Lane', kpi_items=[KpiItem(label='M', value='20')], kpi_endpoints=[[c0,c1]], claims=[c0,c1])
s=compose_module_report(SwimlaneLoad(source=src, bindings=[b], claims=[], unextracted=[], scalars_walked=2))
fan=[bl for bl in s.blocks if bl.kind=='fanout']; assert fan and all(l.href is None for l in fan[0].links)
assert not [bl for bl in s.blocks if bl.kind=='quote']  # no traced quote -> omitted
assert s.missing  # omission disclosed
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Every case in <behavior> holds.
    - No QuoteBlock is emitted unless a traced quote claim exists; its absence is disclosed in missing[].
    - A FanoutBlock stub is always present with every `href is None`.
    - The surface still ships Draft; no numerals authored into quote/fanout/prose text.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stable R-NNN via reused site.Ledger (append-only, own content/module/ids.json)</name>
  <files>src/newsletters/compose.py</files>
  <read_first>
    - src/newsletters/site.py — `Ledger.load`, `Ledger.ref_for(slug, kind)` (existing slug returns
      its recorded ref immutably; a new sequential slug gets `max(existing ordinals)+1`; first report
      => "R-001"), `Ledger.save` (byte-stable JSON; NOT called here), `slugify`. REUSE — never fork,
      never format a ref inline.
    - src/newsletters/worksurface.py `build_work_site` (~417-419) — the `Ledger.load → use → save`
      beat (save deferred to Phase 3 here).
    - content/work/ids.json — precedent: `work` has its OWN ledger starting at R-001 (the sample/real
      boundary the module corpus mirrors).
  </read_first>
  <behavior>
    - compose against a fresh/empty module ledger -> the surface's slug is recorded with ref "R-001".
    - compose again with the SAME slug against the same ledger -> the SAME ref (append-only/immutable).
    - compose does NOT write content/module/ids.json (no save()); the caller owns persistence.
    - the ref is never a hardcoded/f-string literal in compose.py — it comes only from ref_for.
  </behavior>
  <action>
    Add `from .site import Ledger` and extend `compose_module_report`'s signature to accept the ledger:
    `compose_module_report(load, *, author="operator", ledger: Ledger | None = None,
    ledger_path="content/module/ids.json") -> Surface`. If `ledger` is None, `Ledger.load(ledger_path)`
    (reads the own module ledger, or empty if absent). Compute the surface `slug` deterministically
    via `site.slugify` over the config-derived module identity (data, not hardcoded), set it as the
    Surface `id`/slug, and obtain the ref via `ledger.ref_for(slug, "report")` — the SOLE ref source
    (never format `R-NNN` yourself, never `len()+1`). Do NOT call `ledger.save()` — persistence is the
    caller's (Phase 3) job; document this in the docstring. The ref is thus carried via the ledger
    keyed by the surface slug (the same association `Site.from_surfaces` reads later). Do NOT place the
    ref string into any ProseBlock (a bare `R-001` in prose would trip the numeral guard) — it lives
    in the ledger, not in authored prose.

    Update `__all__` to export `compute_delta` and `compose_module_report`. Keep compose disk-write-free
    (only reads a ledger; the loader/site tiers own writes).
  </action>
  <verify>
    <automated>python -c "
import tempfile, pathlib
from newsletters.site import Ledger
from newsletters.semantic import Source, Claim, KpiItem, Trace
from newsletters.swimlane import SwimlaneLoad, SectionBinding
from newsletters.compose import compose_module_report
from newsletters.adapters._timestamps import EPOCH_ZERO
d=pathlib.Path(tempfile.mkdtemp()); lp=d/'ids.json'
src=Source(id='m', context='module-config:m', transcript='10 20', timestamp=EPOCH_ZERO)
c0=Claim(text='10', evidence=[Trace.from_source(src,0,2)]); c1=Claim(text='20', evidence=[Trace.from_source(src,3,5)])
b=SectionBinding(heading='Lane', kpi_items=[KpiItem(label='M', value='20')], kpi_endpoints=[[c0,c1]], claims=[c0,c1])
load=SwimlaneLoad(source=src, bindings=[b], claims=[], unextracted=[], scalars_walked=2)
led=Ledger.load(lp); s=compose_module_report(load, ledger=led)
assert led.entry(s.id) and led.entry(s.id)['ref']=='R-001', led.entry(s.id)
led2=Ledger.load(lp); s2=compose_module_report(load, ledger=led)  # same in-memory ledger -> immutable
assert led.entry(s.id)['ref']=='R-001'
assert not lp.exists()  # compose never saved
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - A fresh module ledger records the surface slug with ref "R-001"; re-compose with the same slug
      returns the same ref (append-only/immutable).
    - `compose.py` contains no literal `R-` ref format and no `len()+1` ordinal; the ref comes only
      from `Ledger.ref_for`. `site.py` is unmodified (reuse, not fork).
    - `compose_module_report` never calls `ledger.save()` / writes the ledger file.
    - `__all__` exports `compute_delta` and `compose_module_report`.
    - `lint-imports` green; `black`/`isort --profile black`/`mypy` on `compose.py`: zero NEW failures.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger file path → Ledger | a caller-supplied ledger path is read (and later, in Phase 3, written) |
| config quote claim → QuoteBlock | untrusted config-derived quote text becomes attributed surface content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | R-NNN assignment | mitigate | Ref comes only from the reused append-only `Ledger.ref_for` (`max+1`, immutable on re-sight); compose never formats a ref, never `len()+1`, never `save()`s — no collision/renumber. Proven by the ledger test (this plan + Plan 02-04) |
| T-02-10 | Spoofing | QuoteBlock text/attr | mitigate | Sourced-or-omit: a QuoteBlock renders only from a traced claim; absence -> omit + missing[] note; unowned -> "unassigned" marker. Never a fabricated quote |
| T-02-11 | Repudiation | ledger integrity | mitigate | Reuse site.Ledger unchanged (existing refs never rewritten); compose is read-only on the ledger, deferring the single write to Phase 3's caller |
| T-02-12 | Information disclosure | quote/fanout config strings | accept | No HTML authored here; downstream render owns HTML-escaping. FanoutLink.href=None (no dead/fabricated link) |

No package installs in this plan (no new dependency); no network/auth surface.
</threat_model>

<verification>
- Both smoke `python -c` checks pass (quote/fanout shape; ledger R-001 + immutability + no save).
- `site.py` unmodified (`git diff --exit-code src/newsletters/site.py`).
- `lint-imports` green; `black`/`isort --profile black`/`mypy` on `compose.py`: zero NEW failures.
</verification>

<success_criteria>
The composer is complete: stable append-only `R-NNN` via the reused ledger against its own
`content/module/ids.json`, a sourced-or-omit owner quote, and a fan-out stub — all Draft-only, all
deterministic, authoring no facts. COMP-04 is implemented.
</success_criteria>

<output>
Create `.planning/phases/02-module-scope-report-composer/02-03-SUMMARY.md` when done.
</output>
