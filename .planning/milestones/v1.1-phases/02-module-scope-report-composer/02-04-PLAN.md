---
phase: 02-module-scope-report-composer
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - tests/test_compose.py
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
must_haves:
  truths:
    - "A test FAILS if the composer emits any claim with zero traces (Hole B) — COMP-03"
    - "A test FAILS if any emitted trace has is_addressed == False (Hole B) — COMP-03"
    - "A numeral-free-prose guard FAILS if any non-ClaimsBlock block's text carries a digit run not drawn from a traced claim (Hole A) — COMP-03"
    - "A no-auto-publish adversarial test proves the composed Draft surface cannot reach Published without the gate — COMP-04"
    - "A delta-reproducibility test recomputes every rendered delta from its two endpoints and asserts byte-equality — COMP-02"
    - "A determinism test: two composes of the same load are byte-identical (model_dump_json) — COMP-02"
    - "A seam/second-kind test composes a non-lane SectionBinding with ZERO composer change — COMP-01"
    - "Edge-case tests: zero-KPI lane, empty lane set (valid Draft + populated missing[]), unowned-lane honesty"
  artifacts:
    - path: "tests/test_compose.py"
      provides: "the phase's trust-guard proof suite over the live composer"
  key_links:
    - from: "tests/test_compose.py"
      to: "newsletters.compose / newsletters.swimlane"
      via: "import the live composer + hand-built SectionBindings (no YAML)"
      pattern: "from newsletters.compose import"
---

<objective>
Land the trust-guard suite that IS this phase's deliverable (Pitfall 10: guarantees enforced by
adversarial tests, not docstrings). Prove the live composer on hand-built `SectionBinding`s (no YAML —
02-CONTEXT.md / SUMMARY: compose is testable with in-memory bindings) closes research Holes A + B at
the composer, computes reproducible deltas, is deterministic, ships Draft-only, and consumes the
kind-agnostic seam. Covers COMP-01..04. `faithfulness.py`/`coverage.py`/`semantic.py` stay UNTOUCHED —
these are NEW, additive guards (Pitfall 11).

Purpose: remove the easy cheats (Hole A un-gated numerals, Hole B un-addressed traces, fabricated
deltas, auto-publish) by making a bypass RED.
Output: `tests/test_compose.py` — the executable contract for the composer.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-scope-report-composer/02-CONTEXT.md
@.planning/phases/02-module-scope-report-composer/02-PATTERNS.md

@src/newsletters/compose.py
@src/newsletters/swimlane.py
@src/newsletters/semantic.py

FORBIDDEN TO EDIT (02-CONTEXT.md / PITFALLS #11 — absolute): `faithfulness.py`, `coverage.py`,
`semantic.py`, `models.py`, `templates.py`, `render.py`, `site.py`, `conftest.py`, ANY existing test,
`.importlinter`, `ci.yml`, `swimlane.py`, `compose.py`. The ONLY writable target is the NEW
`tests/test_compose.py`. If a guard here goes RED, the FIX is in the composer (a prior wave), NEVER a
weakened gate/test.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trust guards — Hole B (zero-trace / un-addressed) + Hole A (numeral-free prose) + no-auto-publish</name>
  <files>tests/test_compose.py</files>
  <read_first>
    - tests/test_worksurface.py (~128-200) — the per-claim `is_traced` + `trace.is_addressed`
      assertion pair, and the "≥1 routed to missing[]" honesty check, to mirror.
    - tests/test_semantic.py (~59-101) — the no-auto-publish shapes: `publish()` without a satisfied
      policy raises; `open_pull_request()` refuses untraced claims; a direct
      `Review(state=PUBLISHED,...)` raises via the model validator (semantic.py ~279-287).
    - src/newsletters/compose.py — the live `compose_module_report` API (params, block kinds).
    - src/newsletters/semantic.py — `ClaimsBlock`, `ProseBlock`, `KpiStripBlock`, `QuoteBlock`,
      `FanoutBlock`; `Surface.gate`, `Surface.publish`, `Surface.open_pull_request`.
  </read_first>
  <action>
    Create `tests/test_compose.py`. Build hand-authored `Source` + `SectionBinding`s in memory (no
    YAML): mint endpoint/finding Claims via `Trace.from_source` against a small crafted transcript so
    they are genuinely content-addressed; assert STRUCTURE/INVARIANTS, never a frozen lane string
    (Pitfall 8) — where a value is checked, read it from the same inputs the composer read. Write:
    - Zero-trace-claim guard: over the composed surface, EVERY claim on EVERY ClaimsBlock has
      `is_traced` True (any zero-trace claim -> the test fails). Prove non-vacuous: include a binding
      whose `claims` contains a deliberately un-traced claim and assert it was routed to `missing[]`
      (NOT left on a ClaimsBlock).
    - All-content-addressed guard: every trace on every surviving ClaimsBlock claim has
      `is_addressed` True (Hole B) — an un-addressed trace would be caught here.
    - Numeral-free-prose guard (Hole A): scan the `.text` of every NON-ClaimsBlock block (ProseBlock,
      and any KpiStripBlock/QuoteBlock/FanoutBlock text/heading) for digit runs; assert each digit run
      is either (a) a `KpiItem.value`/`delta` on a KpiStripBlock traceable to an endpoint, or (b)
      absent from authored prose entirely. Allow-list config-derived STRUCTURAL labels explicitly (not
      by accident). The composer's ProseBlock text must contain NO un-sourced digit run.
    - No-auto-publish adversarial: the composed surface's `gate` is Draft; `surface.publish()` with no
      satisfied policy raises; a hand-constructed `Review(state=PUBLISHED,...)` for its policy raises
      via the model validator. Prove the publish path the model actually enforces refuses.
  </action>
  <verify>
    <automated>pytest tests/test_compose.py -q -k "trace or addressed or numeral or publish or missing or draft"</automated>
  </verify>
  <acceptance_criteria>
    - The zero-trace and un-addressed guards pass on the honest composer AND are proven non-vacuous
      (a planted un-traced claim is shown routed to missing[]).
    - The numeral-free-prose guard passes and would fail on an un-sourced digit run in authored prose.
    - The no-auto-publish tests pass: Draft state; `publish()` raises; direct PUBLISHED review raises.
    - `faithfulness.py`/`coverage.py`/`semantic.py` unmodified (`git diff --exit-code` on each).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delta reproducibility + determinism (byte-equality)</name>
  <files>tests/test_compose.py</files>
  <read_first>
    - tests/test_worksurface.py (~111-118) — the two-calls byte-equality shape
      (`[s.model_dump_json() ...] == [...]`).
    - src/newsletters/compose.py — `compute_delta` (importable, pure) and how KpiItem.delta/dir are
      set from `binding.kpi_endpoints`.
  </read_first>
  <action>
    Add to `tests/test_compose.py`:
    - Delta-reproducibility (COMP-02, the trace-analogue for deltas — Pitfall 1): for every
      KpiStripBlock item on the composed surface that carries a non-None `delta`, RECOMPUTE it by
      calling `compute_delta` on the two endpoint Claim `.text` values from the corresponding
      `binding.kpi_endpoints` entry, and assert byte-equality of both `delta` and `dir`. Assert no
      delta was rendered when fewer than two computable endpoints existed (no fabricated 0), and that a
      Δ==0 case yields `dir=None` with the computed zero form.
    - Determinism (COMP-02 / Pitfall 5-6): two `compose_module_report` calls on the same
      `SwimlaneLoad` produce byte-identical `model_dump_json` (this is where `created==EPOCH_ZERO` is
      proven). Include a case with a repeated KPI value across two lanes to prove ordering is stable
      (no set()/non-total sort).
  </action>
  <verify>
    <automated>pytest tests/test_compose.py -q -k "delta or reproduc or determin or byte"</automated>
  </verify>
  <acceptance_criteria>
    - Every rendered delta recomputes byte-equal from its two endpoints via `compute_delta`.
    - No delta is present when <2 computable endpoints exist; Δ==0 -> `dir=None` + zero form.
    - Two composes are byte-identical (`model_dump_json`), including the repeated-value ordering case.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Kind-agnostic seam (second-kind) + edge cases</name>
  <files>tests/test_compose.py</files>
  <read_first>
    - src/newsletters/swimlane.py — `SectionBinding` is a generic per-section carrier (heading,
      kpi_items, kpi_endpoints, claims, missing); the composer must consume it without knowing "lanes".
    - src/newsletters/compose.py — behavior for zero-KPI bindings and an empty bindings list.
    - 02-CONTEXT.md "Edge cases" and the COMP-01 seam decision.
  </read_first>
  <action>
    Add to `tests/test_compose.py`:
    - Seam / second-kind (COMP-01): build a `SectionBinding` representing a NON-lane section kind
      (e.g. a section with a heading + traced claims but a different conceptual purpose than a swim
      lane), compose it, and assert it produces a valid Draft surface (heading + ClaimsBlock) with
      ZERO composer change — proving the composer keys on `SectionBinding[]`, not on "lanes". Assert
      from the same inputs, never a frozen string.
    - Zero-KPI lane: a binding with `kpi_items == []` -> its KpiStripBlock omitted (or empty) and
      disclosed in `missing[]`; its ClaimsBlock still emitted.
    - Empty lane set: `compose_module_report` on a `SwimlaneLoad` with `bindings == []` returns a
      valid Draft REPORT Surface (never None, never raising) whose `missing[]` is populated.
    - Unowned-lane honesty: a binding with no owner quote claim -> no QuoteBlock; an "unassigned"-style
      marker / missing[] note rather than a fabricated attribution.
    Keep the whole file import-clean (import the live `compose`/`swimlane`/`semantic` symbols; no
    inline literal duplication of the modules' constants).
  </action>
  <verify>
    <automated>pytest tests/test_compose.py -q && python -m lint_imports 2>/dev/null || lint-imports</automated>
  </verify>
  <acceptance_criteria>
    - A non-lane `SectionBinding` composes into a valid Draft surface with zero composer change.
    - Zero-KPI lane, empty lane set, and unowned-lane cases all behave per <action> (valid Draft +
      populated missing[]; never None; never fabricated).
    - The FULL `tests/test_compose.py` suite passes; `lint-imports` green.
    - `black`/`isort --profile black`/`mypy` on `tests/test_compose.py`: zero NEW failures vs baseline.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer output → gate/tests | this suite is the enforcement boundary that makes the composer's trust guarantees observable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | Hole B (un-addressed/zero-trace) | mitigate | Zero-trace + all-content-addressed guards fail on any un-traced/un-addressed composed claim; proven non-vacuous by a planted un-traced claim routed to missing[] |
| T-02-14 | Spoofing | Hole A (un-gated numerals) | mitigate | Numeral-free-prose guard fails on any un-sourced digit run in non-ClaimsBlock text; delta-reproducibility covers the one legitimate derived number |
| T-02-15 | Elevation of privilege | auto-publish | mitigate | No-auto-publish adversarial proves Draft-only + publish path raises without the gate |
| T-02-16 | Tampering | gate-weakening to go green (Pitfall 11) | mitigate | Suite asserts `faithfulness.py`/`coverage.py`/`semantic.py`/`site.py` unmodified via `git diff --exit-code`; a RED guard is fixed in the composer, never by relaxing a gate |

No package installs in this plan (no new dependency); no network/auth surface.
</threat_model>

<verification>
- `pytest tests/test_compose.py` green (all guards).
- `git diff --exit-code src/newsletters/faithfulness.py src/newsletters/distill/faithfulness.py src/newsletters/distill/coverage.py src/newsletters/semantic.py src/newsletters/site.py` clean (no gate weakened).
- `lint-imports` green.
- `black`/`isort --profile black`/`mypy` on the touched test file: zero NEW failures vs baseline.
- Milestone enforced gate set (independently re-run): `pytest` full suite, `lint-imports`,
  `newsletters check` over all corpora, byte-stable double-render, bare-install — all green ("agent
  says green ≠ green": re-run before accepting).
</verification>

<success_criteria>
The composer's trust guarantees are enforced by adversarial tests: Holes A and B are closed at the
composer, deltas are reproducible, output is deterministic and Draft-only, and the kind-agnostic seam
is proven by a second-kind binding. COMP-01..04 are provably satisfied with no gate touched.
</success_criteria>

<output>
Create `.planning/phases/02-module-scope-report-composer/02-04-SUMMARY.md` when done.
</output>
