---
phase: 03-worked-synthetic-module-report
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/newsletters/cli.py
  - tests/test_module_cli.py
requirements: [MODA-02]
autonomous: true

must_haves:
  truths:
    - "`newsletters build --corpus module` renders the module corpus to content/module/site (additive CorpusName choice, unchanged rev1/work behavior)"
    - "`newsletters check --corpus module` runs the SAME unforked review_blockers gate over the module corpus"
    - "The clean module corpus exits 0 (the composed report is Draft — exempt); a planted PUBLISHED blocker makes it exit nonzero"
  artifacts:
    - path: "src/newsletters/cli.py"
      provides: "Additive --corpus module routing for build + check (enum choice + default out + builder dispatch)"
      contains: "module"
    - path: "tests/test_module_cli.py"
      provides: "Gate-both-ways proof (clean exit 0 / planted blocker nonzero) + build routing smoke"
  key_links:
    - from: "src/newsletters/cli.py"
      to: "src/newsletters/modulesite.py::build_module_site"
      via: "build --corpus module dispatch"
      pattern: "build_module_site"
    - from: "src/newsletters/cli.py"
      to: "src/newsletters/modulesite.py::build_module_surfaces"
      via: "check --corpus module dispatch into review_blockers"
      pattern: "build_module_surfaces"
    - from: "tests/test_module_cli.py"
      to: "src/newsletters/review.py::review_blockers"
      via: "the same unforked gate, proven blocking on a planted PUBLISHED surface"
      pattern: "review_blockers|BLOCK"
---

<objective>
Wire the `module` corpus into the LIVE CLI additively: `newsletters build --corpus module` renders
it, `newsletters check --corpus module` gates it through the SAME unforked `review_blockers` merge
gate — and prove the gate fires BOTH ways (exit 0 on the clean corpus, nonzero on a planted
PUBLISHED blocker). This is the CLI/gate half of MODA-02.

Purpose: the module corpus must run the identical trust gate as rev1/work — the selector routes the
builder, never forks the gate. A gate that only sees clean input proves nothing (Phase-10/11 norm),
so the both-ways proof is mandatory.

Output: additive `--corpus module` routing in cli.py + `tests/test_module_cli.py`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-worked-synthetic-module-report/03-CONTEXT.md
@.planning/research/PITFALLS.md
@src/newsletters/cli.py
@tests/test_worksurface.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive --corpus module routing to cli.py</name>
  <files>src/newsletters/cli.py</files>
  <read_first>
    - src/newsletters/cli.py (the LIVE --corpus shape: `CorpusName` enum {rev1, work},
      `_DEFAULT_OUT` map, the `build` dispatch that lazy-imports the builder, and the `check`
      dispatch that lazy-imports the corpus surfaces builder then runs `review_blockers`. Extend it
      ADDITIVELY — never fork the gate, never touch review.py.)
    - src/newsletters/modulesite.py (build_module_site for `build`; build_module_surfaces for `check`)
  </read_first>
  <action>
    Extend cli.py ADDITIVELY ONLY (per D in CONTEXT: extend the corpus choices; never fork the gate;
    review.py untouched):
    - Add `module = "module"` to the `CorpusName` enum (with a one-line docstring note: the synthetic
      worked-example corpus, `modulesite.py`, rendered to content/module/site — running the SAME
      corpus-agnostic merge-block gate).
    - Add `CorpusName.module: "content/module/site"` to `_DEFAULT_OUT`.
    - In `build`: add an `elif corpus is CorpusName.module:` branch that lazy-imports
      `from .modulesite import build_module_site` and calls `build_module_site(target)`; index name
      `"library.html"` (same as work). Keep rev1/work branches unchanged.
    - In `check`: add an `elif corpus is CorpusName.module:` branch that lazy-imports
      `from . import modulesite` and sets `surfaces = modulesite.build_module_surfaces()`. The rest
      of `check` (the `{source_id: Source}` lookup, `review_blockers`, the report + exit code) is
      UNCHANGED and corpus-agnostic — the selector routes only the builder (T-11-13). Update the
      build/check help strings to mention `module`.
    Lazy-import the module builder inside the branch (keep the bare install light + AI-free).
  </action>
  <verify>
    <automated>python -c "from typer.testing import CliRunner; from newsletters.cli import app; r=CliRunner().invoke(app,['check','--corpus','module']); assert r.exit_code==0, r.output; assert 'All published surfaces clean' in r.output; print('check --corpus module exit0 ok')"</automated>
  </verify>
  <done>
    `newsletters check --corpus module` exits 0 on the clean corpus and `newsletters build --corpus
    module` routes to build_module_site; rev1/work behavior is byte-for-byte unchanged; review.py is
    untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate fires both ways over the module corpus</name>
  <files>tests/test_module_cli.py</files>
  <read_first>
    - tests/test_worksurface.py (the corpus-level gate-both-ways pattern:
      `_blocked_published_work_surface` builds a PUBLISHED Report with an UNENTAILED addressed trace,
      `test_check_gates_work_corpus` monkeypatches the surfaces builder and asserts exit_code!=0 +
      the BLOCK/merge-blocked report. MIRROR this for the module corpus. Also
      `test_build_corpus_work_smoke` for the build routing smoke.)
    - src/newsletters/modulesite.py (build_module_surfaces — the monkeypatch target)
  </read_first>
  <action>
    Create `tests/test_module_cli.py` mirroring the worksurface CLI tests, over the `module` corpus.
    Do NOT edit tests/test_worksurface.py or any existing test.

    - `test_check_module_clean_exits_zero`: `CliRunner().invoke(app, ["check","--corpus","module"])`
      → exit_code == 0, "All published surfaces clean" in output (the composed report is Draft →
      exempt; publication is the trust boundary — note this Draft-vacuous caveat in a comment).
    - `test_check_module_blocks_on_planted_blocker`: build ONE PUBLISHED Report carrying a single
      deterministic blocker (mirror `_blocked_published_work_surface`: a `Trace.from_source` that is
      addressed but whose span omits the claim text → exactly one UNENTAILED blocker), monkeypatch
      `newsletters.modulesite.build_module_surfaces` to return `[that surface]`, invoke
      `check --corpus module`, and assert exit_code != 0, "BLOCK" in output, the surface id in
      output, "merge blocked" in output. The blocker fixture is CONSTRUCTED IN THE TEST — never by
      committing a dirty corpus (per CONTEXT: planted blocker as a TEST, not a committed dirty corpus).
    - `test_build_module_smoke(tmp_path)`: `build --corpus module --out <tmp>` exits 0 and writes
      `report-module-a.html` + `library.html` under the out dir.

    This proves MODA-02's "same unforked merge-block gate, exit 0 clean / nonzero on a planted
    blocker."
  </action>
  <verify>
    <automated>python -m pytest tests/test_module_cli.py -x -q</automated>
  </verify>
  <done>
    tests/test_module_cli.py passes: the clean module corpus exits 0, a planted PUBLISHED blocker
    forces nonzero with a BLOCK/merge-blocked report naming the surface, and build routing works. No
    committed dirty corpus; no existing test edited; review.py untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arg → builder dispatch | `--corpus` selector chooses a builder; must not fork/skip the gate |
| composed surface → review_blockers | The merge-block trust boundary (publication) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Elevation/Bypass | `check --corpus module` | mitigate | The selector routes only the builder; the SAME unforked `review_blockers` runs (review.py untouched). Proven by the both-ways test — a corpus selector must not let an unsafe corpus bypass the gate (T-11-13). |
| T-03-07 | Tampering | committed corpus vs planted blocker | mitigate | The blocker is a TEST fixture constructed in-memory (monkeypatch), never a committed dirty corpus — the committed corpus stays clean + Draft |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase |
</threat_model>

<verification>
- `newsletters check --corpus module` exits 0; `newsletters check` (default rev1) still exits 0
  (unchanged).
- `python -m pytest tests/test_module_cli.py -q` green.
- Enforced gates (re-run independently): full `pytest`, `lint-imports`, `newsletters check` over all
  corpora (rev1, work, module) exit 0. Advisory: black / isort --profile black / mypy on cli.py +
  the new test — no NEW failures vs baseline.
</verification>

<success_criteria>
- cli.py additively supports `--corpus module` for build + check; rev1/work unchanged; the gate is
  not forked; review.py untouched.
- The gate is proven to fire BOTH ways over the module corpus (clean 0 / planted blocker nonzero).
</success_criteria>

<output>
Create `.planning/phases/03-worked-synthetic-module-report/03-02-SUMMARY.md` when done.
</output>
