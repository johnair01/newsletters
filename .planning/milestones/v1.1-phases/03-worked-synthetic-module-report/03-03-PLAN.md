---
phase: 03-worked-synthetic-module-report
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/test_modulesite.py
requirements: [MODA-01, MODA-02]
autonomous: true

must_haves:
  truths:
    - "Every claim on the built module surface is traced AND content-addressed; nothing untraced or fabricated survives onto a ClaimsBlock (Hole B), everything unprovable is in missing[]"
    - "The rendered module Library shows claim-beside-verbatim-trace and a POPULATED honesty panel — the planted single-endpoint disclosure is visible in the HTML"
    - "R-001 is stable across a rebuild (the ledger is append-only — proven by rebuild, not asserted)"
    - "build_module_site is byte-stable across two renders (SITE-06) and the committed content/module/site == a fresh build"
    - "The module output auto-loads zero external resources (self-hosted fonts)"
    - "Committed content/module/ carries only synthetic fabricated names — no real-looking org/tool/metric/site/program nomenclature"
  artifacts:
    - path: "tests/test_modulesite.py"
      provides: "The end-to-end + trust-guard + determinism + confidentiality suite for the module corpus"
  key_links:
    - from: "tests/test_modulesite.py"
      to: "src/newsletters/modulesite.py::build_module_site"
      via: "end-to-end build → render assertions"
      pattern: "build_module_site"
    - from: "tests/test_modulesite.py"
      to: "content/module/"
      via: "committed==fresh-build + synthetic-name confidentiality check"
      pattern: "content/module"
---

<objective>
Land the end-to-end test suite that proves the worked example honors the REAL contract: every claim
traced+addressed, a populated honesty panel with the planted single-endpoint disclosure VISIBLE in
the rendered HTML, R-001 stable across rebuild (append-only proven), byte-stable double-render over
content/module/site, no external calls, committed==fresh-build, and a synthetic-name confidentiality
check over committed content/module/. This closes MODA-01 (gate-visible worked example) and MODA-02
(byte-stable double-render over the new corpus).

Purpose: the worked example is only proof if it is gate-visible and honest — assertions on
STRUCTURE and INVARIANTS (not lane names), so the example proves the contract, not a happy path.

Output: `tests/test_modulesite.py`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-worked-synthetic-module-report/03-CONTEXT.md
@.planning/research/PITFALLS.md
@tests/test_worksurface.py
@src/newsletters/modulesite.py
@src/newsletters/render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: End-to-end + trust-guard + honesty-panel tests</name>
  <files>tests/test_modulesite.py</files>
  <read_first>
    - tests/test_worksurface.py (the corpus-level patterns to MIRROR: traced-structure invariant
      assertions, `test_provenance_lineage_visible` reading the rendered HTML for `class="claim-span"`
      + `class="honesty"` + the missing[] text surfaced, `test_no_external_calls_in_work_output`,
      `test_work_site_byte_stable`.)
    - src/newsletters/render.py (`_honesty_panel`: missing[] entries render as
      `<li><span class="kind">unsubstantiated</span>{escaped}</li>` inside `class="honesty"`; every
      interpolation is `_e`-escaped. Claim spans render as `class="claim-span"`.)
    - src/newsletters/modulesite.py (build_module_surfaces / build_module_site)
  </read_first>
  <action>
    Create `tests/test_modulesite.py`. Assert on STRUCTURE and INVARIANTS (Pitfall 8: never assert a
    specific lane/metric string; where a value must be checked, read it from the same config the code
    reads). Do NOT edit conftest or any existing test.

    - `test_every_claim_traced_and_addressed`: over `build_module_surfaces()[0]`, every ClaimsBlock
      claim `is_traced` AND every trace `is_addressed` (closes Hole B); anything unprovable is in
      `surface.missing`, never a bare/untraced claim on a ClaimsBlock. Assert `surface.missing` is
      non-empty (the honesty panel is genuinely populated) AND at least one ClaimsBlock claim exists
      (not an empty body dumped into missing[] — Pitfall 11).
    - `test_single_endpoint_disclosure_visible_in_html(tmp_path)`: build the site into tmp_path, read
      the `report-module-a.html`, assert `class="honesty"` present and the single-endpoint disclosure
      text (the composer's "only one endpoint is usable" phrasing — read the substring from the
      composed surface.missing, don't hardcode a lane name) appears in the rendered HTML. Also assert
      `class="claim-span"` present (claim-beside-verbatim-trace renders — PROV-03).
    - `test_owner_quote_rendered_from_sourced_path(tmp_path)`: the rendered HTML contains the owner
      quote text (read from the composed QuoteBlock) — the sourced-or-omit path rendered a real,
      traced quote (not fabricated, not omitted).
    - `test_no_external_calls(tmp_path)`: mirror test_no_external_calls_in_work_output over the module
      output — no google-fonts host, no `url(http`, no `<link href="http">`, `src="http"`; and a
      self-hosted `fonts/*.woff2` present in the output.

    Every assertion reads config-derived values from the SAME surface/config the code reads, never a
    hardcoded lane/metric string — so the abstraction guard is honored.
  </action>
  <verify>
    <automated>python -m pytest tests/test_modulesite.py -x -q -k "traced or disclosure or quote or external"</automated>
  </verify>
  <done>
    Every module-surface claim is traced+addressed; the rendered HTML shows claim-beside-trace + a
    populated honesty panel including the visible single-endpoint disclosure; the owner quote renders
    from the sourced path; zero external calls with self-hosted fonts. No hardcoded config strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Determinism, ledger-stability, committed==fresh-build, confidentiality</name>
  <files>tests/test_modulesite.py</files>
  <read_first>
    - tests/test_worksurface.py (`test_work_site_byte_stable` — the double-render byte-equality
      pattern)
    - src/newsletters/site.py (Ledger.load/ref_for/save; append-only immutable-on-re-sight)
    - .planning/research/PITFALLS.md (Pitfall 7 ledger append-only / no R-001 collision; Pitfall 8
      confidentiality synthetic-name check on committed content)
  </read_first>
  <action>
    Extend `tests/test_modulesite.py` with the determinism + integrity suite:

    - `test_byte_stable_double_render(tmp_path)`: build into two dirs; assert identical file sets and
      byte-identical contents for every file (SITE-06 extended to the module corpus — MODA-02).
    - `test_r001_stable_across_rebuild(tmp_path)`: load a Ledger over a fresh tmp ids.json path,
      compose/assign a ref, save; reload + rebuild and assert the SAME ref (R-001) — the append-only
      ledger never renumbers on re-sight (prove by rebuild, not by asserting a literal). Assert the
      first assigned report ref is `R-001` for a fresh ledger.
    - `test_committed_equals_fresh_build(tmp_path)`: build into tmp_path, then compare each generated
      HTML + the fonts + the ledger JSON against the COMMITTED `content/module/site/` +
      `content/module/ids.json` byte-for-byte (the committed==fresh-build norm). Note in a comment:
      this is why Plan 01 committed a byte-stable render.
    - `test_no_datetime_now_reachable`: assert the composed surface `created` and the source
      `timestamp` are `EPOCH_ZERO` (no wall clock — Pitfall 5).
    - `test_committed_content_is_synthetic`: scan every committed file under `content/module/`
      (the .yml + the rendered site/*.html + ids.json) for a DENYLIST of real-looking nomenclature
      patterns and assert none appear; positively assert the fabricated scheme markers
      (`module-a`, `area-bem`, `owner-`, `eng-`, `toolset-`) are the naming used. Follow PITFALLS
      #confidentiality: fabricated names only, no real org/tool/metric/site/program names. Keep the
      denylist generic (real-name shapes), not a list of secrets.

    All file paths absolute or repo-root-relative; read committed content via the repo root
    (mirror test_worksurface's `Path(__file__).resolve().parent.parent`).
  </action>
  <verify>
    <automated>python -m pytest tests/test_modulesite.py -x -q -k "byte_stable or r001 or committed or datetime or synthetic"</automated>
  </verify>
  <done>
    Double-render is byte-stable; R-001 is stable across rebuild (append-only proven); committed
    content/module/ equals a fresh build; created/timestamp are EPOCH_ZERO; committed content passes
    the synthetic-name confidentiality check. Full `pytest tests/test_modulesite.py` green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config content → rendered HTML | Config-derived text crosses into the committed HTML (escaping + confidentiality) |
| committed content/module/ | Public-repo committed artifacts must carry no real nomenclature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Injection (XSS) | rendered HTML over config content | mitigate | Assert config content is `_e`-escaped in output (render.py's existing escaping is the protection — reuse verified, not re-implemented) |
| T-03-03 | Information Disclosure | committed content/module/ | mitigate | `test_committed_content_is_synthetic` denylist/allowlist check over ALL committed module content (Pitfall 8 / PITFALLS #confidentiality) |
| T-03-05 | DoS/Network | rendered output | mitigate | `test_no_external_calls` asserts self-hosted fonts + zero external fetch |
| T-03-08 | Tampering (determinism) | committed vs fresh build | mitigate | Byte-stable double-render + committed==fresh-build + EPOCH_ZERO tests |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase |
</threat_model>

<verification>
- `python -m pytest tests/test_modulesite.py -q` green.
- Enforced gates (re-run independently): full `pytest`, `lint-imports`, `newsletters check` over all
  corpora (rev1, work, module), byte-stable double-render holds. Advisory: black / isort --profile
  black / mypy on the new test — no NEW failures vs baseline.
</verification>

<success_criteria>
- Every module-surface claim traced+addressed; honesty panel populated with the visible
  single-endpoint disclosure; claim-beside-verbatim-trace renders.
- R-001 stable across rebuild; byte-stable double-render; committed==fresh-build.
- Zero external calls; committed content passes the synthetic-name confidentiality check.
</success_criteria>

<output>
Create `.planning/phases/03-worked-synthetic-module-report/03-03-SUMMARY.md` when done.
</output>
