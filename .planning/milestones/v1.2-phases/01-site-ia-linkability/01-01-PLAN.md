# Plan 01-01 — Records strip + 404 renderer + regeneration (PUB-03)

**Wave:** single. **Commits:** 2 atomic (code+tests+docs; regenerated corpora).
**Gates:** pytest full suite · lint-imports · `newsletters check` ×3 · double-render byte-stable
· ledgers unchanged.

## Tasks

### T1 — `render.py`: `_records_strip` + `records` params + `render_404` (+ CSS)
- `.nl-records` CSS after the `.nl-foot` block: mono 11px row, `--text-dim` label
  (uppercase, `.16em` tracking), links `--text` with hairline underline hover → `--signal`,
  `·` separators in `--line`; mobile padding via the existing 820px media query. Existing
  tokens only.
- `_records_strip(records)` → `<section class="nl-records" aria-label="Other records">` with
  label `Records on this site` + one `<a>` per `(label, href)`; `None`/empty → `""`.
- `render_home(site, *, theme="light", records=None)` / `render_library(site, *, theme="light",
  records=None)` — strip appended as a top-level section after the main content, before the
  footer (`_page` unchanged; per-surface pages untouched).
- `render_404(*, base_path="/newsletters/", theme="light")` — `_page`-rendered edge-state panel
  (eyebrow `404 — not in the record`, display headline, links to Home + Library under
  `base_path`); post-process the page's relative URLs (`url('fonts/`, footer
  `href="library.html"`, nav targets) to `base_path`-absolute. Deterministic string ops only.

### T2 — Builders declare neighbors (assembled-tree-relative)
- `dogfood.build_site`: rev1 Home + Library get `(("The work record","work/library.html"),
  ("The module record","module/library.html"))`.
- `worksurface.build_work_site`: `(("The Rev1 record","../index.html"),
  ("The module record","../module/library.html"))`.
- `modulesite.build_module_site`: `(("The Rev1 record","../index.html"),
  ("The work record","../work/library.html"))`.

### T3 — Tests
- `test_records_strip_on_chrome_pages_only` — strip present on rev1 index+library, work/module
  library; absent from every per-surface page.
- `test_records_strip_empty_renders_nothing` — `records=None` → no `nl-records` markup.
- `test_render_404_base_path_absolute` — every `href`/font `url(` in `render_404()` output is
  `base_path`-absolute; generated marker present.
- Adapt `test_no_dead_link_every_internal_href_resolves`: hrefs containing `/` must match the
  cross-corpus shapes (`work/…`, `module/…`, `../…`) — explicitly collected, resolver of
  record = Phase 2 assembled-tree test.

### T4 — Regenerate all three corpora (separate commit)
`newsletters build --corpus rev1|work|module`; assert `git diff --stat` touches ONLY
`content/*/site/*.html` (ledgers byte-identical); double-render + committed==fresh green.

### T5 — Spec delta
`docs/surfaces.md` § Navigation & IA: "Cross-corpus Records strip" + "404" subsections
(placement rules, chrome-only, base-path-absolute rationale, standalone-checkout caveat).

## Verification (independent re-run)
pytest -q (all green incl. new) · lint-imports · `newsletters check --corpus rev1|work|module`
· fresh double-build byte-compare · `git diff content/*/ids.json` empty.
