# Plan 02-01 — publish.py + assemble CLI + guarantees + workflows (PUB-01/02/04/05)

**Wave:** single. **Commits:** 3 atomic (publish module + CLI + tests; CI wiring; deploy rewrite).
**Gates:** pytest full suite · lint-imports · `newsletters check` ×3 · assembled click-through.

## Tasks

### T1 — `src/newsletters/publish.py` + `newsletters assemble` + tests
- `assemble_site(out_dir="dist/site", *, base_path="/newsletters/", repo_root=".") -> list[Path]`:
  layout `content/rev1/site → .` / `content/work/site → work/` / `content/module/site →
  module/`; missing corpus → `FileNotFoundError` (fail-loud); non-assembly `out_dir` → refuse;
  committed bytes copied verbatim (sorted walk); writes `.nojekyll` + `404.html`
  (`render_404(base_path)`); returns written paths.
- CLI `assemble --out dist/site --base-path /newsletters/` (lazy import). `dist/` gitignored.
- `tests/test_publish.py`:
  1. `test_assemble_composes_three_corpora_and_chrome`
  2. `test_assemble_is_byte_stable`
  3. `test_assemble_copies_committed_bytes_verbatim`
  4. `test_assemble_fails_loud_on_missing_corpus` / `test_assemble_refuses_foreign_out_dir`
  5. `test_assembled_internal_links_resolve` — every `href`/`src` in every assembled page
     resolves relative to its page dir; `404.html` asserted base-path-absolute against tree root
  6. `test_committed_rev1_equals_fresh_build` + `test_committed_work_equals_fresh_build`
     (mirror `test_modulesite.py::test_committed_equals_fresh_build`)
  7. `test_fonts_referenced_are_present` (every CSS font url resolves; OFL licenses travel)
  8. `test_every_assembled_page_carries_generated_marker`

### T2 — CI wiring (`.github/workflows/ci.yml`)
- `merge-block` job: `.[test,config]` install; `newsletters check` ×3 (in-YAML justification).
- New `site-integrity` job: `.[test,config]`; pytest over
  `tests/test_publish.py tests/test_render.py tests/test_site.py tests/test_worksurface.py
  tests/test_modulesite.py`.
- `bare-install` untouched.

### T3 — `.github/workflows/deploy-pages.yml` full rewrite
Trigger push→`main` (paths content/**, src/newsletters/**, tests/test_publish.py, self) +
ref-guarded dispatch; `permissions: contents: write`; steps: install `.[test,config]` → check ×3
→ pytest test_publish.py → warn-only Pages-source preflight → `newsletters assemble --out _site`
→ git init/commit/force-push `gh-pages` ("Publish record from ${GITHUB_SHA}"). Design notes as
YAML comments (channel rationale, RETRO link, mirror semantics).

## Verification (independent)
pytest -q · lint-imports · check ×3 · `newsletters assemble` + `python -m http.server`
click-through (strips, /module/report-module-a.html, fonts, 404) · `actionlint`-style read of
both workflows.
