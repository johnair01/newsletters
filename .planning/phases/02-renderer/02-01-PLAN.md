---
phase: 02-renderer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/newsletters/pptx_writer.py
  - tests/fixtures/weekly/_determinism.py
  - tests/fixtures/weekly/_author_template.py
  - tests/fixtures/weekly/_record_determinism_evidence.py
  - tests/fixtures/pptx/_author_fixtures.py
  - tests/test_pptx_determinism.py
  - tests/test_ai_optional.py
  - tests/test_pptx_writer.py
  - pyproject.toml
autonomous: true
requirements: [WKLY-01]

must_haves:
  truths:
    - "`newsletters.pptx_writer` imports on a bare install (no `[pptx]` extra) without importing python-pptx"
    - "`tests/fixtures/weekly/_determinism.py` no longer exists; there is exactly ONE OPC normalizer in the repo's writer path"
    - "No test or fixture script mutates `sys.path` to reach the normalizer (IN-03 closed)"
    - "`tests/test_pptx_determinism.py` and the evidence `--check` are green with zero committed binary regenerated"
    - "Five throwaway `.pptx` decks can be authored in-test on demand, covering group nesting, duplicate shape names, a template that owns the watermark name, a non-text `NL_` shape, and an empty-run slot"
  artifacts:
    - path: "src/newsletters/pptx_writer.py"
      provides: "The promoted stdlib OPC normalizer + the writer's shared constants (module level, bare-importable)"
      exports: ["DOS_EPOCH", "normalize_opc_zip", "part_digest", "differing_parts", "differing_zipinfo_fields", "SLOT_PREFIX", "WATERMARK_NAME", "MARKER", "DRAFT_STATUS", "WATERMARK_TEXT"]
      min_lines: 200
    - path: "tests/test_pptx_writer.py"
      provides: "The phase's proof module — scaffold: in-test deck builders + their self-test"
      min_lines: 150
    - path: "tests/test_ai_optional.py"
      provides: "The two SC-5 boundary guards for the new writer module"
      contains: "pptx_writer"
  key_links:
    - from: "tests/test_pptx_determinism.py"
      to: "newsletters.pptx_writer"
      via: "package import (replaces the sys.path.insert + `from _determinism import ...`)"
      pattern: "from newsletters\\.pptx_writer import"
    - from: "tests/fixtures/weekly/_author_template.py"
      to: "newsletters.pptx_writer"
      via: "package import"
      pattern: "from newsletters\\.pptx_writer import normalize_opc_zip"
    - from: "tests/fixtures/weekly/_record_determinism_evidence.py"
      to: "newsletters.pptx_writer"
      via: "package import"
      pattern: "from newsletters\\.pptx_writer import"
---

<objective>
Land the foundation the writer stands on: one canonical OPC normalizer living in
`src/newsletters/` (stdlib-only, bare-importable), the two AI-optional boundary guards that will
police the writer module for the rest of the milestone, and the in-test deck builders that make the
fail-loud contract testable against realistic decks.

Purpose: every later task in this phase either imports `newsletters.pptx_writer` or authors a deck
to exercise it. Doing the promotion first means the writer is added to a module that is already
guarded, already imported by the determinism suite, and already proven bare-safe — instead of
retrofitting the guard after the fact.

Output: `src/newsletters/pptx_writer.py` (normalizer half only — the writer half is plan 02-02),
`tests/test_pptx_writer.py` (scaffold + self-test), two new guards in `tests/test_ai_optional.py`,
a floor pin in `pyproject.toml`, and three fixture/test modules retargeted off `sys.path` games.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-renderer/02-CONTEXT.md
@.planning/phases/02-renderer/02-PATTERNS.md
@.planning/notes/2026-08-29-pptx-determinism-decision.md
</context>

## Recorded planning decisions (implement, do not reopen)

These were decided by the planner from `02-RESEARCH.md`'s open questions and the orchestrator's
constraints. Each is logged here so plans 02-02/02-03 and Phases 3/4 inherit them as inputs.

| # | Decision | Reason |
|---|----------|--------|
| **P-01** | Module is `src/newsletters/pptx_writer.py` — **no leading underscore** | It carries the phase's public entry point. `render.py` / `publish.py` / `casespec.py` are public module names; `_pptx_loader.py` earns its underscore by having no public surface. The decision note's binding content (destination package + lazy discipline) is satisfied either way (RESEARCH B1). |
| **P-02** | Public API is `render_surface_pptx_bytes` / `render_surface_pptx`, **not** `render_weekly_deck` | D-01 says the weekly reuses `Surface(REPORT)` and the renderer is an output *format*, not a semantic kind. A `weekly` in the writer's name smuggles the kind back into the format layer. |
| **P-03** | The writer takes an explicit `slots: Mapping[str, Sequence[str]]`; the Surface→slots derivation is Phase 3's | The four weekly block kinds do not exist yet; only the composer knows which authored content belongs in which Selection-Pane name (RESEARCH Open Q1, already recorded in `02-VALIDATION.md`). |
| **P-04** | `cp:contentStatus` is implemented **verbatim** per the decision note (`"draft"` if not published else `""`) | The note is binding and Phases 2 and 4 both copy its assertion text. The tri-state amendment (IN_REVIEW is labelled `"draft"`) is raised in the PR body, never deviated from silently (RESEARCH Open Q2). |
| **P-05** | **IN-04 REJECTED**: `_author_template._FIXED` stays `2026-01-01`, deliberately ≠ `EPOCH_ZERO` | Consolidating would set the *template's* `dcterms:created` to the exact value the writer is supposed to write, so the marker read-back assertion would pass even if the writer never set it — a false green in the phase whose method is "green ≠ green". A comment names it a falsifiability control (RESEARCH Open Q3 / B3). |
| **P-06** | The committed `tests/fixtures/weekly/template.pptx` stays **byte-unchanged** this phase; the rich and pathological decks are authored **in-test into `tmp_path`** | `.planning/notes/2026-08-29-pptx-determinism-evidence.json` records `part_digest_a`/`part_digest_b` computed *from that template*, and both are in the recorder's `CHECKED_FIELDS`. Regenerating the template turns the phase's own `determinism --check` gate red and makes the decision note's cited digests stale — retiring committed evidence to buy test convenience. RESEARCH B4's goal (realistic decks for the SC-1 tests) is met by in-test builders, which is how the research itself measured W17/W14. |
| **P-07** | RESEARCH Pitfall 3 mitigation 1 (`word_wrap=True` / `auto_size=NONE`) is applied to the **in-test** decks and carried forward to Phase 4's `docs/weekly.md` operator recipe for the shipped template | Follows from P-06. Recorded as a carry-forward, not dropped. |
| **P-08** | `tests/fixtures/pptx/_author_fixtures._normalize_zip` does **NOT** delegate this phase | Delegation swaps `_FIXED_ZIP_DATE_TIME` (2026-01-01) for `DOS_EPOCH` and adds `create_system=0`, so the nine ADAPT-06 binaries only stay correct if regenerated — which this phase's own pre-verification gate `git diff --exit-code -- tests/fixtures/pptx/*.pptx` forbids, and which the orchestrator constrained explicitly ("`test_pptx_golden` must stay green WITHOUT regenerating any golden binary"). A byte-neutral delegation would mean parameterizing the promoted module on the exact axes (`date_time`, `create_system`) the promotion exists to pin. **IN-02's second half is satisfied the other way CONTEXT permits — the fixture normalizer is *superseded*** (the weekly copy is deleted and its importers use the promoted module, which already carries the `create_system=0` pin). Task 1 fixes the now-stale pointer in `_author_fixtures.py`'s docstring and records the ADAPT-06 delegation as a Phase 4 carry-forward with its cost. |

## Artifacts this plan produces

| Kind | Name | Location |
|------|------|----------|
| module | `newsletters.pptx_writer` | `src/newsletters/pptx_writer.py` |
| constant | `DOS_EPOCH` | promoted verbatim |
| function | `normalize_opc_zip(raw: bytes) -> bytes` | promoted verbatim |
| function | `part_digest(raw: bytes) -> str` | promoted verbatim |
| function | `differing_parts(a: bytes, b: bytes) -> list[str]` | promoted verbatim |
| function | `differing_zipinfo_fields(a: bytes, b: bytes) -> list[str]` | promoted verbatim |
| function | `_reject_duplicate_member_names(archive)` | promoted verbatim (security-critical) |
| constant | `SLOT_PREFIX = "NL_"` | new |
| constant | `WATERMARK_NAME = "NL_DRAFT_WATERMARK"` | new |
| constant | `MARKER = "generated-by:newsletters"` | new |
| constant | `DRAFT_STATUS = "draft"` | new |
| constant | `WATERMARK_TEXT = "DRAFT"` | new |
| test module | `tests/test_pptx_writer.py` | new |
| test helper | `build_rich_template(path) -> pathlib.Path` | `tests/test_pptx_writer.py` |
| test helper | `build_duplicate_name_template(path)` | `tests/test_pptx_writer.py` |
| test helper | `build_watermark_owning_template(path)` | `tests/test_pptx_writer.py` |
| test helper | `build_nontext_slot_template(path)` | `tests/test_pptx_writer.py` |
| test helper | `build_empty_run_template(path)` | `tests/test_pptx_writer.py` |
| test constant | `RICH_SLOTS` | `tests/test_pptx_writer.py` |
| test | `test_test_decks_have_the_expected_shape_inventory` | `tests/test_pptx_writer.py` |
| test | `test_group_nesting_hides_a_slot_from_slide_shapes` (W17 as a repo-owned fact) | `tests/test_pptx_writer.py` |
| test | `test_pptx_writer_has_no_toplevel_pptx_import` | `tests/test_ai_optional.py` |
| test | `test_pptx_writer_imports_without_pptx` | `tests/test_ai_optional.py` |
| deleted | `tests/fixtures/weekly/_determinism.py` | superseded by the promotion |

<tasks>

<task type="auto">
  <name>Task 1: Promote the OPC normalizer into src/newsletters/pptx_writer.py and retire the fixture copy</name>
  <files>src/newsletters/pptx_writer.py, tests/fixtures/weekly/_determinism.py, tests/test_pptx_determinism.py, tests/fixtures/weekly/_author_template.py, tests/fixtures/weekly/_record_determinism_evidence.py, tests/fixtures/pptx/_author_fixtures.py</files>
  <read_first>
    - `tests/fixtures/weekly/_determinism.py` (the whole 203-line module — this is what moves)
    - `src/newsletters/adapters/_timestamps.py` (the stdlib-leaf placement precedent: `from __future__ import annotations`, `__all__` right after imports, WHY-first docstring)
    - `src/newsletters/adapters/_pptx_loader.py` lines 1–16 and 40–61 (the docstring shape and the lazy-import idiom the writer half will reuse in plan 02-02)
    - `tests/test_pptx_determinism.py` lines 55–90 (the import block being retargeted; `sys` becomes unused)
    - `tests/fixtures/weekly/_author_template.py` lines 50–65
    - `tests/fixtures/weekly/_record_determinism_evidence.py` lines 45–60 and its `CHECKED_FIELDS`
    - `tests/fixtures/pptx/_author_fixtures.py` lines 32–38 (the docstring paragraph whose pointer goes stale)
    - `.planning/phases/02-renderer/02-PATTERNS.md` §`src/newsletters/pptx_writer.py` Analogs B and C
  </read_first>
  <action>
Create `src/newsletters/pptx_writer.py` and move the five public names plus
`_reject_duplicate_member_names` and `_COMPARED_FIELDS` out of `tests/fixtures/weekly/_determinism.py`
into it **verbatim** — function bodies, comments and the module docstring's explanatory prose
carried across unchanged. RESEARCH §Security requires a reviewer to be able to diff the promoted
module against the fixture original and see that nothing was "tidied": in particular the in-memory
read/`writestr` loop must be preserved character-for-character, because the zip-slip property is
"closed by construction" only while no member name reaches the filesystem. Do not reformat, do not
rename locals, do not add parameters (see P-08 — parameterizing `date_time`/`create_system` would
dissolve the very pin the promotion exists to make canonical).

Extend the module docstring with a short WHY-first preamble in the house style used by
`_timestamps.py` and `_pptx_loader.py` — WHY THIS EXISTS → THE FIX → SCOPE OF THE CLAIM → SECURITY
PROPERTY → the guard that enforces it — stating: the module's top half is stdlib-only and therefore
importable on a bare install; the writer half (plan 02-02) obtains python-pptx lazily through the
existing `adapters._pptx_loader._load_pptx()` boundary; and there must never be a column-0
`import pptx` / `from pptx` line in this file, enforced by
`tests/test_ai_optional.py::test_pptx_writer_has_no_toplevel_pptx_import` (added in task 2).

Add the writer's shared string constants at module level with a one-line comment each naming the
decision-note field they carry: `SLOT_PREFIX = "NL_"`, `WATERMARK_NAME = "NL_DRAFT_WATERMARK"`,
`MARKER = "generated-by:newsletters"` (goes to `cp:category`), `DRAFT_STATUS = "draft"` (goes to
`cp:contentStatus` while not published, per P-04), `WATERMARK_TEXT = "DRAFT"`. Put every one of
them in `__all__` alongside the five promoted names. Follow the `_pptx_loader.MISSING_PPTX_MESSAGE`
precedent: constants exist so tests assert against them without string-duplication drift.

Do NOT add `pptx_writer` to `src/newsletters/__init__.py` — follow the `adapters` precedent
(callers do `from newsletters.pptx_writer import ...`); `__init__.py` eagerly imports its members
and widening it widens the bare-install blast radius for zero benefit.

Delete `tests/fixtures/weekly/_determinism.py`.

Retarget its three importers to plain package imports and delete the `sys.path.insert(...)` line in
each (this is what closes IN-03 — a `sys.path.insert` plus a stale `__pycache__/_determinism.*.pyc`
is exactly the shadowing mechanism the review warned about, and deleting the insert removes the
mechanism, not just the symptom):
`tests/test_pptx_determinism.py` (imports all four of `differing_parts`,
`differing_zipinfo_fields`, `normalize_opc_zip`, `part_digest`; drop the now-unused `import sys`),
`tests/fixtures/weekly/_author_template.py` (imports `normalize_opc_zip`; drop the unused `sys`
import and the `HERE`-based insert but keep `HERE` if it is still used for `TEMPLATE`), and
`tests/fixtures/weekly/_record_determinism_evidence.py` (same shape). Place each new import beside
the existing `from newsletters.adapters._timestamps import EPOCH_ZERO` line with the same
`# noqa: E402` comment, because it still sits below the module-level `importorskip`.

Delete the stale `tests/fixtures/weekly/__pycache__/` directory so no compiled `_determinism`
lingers.

In `tests/fixtures/weekly/_author_template.py`, add a comment on the `_FIXED = datetime(2026, 1, 1, 0, 0, 0)`
line naming it a **falsifiability control** per P-05: it is deliberately NOT `EPOCH_ZERO` so that
the marker read-back assertion in plan 02-02 fails if the writer never sets `dcterms:created`.
State that IN-04's literal consolidation is rejected for that reason.

In `tests/fixtures/pptx/_author_fixtures.py`, edit **only the docstring paragraph at lines 32–38**
(no code change, so the nine committed binaries stay byte-identical): update the pointer from
`tests/fixtures/weekly/_determinism.py` to `src/newsletters/pptx_writer.py`, and replace "delegates
to it then, not now" with the recorded P-08 disposition — the delegation is deferred past Phase 2
because it would swap `_FIXED_ZIP_DATE_TIME` for `DOS_EPOCH` and add `create_system=0`, requiring
all nine binaries to be regenerated, which Phase 2's own `git diff --exit-code -- tests/fixtures/pptx/*.pptx`
gate forbids; it is carried to Phase 4, where the committed==fresh `.pptx` gate work lives.

Finally, amend `tests/test_pptx_determinism.py::_render_bytes`'s docstring: it currently calls
itself "The Phase 2 writer in miniature", which becomes misleading the moment
`newsletters.pptx_writer.fill_slot` exists. Say instead that it is the *container* measurement, that
its `text_frame.text = ...` fill is deliberately naive, and that it must NOT be copied into the
writer — `tf.text` was measured to destroy the operator's run and paragraph formatting (RESEARCH
W3). Keep every existing assertion, especially the negative control
`test_unnormalized_double_write_differs_only_in_zip_date_time` (assertion C): RESEARCH names it the
assertion most likely to be dropped as redundant once the writer exists, and without it the byte
equality in plan 02-03 proves nothing.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pptx_determinism.py tests/test_pptx_golden.py tests/test_pptx_adapter.py -q</automated>
    <automated>.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check; echo "exit=$?"</automated>
    <automated>git diff --exit-code -- tests/fixtures/pptx/ tests/fixtures/weekly/template.pptx src/newsletters/semantic.py; echo "exit=$?"</automated>
    <automated>grep -rn "sys.path.insert" tests/test_pptx_determinism.py tests/fixtures/weekly/ | grep -v '^$'; echo "matches=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pptx_determinism.py tests/test_pptx_golden.py tests/test_pptx_adapter.py -q` exits 0 with **0 failures and 0 errors**.
    - `.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check` prints no `FAIL` and exits **0** (the committed `part_digest_a`/`part_digest_b` are unchanged, proving the template was not regenerated).
    - `git diff --exit-code -- tests/fixtures/pptx/ tests/fixtures/weekly/template.pptx src/newsletters/semantic.py` exits **0** — no committed binary and no gate source changed.
    - `git status --porcelain tests/fixtures/weekly/_determinism.py` reports the file **deleted**.
    - `grep -rn "sys.path.insert" tests/test_pptx_determinism.py tests/fixtures/weekly/` returns **no matches** (grep exit code 1).
    - `grep -c -v '^#' src/newsletters/pptx_writer.py` — the module contains `normalize_opc_zip`, `part_digest`, `differing_parts`, `differing_zipinfo_fields`, `DOS_EPOCH`, `SLOT_PREFIX`, `WATERMARK_NAME`, `MARKER`, `DRAFT_STATUS`, `WATERMARK_TEXT` in `__all__`.
    - `.venv/bin/python -c "import newsletters.pptx_writer as w; print(sorted(w.__all__))"` succeeds and lists all ten names.
    - `git diff -- tests/fixtures/pptx/_author_fixtures.py` touches **only** lines inside the module docstring.
  </acceptance_criteria>
  <done>One canonical OPC normalizer exists at `src/newsletters/pptx_writer.py`; `tests/fixtures/weekly/_determinism.py` is gone; no `sys.path` mutation remains in the weekly fixture path (IN-03 closed); the determinism suite, the golden corpus and the evidence `--check` are all green with zero binary regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: The two AI-optional boundary guards for the writer, and the python-pptx floor pin</name>
  <files>tests/test_ai_optional.py, pyproject.toml</files>
  <read_first>
    - `tests/test_ai_optional.py` lines 440–520 (`PPTX_LOADER_PATH`, `test_pptx_loader_has_no_toplevel_pptx_import`, `test_adapters_package_imports_without_pptx`, `test_pptx_extra_declared`, and `_req_name`)
    - `pyproject.toml` lines 33–46 (the `[pptx]` extra and the comment block above it)
    - `.planning/phases/02-renderer/02-PATTERNS.md` §`tests/test_pptx_writer.py` → "Guard-test analogs from `tests/test_ai_optional.py`"
  </read_first>
  <action>
Add two guard tests to `tests/test_ai_optional.py` — **not** to `tests/test_pptx_writer.py`. This
placement is load-bearing and must be stated in a comment: `test_pptx_writer.py` guards itself with
a module-scope `pytest.importorskip("pptx")`, so a bare install skips that whole file — a guard
that proves "this module imports WITHOUT the extra" cannot live in a module that is skipped when
the extra is absent. `test_ai_optional.py` has no importorskip and runs on the `bare-install` CI
job, which is the only place these two assertions mean anything.

Guard 1 — `test_pptx_writer_has_no_toplevel_pptx_import`. Mirror
`test_pptx_loader_has_no_toplevel_pptx_import` exactly: add a `PPTX_WRITER_PATH` module constant
beside the existing `PPTX_LOADER_PATH`, read the file's text, collect every line that
`startswith("import pptx")` or `startswith("from pptx")`, and assert the list is empty with a
failure message naming the offending lines and saying this breaks the bare install. Keep the
existing comment explaining that column-0 matching is the point — indented imports live inside
functions or a `TYPE_CHECKING` guard and are not executed on a bare runtime import.

Guard 2 — `test_pptx_writer_imports_without_pptx`. Copy the meta-path-blocker subprocess idiom from
`test_adapters_package_imports_without_pptx` verbatim, changing only the import lines under test to
`import newsletters` followed by `from newsletters import pptx_writer`, and keeping the final
`assert 'pptx' not in sys.modules` check inside the subprocess. Keep
`env = {**os.environ, "PYDANTIC_DISABLE_PLUGINS": "true"}` and `cwd=REPO_ROOT`. The assertion
message must explain what a red means: the writer module has acquired an eager python-pptx edge and
`pip install .` can no longer import it.

In `pyproject.toml`, change the `[pptx]` extra from `pptx = ["python-pptx"]` to
`pptx = ["python-pptx>=1.0.2"]`. Add a short comment on the line recording WHY a floor and not a
ceiling: python-pptx makes no documented byte-output-stability promise across versions, and the
recorded determinism decision is scoped in writing to a fixed (python-pptx, zlib) pair — 1.0.2 is
the exercised version. Note that this needs **no test edit**: `test_pptx_extra_declared` compares
`_req_name(r)`, which strips version specifiers, and that was verified by executing the live
`_req_name` against `"python-pptx>=1.0.2"` (RESEARCH W19 closes assumption A7). Do not add any other
package; the phase installs nothing new.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_ai_optional.py -q</automated>
    <automated>.venv/bin/lint-imports</automated>
    <automated>.venv/bin/python -m pytest tests/test_ai_optional.py::test_pptx_writer_imports_without_pptx tests/test_ai_optional.py::test_pptx_writer_has_no_toplevel_pptx_import tests/test_ai_optional.py::test_pptx_extra_declared -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_ai_optional.py -q` exits 0; the three named tests appear as **passed**, not skipped.
    - `.venv/bin/lint-imports` exits 0 and reports every contract **KEPT** (no new core→extra edge).
    - `grep -n 'python-pptx>=1.0.2' pyproject.toml` returns exactly one match inside the `[pptx]` extra.
    - The guard-2 subprocess exits 0, proving `newsletters.pptx_writer` imports with `pptx` blocked on `sys.meta_path`.
  </acceptance_criteria>
  <done>The writer module is policed by the same two guards that police the loader, both running on the bare-install job; the `[pptx]` extra carries a floor pin scoping the determinism claim to its exercised version, with `test_pptx_extra_declared` unchanged and green.</done>
</task>

<task type="auto">
  <name>Task 3: The in-test deck builders and their self-test (tests/test_pptx_writer.py scaffold)</name>
  <files>tests/test_pptx_writer.py</files>
  <read_first>
    - `tests/test_pptx_determinism.py` lines 55–90 (the `importorskip` block, the `FIXTURE_DIR`/`TEMPLATE` constants, and the comment explaining why `importorskip` rather than `pytestmark`)
    - `tests/fixtures/weekly/_author_template.py` lines 60–120 (`_CORE_PROPERTIES`, the `_SHAPES` table, `build_template_bytes`, and the normalize-before-write discipline)
    - `.planning/phases/02-renderer/02-RESEARCH.md` §Pattern 2 (W17 group recursion), §Pattern 4 (W14 duplicate watermark name), §Pitfall 3 (`word_wrap` / `auto_size`), §Pitfall 6 (the empty-run slot)
    - `.planning/phases/02-renderer/02-PATTERNS.md` §`tests/test_pptx_writer.py`
  </read_first>
  <action>
Create `tests/test_pptx_writer.py` with a WHY-first module docstring in the house style, stating
what this module proves (the five ROADMAP Phase 2 criteria), and stating P-06 explicitly: the
committed `tests/fixtures/weekly/template.pptx` is deliberately NOT regenerated by this phase
because `.planning/notes/2026-08-29-pptx-determinism-evidence.json` records `part_digest_a`/
`part_digest_b` computed from it and both are in the recorder's `CHECKED_FIELDS` — so the rich and
pathological decks this suite needs are authored into `tmp_path` here instead.

Open with the module-scope guard copied from `tests/test_pptx_determinism.py`: the
`pytest.importorskip("pptx", reason="optional [pptx] extra (python-pptx) not installed")` call, the
comment explaining that `importorskip` (not `pytestmark`) is required because a marker is evaluated
after import, and `Presentation = pptx.Presentation`. Then the `# noqa: E402` package imports:
`EPOCH_ZERO` from `newsletters.adapters._timestamps`, and `normalize_opc_zip`, `part_digest`,
`MARKER`, `DRAFT_STATUS`, `SLOT_PREFIX`, `WATERMARK_NAME`, `WATERMARK_TEXT` from
`newsletters.pptx_writer`. Also define `FIXTURE_DIR` and `COMMITTED_TEMPLATE` pointing at
`tests/fixtures/weekly/template.pptx` (plan 02-03 renders through it).

Write five deck builders. Each takes a `pathlib.Path` directory, writes exactly one `.pptx` into it,
and returns the written path. Each saves to `io.BytesIO`, passes the bytes through
`normalize_opc_zip`, and does a single `write_bytes` — the same discipline the writer itself uses,
so a deck built here is a fixed point of the normalizer and never contributes non-determinism to a
test. Every deck starts from `Presentation()` on `slide_layouts[6]` (blank) and sets the same
neutral, fabricated core properties as `_author_template._CORE_PROPERTIES` (no person, no org, no
third-party tool name — the abstraction guard and the scrub guard both apply to anything a test
writes). Every text frame gets `word_wrap = True` and `auto_size = MSO_AUTO_SIZE.NONE` (P-07 /
Pitfall 3: the `add_textbox` defaults are the worst combination — no wrapping plus a stored size
PowerPoint only fixes when a human edits the box).

`build_rich_template(directory)` — the happy-path deck the SC-1/fidelity/watermark tests bind
against. Two slides, authored in a fixed order so shape order is deterministic.
Slide 1 top level: `NL_WEEK_TITLE`, `NL_MODULE`, `NL_HIGHLIGHTS`, `NL_LOWLIGHTS`, a decorative
unprefixed `Footer`, and an unprefixed group shape named `Narrative Group` (created with
`slide.shapes.add_group_shape()`) containing a single textbox named `NL_INSIDE_GROUP`.
The group is deliberately **not** `NL_`-prefixed: it is a container, not a slot, and giving it the
reserved prefix would make it an unfilled reserved slot in every happy-path render. Slide 2 carries
one textbox named `NL_NEXT_WEEK`, so "the watermark is on every slide" is not a vacuous assertion.
`NL_HIGHLIGHTS` is the formatting-fidelity carrier: its paragraph 0 gets one run at `Pt(20)` bold,
and a `buChar` bullet injected into `p._p.get_or_add_pPr()` via `lxml.etree.SubElement` with the
DrawingML namespace and `char="•"`. Injecting bullet XML is legitimate **here and only here** —
this script stands in for an operator authoring their deck in PowerPoint; RESEARCH's ban on
synthesizing bullet XML applies to the *writer*, which must inherit formatting, never construct it.
Note in a comment that `SubElement` operates on an already-parsed tree and constructs no XML parser,
so the upstream `resolve_entities=False` mitigation is not weakened (threat T-02-03).

`build_duplicate_name_template(directory)` — two top-level textboxes both named `NL_WEEK_TITLE`.
Duplicate shape names are legal in OOXML and are how an operator's copy-paste presents; a naive
`{sh.name: sh}` comprehension is last-wins and silently drops a slot.

`build_watermark_owning_template(directory)` — one fillable `NL_WEEK_TITLE` plus a textbox already
named `NL_DRAFT_WATERMARK` (W14: adding the watermark to such a deck writes two shapes with one
name, silently).

`build_nontext_slot_template(directory)` — one fillable `NL_WEEK_TITLE` plus an empty group shape
named `NL_NOT_TEXT` (a group reports `has_text_frame == False`, so a naive
`by_name[n].text_frame.text = ...` raises `AttributeError` rather than a teaching error).

`build_empty_run_template(directory)` — one textbox named `NL_WEEK_TITLE` whose text frame is set to
the empty string, leaving paragraph 0 with **zero runs** and therefore no formatting carrier
(Pitfall 6).

Define a module constant `RICH_SLOTS` — the complete happy-path content mapping for the rich
template, `str -> list[str]`, covering all six `NL_` names including `NL_INSIDE_GROUP` and
`NL_NEXT_WEEK`, with `NL_HIGHLIGHTS` carrying three lines so the multi-line clone path is exercised
and one line containing `café — "smart" … 🎯 <&>` so Unicode and XML metacharacters are on the happy
path (W11).

Write two self-tests. `test_test_decks_have_the_expected_shape_inventory` builds the rich template
into `tmp_path`, reopens the written file, and asserts: slide count is 2; the recursive shape-name
set (top level plus group members, gathered by a small local walker) equals the exact expected set;
`NL_HIGHLIGHTS`'s paragraph 0 run reads back `font.size == Pt(20)` and `font.bold is True`; and its
`pPr` still contains a `buChar` element. `test_group_nesting_hides_a_slot_from_slide_shapes` asserts
that `NL_INSIDE_GROUP` is **absent** from `[sh.name for sh in slide.shapes]` while being present in
the recursive walk, and that `Narrative Group` reports `has_text_frame is False` — this turns
RESEARCH W17 from a research claim into a repo-owned, re-proved fact, and it is what makes the
group-recursive binding map in plan 02-02 falsifiable.

Every assertion carries a message explaining what a red means, not what the values were — the
`tests/test_pptx_determinism.py` house style.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pptx_writer.py -q</automated>
    <automated>.venv/bin/python -m pytest tests/test_pptx_determinism.py::test_weekly_fixture_corpus_is_exactly_the_committed_template tests/test_pptx_determinism.py::test_committed_template_is_scrubbed_and_normalized -q</automated>
    <automated>git diff --exit-code -- tests/fixtures/weekly/template.pptx; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pptx_writer.py -q` exits 0 with **2 passed, 0 skipped** in the repo `.venv` (python-pptx is installed there).
    - `test_test_decks_have_the_expected_shape_inventory` asserts the recursive name set equals exactly `{"NL_WEEK_TITLE", "NL_MODULE", "NL_HIGHLIGHTS", "NL_LOWLIGHTS", "NL_INSIDE_GROUP", "NL_NEXT_WEEK", "Footer", "Narrative Group"}`.
    - `test_group_nesting_hides_a_slot_from_slide_shapes` passes, proving `NL_INSIDE_GROUP` is absent from `slide.shapes` (W17).
    - `test_weekly_fixture_corpus_is_exactly_the_committed_template` still passes — the builders wrote into `tmp_path`, never into `tests/fixtures/weekly/`.
    - `git diff --exit-code -- tests/fixtures/weekly/template.pptx` exits **0**.
    - No builder writes outside its `directory` argument; `grep -n "FIXTURE_DIR /" tests/test_pptx_writer.py` shows `FIXTURE_DIR` used for reading only.
  </acceptance_criteria>
  <done>The proof module exists with five named deck builders covering every pathological template case the fail-loud contract must refuse, plus a self-test that re-proves W17 in-repo; the committed fixture corpus and its evidence are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator `.pptx` → normalizer | An untrusted binary (a zip of XML) is read; member names are attacker-controlled strings |
| fixture author script → committed repo binary | A generated artifact enters version control and becomes an assertion input |
| bare install → `newsletters` package | A runtime with no `[pptx]` extra must still import the package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `normalize_opc_zip` (promoted) | mitigate | `_reject_duplicate_member_names` is promoted verbatim and keeps raising; `tests/test_pptx_determinism.py::test_duplicate_member_names_are_refused_loudly` must stay green after the move (task 1 verify) |
| T-02-02 | Tampering (zip-slip) | `normalize_opc_zip` (promoted) | mitigate | Verbatim promotion of the in-memory `ZipFile.read(name)` / `ZipFile.writestr(ZipInfo, data)` loop — no `open()`, no `Path` join, no `extract`. Task 1 forbids reformatting so a reviewer can diff the move and see nothing was tidied |
| T-02-03 | Information disclosure (XXE) | in-test deck builders | mitigate | Mitigated upstream by python-pptx's `resolve_entities=False`; task 3 constructs **no** XML parser — `lxml.etree.SubElement` operates on an already-parsed tree. Stated in a comment so a later edit cannot reintroduce a parser silently |
| T-02-04 | Tampering | `[pptx]` extra contents | mitigate | Floor pin only (`python-pptx>=1.0.2`); no new package this phase. `test_pptx_extra_declared` keeps the extra to exactly one requirement |
| T-02-10 | Information disclosure | committed / in-test fixtures | mitigate | All fixture content is fabricated and neutral; builders write only into `tmp_path`; `test_weekly_fixture_corpus_is_exactly_the_committed_template` and the scrub guard both re-run in task 3's verify |
| T-02-11 | Tampering (module shadowing) | `sys.path.insert` + stale `__pycache__` | mitigate | Task 1 deletes the three `sys.path.insert` lines and the stale `__pycache__`, removing the shadowing mechanism rather than the symptom (IN-03) |
| T-02-SC | Tampering | package installs | mitigate | **No package is installed by this phase.** `python-pptx` is pre-existing; its audit verdict is `SUS` on the single signal `unknown-downloads` (the known PyPI-API false positive, already adjudicated in `pyproject.toml`'s `[pptx]` comment and re-adjudicated in `02-RESEARCH.md` §Package Legitimacy Audit). No `[SLOP]` package appears. No legitimacy checkpoint is required because nothing new is installed; the planner must not add any other package (no `python-pptx-interface`, no `pptx-templater`, no image helper) |
</threat_model>

<verification>
Run once per check, independently — "the agent says green" is not green:

1. `.venv/bin/python -m pytest -q` — full suite, no regression vs the 567 passed / 64 skipped baseline (plus this plan's new tests)
2. `.venv/bin/lint-imports` — every contract KEPT
3. `.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check` — exit 0
4. `git diff --exit-code -- src/newsletters/semantic.py tests/fixtures/pptx/ tests/fixtures/weekly/template.pptx` — exit 0
5. `git status --porcelain` shows `tests/fixtures/weekly/_determinism.py` deleted and no untracked `.pptx` anywhere
</verification>

<success_criteria>
- `src/newsletters/pptx_writer.py` exists, is stdlib-only at module level, and imports under a `sys.meta_path` python-pptx block (SC-5)
- Zero column-0 `import pptx` / `from pptx` lines in `src/newsletters/pptx_writer.py` (SC-5)
- `tests/fixtures/weekly/_determinism.py` deleted; exactly one `normalize_opc_zip` implementation reachable from the writer path
- No `sys.path.insert` remains under `tests/fixtures/weekly/` or in `tests/test_pptx_determinism.py` (IN-03 closed)
- `pyproject.toml` `[pptx]` reads `python-pptx>=1.0.2` and `test_pptx_extra_declared` passes unmodified (W19)
- `tests/test_pptx_writer.py` provides five named deck builders plus `RICH_SLOTS`, and re-proves W17 in-repo
- Zero committed `.pptx` binary changed; determinism `--check` exits 0
</success_criteria>

<output>
Create `.planning/phases/02-renderer/02-01-SUMMARY.md` when done. Record P-01..P-08 as decisions
taken (they are inputs for plans 02-02/02-03 and for Phases 3 and 4), and record two carry-forwards
explicitly: the ADAPT-06 `_author_fixtures._normalize_zip` delegation (P-08) and the operator-template
`word_wrap`/`auto_size` guidance for `docs/weekly.md` (P-07), both to Phase 4.
</output>
