---
phase: 08-site-content-model-stable-ids
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/newsletters/render.py
  - src/newsletters/dogfood.py
  - docs/surfaces.md
  - docs/architecture.md
  - tests/test_site.py
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "The rendered Library shows each surface's per-type ref (R-001 / EP01 / A-001), not a positional index"
    - "build_site() builds a Site from build_surfaces() via the ledger and renders Page-driven"
    - "Every existing content/rev1/site/*.html filename and internal href is byte-identical after the rewrite (no link rot)"
    - "docs/surfaces.md and docs/architecture.md document the ID convention + the Site/Collection/Page model"
  artifacts:
    - path: "src/newsletters/render.py"
      provides: "render_library(site) consuming Page.ref + Page.href (enumerate index removed)"
      contains: "def render_library"
    - path: "src/newsletters/dogfood.py"
      provides: "build_site() that builds + persists the Site/ledger and renders from it"
      contains: "Site.from_surfaces"
    - path: "docs/surfaces.md"
      provides: "L1 ID convention + Site/Collection/Page model documented in the spec"
      contains: "Site"
  key_links:
    - from: "src/newsletters/render.py"
      to: "src/newsletters/site.py"
      via: "render_library consumes Site/Page"
      pattern: "Site|Page"
    - from: "src/newsletters/dogfood.py"
      to: "src/newsletters/site.py"
      via: "build_site builds Site.from_surfaces + Ledger.save"
      pattern: "Site\\.from_surfaces"
    - from: "src/newsletters/render.py"
      to: "page.ref"
      via: "Library row renders the stable ref, not enumerate index"
      pattern: "\\.ref"
---

<objective>
Wire the existing renderer to be PAGE-DRIVEN and document the convention in the spec. Rewrite
`render_library()` to consume the `Site` model from Plan 01 — render `Page.ref` in place of the
positional `{i:02d}` (render.py:376) and `Page.href` for the link — removing the `enumerate` source
of the rot (render.py:373). Update `build_site()` (dogfood.py) to build a `Site` via
`Site.from_surfaces(build_surfaces(), ledger=...)`, persist the ledger, and render every page +
the Library from the Site. Then close the spec gap (L2): add the L1 ID convention and the
Site/Collection/Page model to `docs/surfaces.md` and `docs/architecture.md` IN THIS SAME CHANGE.

Purpose: this is where SITE-01 becomes user-visible — the regenerated Library stops renumbering on
reorder. Backward-compat is the hard constraint: `Page.slug == Surface.id` (L3) means every existing
`content/rev1/site/*.html` filename and href stays byte-identical, so no committed link rots.
Implements L2 (spec is source of truth — fill the gap), L3 (backward-compat), and the renderer
half of L6 (Page-driven wiring, NOT the Phase-9 IA/nav/Home/gate-board).

Output: edited `render.py` + `dogfood.py`, updated `docs/surfaces.md` + `docs/architecture.md`, and
a regression test asserting the existing site filenames/hrefs do not rot.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-site-content-model-stable-ids/08-CONTEXT.md
@.planning/phases/08-site-content-model-stable-ids/08-RESEARCH.md
@.planning/phases/08-site-content-model-stable-ids/08-01-SUMMARY.md   # the site.py API this plan consumes

# Files this plan edits (read once, extract the exact markup to preserve):
@src/newsletters/render.py    # render_library @369-396 (the enumerate loop @373, {i:02d} @376, href @375); keep intro/fanout markup verbatim
@src/newsletters/dogfood.py   # build_site @650-665 (write loop @657, listed filter @661)
@docs/surfaces.md             # "## The Hub / Library" @121; surface sections @80-117 — where the convention is documented
@docs/architecture.md         # "## 1. The typed semantic model" @9; kind enum @20 — where the model is documented
</context>

<interface_context>
From Plan 01 (`src/newsletters/site.py`, confirm against 08-01-SUMMARY):
- `Site.from_surfaces(surfaces, *, ledger) -> Site` — pure given a ledger; does NOT save.
- `Site.pages() -> list[Page]`; `Site.collections: list[Collection]`; `Site.by_slug(slug) -> Page|None`.
- `Page`: `slug`, `ref`, `title`, `kind`, `gate: ReviewState`, `signal_color: SignalColor`,
  `href` (== f"{slug}.html" == f"{surface.id}.html"), `issue`, `date`, `surface: Surface`.
- `Collection`: `kind`, `display_name`, `pages`.
- `Ledger.load(path)` / `Ledger.save()`.

Current render_library (render.py:369-396) reads off each Surface: `s.id` (href, line 375),
`{i:02d}` (the bug, line 376), `s.title` (377), `s.template.tagline` (378),
`s.template.signal_color.css_var` (375), `s.template.display_name` + `s.template.cadence.label`
(379), `s.gate.value` (380). All map to Page fields or `page.surface.template`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Make render_library Page-driven + build_site Site-driven (remove the positional index)</name>
  <files>src/newsletters/render.py, src/newsletters/dogfood.py</files>
  <action>
    render.py — rewrite `render_library` to accept a `Site` (signature `render_library(site, *,
    theme="light")`). Iterate `site.pages()` (preserving the same listing the old code produced — see
    dogfood listed filter below) and for each Page render the row using `page.ref` in place of the old
    `{i:02d}` (the rot fix, decision 2 / L6 renderer half) and `page.href` for the link. Read the
    remaining display values off `page.surface.template` (tagline, display_name, cadence.label) and
    `page` (title, signal_color.css_var, gate). Keep the intro/fan-out masthead markup (render.py:382-393)
    VERBATIM — do not touch nav (`_NAV_ITEMS`/`_nav`), the Home, or add a gate-state board (L6 —
    Phase 9). Continue escaping every rendered field via the existing `_e()` (Security T-08:
    HTML-injection). Remove the `enumerate(surfaces, 1)` import-of-position entirely.

    dogfood.py — update `build_site()`: load the ledger (`Ledger.load("content/rev1/ids.json")`),
    build `site = Site.from_surfaces(build_surfaces(), ledger=ledger)`, then `ledger.save()` (persist
    any newly-assigned refs append-only). Write each page to `out / page.href` (== `{slug}.html` ==
    `{surface.id}.html` today, so filenames are UNCHANGED, L3) via `render_surface(page.surface)`.
    Build the Library `Site` for the listed subset using the same filter as today (one representative
    newsletter: drop newsletters whose id != "newsletter-jj", dogfood.py:661) and pass that Site to
    `render_library`. Keep writing `index.html`. Do NOT change the output directory or filenames.

    Determinism/back-compat constraint: after this edit, re-rendering must produce byte-identical
    `content/rev1/site/*.html` filenames and identical hrefs to the committed ones; the only intended
    visible change is the Library row number switching from a positional `01..NN` to the stable
    `page.ref`. Verify the rendered set of filenames equals the current listing.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from newsletters.dogfood import build_site; import tempfile,os; d=tempfile.mkdtemp(); w=build_site(d); names=sorted(os.path.basename(p) for p in w); import pathlib; cur=sorted(p.name for p in pathlib.Path('content/rev1/site').glob('*.html')); assert names==cur, (names,cur); idx=(pathlib.Path(d)/'index.html').read_text(); assert 'R-001' in idx and 'EP01' in idx, 'refs not rendered'; assert 'lib-idx\">01<' not in idx, 'positional index still present'; print('page-driven render ok', len(names), 'files')" && .venv/bin/python -m pytest -q && .venv/bin/lint-imports</automated>
  </verify>
  <done>render_library consumes a Site and renders page.ref (no `{i:02d}`); build_site builds the Site,
  saves the ledger, and writes the identical filename set; the Library index contains R-001/EP01/A-001
  refs; full pytest + lint-imports green.</done>
</task>

<task type="auto">
  <name>Task 2: Document the ID convention + Site/Collection/Page model in the spec, and lock no-rot with a regression test</name>
  <files>docs/surfaces.md, docs/architecture.md, tests/test_site.py</files>
  <action>
    Close the spec gap (L2 — specs are source of truth; this is a gap not a conflict per 08-RESEARCH
    Q-B). In `docs/architecture.md` under "## 1. The typed semantic model" (near the kind enum @20),
    add a short subsection documenting the `Site → Collection → Page` content model: Site holds
    ordered Collections; Collection groups Pages by surface TYPE (ordered by template.distance);
    Page carries the stable identity (slug, ref, issue/date) + points at its Surface; ORDER is data,
    never the source of identity. Note the append-only ledger at `content/rev1/ids.json` is the
    source of truth for refs. In `docs/surfaces.md` (in "## The Hub / Library", @121, and/or a short
    "ID conventions" note) document the L1 per-type ref convention: Report `R-NNN` (3-digit), Article
    `A-NNN` (3-digit), Show/episode `EPNN` (2-digit), Newsletter keyed by `issue` + `date`, Learning
    `L-NNN`; slug is the content-derived canonical link key (defaults to Surface.id for Rev1); refs
    are ledger-assigned ONCE and never renumber on reorder. State WHY (criterion 2 / determinism).
    Do NOT document Phase-9 IA (Home/nav/gate-board) — out of scope (L6).

    Add a regression test to tests/test_site.py: `test_existing_links_do_not_rot` — build the Site for
    the Rev1 corpus and assert the set of `page.href` for the listed surfaces is exactly the set of
    existing `content/rev1/site/*.html` filenames (minus index.html), proving L3 backward-compat holds
    structurally (Pitfall 2 guard). Use the real corpus + the committed ledger (read-only; do not
    save).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_site.py::test_existing_links_do_not_rot -x -q && .venv/bin/python -c "import pathlib; s=pathlib.Path('docs/surfaces.md').read_text(); a=pathlib.Path('docs/architecture.md').read_text(); assert 'R-NNN' in s or 'R-001' in s, 'surfaces.md missing ID convention'; assert 'EP' in s, 'surfaces.md missing episode convention'; assert 'Collection' in a and 'Page' in a, 'architecture.md missing model'; print('spec documents convention + model')"</automated>
  </verify>
  <done>docs/architecture.md documents the Site/Collection/Page model; docs/surfaces.md documents the
  L1 ID convention with rationale; test_existing_links_do_not_rot passes; full suite + lint-imports
  green (run once at wave merge).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Page fields → rendered HTML | title/ref/href flow into the Library HTML; must stay escaped. |
| slug → output filename | build_site writes `out / page.href`; slug already sanitized in Plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / XSS | render_library row fields | mitigate | All Page fields rendered into HTML keep the existing `_e()` escape (render.py:167-168); no field bypasses html.escape. |
| T-08-05 | Information disclosure | dogfood build output | accept | Renders only the already-published Rev1 corpus to static HTML; no corpus/secret is serialized (existing Surface invariant). |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — pure stdlib + pydantic. No slopcheck needed. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — full suite green (incl. test_existing_links_do_not_rot).
- `.venv/bin/lint-imports` — 1 kept / 0 broken.
- `.venv/bin/mypy src/newsletters/render.py src/newsletters/dogfood.py` — clean.
- `build_site()` into a temp dir produces the identical filename set as `content/rev1/site/*.html`;
  the Library index renders stable refs (R-001/EP01/A-001) and no positional `lib-idx">01<`.
- `docs/surfaces.md` + `docs/architecture.md` document the convention + model (L2).
</verification>

<success_criteria>
- ROADMAP criterion 1: the Library is rendered from the Site/Collection/Page model carrying stable
  content-derived IDs (visible in the regenerated index.html). (Task 1)
- ROADMAP criterion 2 (user-visible half): reordering build_surfaces() no longer renumbers the
  Library — refs come from the Page/ledger, and every existing .html link target is preserved. (Task 1
  + test_existing_links_do_not_rot)
- SITE-01: renderer is Page-driven; the ID convention + content model are documented in the spec (L2).
</success_criteria>

<output>
Create `.planning/phases/08-site-content-model-stable-ids/08-02-SUMMARY.md` when done.
</output>
