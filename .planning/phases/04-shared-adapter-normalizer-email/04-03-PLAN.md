---
phase: 04-shared-adapter-normalizer-email
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/fixtures/eml/plain_simple.eml
  - tests/fixtures/eml/rfc2047_subject.eml
  - tests/fixtures/eml/multipart_alternative.eml
  - tests/fixtures/eml/html_only.eml
  - tests/fixtures/eml/mixed_with_pdf.eml
  - tests/fixtures/eml/forwarded_rfc822.eml
  - tests/fixtures/eml/mislabeled_charset.eml
  - tests/fixtures/eml/malformed_boundary.eml
  - tests/test_email_golden.py
autonomous: true
requirements: [ADAPT-06]
must_haves:
  truths:
    - "A committed corpus of small .eml fixtures drives EmailAdapter end-to-end and asserts the expected typed claims+traces+coverage"
    - "Every fixture asserts the zero-silent-drops accounting identity: len(claims) + len(coverage.unextracted) == expected total units walked"
    - "Every emitted claim is verbatim (claim.text == claim.evidence[0].span) and content-addressed (trace.is_addressed is True)"
    - "Coverage honesty is asserted per fixture: coverage.complete == (len(coverage.unextracted) == 0)"
    - "assert_conforms(EmailAdapter, [source]) passes for the corpus (drives span-containment + lossless JSON round-trip), and re-parsing a fixture twice yields an equal DistillationResult (determinism)"
  artifacts:
    - path: "tests/fixtures/eml/plain_simple.eml"
      provides: "text/plain ASCII, 2 paragraphs — the clean complete case"
    - path: "tests/fixtures/eml/forwarded_rfc822.eml"
      provides: "nested message/rfc822 — U1 routing fixture"
    - path: "tests/fixtures/eml/mislabeled_charset.eml"
      provides: "declares utf-8, contains a latin-1 byte — U3/U4 routing fixture"
    - path: "tests/test_email_golden.py"
      provides: "Golden-file test (ADAPT-06): 8 fixtures + accounting identity + faithful-span + conformance/round-trip + determinism"
      min_lines: 40
  key_links:
    - from: "tests/test_email_golden.py"
      to: "newsletters.adapters.email_adapter.EmailAdapter"
      via: "drives each fixture through distill() / assert_conforms"
      pattern: "EmailAdapter"
    - from: "tests/test_email_golden.py"
      to: "newsletters.distill.assert_conforms"
      via: "conformance + lossless JSON round-trip per fixture"
      pattern: "assert_conforms"
---

<objective>
Build the golden-file test corpus for the Email adapter (ADAPT-06, success criterion 3; CONTEXT
decision 4). Eight small committed `.eml` fixtures exercise every code path of the adapter — clean
plain mail, RFC2047 subject, multipart/alternative, HTML-only, mixed-with-PDF, forwarded rfc822,
mislabeled charset, and a malformed boundary — and the test asserts, for each, the zero-silent-drops
accounting identity (`#claims + #unextracted == #units walked`), verbatim + content-addressed claims,
coverage honesty, conformance + lossless JSON round-trip, and determinism.

Purpose: This is the phase's proof of correctness — the success criterion that "no content is silently
dropped" is only believable when a committed corpus demonstrates it across the full routing matrix.
Output: `tests/fixtures/eml/*.eml` (8 fixtures) and `tests/test_email_golden.py`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-adapter-normalizer-email/04-CONTEXT.md
@.planning/phases/04-shared-adapter-normalizer-email/04-RESEARCH.md   # "Golden-File Test Plan (Q6)" — fixture set + core assertions
@.planning/phases/04-shared-adapter-normalizer-email/04-02-SUMMARY.md  # the EmailAdapter contract + how it is constructed/invoked

# Interface contracts the test drives:
@src/newsletters/adapters/email_adapter.py  # EmailAdapter — distill(); name "email"
@src/newsletters/distill/conformance.py     # assert_conforms — span-containment + JSON round-trip (conformance.py:38-94)
@src/newsletters/distill/coverage.py        # Coverage(complete, unextracted) honesty contract
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the 8 committed .eml golden fixtures</name>
  <files>tests/fixtures/eml/plain_simple.eml, tests/fixtures/eml/rfc2047_subject.eml, tests/fixtures/eml/multipart_alternative.eml, tests/fixtures/eml/html_only.eml, tests/fixtures/eml/mixed_with_pdf.eml, tests/fixtures/eml/forwarded_rfc822.eml, tests/fixtures/eml/mislabeled_charset.eml, tests/fixtures/eml/malformed_boundary.eml</files>
  <action>
Create `tests/fixtures/eml/` and author the 8 minimal hand-written `.eml` fixtures from RESEARCH
"Golden-File Test Plan (Q6)" fixture set. Keep each a handful of lines; use explicit
`Content-Transfer-Encoding` and `charset` so the decode path is exercised, not incidental:

- `plain_simple.eml` — text/plain, ASCII, 2 paragraphs → 4 header + 2 body claims, complete=True.
- `rfc2047_subject.eml` — `=?utf-8?q?...?=` Subject with an em-dash / accented char → Subject claim
  text is the DECODED unicode, verbatim in the transcript.
- `multipart_alternative.eml` — text/plain + text/html alternatives → body claims come from the PLAIN
  part; the html alternative is NOT double-counted and NOT in unextracted (it is the same content).
- `html_only.eml` — text/html, no plain alternative → best-effort paragraph claims + a U5 entry;
  complete=False.
- `mixed_with_pdf.eml` — multipart/mixed, text body + a tiny `application/pdf` attachment → body claims
  + a U2 entry; complete=False. (Keep the attachment payload a couple of bytes — the adapter never
  decodes it, only records type/filename.)
- `forwarded_rfc822.eml` — a nested `message/rfc822` part → top body claims + a U1 entry; complete=False.
- `mislabeled_charset.eml` — declares `charset=utf-8` but the body contains a latin-1 byte (`0xe9`) →
  a U3 (and/or U4 if U+FFFD) entry; complete=False.
- `malformed_boundary.eml` — a multipart with a missing close boundary → a U7 entry from `defects`;
  still extracts the readable body.

Author the fixtures as raw bytes with correct CRLF/headers so the stdlib `email` parser exercises the
intended branch. These are committed test data (do NOT gitignore them).
  </action>
  <verify>
    <automated>cd /home/user/newsletters && .venv/bin/python -c "import email, email.policy, pathlib; d=pathlib.Path('tests/fixtures/eml'); files=sorted(d.glob('*.eml')); assert len(files)==8, [f.name for f in files]; [email.message_from_bytes(f.read_bytes(), policy=email.policy.default) for f in files]; print('8 fixtures parse:', [f.name for f in files])"</automated>
  </verify>
  <done>All 8 `.eml` fixtures exist under `tests/fixtures/eml/`, are committed (not gitignored), and each parses under `policy.default` exercising its intended routing branch.</done>
</task>

<task type="auto">
  <name>Task 2: Golden test — accounting identity, faithful spans, coverage honesty, conformance + determinism</name>
  <files>tests/test_email_golden.py</files>
  <action>
Write `tests/test_email_golden.py` driving each fixture in `tests/fixtures/eml/` through the
`EmailAdapter` (parametrize over the fixture set). Per RESEARCH "Core assertions (every fixture)":

1. Zero silent drops: `len(result.distillation.claims) + len(result.coverage.unextracted) ==
   expected_total_units` for that fixture (the accounting identity — CONTEXT decision 4 success
   criterion). Encode the expected per-fixture counts/routing in a table in the test (or alongside as
   `<fixture>.expected.json` compared via `model_dump()`, dropping any volatile field).
2. Faithful spans: for every claim, `claim.text == claim.evidence[0].span` AND
   `source.transcript[trace.start:trace.end] == claim.text` (re-slice check).
3. Content-addressed: every claim's trace `is_addressed is True`.
4. Coverage honesty: `coverage.complete == (len(coverage.unextracted) == 0)`.
5. Conformance + round-trip: `assert_conforms(EmailAdapter(...), [source])` passes for each fixture
   (drives span-containment + the lossless JSON round-trip, conformance.py:38-94).
6. Determinism: parsing the same fixture twice yields equal `DistillationResult` (no `now()` in claim
   text; timestamp from the Date header).

Add targeted per-fixture routing assertions (U1 in forwarded, U2 in mixed_with_pdf, U3/U4 in
mislabeled_charset, U5 in html_only, U7 in malformed_boundary; plain_simple complete with no drops;
multipart_alternative draws from plain with no double-count). Then re-run the standing-invariant gates
this plan must keep green: full pytest, mypy over the adapters package + distill, lint-imports, and
`newsletters build`.
  </action>
  <verify>
    <automated>cd /home/user/newsletters && .venv/bin/python -m pytest tests/test_email_golden.py -x -q && .venv/bin/python -m pytest -q && .venv/bin/python -m mypy src/newsletters/adapters src/newsletters/distill && .venv/bin/lint-imports && .venv/bin/python -m newsletters.cli build --out /tmp/site_p04_03</automated>
  </verify>
  <done>`tests/test_email_golden.py` passes across all 8 fixtures: the accounting identity holds (zero silent drops), claims are verbatim + content-addressed, coverage honesty is asserted, `assert_conforms` + JSON round-trip pass, and re-parsing is deterministic; full suite, mypy, lint-imports, and `newsletters build` all stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `.eml` fixtures → test harness | The fixtures stand in for untrusted real-world mail; the golden test is the proof that the adapter's untrusted-input handling (decode failures, defects, attachments) routes to `unextracted[]` rather than crashing or fabricating. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information disclosure (faithfulness) | a silent drop slipping through unnoticed | mitigate | The accounting-identity assertion (`#claims + #unextracted == #units walked`) per fixture makes a silent drop a TEST FAILURE — it is the executable form of CONTEXT decision 4's "no silent drops". |
| T-04-09 | Tampering | source drift / non-deterministic claim text | mitigate | The determinism assertion (re-parse twice → equal result) plus content-addressed traces (`is_addressed`) catch any `now()`/random leaking into claim text or hashing. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (stdlib-only; pytest already in `[dev]`); RESEARCH Package Legitimacy Audit is N/A; no legitimacy checkpoint required. |
</threat_model>

<verification>
- 8 committed fixtures exist under `tests/fixtures/eml/` and parse under `policy.default`.
- `tests/test_email_golden.py` asserts, per fixture: accounting identity, verbatim + content-addressed
  claims, coverage honesty, `assert_conforms` (span-containment + JSON round-trip), determinism.
- Standing invariants green: full `pytest`, `mypy src/newsletters/adapters src/newsletters/distill`,
  `lint-imports`, and `newsletters build`.
</verification>

<success_criteria>
ADAPT-06 (success criterion 3) is met: a golden-file test (fixture `.eml` → expected typed
claims+traces) covers the Email adapter and asserts zero silent drops across the full routing matrix.
No new dependency; the AI-optional contract stays green.
</success_criteria>

<output>
Create `.planning/phases/04-shared-adapter-normalizer-email/04-03-SUMMARY.md` when done.
</output>
