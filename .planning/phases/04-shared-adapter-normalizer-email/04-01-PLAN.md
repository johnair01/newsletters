---
phase: 04-shared-adapter-normalizer-email
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/newsletters/adapters/__init__.py
  - src/newsletters/adapters/normalize.py
  - tests/test_normalize.py
autonomous: true
requirements: [ADAPT-01]
must_haves:
  truths:
    - "A shared normalize(source, units) mints a typed Claim(+Trace) for every unit it can locate verbatim in source.transcript"
    - "normalize() locates each unit by a cursor-advancing str.find, so two identical units resolve to DISTINCT offsets (correct per-claim provenance)"
    - "Any unit that is not verbatim-locatable is routed to a returned unextracted[] list, never fabricated and never approximated"
    - "Every Claim normalize() emits is content-addressed: claim.text == trace.span == source.transcript[start:end] and trace.is_addressed is True"
    - "The faithful-extraction rule (verbatim-or-unextracted) lives in exactly ONE place — normalize() — and adapters never hand-mint hashes"
  artifacts:
    - path: "src/newsletters/adapters/normalize.py"
      provides: "The single shared faithful normalize() — cursor-advancing offset location + Trace.from_source minting"
      exports: ["normalize"]
      min_lines: 30
    - path: "src/newsletters/adapters/__init__.py"
      provides: "The adapters package; exports normalize"
      exports: ["normalize"]
    - path: "tests/test_normalize.py"
      provides: "Unit coverage for ADAPT-01: offset location, duplicate disambiguation, non-locatable routing, content-addressing"
  key_links:
    - from: "src/newsletters/adapters/normalize.py"
      to: "newsletters.semantic.Trace.from_source"
      via: "the SOLE trace-minting call (adapters never compute hashes)"
      pattern: "Trace\\.from_source"
    - from: "src/newsletters/adapters/normalize.py"
      to: "newsletters.distill.coverage.Unextracted"
      via: "non-locatable units appended to the returned unextracted list"
      pattern: "Unextracted\\("
---

<objective>
Build the shared `normalize()` — the ONE place the faithful-extraction rule lives (ADAPT-01, success
criterion 1; CONTEXT decision 1). It converts an adapter's raw "units" (verbatim strings the adapter
sliced from the transcript) into typed `Claim(+Trace)` by locating each unit in `Source.transcript`
via a cursor-advancing `str.find` and minting the trace through `Trace.from_source`. Anything not
verbatim-locatable is routed to a returned `unextracted[]` list — never fabricated.

This is the leaf dependency for the whole phase: the Email adapter (Plan 02) imports it. Keeping the
trust rule here (and ONLY here) is the architectural point of the phase — adapters do format-specific
extraction; `normalize()` is the single faithfulness gate they all pass their units through.

Purpose: Establish "one trust rule, one place" so every present and future adapter (Excel, PPTX, Power
BI) inherits identical faithful minting without reimplementing it.
Output: New `src/newsletters/adapters/` package with `normalize.py`; `tests/test_normalize.py`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-shared-adapter-normalizer-email/04-CONTEXT.md
@.planning/phases/04-shared-adapter-normalizer-email/04-RESEARCH.md

# Interface contracts normalize() mints against (read these — they ARE the contract):
@src/newsletters/semantic.py        # Source, Claim, Trace.from_source (semantic.py:109-153) — the SOLE hash-minting site; validates offsets BEFORE slicing
@src/newsletters/locators.py        # FreeLocator — the anchor for an unextracted unit; ordinal/positional anchors are FORBIDDEN
@src/newsletters/distill/coverage.py # Unextracted(locator, reason) — one unreadable gap
@src/newsletters/distill/faithfulness.py # SpanContainmentFaithfulness._normalize (faithfulness.py:42-74) — the gate normalize()'s output must survive
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the adapters package and the shared normalize() with cursor-advancing location</name>
  <files>src/newsletters/adapters/__init__.py, src/newsletters/adapters/normalize.py</files>
  <behavior>
    - Given a Source with transcript "Hello.\n\nWorld." and units ["Hello.", "World."], normalize returns 2 Claims and 0 unextracted; each claim.text equals the corresponding unit; each claim.evidence[0].span equals claim.text; each trace.is_addressed is True.
    - Duplicate disambiguation: transcript "ab\n\nab" with units ["ab", "ab"] returns 2 Claims whose traces have DISTINCT (start,end) — the second resolves to the later occurrence via the advancing cursor (start 4, not 0).
    - Non-locatable: a unit not present verbatim in the transcript (or whose verbatim text was already consumed before the cursor) yields NO Claim and ONE Unextracted whose reason names "not verbatim-locatable" and whose locator is a FreeLocator carrying a truncated preview of the unit (content anchor, never an ordinal index).
    - Content-addressing: every emitted trace was minted via Trace.from_source (trace.content_hash == source.content_hash(); source.transcript[trace.start:trace.end] == claim.text).
    - Empty units list returns ([], []).
  </behavior>
  <action>
Create `src/newsletters/adapters/__init__.py` (new package) and `src/newsletters/adapters/normalize.py`.

Implement `normalize(source: Source, units: list[str]) -> tuple[list[Claim], list[Unextracted]]` per
RESEARCH "Cursor-advancing offset location (Q5)" (the heart of normalize). Single forward pass with a
running `cursor`: for each unit, `idx = transcript.find(unit, cursor)`; if `idx == -1`, append an
`Unextracted(locator=FreeLocator(text=unit[:60]), reason="unit not verbatim-locatable in transcript
(faithful, not suggestive)")` and continue (NEVER fabricate); else `end = idx + len(unit)`, mint
`trace = Trace.from_source(source, idx, end)`, append `Claim(text=unit, evidence=[trace])`, and set
`cursor = end`. This advancing cursor is what disambiguates duplicate paragraphs (RESEARCH Pitfall 5).

This module is the ONLY place the faithful rule lives (CONTEXT decision 1). It MUST NOT import `email`,
compute any SHA-256, or hand-mint a `content_hash` — `Trace.from_source` is the sole minting path
(it already validates offsets before slicing and pins the hash, semantic.py:109-153). Do NOT use a
positional/ordinal locator — FORBIDDEN per locators.py:17-19; the FreeLocator preview is a content
anchor. Export `normalize` from `adapters/__init__.py`. Keep the package stdlib-only (no AI import
reachable) so the existing import-linter `forbid-ai-in-core` contract (source_modules=newsletters,
which already covers the new sub-package) stays KEPT with no `.importlinter` edit.
  </action>
  <verify>
    <automated>cd /home/user/newsletters && .venv/bin/python -c "from newsletters.adapters import normalize; from newsletters.semantic import Source; s=Source(id='x', transcript='ab\n\nab'); c,u=normalize(s,['ab','ab']); assert len(c)==2 and len(u)==0; assert c[0].evidence[0].start==0 and c[1].evidence[0].start==4; assert all(cl.evidence[0].is_addressed for cl in c); print('normalize OK')"</automated>
  </verify>
  <done>`normalize()` mints verbatim, content-addressed Claims via `Trace.from_source`; duplicate units get distinct offsets; non-locatable units route to a returned `unextracted[]` with a content (non-ordinal) locator; the adapters package imports with zero AI edge.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for normalize() (ADAPT-01) and standing-invariant gates</name>
  <files>tests/test_normalize.py</files>
  <action>
Write `tests/test_normalize.py` covering every behavior in Task 1's `<behavior>` block plus the
gate-survival property that makes the phase work:

- happy path (N units -> N claims, 0 unextracted; claim.text == unit == trace.span);
- duplicate-paragraph disambiguation (distinct offsets via the advancing cursor);
- non-locatable routing (1 unextracted, 0 claims; reason mentions "not verbatim-locatable"; locator is
  a FreeLocator content anchor, NOT an ordinal);
- content-addressing (`trace.is_addressed`, `trace.content_hash == source.content_hash()`,
  re-slice identity `source.transcript[trace.start:trace.end] == claim.text`);
- gate survival: build the emitted claims and assert
  `SpanContainmentFaithfulness().entails(claim) is True` for each (proves normalize's output passes the
  Phase-3 gate it must survive, faithfulness.py:62-74);
- empty units -> ([], []).

Use the real `Source`/`Trace`/`Claim` types (no mocks). Then re-run the standing-invariant gates this
plan must keep green: full pytest, mypy over the new package + distill, lint-imports.
  </action>
  <verify>
    <automated>cd /home/user/newsletters && .venv/bin/python -m pytest tests/test_normalize.py -x -q && .venv/bin/python -m mypy src/newsletters/adapters src/newsletters/distill && .venv/bin/lint-imports && .venv/bin/python -m pytest -q</automated>
  </verify>
  <done>`tests/test_normalize.py` passes; mypy clean over `adapters` + `distill`; `lint-imports` reports the AI-forbidden contract KEPT; the full suite stays green (no regression on the 85 baseline tests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter raw units → normalize() | Units are strings an adapter sliced from a transcript built from untrusted input (`.eml` bytes, Plan 02). normalize() must treat a unit it cannot locate as a gap, never as a fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | normalize() span location | mitigate | Mint exclusively via `Trace.from_source`, which validates `0 <= start <= end <= len(transcript)` before slicing (semantic.py:130-145) — a bad offset raises, never silently clips/mis-attributes. |
| T-04-02 | Information disclosure (faithfulness) | a unit that is not a verbatim substring | mitigate | `find == -1` routes to `unextracted[]`; normalize NEVER approximates or fabricates a span — the verbatim-or-unextracted rule. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (stdlib-only, CONTEXT decision 5); RESEARCH Package Legitimacy Audit is N/A. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `normalize()` exists in `src/newsletters/adapters/normalize.py`, exported from the package.
- Verbatim-or-unextracted rule lives only here; no SHA-256 / hand-minted hash in the adapter code
  (`grep -v '^#' src/newsletters/adapters/normalize.py | grep -c 'hashlib\|sha256'` is 0).
- Standing invariants green: full `pytest`, `mypy src/newsletters/adapters src/newsletters/distill`,
  `lint-imports`.
</verification>

<success_criteria>
ADAPT-01 (success criterion 1) is met: a shared `normalize()` converts raw extraction into typed
`Claim(+Trace)` with source locators, content-addressed via `Trace.from_source`; non-locatable units
route to `unextracted[]`; the faithful-extraction rule lives in exactly one place. All standing
invariants remain green.
</success_criteria>

<output>
Create `.planning/phases/04-shared-adapter-normalizer-email/04-01-SUMMARY.md` when done.
</output>
