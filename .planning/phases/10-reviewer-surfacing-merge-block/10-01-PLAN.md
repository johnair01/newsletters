---
phase: 10-reviewer-surfacing-merge-block
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/newsletters/semantic.py
  - src/newsletters/review.py
  - tests/test_review.py
autonomous: true
requirements: [PROV-04]
must_haves:
  truths:
    - "A published Surface can carry an open missing[] list onto the rendered/checked layer (Surface.missing carrier)"
    - "review_blockers() returns a STALE Blocker for a published claim whose trace drifted from its source"
    - "review_blockers() returns an UNENTAILED Blocker for a published addressed claim whose span omits the claim text"
    - "review_blockers() returns an OPEN_MISSING Blocker for a published surface with a non-empty missing[]"
    - "review_blockers() returns [] for a Draft or In-Review surface (published-only scope)"
    - "review.py imports no AI module"
  artifacts:
    - path: "src/newsletters/review.py"
      provides: "Blocker model + review_blockers() pure AI-free function"
      contains: "def review_blockers"
    - path: "tests/test_review.py"
      provides: "The gate-FIRES negative tests (STALE / unentailed / open-missing) + draft-exempt + no-AI guard"
      contains: "def test_blocks_stale_published_claim"
  key_links:
    - from: "src/newsletters/review.py"
      to: "src/newsletters/semantic.py"
      via: "Claim.is_stale + Surface.is_published + Surface.missing"
      pattern: "is_stale|is_published|\\.missing"
    - from: "src/newsletters/review.py"
      to: "src/newsletters/distill/faithfulness.py"
      via: "SpanContainmentFaithfulness().entails"
      pattern: "SpanContainmentFaithfulness"
---

<objective>
Add the `Surface.missing` carrier (L1) and build the deterministic, AI-free merge-block
checker (L2) — `Blocker` + `review_blockers(surface, sources) -> list[Blocker]` — proven to
FIRE by three crafted PUBLISHED negative fixtures (L7). This is the trust core of PROV-04: one
place that decides whether a published surface is safe to merge, reusing the existing STALE and
faithfulness predicates (no new trust logic).

Purpose: PROV-04 needs a single, testable, pure function the CLI and CI both call. Today
`Surface` has no `missing[]` field, so an unsubstantiated item has no path onto a rendered/checked
surface — L1 closes that plumbing gap. The checker must be PROVEN to block, not merely to pass a
clean corpus (the Phase-7 lesson); the negative fixtures are the actual proof PROV-04 holds.

Output: `src/newsletters/review.py` (new), an optional `Surface.missing` field on `semantic.py`,
and `tests/test_review.py` (new) with the carrier round-trip + the three gate-fires negatives +
draft/in-review exemption + the no-AI guard. (The review-view spec in `docs/surfaces.md` —
including the published-only block scope — is owned by Plan 03, where the panel/inline-span are
built; keeping a single spec-edit owner avoids a same-wave file conflict.)
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-CONTEXT.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-RESEARCH.md
@src/newsletters/semantic.py
@src/newsletters/distill/faithfulness.py
@tests/test_faithfulness_gate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the optional Surface.missing carrier (L1)</name>
  <files>src/newsletters/semantic.py, tests/test_review.py</files>
  <behavior>
    - A Surface constructed with no missing key validates and defaults missing == [] (backward-compat).
    - A Surface(..., missing=["unsubstantiated X"]) round-trips through model_dump_json() / model_validate_json() preserving the list.
    - A published Surface with a non-empty missing[] still publishes (the carrier is additive; it does NOT change the publish/review gate — invariant 2 + no-auto-publish unchanged).
    - missing[] carries only plain str entries — no Corpus / Source / Distillation object — so invariant 3 (private corpus never serialized) is preserved.
  </behavior>
  <action>
    Add one additive field to the `Surface` model in semantic.py (the field set is at
    semantic.py:474-485): `missing: list[str] = Field(default_factory=list)` with a docstring
    explaining it is the PROV-03 carrier — the surface-level mirror of `Distillation.missing`,
    populated at the capture/promote seam in a later plan, carrying NO corpus data so it is
    invariant-3-safe. Place it near `traces` / `audience_label`. Do NOT touch the Review gate,
    `publish`, `open_pull_request`, or `content_hash` — this is a pure carrier (per L1, additive,
    invariant-3-safe). Mirror the existing optional-additive style of `Source.extraction`
    (semantic.py:65) for the field comment. Start the new `tests/test_review.py` file in this task
    with the carrier round-trip + backward-compat tests above (the file is extended by Task 3).
    Keep `from __future__ import annotations` and the existing import ordering intact.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_review.py -x -q -k "carrier or missing or roundtrip"</automated>
  </verify>
  <done>Surface.missing exists, defaults to [], round-trips through JSON, does not alter the publish gate; new tests/test_review.py covers the carrier; full pytest still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build review.py — Blocker + review_blockers() (L2, L3)</name>
  <files>src/newsletters/review.py</files>
  <behavior>
    - review_blockers(draft_surface, sources) == [] and review_blockers(in_review_surface, sources) == [] (published-only scope, L3).
    - For a published surface with a STALE claim (a drifted addressed trace), review_blockers returns a Blocker(kind=STALE) for that claim.
    - For a published surface with an addressed claim whose span does NOT contain the claim text, returns a Blocker(kind=UNENTAILED).
    - For a published surface with a non-empty Surface.missing, returns one Blocker(kind=OPEN_MISSING) per entry.
    - STALE is checked first and elif-guards UNENTAILED, so a drifted addressed claim reports STALE (the truer diagnosis), not UNENTAILED.
    - A published surface with all-clean claims and empty missing[] returns [].
  </behavior>
  <action>
    Create `src/newsletters/review.py` as a TOP-LEVEL sibling of render.py (NOT inside distill/,
    which must stay modality-agnostic — per L2). Define `BlockerKind(StrEnum)` with members
    `STALE="stale"`, `UNENTAILED="unentailed"`, `OPEN_MISSING="open_missing"`; a Pydantic
    `Blocker(surface_id: str, kind: BlockerKind, detail: str, locator: str = "")` mirroring the
    `Unextracted`/`Claim` style (coverage.py:30). Implement
    `review_blockers(surface: Surface, sources: dict[str, Source] | None = None) -> list[Blocker]`:
    return `[]` immediately when `not surface.is_published` (semantic.py:501, L3 published-only
    scope); when `sources is None` self-build `{s.id: s for s in surface.traces}` (mirroring
    `Distillation.stale_claims`, semantic.py:318). Collect claims from every `ClaimsBlock` in
    `surface.blocks` (mirror `Surface._published_claims`, semantic.py:504). For each claim:
    if `claim.is_stale(sources)` (semantic.py:199) append a STALE Blocker; `elif not
    SpanContainmentFaithfulness().entails(claim)` (faithfulness.py:62) append an UNENTAILED Blocker
    — STALE first, elif-guarded, per L2. Then for each entry in `surface.missing` append an
    OPEN_MISSING Blocker. Set `detail = claim.text[:80]` (truncated) for claim blockers and the
    verbatim missing entry for OPEN_MISSING; `locator` may carry the trace source_id when present.
    REUSE the existing predicates — do NOT re-derive STALE or span-containment (anti-pattern in
    RESEARCH). Do NOT mutate the surface and do NOT route to missing[] (that is
    route_unfaithful_to_missing's job at the distill boundary). Import ONLY `..semantic` (relative:
    `from .semantic import ...`) and `from .distill.faithfulness import SpanContainmentFaithfulness`
    — both AI-free — plus stdlib + pydantic, to keep the import graph acyclic and the bare install
    green. Add `from __future__ import annotations` and a module docstring stating the AI-optional /
    one-trust-rule-one-place contract.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys, newsletters.review; bad=[m for m in ('langchain','langgraph','langsmith','pydantic_ai','openai','anthropic') if m in sys.modules]; assert not bad, bad; print('review.py AI-free')"</automated>
  </verify>
  <done>review.py exists with Blocker + review_blockers; published-only scope; STALE/UNENTAILED/OPEN_MISSING paths implemented reusing is_stale + entails; imports no AI; lint-imports stays 1 kept / 0 broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the gate FIRES — three crafted PUBLISHED fixtures + no-AI guard (L7)</name>
  <files>tests/test_review.py</files>
  <behavior>
    - test_blocks_stale_published_claim: mint Trace.from_source(src, 0, len(transcript)) onto a published claim, then mutate src.transcript so the live hash drifts -> review_blockers returns exactly one Blocker(kind=STALE).
    - test_blocks_unentailed_published_claim: an addressed Trace.from_source over a source whose span does NOT contain the claim text -> exactly one Blocker(kind=UNENTAILED) (mirror test_faithfulness_gate.py:68).
    - test_blocks_open_missing_published_surface: a published surface with missing=["..."] -> exactly one Blocker(kind=OPEN_MISSING) per entry.
    - test_draft_surface_is_exempt + test_in_review_surface_is_exempt: the same blocker content on a Draft / In-Review surface -> review_blockers == [] (L3).
    - test_clean_published_surface_has_no_blockers: a published surface with an addressed, entailed, non-stale claim and empty missing[] -> [].
    - test_review_module_imports_no_ai: a subprocess importing newsletters.review loads no AI module (copy test_faithfulness_gate.py:134-143, swapping the module + the AI_MODULES tuple).
  </behavior>
  <action>
    Extend `tests/test_review.py` (started in Task 1) with the six behaviors above. Build each
    fixture INLINE (do not add a blocker to the shipped dogfood corpus — that would correctly fail
    the CI gate; keep fixtures in tests/, per RESEARCH Q-A). To construct a published surface, build
    a `Surface` with a real `SurfaceTemplate` (use a preset whose ReviewPolicy a single approval
    satisfies — e.g. the Report template; check templates.py for `min_approvals=1`,
    `require_peer=False`) carrying a `ClaimsBlock`, then call `surface.publish(reviewer="r")`
    (semantic.py:534). For STALE: mint the trace with `Trace.from_source(src, 0, len(src.transcript))`
    (semantic.py:126) so it is addressed and entailing, put it on the claim, publish, THEN
    `src.transcript = src.transcript + " drift"` so `claim.is_stale({src.id: src})` is True (mirror
    the drift pattern in tests/test_provenance_migration.py). For UNENTAILED: address a trace over a
    source whose transcript text does NOT contain the claim text. For OPEN_MISSING: set
    `missing=["unsubstantiated: <text>"]` on the published surface. Assert the exact `Blocker.kind`
    and count for each. For the no-AI guard, define a module-level `AI_MODULES` tuple and copy the
    subprocess pattern verbatim from test_faithfulness_gate.py:134-143, swapping
    `newsletters.distill.faithfulness` -> `newsletters.review`. Every assertion uses the public API
    (`review_blockers`, the model) — no ad-hoc probes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_review.py -x -q</automated>
  </verify>
  <done>All three gate-fires negatives pass (STALE/UNENTAILED/OPEN_MISSING each asserting the right Blocker), draft/in-review exemption holds, a clean published surface returns [], the no-AI subprocess guard passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus data → checker | Source transcripts / claim text / missing[] entries are untrusted content the checker reads (never executes) |
| AI extra → core | review.py must never pull an AI module into the bare-install runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | review_blockers swallows a blocker → unsafe surface passes | mitigate | The three negative gate-fires fixtures (L7) prove each blocking path returns the right Blocker; clean-only testing is forbidden (Phase-7 lesson) |
| T-10-02 | Repudiation | A STALE/un-entailed claim presented as fact without diagnosis | mitigate | STALE checked first, elif-guarded UNENTAILED — every drifted/un-entailed published claim yields a typed Blocker with detail |
| T-10-03 | Elevation/contract-break | AI import leaks into review.py (breaks PKG-03/04) | mitigate | review.py imports only ..semantic + .distill.faithfulness (both AI-free); a subprocess no-AI test guards it; lint-imports stays green |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency this phase (typer[all]/pydantic already core, stdlib only) — no install task, no package legitimacy surface |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_review.py -q` — all gate-fires negatives + exemption + clean + no-AI pass.
- `.venv/bin/python -m pytest -q` — full suite green (no regression; existing no-auto-publish + provenance tests untouched).
- `.venv/bin/python -m mypy src/newsletters/semantic.py src/newsletters/review.py` — no NEW errors beyond the documented pre-existing baseline (deferred-items.md).
- `.venv/bin/lint-imports` — 1 contract kept / 0 broken (review.py is AI-free).
- `.venv/bin/python -c "import sys, newsletters; bad=[m for m in ('langchain','langgraph','langsmith','pydantic_ai','openai','anthropic') if m in sys.modules]; assert not bad, bad"` — core stays AI-free after adding review.py.
</verification>

<success_criteria>
- Surface.missing carrier exists (optional, additive, invariant-3-safe), JSON round-trips, leaves the publish gate untouched.
- review_blockers is a pure, AI-free function: published-only scope (L3), STALE/UNENTAILED/OPEN_MISSING via reused predicates (L2), STALE-first elif ordering.
- The gate is PROVEN TO FIRE: three crafted PUBLISHED negatives each assert the right Blocker (L7); draft/in-review exempt; clean published surface returns [].
- Zero new dependency; no human checkpoints. Maps to PROV-04 (the checker core).
</success_criteria>

<output>
Create `.planning/phases/10-reviewer-surfacing-merge-block/10-01-SUMMARY.md` when done.
</output>
