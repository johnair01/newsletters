---
phase: 10-reviewer-surfacing-merge-block
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/newsletters/cli.py
  - .github/workflows/ci.yml
  - tests/test_review_cli.py
autonomous: true
requirements: [PROV-04]
must_haves:
  truths:
    - "newsletters check runs review_blockers across the corpus's PUBLISHED surfaces and prints a per-surface report"
    - "newsletters check exits 0 on the clean dogfood corpus"
    - "newsletters check exits nonzero when a corpus contains a blocked published surface"
    - "A third CI job runs newsletters check on the bare .[test] install and fails the build on any blocker"
  artifacts:
    - path: "src/newsletters/cli.py"
      provides: "The `check` Typer command — exit 1 on any blocker, exit 0 when clean"
      contains: "def check"
    - path: ".github/workflows/ci.yml"
      provides: "The third merge-block (PROV-04) CI job"
      contains: "merge-block"
    - path: "tests/test_review_cli.py"
      provides: "Exit-code e2e tests (clean -> 0; blocked -> nonzero) + CLI no-AI guard"
      contains: "def test_cli_check"
  key_links:
    - from: "src/newsletters/cli.py"
      to: "src/newsletters/review.py"
      via: "review_blockers over build_surfaces()"
      pattern: "review_blockers"
    - from: ".github/workflows/ci.yml"
      to: "newsletters check"
      via: "merge-block job step"
      pattern: "newsletters check"
---

<objective>
Wire the checker into an operator + CI entry point: a `newsletters check` Typer command (L4) that
runs `review_blockers` across the corpus's PUBLISHED surfaces, prints a clear per-surface report,
and exits nonzero on any blocker (0 when clean); and a third CI job (L5) that runs it on the bare
`.[test]` install so an unsafe surface cannot merge. Prove the exit-code contract with e2e tests
(clean corpus → 0; a crafted blocked corpus → nonzero) plus a CLI no-AI import guard.

Purpose: PROV-04 says CI blocks merge of any surface with a STALE / un-entailed / open-missing
claim. The pure `review_blockers` (Plan 01) is the logic; this plan makes it ENFORCED — the exit
code is the CI contract, and a separate red job gives an independent merge-block signal.

Output: the `check` command in cli.py, the `merge-block` job in ci.yml, and tests/test_review_cli.py
proving exit 0 (clean) / nonzero (blocked) + AI-free.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-CONTEXT.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-RESEARCH.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-01-SUMMARY.md
@src/newsletters/cli.py
@.github/workflows/ci.yml
@src/newsletters/review.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `newsletters check` command + exit-code e2e tests (L4, L7)</name>
  <files>src/newsletters/cli.py, tests/test_review_cli.py</files>
  <action>
    Add a `check` `@app.command()` to cli.py (mirror the `build` command at cli.py:26-37). Lazy-import
    inside the function body (`from .dogfood import build_surfaces` and `from .review import
    review_blockers`) so a bare CLI import stays light, matching the existing build/version pattern.
    Build the corpus via `build_surfaces()`, build a corpus-wide `sources = {s.id: s for surf in
    surfaces for s in surf.traces}` lookup, then collect `blockers = [b for surf in surfaces for b in
    review_blockers(surf, sources)]`. Print a clear per-surface, per-kind line for each blocker via
    `typer.echo` (e.g. `BLOCK [<kind>] <surface_id>: <detail>`). On any blocker, echo a summary count
    and `raise typer.Exit(1)` (Typer's idiom — NOT sys.exit, per L4 / Don't-Hand-Roll). When clean,
    echo "All published surfaces clean — no blockers." and return (exit 0) — the clean dogfood corpus
    MUST pass so the gate is green on main (the exit-0 contract, RESEARCH Q-E). Add a docstring stating
    the published-only, exit-nonzero-on-blocker contract.

    Create `tests/test_review_cli.py` with: (1) test_cli_check_clean_corpus_passes — invoke the
    command on the real (clean) corpus via Typer's `CliRunner` and assert `result.exit_code == 0`;
    (2) test_cli_check_exits_nonzero_on_blocker — monkeypatch `newsletters.dogfood.build_surfaces`
    (or patch the symbol the command imports) to return a list containing ONE crafted published
    blocked surface (reuse a fixture builder mirroring Plan 01's STALE/open-missing fixture) and
    assert `result.exit_code != 0` and that the report names the surface; (3) a CLI no-AI subprocess
    guard mirroring tests/test_faithfulness_gate.py:134-143 — `python -c "import newsletters.cli"`
    loads no AI module. Use only the public CLI surface (CliRunner / subprocess) — no internal probes.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_review_cli.py -x -q</automated>
  </verify>
  <done>`newsletters check` exits 0 on the clean corpus and nonzero on a corpus containing a blocker, printing a per-surface report; the CLI imports no AI; CliRunner e2e tests assert both exit codes.</done>
</task>

<task type="auto">
  <name>Task 2: Add the third merge-block CI job (L5)</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a third job `merge-block` to .github/workflows/ci.yml alongside `bare-install` and
    `import-linter`, composed like `bare-install` (ci.yml:19-73) so it runs on the SAME bare `.[test]`
    install (stdlib + Pydantic + Typer only — AI-free, keeping PKG-03/PKG-04 the source of truth, per
    L5). Steps: `actions/checkout@v4`; `actions/setup-python@v5` with python-version "3.12";
    "Install with NO AI extras" running `python -m pip install --upgrade pip` then `pip install
    ".[test]"` (do NOT add [ai]/[dev]/[panel] — match the bare-install comment); "Run the merge-block
    checker on the corpus (AI-free)" running `newsletters check`. The job's value is the EXIT CODE —
    a blocker fails the build so an unsafe surface cannot merge. Name it
    `name: merge-block gate (PROV-04)`. Keep it a SEPARATE job (per RESEARCH A2 — an independently-red
    signal) on the existing `[push, pull_request]` trigger (ci.yml:16). Do not alter the two existing
    jobs.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']; assert 'merge-block' in j, list(j); steps=j['merge-block']['steps']; txt=str(steps); assert 'newsletters check' in txt and '.[test]' in txt and '[ai]' not in txt, txt; print('merge-block job present, AI-free, runs newsletters check')"</automated>
  </verify>
  <done>ci.yml has a third merge-block job that installs the bare .[test] runtime and runs `newsletters check`; the two existing jobs are unchanged; YAML parses and the job asserts present + AI-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → merge | The exit code of `newsletters check` gates whether an unsafe surface can merge |
| AI extra → CI runtime | The merge-block job must run on the bare AI-free install (PKG-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Repudiation/Tampering | A blocker is silently swallowed → unsafe surface merges | mitigate | CLI raises typer.Exit(1) + prints a per-surface report; the CI job fails the build; the nonzero-exit e2e test proves the contract can't regress |
| T-10-05 | Tampering | False merge-pass on a clean run masks a regression | mitigate | The clean-corpus exit-0 test pins the happy path; the nonzero test pins the blocking path — both required |
| T-10-06 | Elevation/contract-break | AI import leaks into the CLI / CI runtime | mitigate | check command lazy-imports only review.py + dogfood (AI-free); the job runs on bare .[test]; a CLI no-AI subprocess guard test |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency (typer[all] already core); no install task, no package legitimacy surface |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_review_cli.py -q` — exit 0 (clean) + nonzero (blocked) + CLI no-AI guard pass.
- `.venv/bin/newsletters check` — exits 0 on the clean shipped corpus (gate green on main).
- `.venv/bin/python -m pytest -q` — full suite green.
- `.venv/bin/lint-imports` — 1 kept / 0 broken (the CLI path stays AI-free).
- YAML: ci.yml parses; the merge-block job is present, runs `newsletters check`, installs bare `.[test]`.
</verification>

<success_criteria>
- `newsletters check` is the operator/CI entry: published-only, per-surface report, exit 0 clean / exit 1 on any blocker (L4).
- A third CI job enforces it on the bare AI-free install so an unsafe surface cannot merge (L5), without disturbing the two standing PKG-03/PKG-04 jobs.
- Exit-code contract proven both ways (clean → 0, blocked → nonzero) + CLI AI-free.
- Zero new dependency; no human checkpoints. Maps to PROV-04 (enforcement).
</success_criteria>

<output>
Create `.planning/phases/10-reviewer-surfacing-merge-block/10-02-SUMMARY.md` when done.
</output>
