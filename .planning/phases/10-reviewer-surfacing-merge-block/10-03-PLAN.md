---
phase: 10-reviewer-surfacing-merge-block
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/newsletters/render.py
  - src/newsletters/dogfood.py
  - tests/test_render.py
  - docs/surfaces.md
  - content/rev1/site
autonomous: true
requirements: [PROV-03]
must_haves:
  truths:
    - "Every rendered surface shows a What's-not-here/not-verified honesty panel listing Surface.missing[] and Source.extraction.unextracted[]"
    - "A surface with no gaps shows a positive 'Fully traced — nothing outstanding' confirmation (the panel's presence is the proof)"
    - "Each claim renders next to its verbatim Trace.span by default — visible without a click"
    - "A STALE or un-entailed claim carries an inline amber badge"
    - "content/rev1/site regenerates byte-stable from the renderer (no hand-edited HTML, SITE-06)"
  artifacts:
    - path: "src/newsletters/render.py"
      provides: "The amber honesty panel + claim-beside-verbatim-span view threaded with a sources lookup"
      contains: "honesty"
    - path: "tests/test_render.py"
      provides: "Panel-lists-missing+unextracted test + claim-inline-span+badge test + byte-stable regen"
      contains: "honesty"
  key_links:
    - from: "src/newsletters/render.py"
      to: "Surface.missing + Source.extraction.unextracted"
      via: "honesty panel render in render_surface"
      pattern: "missing|unextracted"
    - from: "src/newsletters/render.py"
      to: "src/newsletters/distill/faithfulness.py"
      via: "inline STALE/unfaithful badge reuses entails + is_stale"
      pattern: "SpanContainmentFaithfulness|is_stale"
    - from: "src/newsletters/dogfood.py"
      to: "render_surface"
      via: "sources threaded into the renderer"
      pattern: "render_surface"
---

<objective>
Make the review gate REAL on every rendered surface (PROV-03): a per-surface amber
"What's not here / not verified" honesty panel listing `Surface.missing[]` + every Source's
`unextracted[]`, and a claim-beside-verbatim-trace view where each claim renders next to its
`Trace.span` by default with an inline STALE/unfaithful badge — so an unfaithful claim is visible
WITHOUT a click. No JS, deterministic, every interpolation `_e()`-escaped; the built site
regenerates byte-stable (SITE-06).

Purpose: PROV-03 + the 3rd success criterion. Today render.py shows only a single inline
"unsubstantiated → missing[]" chip per untraced claim (render.py:444) and never surfaces
`missing[]`/`unextracted[]` per surface, and the verbatim span is hidden. This plan makes "shown to
the reviewer" literally true on every surface.

Output: the honesty panel + inline-span view in render.py (with a `{s.id: s}` sources lookup
threaded into `_block_html`), dogfood threading sources into the renderer, regenerated
`content/rev1/site`, and tests in test_render.py.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-CONTEXT.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-RESEARCH.md
@.planning/phases/10-reviewer-surfacing-merge-block/10-01-SUMMARY.md
@src/newsletters/render.py
@src/newsletters/distill/coverage.py
@src/newsletters/locators.py
@docs/design-system.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread sources + render the claim-beside-verbatim-span view with inline badges (PROV-03 / SC3)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - A claim with an addressed trace renders its verbatim Trace.span inline beside the evidence chip (the span text appears in the HTML without a click / no JS toggle).
    - An un-addressed (empty-span) Rev1 trace shows the chip alone — no empty span box (faithful, not suggestive).
    - A claim whose addressed span does NOT contain the claim text carries an inline amber "unfaithful" badge.
    - A claim whose trace has drifted from its live source carries an inline amber "STALE" badge.
    - A clean addressed+entailed+non-stale claim carries no badge.
    - The existing untraced-claim "unsubstantiated -> missing[]" chip behavior is preserved.
  </behavior>
  <action>
    Extend the `ClaimsBlock` branch of `_block_html` (render.py:439-452). Give `_block_html` an
    optional `sources: dict[str, Source] | None = None` param (mirror how `site` is already threaded,
    render.py:434) and pass it through from `render_surface` built as
    `{s.id: s for s in surface.traces}` (render.py:703 is the `_block_html` call site). For each
    claim, under the claim text render each addressed Trace's verbatim `span` inline (`_e`-escaped)
    beside its `_ev_chip` — only when `trace.is_addressed` and the span is non-empty (an un-addressed
    Rev1 trace shows the chip alone; no empty span box — faithful, not suggestive, per L6 / RESEARCH
    Q-B). Add an inline badge: when `sources` is provided and `claim.is_stale(sources)`
    (semantic.py:199), a "STALE" badge; elif `not SpanContainmentFaithfulness().entails(claim)`
    (faithfulness.py:62), an "unfaithful" badge — both styled with `--color-amber` (design-system §6;
    amber = the single status-warning token). Reuse `_e` for ALL interpolation; never put span text in
    an href. Add ONE new CSS rule for the inline span + badge (e.g. `.claim-span` mono/quoted block,
    `.claim-badge` amber uppercase mono) mirroring the existing `.claim` / `.ev-chip` token style in
    `_CSS` (render.py:188-193). Preserve the untraced fallback chip at render.py:444. Import
    `SpanContainmentFaithfulness` from `.distill.faithfulness` (AI-free) at module top alongside the
    existing imports.

    Add tests to tests/test_render.py: test_claim_renders_inline_verbatim_span (an addressed claim's
    span text is present in the rendered HTML), test_claim_flags_stale_and_unfaithful_inline (a STALE
    claim and an un-entailed claim each render their amber badge; a clean claim renders none),
    test_unaddressed_trace_shows_chip_without_empty_span_box. Build fixtures inline (a small published
    surface with addressed/drifted/un-entailed claims) — do not touch the shipped corpus here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_render.py -x -q -k "span or unfaithful or stale or inline"</automated>
  </verify>
  <done>Each claim renders beside its verbatim addressed span by default; STALE/un-entailed claims show an inline amber badge; un-addressed traces show no empty box; sources are threaded render_surface -> _block_html; render.py imports no AI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the per-surface "What's not here / not verified" amber honesty panel (PROV-03)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - A surface with Surface.missing=["x"] renders an amber honesty panel listing "x".
    - A surface whose traces carry Source.extraction.unextracted[] renders each Unextracted's locator.display + reason in the panel.
    - A surface with both empty renders a positive "Fully traced — nothing outstanding" confirmation (the panel is always present; its presence is the proof).
    - The panel uses --color-amber and a mono uppercase header; it contains no JS; all interpolation is _e-escaped.
    - The blocking items (missing[] / unextracted[]) are never collapsed-by-default.
  </behavior>
  <action>
    Add a `_honesty_panel(surface: Surface) -> str` helper and render it once per surface inside
    `render_surface`, after `blocks` (render.py:703/717 — append into the `body` main, e.g. between
    `{blocks}` and `{prevnext}`). It lists: every entry in `surface.missing` (the L1 carrier — each an
    unsubstantiated/un-entailed item); and for every `s in surface.traces` with `s.extraction`, each
    `ExtractionRecord.unextracted[]` entry (locators.py:113-121) rendered as
    `locator.display` + `reason`. Style it like the existing `.rationale` inset (render.py:201-202) but
    AMBER: a 3px left border in `--color-amber` (`#c8860a` light / `#d99a2a` dark, design-system
    §1/§2/§6), a mono uppercase eyebrow header "What's not here / not verified". NEVER collapse the
    blocking items (CONTEXT decision 1 / L6). When BOTH lists are empty, render the same panel shell
    with a small positive "Fully traced — nothing outstanding" line (non-blocking confirmation) so the
    panel's presence is itself the proof on every surface. Add ONE `.honesty` CSS rule to `_CSS`
    mirroring `.rationale` (swap the accent to `--color-amber`). Reuse `_e` for every interpolation;
    no JS, no positional logic — pure render from typed data (SITE-06 holds).

    Add tests to tests/test_render.py: test_honesty_panel_lists_missing_and_unextracted (a surface with
    missing[] + a Source carrying an ExtractionRecord renders both, amber, no script),
    test_honesty_panel_clean_surface_shows_positive_confirmation (empty/empty → the "Fully traced"
    line), test_honesty_panel_has_no_script. Build fixtures inline.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_render.py -x -q -k "honesty"</automated>
  </verify>
  <done>Every rendered surface carries the honesty panel; it lists missing[] + unextracted[] when present (amber, no JS, _e-escaped, never collapsed), and a positive confirmation when clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thread sources from dogfood + REGENERATE the site, then byte-stable regen test (SITE-06)</name>
  <files>src/newsletters/dogfood.py, src/newsletters/render.py, tests/test_render.py, content/rev1/site, docs/surfaces.md</files>
  <action>
    PROCESS ORDER IS LOAD-BEARING (Phase-9 lesson): regenerate the built site FIRST, then run the
    gates LAST — a test that reads the committed built dir before regeneration is a stale green.

    (1) Ensure the dogfood build path passes each surface's sources into the renderer so the inline
    STALE/unfaithful badge has its lookup: confirm `build_site` (dogfood.py:651) calls
    `render_surface(surface, ..., site=site, page=page)` and that `render_surface` self-builds the
    `{s.id: s for s in surface.traces}` lookup it threads to `_block_html` (added in Task 1) — no
    dogfood signature change is needed if render_surface derives the lookup from `surface.traces`
    internally; verify and adjust the call site only if a sources param is required. (The shipped
    corpus is all-clean, so the panel renders the positive confirmation and no badges fire — that is
    correct; the gate-fires proof lives in tests, per Plan 01.)
    (2) Regenerate the committed site: run `.venv/bin/newsletters build` (writes content/rev1/site).
    Commit the regenerated HTML — it must differ ONLY by the new honesty panel + inline spans,
    produced from data (no hand edits).
    (3) Update docs/surfaces.md (specs are the source of truth): document the per-surface honesty
    panel + the claim-beside-verbatim-span review view + the published-only block scope, in the
    review-view section. Keep the change minimal and behavior-accurate.
    (4) Add/confirm a byte-stable regen test in tests/test_render.py mirroring the existing
    test_site_is_byte_stable_across_two_renders (render.py regen pattern, test_render.py:546): two
    successive `build_site` runs into tmp dirs produce byte-identical output, AND a rendered dogfood
    surface contains the honesty panel marker. Run AFTER step 2.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>content/rev1/site is regenerated (panel + inline spans, no hand edits); two builds are byte-identical; a dogfood surface shows the panel; docs/surfaces.md documents the review view + block scope; per-surface filenames stay stable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source content → HTML | missing[] / Unextracted.reason / Trace.span are user/source strings interpolated into HTML |
| AI extra → renderer | render.py must stay AI-free (PKG-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering (XSS) | unescaped Trace.span / Unextracted.reason / missing[] in the panel or inline span | mitigate | Every interpolation passes through render._e (render.py:332); never place span text in an href (SITE-05); a no-script test asserts no JS in the panel |
| T-10-08 | Information disclosure | the honesty data hidden/collapsed so a reviewer misses a gap | mitigate | The panel is always rendered, blocking items never collapsed-by-default (CONTEXT decision 1); a clean surface shows the positive confirmation so the panel's presence is the proof |
| T-10-09 | Tampering | hand-edited built HTML drifts from the renderer (breaks SITE-06) | mitigate | Panel + spans rendered from typed data; the byte-stable regen test proves no hand edits; regenerate-first / gates-last process order |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency (stdlib + existing core only); no install task, no package legitimacy surface |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_render.py -q` — panel + inline-span + badge + byte-stable regen tests pass.
- `.venv/bin/newsletters build` — renders content/rev1/site with no errors; index.html written.
- `.venv/bin/python -m pytest -q` — full suite green (run LAST, after regeneration).
- `.venv/bin/lint-imports` — 1 kept / 0 broken (render.py stays AI-free).
- Per-surface filenames in content/rev1/site are unchanged from the prior build (stable IDs, SITE-01).
</verification>

<success_criteria>
- Every rendered surface shows the amber honesty panel (missing[] + unextracted[], never hidden) — PROV-03 criterion 1.
- Each claim renders beside its verbatim trace span by default, with an inline STALE/unfaithful badge — PROV-03 criterion 3 (the unfaithful thing visible without a click).
- The built site regenerates byte-stable from data (SITE-06); docs/surfaces.md updated; filenames stable.
- Zero new dependency; no human checkpoints. Maps to PROV-03.
</success_criteria>

<output>
Create `.planning/phases/10-reviewer-surfacing-merge-block/10-03-SUMMARY.md` when done.
</output>
