---
phase: 07-power-bi-adapter
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - tests/fixtures/powerbi/_author_fixtures.py
  - tests/fixtures/powerbi/sample.PBIP-tree/
  - tests/fixtures/powerbi/fake.pbix
  - tests/test_powerbi_golden.py
autonomous: true
requirements: [ADAPT-05, ADAPT-06]
must_haves:
  truths:
    - "A hand-authored, byte-reproducible PBIP/TMDL fixture tree (stdlib write_text, NO authoring dep) drives PowerBiAdapter end-to-end"
    - "The golden test asserts zero silent drops (len(claims)+len(unextracted) == units walked) for every fixture"
    - "Every claim is verbatim (claim.text == trace.span == transcript slice) and content-addressed (trace.is_addressed)"
    - "The Top-N + summarized visual emits the EXACT _R_TOPN + _R_AGGREGATED reasons; every model export emits _R_NO_DATA_ROWS once -> Coverage.complete is False (criterion 2)"
    - "The .pbix deferral fixture emits exactly _R_PBIX_BINARY, empty transcript, complete is False, no crash (L1)"
    - "Conformance (span-containment + JSON round-trip), Source-determinism (L5), and round-trip coverage parity hold per fixture"
  artifacts:
    - path: "tests/fixtures/powerbi/_author_fixtures.py"
      provides: "stdlib write_text generator for the byte-reproducible PBIP/TMDL tree + the .pbix deferral byte fixture (L6)"
      min_lines: 90
    - path: "tests/test_powerbi_golden.py"
      provides: "golden corpus end-to-end suite mirroring test_pptx_golden.py invariants (ADAPT-06)"
      min_lines: 120
  key_links:
    - from: "tests/test_powerbi_golden.py"
      to: "newsletters.adapters.powerbi_adapter._R_TOPN/_R_AGGREGATED/_R_NO_DATA_ROWS/_R_PBIX_BINARY"
      via: "import the reason constants from the adapter so any taxonomy drift on EITHER side fails"
      pattern: "from newsletters.adapters import powerbi_adapter"
    - from: "tests/test_powerbi_golden.py"
      to: "PowerBiAdapter.parse_path / parse"
      via: "drive the LIVE adapter over the committed corpus (pin reasons by driving, never assumed)"
      pattern: "parse_path|\\.parse\\("
---

<objective>
Author the Power BI golden corpus (ADAPT-06; ROADMAP Phase-7 success-criterion-3) — a tiny,
HAND-AUTHORED, byte-reproducible PBIP/TMDL fixture tree written with stdlib `write_text` (NO authoring
dependency — the headline advantage over the Excel/PPTX fixtures, which needed openpyxl/python-pptx)
plus a few-byte `.pbix` deferral fixture, and the golden test that drives the LIVE `PowerBiAdapter`
over it. This is the phase's proof of correctness: zero silent drops, verbatim + content-addressed
claims, the exact row-cap/aggregation `unextracted[]` reasons, conformance, determinism, and
round-trip coverage parity.

This plan RESOLVES research Assumption A1 / Open-Q1 the way Excel/PPTX did: the Top-N/summarized
`visual.json` is authored against the documented `microsoft/json-schemas .../visualContainer` +
PowerBI-filter shape, and the `_R_TOPN`/`_R_AGGREGATED` reason strings are PINNED by driving the LIVE
adapter (not assumed) — if a key-name tweak is needed, fix the fixture and/or the Plan-02 detector and
re-pin, never weaken the assertion.

Purpose: satisfy ADAPT-06 (golden-file test, zero silent drops) and close criterion 3. Output:
`tests/fixtures/powerbi/_author_fixtures.py` (the generator), the committed `sample.PBIP-tree/` +
`fake.pbix`, and `tests/test_powerbi_golden.py`. ZERO new dependency.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-power-bi-adapter/07-CONTEXT.md
@.planning/phases/07-power-bi-adapter/07-RESEARCH.md

# The golden + fixture-authoring patterns to mirror EXACTLY
@tests/test_pptx_golden.py
@tests/fixtures/pptx/_author_fixtures.py

# The live adapter under test (read its _R_* constants + entrypoints)
@src/newsletters/adapters/powerbi_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the byte-reproducible PBIP/TMDL fixture tree + the .pbix deferral fixture</name>
  <files>tests/fixtures/powerbi/_author_fixtures.py, tests/fixtures/powerbi/sample.PBIP-tree/, tests/fixtures/powerbi/fake.pbix</files>
  <action>
    Create `tests/fixtures/powerbi/_author_fixtures.py` (mirror `tests/fixtures/pptx/_author_fixtures.py`
    header + `main()` style) writing the committed corpus with stdlib `pathlib.write_text` only — NO
    third-party authoring dep, so `sha256(tree)` is stable across processes (no `now()`, no `random`).
    Author the tree from 07-RESEARCH.md Q-G:
    - `sample.PBIP-tree/sample.pbip` (entry pointer JSON), `definition.pbism`.
    - `sample.SemanticModel/definition/`: `model.tmdl` (culture; `ref` tables), `relationships.tmdl`
      (1 relationship Sales->Product with fromColumn/toColumn), `tables/Sales.tmdl` (table description;
      2-3 columns with dataType + description + summarizeBy; 1 measure `'Total Sales'` with a verbatim
      DAX `= SUMX(...)` + formatString), `tables/Product.tmdl` (1 key column).
    - `sample.Report/definition/`: `definition.pbir` (datasetReference byPath ../sample.SemanticModel),
      `report.json`, `version.json`, `pages/pages.json`, `pages/Overview/page.json` (displayName
      "Overview"), `pages/Overview/visuals/plainTable/visual.json` (a plain table: title + field refs ->
      claims), `pages/Overview/visuals/topProducts/visual.json` (a Top-N-filtered + Sum-summarized
      visual: `filterType: TopN`, `itemCount: 5`, a Sum aggregation -> row-cap + aggregation
      unextracted). AUTHOR the topProducts visual.json against the documented visualContainer / PowerBI
      filter shape (A1) so the live adapter detects TopN + Sum.
    - `tests/fixtures/powerbi/fake.pbix`: a few-byte NON-PBIP binary (need NOT be a real `.pbix` — only
      the deferral is tested since pbixray is DEFERRED) via `write_bytes`.
    Run `.venv/bin/python tests/fixtures/powerbi/_author_fixtures.py` to write the corpus; commit the
    generator + the committed fixtures: `test(07-04): author byte-reproducible PBIP/TMDL golden corpus`.
  </action>
  <verify>
    <automated>.venv/bin/python tests/fixtures/powerbi/_author_fixtures.py && .venv/bin/python -c "import hashlib,pathlib; print('ok' if list(pathlib.Path('tests/fixtures/powerbi/sample.PBIP-tree').rglob('*')) else 'empty')"</automated>
  </verify>
  <done>The committed `sample.PBIP-tree/` (TMDL model + relationship + plain + Top-N/summarized visuals) and `fake.pbix` exist; `_author_fixtures.py` regenerates them byte-for-byte with stdlib only.</done>
</task>

<task type="auto">
  <name>Task 2: Golden suite — drive the LIVE adapter; pin reasons; assert all ADAPT-06 invariants</name>
  <files>tests/test_powerbi_golden.py</files>
  <action>
    Create `tests/test_powerbi_golden.py` mirroring `tests/test_pptx_golden.py` structure (the
    `Expected`/`EXPECTED`/parametrized-invariants idiom). NO skip-mark (the adapter is stdlib-only, so
    the corpus runs on a bare install — unlike the pptx/excel goldens which skip without their extra).
    Import the `_R_*` constants FROM the adapter module so a drift on EITHER side is a failure. Drive the
    LIVE adapter via `parse_path("sample.PBIP-tree")` for the model+report case and `parse(raw,
    "fake.pbix")` for the deferral. Assert, per the research Q-G executable contract (derive the exact
    counts by driving the live adapter, NEVER assumed):
    1. Zero silent drops: `len(claims) + len(unextracted) == units walked`.
    2. TMDL extraction: verbatim claims for table description, column names + dataTypes + descriptions,
       the measure name + the DAX `SUMX(...)` TEXT (never a value) + formatString.
    3. Relationship: fromColumn/toColumn claims.
    4. Plain report visual: the visual title + field-reference claims.
    5. Row-cap/aggregation: the topProducts visual emits EXACTLY `_R_TOPN` + `_R_AGGREGATED`; plus the
       whole-source `_R_NO_DATA_ROWS` once -> `Coverage.complete is False` (criterion 2, fail loud).
    6. `.pbix` deferral: exactly `_R_PBIX_BINARY`, empty transcript, `complete is False`, no crash.
    7. Faithful spans: `claim.text == trace.span` AND `transcript[start:end] == claim.text`.
    8. Content-addressed: `trace.is_addressed`.
    9. Coverage honesty: `complete == (len(unextracted) == 0)`.
    10. Conformance + JSON round-trip: `assert_conforms(PowerBiAdapter(), [source])`.
    11. Source-determinism (L5): two parses -> equal result + byte-identical Source `model_dump_json`.
    12. Round-trip coverage parity: persist the Source, distill on a FRESH adapter -> coverage unchanged.
    If the live `_R_TOPN`/`_R_AGGREGATED` do not fire on the authored topProducts visual, fix the fixture
    key paths (and, if needed, the Plan-02 `_pbir` detector) until they DO, then pin the exact strings —
    never weaken the assertion (A1 resolution method). Run green; commit
    `test(07-04): golden corpus proving zero silent drops + the row-cap taxonomy (ADAPT-06)`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_powerbi_golden.py -q</automated>
  </verify>
  <done>The golden suite passes: zero-silent-drops identity holds for every fixture; the measure DAX is a verbatim claim; the Top-N/summarized visual pins `_R_TOPN`+`_R_AGGREGATED`; every model export forces complete=False via `_R_NO_DATA_ROWS`; the `.pbix` deferral pins `_R_PBIX_BINARY`; conformance, determinism, and round-trip parity all hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed fixture corpus → CI | The fixtures stand in for untrusted real-world PBIP projects; the golden test is the regression seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Spoofing completeness | a silent drop slips past the corpus | mitigate | The accounting identity `#claims + #unextracted == #units` is asserted per fixture — a silent drop is, by construction, a test failure (ADAPT-06). |
| T-07-15 | Tampering | the row-cap reason taxonomy drifts unnoticed | mitigate | The golden IMPORTS `_R_*` from the adapter and pins exact strings by driving the LIVE adapter (A1) — any drift on either side fails the suite. |
| T-07-16 | Tampering w/ provenance | non-byte-reproducible fixtures | mitigate | Generator uses stdlib `write_text` only, no `now()`/`random`; the Source-determinism property (L5) is asserted on the PARSED Source (immune to any file re-write drift). |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency; the corpus is hand-authored plain text (no authoring dep, unlike excel/pptx) — no install task, supply-chain checkpoint inapplicable. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_powerbi_golden.py -q` green.
- Full `.venv/bin/pytest -q` green (the golden joins the suite; the parity/determinism matrices from
  Plan 03 stay green).
- `.venv/bin/mypy src/newsletters/adapters src/newsletters/distill` clean (no src change here, sanity).
- `.venv/bin/lint-imports` 1 kept / 0 broken.
- Bare import green: `.venv/bin/python -c "import newsletters; import newsletters.adapters"`.
- `newsletters build` succeeds.
</verification>

<success_criteria>
- ADAPT-06: a golden-file test covers the Power BI adapter against a hand-authored PBIP fixture,
  asserting zero silent drops (criterion 3).
- The full row-cap/aggregation taxonomy + the `.pbix` deferral are pinned against the LIVE adapter,
  with verbatim content-addressed claims, conformance, determinism, and round-trip coverage parity.
- ZERO new dependency; no human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/07-power-bi-adapter/07-04-SUMMARY.md` when done.
</output>
