---
phase: 11-work-surface-installation
plan: 04
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - src/newsletters/worksurface.py
  - tests/test_worksurface.py
  - content/work/site/
  - content/work/ids.json
autonomous: true
requirements: [WORK-03]
must_haves:
  truths:
    - "build_work_site renders the work corpus to content/work/site/ as standalone HTML"
    - "Each work surface shows provenance: a claim->repo-file link, the verbatim trace span beside the claim, and the honesty panel"
    - "Each work surface shows lineage: the 'derived from' / 'captured via' masthead summary (from the populated Surface.lineage + Provenance) and the fan-out chain"
    - "The work site contains ZERO auto-loading external resource URLs and self-hosts its fonts"
    - "The work site regenerates byte-stable across two renders (SITE-06)"
  artifacts:
    - path: "src/newsletters/worksurface.py"
      provides: "build_work_site(out_dir) -> list[Path] reusing render.py devices + emitting fonts"
      contains: "def build_work_site"
    - path: "content/work/site/"
      provides: "The rendered work Library (HTML + self-hosted fonts), no external URL"
  key_links:
    - from: "content/work/site/*.html"
      to: "the cited repo files + the fan-out surfaces"
      via: "link_for_source (file-path locator) + masthead 'derived from' + _fanout_row"
      pattern: "(derived from|fan-row|claim-span)"
    - from: "content/work/site/"
      to: "content/work/site/fonts/*.woff2 (or the DM-first fallback)"
      via: "the Plan 11-01 @font-face block; build_work_site emits the same fonts into the work output"
      pattern: "@font-face"
---

<objective>
Publish the hand-authored work corpus and surface "how the work was done" — WORK-03. REUSE the
Phase 9/10 provenance/lineage devices (already in render.py): `link_for_source` turns each claim's
file-path locator into a working repo link; `_claim_spans` shows the verbatim trace beside the claim;
`_honesty_panel` shows gaps on every surface; the masthead emits `captured via` + `derived from`
(now real because Plan 11-03 populated `Surface.lineage`); `_fanout_row` renders the fan-out chain.
The populated lineage means NO new renderer code is needed for the summary — verify the masthead line
reads as a real provenance/lineage summary; add only a tiny addition if a gap shows. `build_work_site`
renders the corpus to `content/work/site/` and emits the self-hosted fonts (from Plan 11-01) into the
work output so the work Library is also zero-external-call.

This is L4 (provenance/lineage surfacing) + the render half of the corpus. It depends on Plan 11-03
(the corpus + lineage) and Plan 11-01 (the font fix it reuses for the work output), and continues
`worksurface.py` (file-ownership overlap → Wave 3, matching the strict chain).

Purpose: The published Library shows the process — every claim traceable to its repo file, gaps shown,
the fan-out and provenance/lineage visible. WORK-03's "process visible on each surface" made real.
Output: `build_work_site` in worksurface.py, the rendered content/work/site/ (HTML + fonts),
provenance/lineage-visible + no-external-call + byte-stable tests over the work corpus.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-work-surface-installation/11-CONTEXT.md
@.planning/phases/11-work-surface-installation/11-RESEARCH.md

# The devices to REUSE: link_for_source, _claim_spans, _honesty_panel, _fanout_row, render_surface masthead (captured via / derived from); the @font-face block from Plan 11-01
@src/newsletters/render.py
# The build_site pattern to MIRROR (Ledger.load -> Site.from_surfaces -> render each page -> write) + how fonts/ would be emitted into the output
@src/newsletters/dogfood.py
# build_work_surfaces + the populated Surface.lineage from Plan 11-03
@src/newsletters/worksurface.py
# The byte-stable regen precedent
@tests/test_render.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provenance/lineage-visible + no-external-call + byte-stable tests over the work corpus (RED)</name>
  <files>tests/test_worksurface.py</files>
  <behavior>
    - `test_provenance_lineage_visible`: `build_work_site(tmp_path)`, read the work Report HTML, assert
      ALL present: (a) a claim->repo-file link (a `link_for_source` output href into the repo); (b) a
      `claim-span` div (the verbatim trace beside the claim); (c) the `honesty` panel; (d) a masthead
      lineage/provenance bit ("derived from" and/or "captured via"); and where a fan-out exists, a
      `fan-row`. This is WORK-03's "process visible on each surface".
    - `test_no_external_calls_in_work_output`: read every work `*.html` and assert zero auto-loading
      external resource URL (`fonts.googleapis.com`, `@import url('http`, `src="http`, CSS `url(http`),
      permitting clickable `<a href="https://...">` repo links (A2) — the WORK corpus must be as clean
      as rev1 (carries the Plan 11-01 fix + self-hosted fonts).
    - `test_work_site_byte_stable`: render twice into two tmp dirs and assert byte-identical output
      (SITE-06).
    - Also FILL the e2e scaffold from Plan 11-02 up to render (ingest → report → publish → render),
      leaving the `check --corpus work` stage for Plan 11-05.
    - RED first: fails because `build_work_site` does not exist yet.
  </behavior>
  <action>Add `test_provenance_lineage_visible`, `test_no_external_calls_in_work_output`, and
    `test_work_site_byte_stable` to tests/test_worksurface.py (mirror the rev1 no-external-call +
    byte-stable precedents). Un-skip the e2e scaffold through the render stage. Run them RED.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worksurface.py::test_provenance_lineage_visible tests/test_worksurface.py::test_no_external_calls_in_work_output tests/test_worksurface.py::test_work_site_byte_stable -x</automated>
  </verify>
  <done>The three tests exist and FAIL (build_work_site missing); they encode provenance+lineage
    visibility, zero external calls on the work output, and byte-stability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_work_site — render the work corpus reusing render.py devices + emit fonts (GREEN)</name>
  <files>src/newsletters/worksurface.py</files>
  <behavior>
    - `build_work_site(out_dir: str | Path = "content/work/site") -> list[Path]`: mirror dogfood
      `build_site` — `Ledger.load("content/work/ids.json")` → `Site.from_surfaces(build_work_surfaces(),
      ledger=ledger)` → `ledger.save()` → for each page `render_surface(page.surface, site=site,
      page=page)` written to `out / page.href`; render the Home/Library index as appropriate. REUSE
      render.py; do NOT add a new renderer.
    - Provenance/lineage surface FOR FREE from the populated data + existing devices: `link_for_source`
      (file-path locator → repo link), `_claim_spans` (verbatim span), `_honesty_panel`, the masthead
      `captured via`/`derived from` (Surface.lineage populated in Plan 11-03), `_fanout_row`. Finalize
      `Surface.lineage.produced` with the fan-out ids now that the fan-out exists. If the masthead
      summary has a real gap (verify against Task 1), add ONLY a thin lineage summary — no broad
      renderer rewrite (L4 minimal addition).
    - Emit the self-hosted fonts into the work output: copy/write the same `content/rev1/site/fonts/`
      assets into `content/work/site/fonts/` (so the relative `@font-face` URLs resolve) IF Plan 11-01
      vendored woff2; if Plan 11-01 took the DM-first fallback, no fonts dir is needed and the fallback
      stack applies. EITHER way the work output has zero external URL.
    - Deterministic + byte-stable (sorted, EPOCH_ZERO from the ingest, append-only ledger). Zero AI,
      zero new runtime dependency.
  </behavior>
  <action>In worksurface.py add `build_work_site` mirroring dogfood.build_site (Ledger → Site →
    render_surface per page → write to content/work/site), reusing render.py's provenance/lineage
    devices with no new renderer. Finalize `Surface.lineage.produced`. Emit the Plan-11-01 fonts into
    content/work/site/fonts/ if vendored. Add a thin lineage summary ONLY if Task 1 shows a real gap.
    PROCESS LESSON (Phase 9): in Task 3 regenerate content/work/site FIRST, then run gates LAST. Make
    Task 1 GREEN.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worksurface.py -x && .venv/bin/lint-imports</automated>
  </verify>
  <done>build_work_site renders the work corpus to content/work/site reusing render.py; provenance
    (claim->file link, verbatim span, honesty panel) and lineage (derived from / captured via /
    fan-out) are visible; the three Task-1 tests pass; lint-imports clean; zero new runtime dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate content/work/site (committed) byte-stable — regen FIRST, gates LAST</name>
  <files>content/work/site/, content/work/ids.json</files>
  <action>PROCESS LESSON (Phase 9): regenerate the committed work site FIRST, then run ALL gates LAST
    (the byte-stable/visibility tests read the committed built dir — a pre-regen pytest is a stale
    green). Run `.venv/bin/python -c "from newsletters.worksurface import build_work_site;
    build_work_site()"` to write content/work/site/ (+ fonts/ if vendored) and persist any newly
    assigned ledger refs. THEN run the work-corpus tests + full suite. Verify the committed work site
    contains no external URL and is byte-stable.</action>
  <verify>
    <automated>.venv/bin/python -c "from newsletters.worksurface import build_work_site; build_work_site()" >/dev/null && .venv/bin/python -m pytest tests/test_worksurface.py -x</automated>
  </verify>
  <done>content/work/site is regenerated and committed (HTML + fonts if vendored); the work-corpus
    provenance/lineage + no-external-call + byte-stable tests pass on the committed dir (regen-first
    discipline honored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| work HTML → end-user browser | Opened offline/self-hosted; any baked URL auto-fetches |
| claim span / file content → HTML | Span and free text are escaped; never enter an href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information Disclosure | The work output carrying a leftover external resource URL | mitigate | build_work_site reuses the Plan-11-01 @font-face/fallback and emits local fonts; the work no-external-call test enforces zero auto-loading URLs (A2 scope) |
| T-11-11 | Tampering | HTML/script injection via file content in claims/spans | mitigate | All interpolations `_e`-escaped (render.py); span/free text never enters an href (T-09-08) |
| T-11-12 | Repudiation | Provenance/lineage not actually visible (process hidden) | mitigate | The provenance/lineage-visible test asserts claim->file link + verbatim span + honesty panel + masthead lineage are present on the work surface |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_worksurface.py -x` (provenance/lineage + no-external-call + byte-stable green)
- `.venv/bin/python -m pytest` (full suite green)
- `.venv/bin/lint-imports` (1 kept / 0 broken — worksurface.py AI-free)
- `.venv/bin/mypy src/newsletters` (exactly 12 pre-existing errors; no new)
- content/work/site committed, byte-stable, zero external URL
</verification>

<success_criteria>
- The published work Library shows the process: claim->repo-file links, verbatim spans, honesty panel,
  and the provenance/lineage masthead + fan-out on each surface (WORK-03).
- The work site is zero-external-call (self-hosted fonts / DM-first fallback) and byte-stable (SITE-06).
- No new renderer code beyond a thin lineage summary if needed; zero new runtime dependency; no checkpoint.
</success_criteria>

<output>
Create `.planning/phases/11-work-surface-installation/11-04-SUMMARY.md` when done. Record whether a
thin lineage summary was needed or the populated masthead sufficed, and the work-site diff scope.
</output>
