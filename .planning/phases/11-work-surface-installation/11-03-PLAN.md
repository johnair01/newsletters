---
phase: 11-work-surface-installation
plan: 03
type: execute
wave: 2
depends_on: ["11-02"]
files_modified:
  - src/newsletters/worksurface.py
  - tests/test_worksurface.py
  - content/work/ids.json
autonomous: true
requirements: [WORK-02, WORK-03]
must_haves:
  truths:
    - "A hand-authored work Report (about how this build was done) exists, built via capture.build_report (zero AI, manual backend)"
    - "Every ClaimsBlock claim either content-addresses to an ingested file span (Trace.from_source) OR is routed to Surface.missing[] — nothing untraced or fabricated"
    - "The work corpus has its own append-only ledger at content/work/ids.json"
    - "Each work surface carries a populated Surface.lineage (derived_from = ingested file ids, produced = fan-out ids)"
  artifacts:
    - path: "src/newsletters/worksurface.py"
      provides: "build_work_report + build_work_surfaces — the hand-authored work corpus"
      contains: "def build_work_report"
    - path: "content/work/ids.json"
      provides: "The work corpus append-only ledger (mirrors content/rev1/ids.json)"
      contains: "{"
  key_links:
    - from: "worksurface.build_work_report claim.evidence[0]"
      to: "the ingested file Source span"
      via: "Trace.from_source(file_source, start, end) after str.find of the verbatim slice"
      pattern: "Trace\\.from_source"
    - from: "worksurface Surface.lineage"
      to: "ingested file ids + fan-out surface ids"
      via: "derived_from / produced populated at assembly"
      pattern: "lineage"
---

<objective>
Author a Report BY HAND about how this build was done (WORK-02), reusing the manual backend
(`capture.build_report`, zero AI) and the dogfood content-addressing pattern (`_address_report`): each
claim's trace is pinned (content-addressed via `Trace.from_source`) to a VERBATIM span of a real repo
file ingested by `capture_files`. A claim whose text is not verbatim-locatable in its file routes to
`Surface.missing[]` — faithful, never fabricated. Populate `Surface.lineage` (derived_from = ingested
file ids, produced = fan-out ids) so the existing masthead becomes a real provenance/lineage summary
(the L4 minimal addition; the field already exists at semantic.py:496). Create the work corpus's own
append-only ledger at `content/work/ids.json`.

This is L1 (the hand-authored work Report) + the structure half of L4 (lineage population). It depends
on Plan 11-02's `capture_files` and continues building `worksurface.py` (file-ownership overlap forces
it into Wave 2, matching the strict ingest → report chain). Publish + render land in Plan 11-04.

Purpose: An operator authors a Report by hand and it inherits the traced structure — claims pinned to
real, openable repo files, gaps shown honestly. The WORK-02 success criterion made real on this repo.
Output: `build_work_report` + `build_work_surfaces` in worksurface.py, a traced-structure test, the
work ledger, and populated lineage.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-work-surface-installation/11-CONTEXT.md
@.planning/phases/11-work-surface-installation/11-RESEARCH.md

# The WORK-02 author path: WorkSession / Decision / build_report
@src/newsletters/capture.py
# Surface / Trace.from_source / Surface.missing[] / Surface.lineage(derived_from,produced) / open_pull_request invariant
@src/newsletters/semantic.py
# The content-addressing precedent to MIRROR (_address_report) — locate the verbatim span, pin via Trace.from_source
@src/newsletters/dogfood.py
# capture_files (Plan 11-02) — the ingested file Sources whose spans the claims pin to
@src/newsletters/worksurface.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Traced-structure test (RED) — claims content-address to files OR land in missing[]</name>
  <files>tests/test_worksurface.py</files>
  <behavior>
    - `test_work_report_inherits_traced_structure`: build the work surfaces (`build_work_surfaces()`),
      and for EVERY ClaimsBlock claim on the work Report assert it EITHER has
      `evidence[0].is_addressed` with `evidence[0].source_id` ∈ the ingested file ids (the span pins to
      a real file), OR appears in `surface.missing` — nothing untraced or fabricated sneaks through
      (mirrors the open_pull_request invariant 2, semantic.py:525-531).
    - Assert at least one claim is genuinely content-addressed to a real ingested file span (the happy
      path is real, not all-missing), and that the report builds via the zero-AI `capture.build_report`
      path (provenance.tool set, no AI import reachable).
    - Assert each work surface carries a populated `Surface.lineage.derived_from` (the ingested file
      ids it cites) — the structure half of WORK-03.
    - RED first: fails because `build_work_report`/`build_work_surfaces` do not exist yet.
  </behavior>
  <action>Add `test_work_report_inherits_traced_structure` to tests/test_worksurface.py: build the work
    surfaces, assert the traced-or-missing invariant over every claim, assert ≥1 real content-addressed
    claim, and assert `Surface.lineage.derived_from` is populated with ingested file ids. Run it RED.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worksurface.py::test_work_report_inherits_traced_structure -x</automated>
  </verify>
  <done>The test exists and FAILS (build_work_report/build_work_surfaces missing); it encodes the
    traced-or-missing invariant + lineage population.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_work_report + build_work_surfaces — hand-author, content-address, route missing[], populate lineage (GREEN)</name>
  <files>src/newsletters/worksurface.py</files>
  <behavior>
    - `build_work_report(sources, *, surface_id, title, author, narrative) -> Surface`: author a
      `WorkSession` of hand-written `Decision`s about "how this build was done", each `source_id` an
      ingested file id, then `capture.build_report(...)` (zero AI, manual backend) to a Draft Report
      with a traced `ClaimsBlock` + provenance.
    - Content-address each claim trace MIRRORING dogfood `_address_report`: `start = src.transcript.
      find(claim.text)`; if `>= 0`, pin via `Trace.from_source(src, start, start+len)` (the SOLE minting
      path) — span = the verbatim file slice; if `< 0` (paraphrase/not verbatim), route the claim text
      to `Surface.missing[]` — NEVER fabricate an offset (Pitfall 1 / faithful-not-suggestive).
    - The hand-authored Decision texts MUST be verbatim slices of their cited files so they
      content-address (prefer explicit verbatim spans; anything paraphrased goes to missing[]).
    - Ingest a CURATED file list (the files the claims actually cite — e.g. CLAUDE.md,
      src/newsletters/semantic.py, src/newsletters/capture.py, docs/architecture.md,
      .planning/ROADMAP.md), not a broad glob (no orphan Sources).
    - Populate `Surface.lineage`: `derived_from` = the ingested file ids the surface cites; `produced`
      = the fan-out surface ids (filled fully once the fan-out exists in Plan 11-04 — set what is known
      here so the masthead "derived from" line is real now).
    - `build_work_surfaces() -> list[Surface]`: assemble the work corpus (the work Report, published
      via the review gate where appropriate) — the corpus the renderer (11-04) and check gate (11-05)
      consume. Reuse capture/promote/Site machinery; do NOT reinvent.
    - Zero AI, zero new runtime dependency; module stays AI-free.
  </behavior>
  <action>In worksurface.py add `build_work_report` (capture.build_report + the dogfood
    content-addressing pattern: `str.find` the verbatim claim text, pin via `Trace.from_source`, route
    un-locatable claims to `Surface.missing[]`) and `build_work_surfaces`. Author the Decisions as
    verbatim slices of a curated list of REAL repo files ingested by `capture_files`, so claims pin to
    real file content. Populate `Surface.lineage.derived_from` with the cited ingested ids. Never
    fabricate an offset; never hand-mint a hash. Make Task 1 GREEN.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worksurface.py::test_work_report_inherits_traced_structure tests/test_worksurface.py -x && .venv/bin/lint-imports</automated>
  </verify>
  <done>The hand-authored work Report builds via the zero-AI manual backend; every claim is either
    content-addressed to an ingested file span or in missing[]; lineage.derived_from is populated;
    Task 1 passes; lint-imports clean; zero new runtime dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Create the work corpus append-only ledger (content/work/ids.json)</name>
  <files>content/work/ids.json</files>
  <action>Create the work corpus's own append-only ledger at content/work/ids.json (mirror the shape of
    content/rev1/ids.json: a slug -> {ref, type, issue, date} map). Generate it by loading/saving via
    the `Ledger` API over the work surfaces (the same append-only discipline as build_site —
    `Ledger.load(...)` then `Site.from_surfaces(..., ledger=ledger)` then `ledger.save()`), so refs are
    immutable once assigned. Do NOT hand-edit refs. (The full build_work_site that uses this ledger to
    render the site lands in Plan 11-04; here we only establish the ledger so refs are stable from the
    first render.)</action>
  <verify>
    <automated>.venv/bin/python -c "import json,pathlib; d=json.loads(pathlib.Path('content/work/ids.json').read_text()); assert isinstance(d,dict) and d, 'ledger empty'; print('work ledger ok', list(d)[:3])"</automated>
  </verify>
  <done>content/work/ids.json exists as a non-empty append-only slug->ref ledger produced via the
    Ledger API (not hand-edited); refs are stable for the work corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored claim → published surface | A claim must trace to evidence or be shown as missing; nothing unsubstantiated publishes silently |
| file content → claim span | The span is a verbatim slice of a real file; the claim text never enters an href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Repudiation | A claim published without traceable evidence | mitigate | Every claim content-addresses via `Trace.from_source` OR routes to `Surface.missing[]`; the traced-structure test enforces it (invariant 2) |
| T-11-08 | Tampering | Fabricating an offset for a non-verbatim claim | mitigate | `str.find` < 0 → route to missing[]; NEVER mint a Trace for unlocatable text (Pitfall 1 / faithful-not-suggestive) |
| T-11-09 | Tampering | Mutable/hand-edited refs breaking stable identity | mitigate | Refs assigned via the append-only `Ledger` API; existing slugs keep their ref forever |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_worksurface.py -x` (traced-structure + ingest green)
- `.venv/bin/python -m pytest` (full suite green)
- `.venv/bin/lint-imports` (1 kept / 0 broken — worksurface.py AI-free)
- `.venv/bin/mypy src/newsletters` (exactly 12 pre-existing errors; no new)
- content/work/ids.json is a non-empty append-only ledger
</verification>

<success_criteria>
- A hand-authored work Report (zero AI) inherits the traced structure: claims content-address to real
  ingested files or route to missing[] (WORK-02).
- Surface.lineage is populated (derived_from = ingested file ids) — the structure half of WORK-03.
- The work corpus has its own append-only ledger; refs are stable; no human checkpoint.
- Zero new runtime dependency; nothing fabricated.
</success_criteria>

<output>
Create `.planning/phases/11-work-surface-installation/11-03-SUMMARY.md` when done. Record the curated
ingested file list, which claims content-addressed vs routed to missing[], and the lineage populated.
</output>
