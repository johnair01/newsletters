---
phase: 11-work-surface-installation
plan: 05
type: execute
wave: 4
depends_on: ["11-04"]
files_modified:
  - src/newsletters/cli.py
  - tests/test_worksurface.py
  - docs/architecture.md
  - docs/product-spec.md
  - docs/design-system.md
  - tools/check_docs_11.py
autonomous: true
requirements: [WORK-03]
must_haves:
  truths:
    - "newsletters build --corpus {rev1|work} renders the selected corpus (default rev1, byte-stable)"
    - "newsletters check --corpus {rev1|work} gates the selected corpus through the SAME corpus-agnostic review_blockers"
    - "newsletters check --corpus work exits 0 on the clean work corpus and nonzero on a planted blocker"
    - "The install / work-surface story and the self-host-fonts note are documented in the specs (source of truth)"
  artifacts:
    - path: "src/newsletters/cli.py"
      provides: "--corpus selector on build + check, routing to the work corpus"
      contains: "corpus"
    - path: "docs/architecture.md"
      provides: "The work-surface install/dogfood flow documented"
      contains: "work"
  key_links:
    - from: "src/newsletters/cli.py check --corpus work"
      to: "review.review_blockers over build_work_surfaces"
      via: "the corpus-agnostic merge-block gate (review.py:58)"
      pattern: "review_blockers"
---

<objective>
Wire the work corpus into the CLI and the merge-block gate (the Phase-10 forward note, in scope here),
and document the install/work-surface story in the specs. Add `--corpus {rev1|work}` to `build` and
`check`: `build --corpus work` renders via `build_work_site`; `check --corpus work` runs the SAME
corpus-agnostic `review_blockers` (review.py:58) over `build_work_surfaces` — the work corpus passes
the identical merge-block gate (exit 0 clean / nonzero on a blocker). Then document the
install/work-surface flow + the self-host-fonts note in the spec set (specs are the source of truth;
CLAUDE.md: keep code and spec from drifting). A small stdlib doc gate (`tools/check_docs_11.py`) makes
"the specs were updated" a checkable, non-vibey assertion.

This is L5 (the corpus selector + gate wiring) + the spec documentation. It depends on Plan 11-04's
`build_work_site`/`build_work_surfaces` and owns disjoint files (cli.py + docs + tests + the doc gate),
so it runs in Wave 4 after the corpus exists.

Purpose: An operator runs the SAME `build`/`check` over their work corpus and it passes the same trust
gate — the install/work-surface flow is real and documented. Closes WORK-03's gate-wiring half.
Output: a `--corpus` selector on build/check, an e2e check-gate test over the work corpus, and the
documented install/work-surface story + self-host-fonts note (with a doc gate).
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-work-surface-installation/11-CONTEXT.md
@.planning/phases/11-work-surface-installation/11-RESEARCH.md

# The build/check commands to extend with --corpus
@src/newsletters/cli.py
# The corpus-agnostic merge-block gate the work corpus runs through unchanged
@src/newsletters/review.py
# build_work_site + build_work_surfaces (Plan 11-04) the CLI routes to
@src/newsletters/worksurface.py
# The CLI-gate e2e precedent (clean -> exit 0; planted blocker -> nonzero)
@tests/test_review_cli.py
# Spec set to update (source of truth) — architecture/product-spec; the self-host-fonts note belongs near design-system.md:65
@docs/architecture.md
@docs/product-spec.md
@docs/design-system.md
# The stdlib doc gate already scaffolded (proves the specs were updated)
@tools/check_docs_11.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: check --corpus work gate e2e test (RED) — clean exit 0 / planted blocker nonzero</name>
  <files>tests/test_worksurface.py</files>
  <behavior>
    - `test_check_gates_work_corpus`: mirror test_review_cli.py — invoke `newsletters check --corpus
      work` via the Typer runner; assert the clean work corpus exits 0; then monkeypatch the work
      corpus builder the command imports to inject ONE blocked published surface (STALE / open-missing)
      and assert exit nonzero with the blocker named. This proves the work corpus runs through the SAME
      merge-block gate (PROV-04 carried).
    - Add/confirm a `build --corpus work` smoke assertion (renders to a tmp out, returns paths) and
      that `--corpus rev1` (default) is unchanged.
    - Complete the e2e operator-flow scaffold (Plan 11-02): ingest -> report -> publish -> render ->
      `check --corpus work` exit 0 — the full WORK happy path now passes end to end.
    - RED first: fails because `--corpus` is not on the CLI yet.
  </behavior>
  <action>Add `test_check_gates_work_corpus` (clean -> exit 0; planted blocker -> nonzero, mirroring
    test_review_cli.py's monkeypatch pattern but against the work corpus builder) and finish the e2e
    operator-flow test through `check --corpus work`. Run them RED.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worksurface.py::test_check_gates_work_corpus -x</automated>
  </verify>
  <done>The gate e2e test exists and FAILS (no --corpus option yet); it encodes clean-exit-0 /
    blocker-nonzero over the work corpus and completes the operator-flow scaffold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add --corpus {rev1|work} to build + check (GREEN) — work runs the same gate</name>
  <files>src/newsletters/cli.py</files>
  <behavior>
    - Add a `CorpusName` enum (`rev1`, `work`) and a `--corpus` Typer option (default `rev1`) to BOTH
      `build` and `check`.
    - `build --corpus work` -> lazy-import `worksurface.build_work_site` (default out
      `content/work/site`); `--corpus rev1` -> `dogfood.build_site` (default `content/rev1/site`,
      UNCHANGED behavior). Keep the lazy-import discipline (bare install stays light + AI-free).
    - `check --corpus work` -> lazy-import `worksurface.build_work_surfaces`; `--corpus rev1` ->
      `dogfood.build_surfaces`. EITHER way run the SAME `review.review_blockers` over the selected
      corpus (review_blockers is already corpus-agnostic — do not fork the gate). Exit 0 clean /
      nonzero on any blocker, identical contract.
    - Defaults preserve current behavior exactly (no flag = rev1) so the existing CLI tests stay green.
    - Zero new runtime dependency; AI-free.
  </behavior>
  <action>Edit cli.py: add `CorpusName(str, Enum)` and `--corpus` to `build` and `check`, routing work
    -> worksurface (build_work_site / build_work_surfaces) and rev1 -> dogfood (unchanged), running the
    same corpus-agnostic `review_blockers`. Keep lazy imports. Default rev1 so existing behavior and
    tests are unchanged. Make Task 1 GREEN.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_worksurface.py::test_check_gates_work_corpus tests/test_review_cli.py -x && .venv/bin/python -m newsletters check --corpus work && .venv/bin/lint-imports</automated>
  </verify>
  <done>build/check accept --corpus {rev1|work}; `check --corpus work` exits 0 on the clean corpus and
    nonzero on a planted blocker via the same review_blockers; rev1 default unchanged; existing CLI
    tests green; lint-imports clean; zero new runtime dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Document the install / work-surface flow + the self-host-fonts note in the specs</name>
  <files>docs/architecture.md, docs/product-spec.md, docs/design-system.md, tools/check_docs_11.py</files>
  <action>Specs are the source of truth (CLAUDE.md) — document the new behavior so code and spec do not
    drift. In docs/architecture.md (and/or docs/product-spec.md) add the work-surface install/dogfood
    flow: `pip install` -> `capture_files` read-only local ingest (data stays local, no external calls)
    -> hand-author a Report (manual backend, zero AI) -> publish through the review gate -> `newsletters
    build/check --corpus work` renders/gates a Library that shows provenance (claim->file) + lineage
    (fan-out) on each surface. In docs/design-system.md, near line 65 ("self-host in production"),
    record that the renderer now self-hosts the OFL fonts via `@font-face` (or a documented DM-first
    fallback) and bakes zero external font call — marking the standing mandate as executed. Keep edits
    additive and accurate to what shipped in Plans 11-01..04. The doc gate `tools/check_docs_11.py` is
    already scaffolded; adjust its markers only if your wording differs, never weaken it to a no-op.</action>
  <verify>
    <automated>.venv/bin/python tools/check_docs_11.py</automated>
    <human-check>docs/architecture.md (or product-spec.md) describes the pip-install -> capture_files read-only ingest -> hand-authored Report -> publish -> build/check --corpus work flow; docs/design-system.md near line 65 records the self-hosted @font-face / DM-first fallback and zero external font call. Edits are additive and accurate to what shipped.</human-check>
  </verify>
  <done>The install/work-surface flow is documented in architecture/product-spec; the self-host-fonts
    note is recorded in design-system.md; `tools/check_docs_11.py` exits 0; edits are additive and
    accurate (specs match shipped code).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI corpus selection -> renderer/gate | The selector must not let an unsafe corpus bypass the merge-block gate |
| spec vs code | Specs are the source of truth; drift between documented and shipped behavior is a defect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Elevation of Privilege | A corpus selector that skips the merge-block gate for the work corpus | mitigate | Both corpora run the SAME corpus-agnostic `review_blockers`; the gate is not forked; the e2e test asserts blocker -> nonzero |
| T-11-14 | Repudiation | Spec/code drift hiding the actual install/no-external-call behavior | mitigate | Document the install/work-surface flow + self-host-fonts note in the specs; `tools/check_docs_11.py` gates that the docs were updated |
| T-11-15 | Tampering | A new AI import sneaking in via the CLI corpus path | mitigate | Lazy imports kept; `lint-imports` + the AI-isolation gate stay green; bare install runs build/check --corpus work |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_worksurface.py tests/test_review_cli.py -x` (gate e2e + existing CLI green)
- `.venv/bin/python -m newsletters check --corpus work` exits 0; `--corpus rev1` (default) unchanged
- `.venv/bin/python tools/check_docs_11.py` exits 0 (specs document the flow + the fonts note)
- `.venv/bin/python -m pytest` (full suite green)
- `.venv/bin/lint-imports` (1 kept / 0 broken); `.venv/bin/mypy src/newsletters` (exactly 12 pre-existing; no new)
</verification>

<success_criteria>
- `build`/`check` accept `--corpus {rev1|work}`; the work corpus passes the SAME merge-block gate
  (exit 0 clean / nonzero on a blocker) (WORK-03 gate wiring).
- The full operator flow (ingest -> report -> publish -> render -> check) passes end to end.
- The install/work-surface story + self-host-fonts note are documented (specs = source of truth).
- rev1 default behavior unchanged; zero new runtime dependency; no human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/11-work-surface-installation/11-05-SUMMARY.md` when done. Record the CLI
shape, the gate e2e result, and the spec sections updated.
</output>
