---
phase: 11-work-surface-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/newsletters/render.py
  - tests/test_render.py
  - content/rev1/site/fonts/
  - content/rev1/site/
autonomous: true
requirements: [WORK-01]
must_haves:
  truths:
    - "No rendered HTML page makes an external resource-loading call (no fonts.googleapis.com, no @import url('http, no src=\"http, no <link href=\"http)"
    - "The Google-Fonts @import at render.py:104 is removed entirely (not kept as progressive enhancement)"
    - "Clickable repo links (repo_url / link_for_source anchors) stay absolute — navigation, not auto-fetch (A2)"
    - "The existing content/rev1/site regenerates byte-stable except the intended font change"
  artifacts:
    - path: "src/newsletters/render.py"
      provides: "Self-hosted @font-face block (or DM-first system-font fallback) replacing the external @import"
      contains: "@font-face"
    - path: "tests/test_render.py"
      provides: "No-external-call assertion over generated HTML (auto-loaded resources only, per A2)"
      contains: "fonts.googleapis.com"
  key_links:
    - from: "src/newsletters/render.py _CSS"
      to: "content/rev1/site/fonts/*.woff2"
      via: "@font-face src:url('fonts/...woff2')"
      pattern: "@font-face"
---

<objective>
Kill the one baked-in external call in the rendered output — the Google-Fonts `@import` at
`render.py:104` — so the pipeline honors "no external calls baked in" (CLAUDE.md) and WORK-01's
"data stays local" literally. Replace it with self-hosted fonts via `@font-face` (preferred) or a
DM-first system-font fallback stack (if the woff2 fetch is unavailable in-env). EITHER path → zero
auto-loading external URLs. Lock the guarantee with a test, and regenerate the existing rev1 site
byte-stable so the only change committed is the intended font-loading change.

This is L3 (the no-external-call correctness fix) and the L6(a) test. It is a foundation for the
work corpus (Wave 3 reuses the same `_CSS`), but it is verifiable on its own against the shipped
rev1 corpus, so it ships first and independently.

Purpose: A genuinely offline/self-hosted Library — the open-by-default, no-phone-home promise made
real in the rendered artifact, not just the source code.
Output: A patched renderer (`@font-face` or DM-first fallback), vendored OFL woff2 (+ OFL.txt) under
`content/rev1/site/fonts/` if fetchable, a no-external-call test, and a byte-stable rev1 regen.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-work-surface-installation/11-CONTEXT.md
@.planning/phases/11-work-surface-installation/11-RESEARCH.md

# The font @import to remove + the _CSS token block + how _page injects _CSS into every page
@src/newsletters/render.py
# "self-host in production" mandate + the three families + their token stacks
@docs/design-system.md
# The byte-stable regen test precedent (test_site_is_byte_stable_across_two_renders)
@tests/test_render.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: No-external-call test (RED) — assert generated HTML loads zero external resources</name>
  <files>tests/test_render.py</files>
  <behavior>
    - Render at least one full page via the public render path (e.g. `build_site(tmp_path)` from
      dogfood, or `render_surface` over a built surface) and read every produced `*.html`.
    - Assert NONE of the HTML contains an AUTO-LOADING resource URL: substrings
      `fonts.googleapis.com`, `@import url('http`, `@import url("http`, `src="http`, `src='http`,
      and a CSS `url(http` reference. (Scope to auto-fetched resources per A2.)
    - Assert clickable anchors stay PERMITTED: an `<a ... href="https://github.com/johnair01/...">`
      repo link (from `repo_url`/`link_for_source`) is NOT flagged — navigation is correct and stays
      absolute (A2 lock). The test must distinguish a baked fetch from a user-clickable link.
    - RED first: this test FAILS against the current renderer because of the `@import` at
      render.py:104. Run it and confirm the failure names the `@import`/`fonts.googleapis.com` hit.
  </behavior>
  <action>Add `test_no_external_resource_calls_in_rendered_html` to tests/test_render.py. Build the
    rev1 site to a tmp dir (reuse the `build_site`/byte-stable precedent already in this file), glob
    the HTML, and assert the auto-loading-URL substrings above are absent while permitting clickable
    `<a href="https://...">`. Per A2, only resource-loading URLs are forbidden; `repo_url` anchors are
    navigation and stay absolute. Do NOT weaken the test to pass yet — it must RED on the current
    `@import`. This is the WORK-01 no-external-call guarantee (L6a).</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_render.py::test_no_external_resource_calls_in_rendered_html -x</automated>
  </verify>
  <done>The new test exists and FAILS against the unpatched renderer, naming the
    `fonts.googleapis.com`/`@import` hit (RED state confirmed before Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the @import; self-host fonts via @font-face (or DM-first fallback) — GREEN</name>
  <files>src/newsletters/render.py, content/rev1/site/fonts/</files>
  <behavior>
    - The `@import url('https://fonts.googleapis.com/...')` at render.py:104 is GONE from `_CSS`.
    - PREFERRED path (woff2 fetchable in-env): vendor the three OFL families' woff2 — DM Serif
      Display (incl. italic), Instrument Sans, DM Mono — under `content/rev1/site/fonts/`, COMMIT the
      `OFL.txt` license file(s) alongside them, and add a relative-URL `@font-face` block to `_CSS`
      (`src:url('fonts/<file>.woff2') format('woff2'); font-display:swap`). The executor confirms
      `OFL.txt` is present before committing the woff2 (no reviewer checkpoint — OFL for these three is
      established public fact and the license travels with the fonts).
    - FALLBACK path (woff2 fetch unavailable/blocked): do NOT vendor binaries; keep the existing
      `--font-*` token fallbacks (`'DM Serif Display',Georgia,serif` / `'Instrument Sans',system-ui` /
      `'DM Mono','Courier New',monospace` at render.py:113) as the system-font stack with the DM
      families named FIRST, add a commented `@font-face` scaffold ready for production vendoring, and a
      one-line note that production must vendor the OFL woff2 (design-system.md:65 already mandates it).
    - EITHER path → the `_CSS` contains NO `http(s)://` resource URL; the `--font-*` token stacks keep
      DM families first so fidelity degrades gracefully if a woff2 is missing.
    - ZERO new runtime dependency: vendored woff2 are static assets, not a pip dep.
  </behavior>
  <action>Edit `_CSS` in render.py: delete line 104 (the Google-Fonts `@import`). If the woff2 are
    fetchable in-env, vendor the three OFL families under `content/rev1/site/fonts/` with `OFL.txt`,
    insert a relative-`url('fonts/...woff2')` `@font-face` block at the top of `_CSS`, and confirm the
    license file is present. If the fetch is unavailable, instead leave a commented `@font-face`
    scaffold + a "vendor the OFL woff2 in production" note and rely on the DM-first `--font-*` fallback
    stack (no binaries committed). Do NOT keep the `@import` as optional — any baked external URL fails
    Task 1. If a runtime dependency seems required for the font path, STOP and flag (it must not be).
    Then make Task 1 GREEN.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_render.py::test_no_external_resource_calls_in_rendered_html tests/test_render.py -x && .venv/bin/lint-imports && .venv/bin/mypy src/newsletters 2>&1 | grep -v "^#" | grep -c "error:" | grep -qx 12</automated>
  </verify>
  <done>The `@import` is removed; `_CSS` has either a self-hosted `@font-face` block (with vendored
    woff2 + OFL.txt) or a documented DM-first fallback + scaffold; Task 1 PASSES; full
    tests/test_render.py passes; lint-imports clean; mypy still exactly 12 pre-existing errors (no new);
    zero new runtime dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate content/rev1/site byte-stable (only the font change differs)</name>
  <files>content/rev1/site/</files>
  <action>Regenerate the shipped rev1 corpus with the patched renderer so committed HTML matches
    render.py output (SITE-06). PROCESS LESSON (Phase 9): regenerate FIRST, then run the gates LAST —
    the byte-stable test reads the committed built dir, so a pre-regen run is a stale green. Run
    `.venv/bin/python -m newsletters build` (writes to content/rev1/site), then verify the git diff of
    content/rev1/site/ shows ONLY the intended font-loading change (the `@import` line gone, `@font-face`
    or fallback in its place, plus any new fonts/ assets) — no other drift. Note this intended change in
    the SUMMARY.</action>
  <verify>
    <automated>.venv/bin/python -m newsletters build >/dev/null && .venv/bin/python -m pytest tests/test_render.py::test_site_is_byte_stable_across_two_renders -x</automated>
  </verify>
  <done>content/rev1/site regenerates byte-stable; the only committed change vs prior output is the
    font-loading swap (+ vendored fonts/ if any); the byte-stable test passes on the regenerated dir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered HTML → end-user browser | The output is opened offline/self-hosted; any baked URL auto-fetches on render |
| vendored font binaries → repo | Static assets committed into content/rev1/site/fonts/ carry their own license |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | `render.py:104` Google-Fonts `@import` (phone-home on every page) | mitigate | Remove the `@import` entirely; the no-external-call test (Task 1) enforces zero auto-loading resource URLs |
| T-11-02 | Information Disclosure | Leftover absolute resource URL (favicon/SVG/analytics) slipping the test | mitigate | Test scopes to `@import`/`src=`/CSS `url(`/`<link href>`; diagrams are inline SVG (local); permit clickable `<a href>` only (A2) |
| T-11-03 | Tampering | Vendored woff2 without its license | accept-with-control | Executor confirms `OFL.txt` is committed alongside the woff2 (OFL for the three families is established public fact); fallback path commits no binaries |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_render.py -x` (no-external-call + byte-stable green)
- `.venv/bin/python -m pytest` (full suite green — no regression)
- `.venv/bin/lint-imports` (1 contract kept / 0 broken — render.py stays AI-free)
- `.venv/bin/mypy src/newsletters` (exactly 12 pre-existing errors; no new)
- `git diff --stat content/rev1/site/` shows only the intended font-loading change (+ fonts/ if vendored)
</verification>

<success_criteria>
- The rendered HTML contains zero auto-loading external resource URLs (WORK-01 no-external-call).
- The font `@import` is removed; fidelity preserved via self-hosted `@font-face` or a DM-first fallback.
- content/rev1/site is byte-stable with only the intended font change.
- Zero new runtime dependency; clickable repo links stay absolute (A2); no human checkpoint.
</success_criteria>

<output>
Create `.planning/phases/11-work-surface-installation/11-01-SUMMARY.md` when done. Record which font
path was taken (vendored woff2 + OFL.txt, or DM-first fallback + scaffold) and the rev1 diff scope.
</output>
