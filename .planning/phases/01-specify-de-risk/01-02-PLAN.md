---
phase: 01-specify-de-risk
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .planning/notes/2026-08-29-pptx-determinism-decision.md
  - tests/fixtures/pptx/_author_fixtures.py
  - tests/test_pptx_determinism.py
autonomous: true
requirements: []
derisks: [WKLY-01]
user_setup: []

must_haves:
  truths:
    - "The determinism outcome is recorded as ONE decision with a stated scope, citing the committed measurement (ROADMAP SC-2)"
    - "The three recorded milestone decisions (D-01 reuse Surface(REPORT), D-02 asset provenance, D-03 named placeholders fail-loud) are written where the Phase 2 implementer will read them, each with a testable consequence (ROADMAP SC-3)"
    - "The generated-by marker mechanism is chosen with a stated reason AND a stated way to assert it by reading a written file back (ROADMAP SC-3)"
    - "The repo has ONE normalizer contract and ONE determinism scope statement — the older, contradicting claim in the golden-fixture docstring is superseded in writing, not silently contradicted"
    - "Every research open question (Q1-Q5) is answered in the note with its reason, so Phases 2-4 re-litigate nothing"
  artifacts:
    - path: ".planning/notes/2026-08-29-pptx-determinism-decision.md"
      provides: "The recorded determinism + marker + template-contract decision, evidence-cited"
      contains: "status: decided"
      min_lines: 120
  key_links:
    - from: ".planning/notes/2026-08-29-pptx-determinism-decision.md"
      to: ".planning/notes/2026-08-29-pptx-determinism-evidence.json"
      via: "the Decision section cites the measured evidence by path"
      pattern: "2026-08-29-pptx-determinism-evidence\\.json"
    - from: ".planning/notes/2026-08-29-pptx-determinism-decision.md"
      to: "tests/test_pptx_determinism.py"
      via: "the note names the test module that re-proves the decision"
      pattern: "tests/test_pptx_determinism\\.py"
    - from: "tests/fixtures/pptx/_author_fixtures.py"
      to: ".planning/notes/2026-08-29-pptx-determinism-decision.md"
      via: "docstring supersession pointer"
      pattern: "2026-08-29-pptx-determinism-decision"
---

<objective>
Turn plan 01-01's measurement into the recorded decision the Phase 2 implementer will read — one
outcome, one scope, one normalizer contract — and close the two places where the repo currently
says something different.

This is where the three binding milestone decisions get their **testable consequence** written
down: **D-01** (the weekly reuses `Surface(REPORT)`; no new semantic kind, `semantic.py` byte-
unchanged), **D-02** (asset provenance minimum = folder + date + event label; deep link REQUIRED
for a BI screenshot standing in for values; no provenance → `missing[]`), **D-03** (named
placeholders, fail-loud on missing *and* unknown names; the renderer never invents layout). It is
also where the marker mechanism and the template contract stop being research recommendations and
become decisions with a stated reason and a stated read-back assertion.

Purpose: ROADMAP SC-2 and SC-3. "Written down where the implementer will read them" is the
criterion — a decision that lives only in a research document has not been recorded.
Output: `.planning/notes/2026-08-29-pptx-determinism-decision.md`, plus two docstring supersessions
so no file in the repo contradicts it.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-specify-de-risk/01-RESEARCH.md
@.planning/phases/01-specify-de-risk/01-PATTERNS.md
@.planning/phases/01-specify-de-risk/01-CONTEXT.md
@.planning/phases/01-specify-de-risk/01-01-SUMMARY.md
@.planning/notes/2026-08-29-pptx-determinism-evidence.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the recorded determinism + marker + template-contract decision</name>
  <files>.planning/notes/2026-08-29-pptx-determinism-decision.md</files>
  <read_first>
    - `.planning/notes/a2-problem-lifecycle-decision.md` — the shape to copy. Lines 1-8 (YAML frontmatter `title` / `date` / `context` / `status: decided`, then the `#` heading and `## The question`), the section order (`## The question` → `## The hinge` → `## Decision` → `## How it slots into the roadmap` → `## Truths checked (still honest)`), lines 42-51 (the blockquote-scoping move: the decision plus the boundary it must not cross, in one quoted paragraph), lines 81-88 (the truths-checked closing list, one bullet per load-bearing truth with `honored:` / `preserved:` / `extended:`).
    - `.planning/notes/2026-07-09-casespec-implementation-notes.md` — the voice for the "why not the alternative" paragraphs: `## Integration path chosen: X, NOT Y`, then `## The one genuinely new mechanism`, then `## Other decisions` as a terse bullet list.
    - `.planning/notes/2026-08-29-pptx-determinism-evidence.json` (from plan 01-01) — the numbers this note cites. Do NOT re-measure; cite.
    - `.planning/phases/01-specify-de-risk/01-RESEARCH.md` §Summary, §Pattern 1-4, §Pitfall 1/4/5, §Open Questions 1-5, §Assumptions Log A4/A5/A8.
    - `.planning/ROADMAP.md` Phase 1 criteria 2 and 3 (the exact wording the note must satisfy) and Phase 2 criteria 1-5 (the consumer).
  </read_first>
  <action>
    Create `.planning/notes/2026-08-29-pptx-determinism-decision.md` with frontmatter
    `title: "`.pptx` determinism, the generated-by marker, and the template contract: recorded decisions"`,
    `date: 2026-08-29`, `context: "v1.3 Phase 1 (Specify + de-risk) — the evidence-first decision gating WKLY-01"`,
    `status: decided`. Sections, in this order:

    **`## The question`** — three questions Phase 2 cannot start without: is a `.pptx` byte-stable
    across renders, where does the generated-by marker live, and how does the renderer bind content
    to an operator's template.

    **`## The hinge`** — the whole determinism question came down to one line:
    `_ZipPkgWriter.write()` passes a **str** arcname to `zipfile.writestr`, so stdlib stamps
    `time.localtime()` (`pptx/opc/serialized.py:234-242`). Everything else in `Presentation.save()`
    is already deterministic by construction (DFS over insertion-ordered dicts; rels serialized
    sorted by rId; `_next_rId` returns the first free id; media content-deduplicated; core
    properties never auto-bumped). State the false-green trap explicitly: two writes inside one
    wall-clock second are ALREADY byte-identical, so a spike without a delay would have "proved"
    a stability that is not there.

    **`## Decision`** — the blockquote-scoped statement:

    > **BYTE-STABLE, via a declared post-save zip normalization** — `Presentation.save()` into
    > `BytesIO`, rewrite every entry with `date_time=(1980,1,1,0,0,0)`, `create_system=0`,
    > `compress_type=ZIP_DEFLATED`, emitted entry order preserved, then one atomic write.
    > **Scoped to a fixed (python-pptx, zlib) pair.** DEFLATE output is implementation-dependent,
    > so the committed==fresh gate asserts the **part-content digest** — sorted
    > `(part name, sha256(part bytes))` — which is implementation-independent and strictly stronger
    > than "the zips look the same". Neither assertion normalizes XML.

    Then the measured evidence, cited from
    `.planning/notes/2026-08-29-pptx-determinism-evidence.json` by path and quoted by value:
    `raw_bytes_equal`, `varying_parts`, `varying_zip_fields`, `normalized_bytes_equal`, both part
    digests, and the exercised `python_pptx` / `zlib` versions. Name
    `tests/test_pptx_determinism.py` as the module that re-proves it, and state that assertion C
    (the negative control) is what makes the green in B attributable to the normalizer.

    **`## The three assertions Phases 2 and 4 inherit`** — A (content digest, committed==fresh),
    B (full-byte equality, in-process double render), C (negative control), each with the one-line
    reason and the failure it catches. State the diagnostic signature of Pitfall 1: identical part
    contents plus a different file hash means a zlib divergence, not a regression.

    **`## The generated-by marker: OPC core properties, NOT a notes slide`** — the decision table
    (`cp:category` = `"generated-by:newsletters"`; `cp:contentStatus` = `"draft"` while the Surface
    is not Published; `dcterms:created`/`modified` = `EPOCH_ZERO`; `cp:identifier` = the Surface id,
    per open question Q4 — cheap, round-trips, makes a deck found on a desktop self-locating).
    Reason: a notes-slide marker adds **four** parts including a `notesMaster1.xml` to a deck that
    had none, is reader-visible in presenter view, and an operator deletes it by accident; core
    properties add **zero** parts and are visible in File → Info. Not `dc:description`/`comments`:
    it is free prose an operator may legitimately have authored, and the stock template already
    writes `"generated using python-pptx"` there — overwriting it is the deck-level version of
    editorializing. Then the **stated read-back assertion**, as literal Python that Phase 2 copies:
    reopen the written FILE with `Presentation(str(out_path))` and assert `cp.category`,
    `cp.content_status`, `cp.created == EPOCH_ZERO.replace(tzinfo=None)` (Pitfall 5 — `dcterms`
    reads back tz-naive while the repo's `EPOCH_ZERO` is tz-aware) and the presence of a shape
    named `NL_DRAFT_WATERMARK`. Never the writer's return value.

    **`## The template contract: fill existing template slides, NOT `add_slide`` ** — decision
    (RESEARCH Pattern 2a) with the load-bearing reason: `slides.add_slide(layout)` calls
    `clone_placeholder`, which regenerates names via `_next_ph_name` and **discards the operator's
    Selection-Pane naming** (measured: `WEEKLY_LANE_TITLE` → `Title 1`). Record the sanctioned
    escape hatch (Pattern 2b: `add_slide` + restore names from the layout by
    `placeholder_format.idx`) so a variable-slide-count design has one blessed route and not an
    invented one. Record the **`NL_` reserved prefix** decision and its reason (without it,
    "template shape with no Surface content fails loud" rejects every operator logo, footer and
    page number, making D-03 unusable on a real deck — RESEARCH A4, medium confidence, flagged for
    confirmation at the Phase 2 human check). Record that duplicate shape names are **legal** in
    OOXML, so the `{name: shape}` map must raise on collision rather than last-wins silently drop a
    slot. Bind over `slide.shapes`, not `slide.placeholders` (operator textboxes are absent from the
    latter). Give the testable consequence in one line: Phase 2 ships two failing-direction tests —
    Surface content bound to an unknown name, and an unfilled `NL_` slot — each raising a teaching
    error that names the offending placeholder.

    **`## The Draft watermark`** — one textbox per slide named `NL_DRAFT_WATERMARK`, added last
    (top of z-order), `rotation=315.0`, `Pt(96)`, bold, `RGBColor(0xD0,0xD0,0xD0)`. Every property
    is a fixed literal, so it contributes nothing to non-determinism. Not true transparency:
    python-pptx has no alpha API and raw `a:alpha` XML is not worth the fragility — recorded as the
    choice with its reason so Phase 2 does not rediscover it. Not "an element in the template
    toggled off when published": removing a shape needs the undocumented
    `el.getparent().remove(el)` idiom and would make correct behaviour depend on operator template
    content.

    **`## The three milestone decisions, with their testable consequence`** —
    **D-01** the weekly reuses `Surface(REPORT)`: no new Surface kind, no `semantic.py` change →
    consequence: `git diff --exit-code -- src/newsletters/semantic.py` stays empty through
    Phases 2-4, and the writer *reads* `surface.review.state` to choose the watermark and **never**
    writes it (no auto-publish path).
    **D-02** asset provenance minimum = folder + date + event label, deep link REQUIRED iff the
    asset stands in for values → consequence: the exact `missing[]` routing table, specified by
    field name in `docs/weekly-spec.md` (plan 01-03), and `AssetBlock.asset` **required** so a
    provenance-less placement is unrepresentable rather than merely policed.
    **D-03** named placeholders, fail-loud → consequence: the two-direction tests above.

    **`## Other decisions`** (terse bullets, the research open questions closed):
    Q1 → the Weekly Spec schema lives in a **new `docs/weekly-spec.md`**, with a pointer from
    `docs/case-spec.md` and a link from `docs/architecture.md` §1; `docs/weekly.md` stays reserved
    for the WKLY-06 operator recipe. Reason: SC-1 demands a document hand-authorable from alone.
    Q2 → answered above (Pattern 2a, 2b as the escape hatch).
    Q3 → the Phase 4 committed==fresh gate commits the `.pptx` binary **and** asserts the
    part-content digest against it; full-file equality is never asserted across environments.
    Q4 → yes, `cp:identifier` carries the Surface id.
    Q5 → `docs/weekly-spec.md` names the design-system classes each new block reuses, so Phase 3
    has no visual discretion.
    Plus: the Weekly Spec is a **sibling** loader (`weeklyspec.py`), NOT a widened
    `casespec._KNOWN_KEYS` — widening would make a Case Spec silently accept weekly fields and vice
    versa, destroying the strict-schema guarantee in both directions.
    Plus: recommend a floor pin `python-pptx>=1.0.2` in Phase 2 (`test_pptx_extra_declared` compares
    `_req_name(r)`, which strips specifiers — Phase 2 must verify rather than assume, RESEARCH A7).
    Plus: XXE is mitigated upstream (`resolve_entities=False`, `pptx/oxml/__init__.py:21`) — Phase 2
    must not build a second parser without it.
    Plus: the normalizer never joins a path to the filesystem, so zip-slip in an operator template
    cannot reach disk through it.

    **`## How it slots into the roadmap`** — Phase 2 inherits the definition, the marker, the
    template contract and the watermark as *inputs*; Phase 3 inherits the block/asset spec; Phase 4
    inherits assertion A for the committed==fresh gate. Name the one link that could NOT be verified
    in this environment: no `.pptx` consumer exists here (`libreoffice-core` without Impress
    filters), so "a normalized deck opens correctly in real PowerPoint" is unproven and is the
    Phase 2 human check (RESEARCH A8).

    **`## Truths checked (still honest)`** — one bullet each: no auto-publish (`preserved:`
    `semantic.py` byte-unchanged, the writer never mutates review state); AI-optional core
    (`preserved:` no column-0 `pptx` import under `src/`, `lint-imports` KEPT, bare-install CI
    untouched); every claim traces to evidence (`extended:` this decision cites a committed
    measurement, not a recollection); faithful-not-suggestive (`honored:` the marker never
    overwrites operator prose in `dc:description`).
  </action>
  <acceptance_criteria>
    - `test -f .planning/notes/2026-08-29-pptx-determinism-decision.md` succeeds and `grep -c '^status: decided' .planning/notes/2026-08-29-pptx-determinism-decision.md` returns 1.
    - The ONE outcome is stated, not hedged: `grep -ci 'BYTE-STABLE' .planning/notes/2026-08-29-pptx-determinism-decision.md` returns at least 1, and the note contains exactly one `## Decision` heading (`grep -c '^## Decision' ...` returns 1).
    - The decision cites its evidence: `grep -c '2026-08-29-pptx-determinism-evidence.json' .planning/notes/2026-08-29-pptx-determinism-decision.md` returns at least 1, and `grep -c 'tests/test_pptx_determinism.py' ...` returns at least 1.
    - The marker decision is present with a read-back assertion: `grep -c 'cp:category\|core_properties.category' ...` ≥ 1, `grep -c 'content_status' ...` ≥ 1, `grep -c 'cp:identifier' ...` ≥ 1, and `grep -c 'replace(tzinfo=None)' ...` ≥ 1 (the Pitfall-5 comparison is stated, not left to be rediscovered).
    - The template contract is decided: `grep -c 'add_slide' ...` ≥ 1 and `grep -c 'NL_' ...` ≥ 1 and `grep -ci 'duplicate' ...` ≥ 1.
    - All three milestone decisions appear with a consequence: `grep -c 'D-01' ...` ≥ 1, `grep -c 'D-02' ...` ≥ 1, `grep -c 'D-03' ...` ≥ 1.
    - All five open questions are closed: `grep -c 'Q1\|Q2\|Q3\|Q4\|Q5' ...` ≥ 5.
    - `grep -c 'NL_DRAFT_WATERMARK' ...` ≥ 1.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "
import pathlib,sys
p=pathlib.Path('.planning/notes/2026-08-29-pptx-determinism-decision.md'); t=p.read_text()
req=['status: decided','## Decision','BYTE-STABLE','2026-08-29-pptx-determinism-evidence.json','tests/test_pptx_determinism.py','content_status','cp:identifier','replace(tzinfo=None)','add_slide','NL_DRAFT_WATERMARK','D-01','D-02','D-03','Q1','Q2','Q3','Q4','Q5']
missing=[r for r in req if r not in t]
assert t.count('## Decision')==1, 'exactly one recorded outcome required'
assert not missing, f'decision note missing: {missing}'
print('decision note complete', len(t.splitlines()), 'lines')"</automated>
  </verify>
  <done>One recorded outcome (byte-stable via a declared normalization, scoped), evidence-cited; the marker mechanism chosen with a reason and a literal read-back assertion; the template contract, the reserved prefix, the duplicate-name rule and the watermark decided; D-01/D-02/D-03 each carry a testable consequence; Q1-Q5 closed.</done>
</task>

<task type="auto">
  <name>Task 2: Supersede the contradicting claims — one normalizer contract, one determinism scope</name>
  <files>tests/fixtures/pptx/_author_fixtures.py, tests/test_pptx_determinism.py, .planning/notes/2026-08-29-pptx-determinism-decision.md</files>
  <read_first>
    - `tests/fixtures/pptx/_author_fixtures.py` lines 1-45 — TWO claims the spike bears on: the `BYTE-REPRODUCIBILITY (threat T-06-11)` paragraph asserts *"python-pptx does NOT stamp the save-time wall-clock into the OOXML zip — it writes a fixed zip-entry `date_time`"* (a same-second probe would produce exactly this false conclusion — RESEARCH E2), and the `NOTE on the DETERMINISM ASSERTION (06-04 Task 2)` paragraph records the **opposite scope decision** ("determinism asserted on the PARSED `Source`, NOT on re-saved `.pptx` bytes … risk A3"). Phase 1's decision is precisely the one that risk was deferred to.
    - `tests/fixtures/pptx/_author_fixtures.py` lines 106-141 — `_normalize_zip`, the pre-existing normalizer. RESEARCH Pattern 1's `normalize_opc_zip` is the same shape plus `create_system = 0` and an explicit `compress_type`.
    - `tests/fixtures/weekly/_determinism.py` (plan 01-01) — the canonical contract.
    - `CLAUDE.md` §Conventions: "Specs are the source of truth. If code and spec disagree, the spec wins — or you update the spec in the same change and say why. Never let them drift silently."
  </read_first>
  <action>
    Compare the two claims in `tests/fixtures/pptx/_author_fixtures.py`'s module docstring against
    plan 01-01's committed measurement.

    **Docstring text only — no code, no behaviour, no regeneration.** The committed golden `.pptx`
    corpus must not change: a spec phase does not regenerate ADAPT-06's fixtures, and a diff on
    `tests/fixtures/pptx/*.pptx` is a stop-the-line bug.

    (a) If the measurement contradicts the `BYTE-REPRODUCIBILITY` claim (the evidence records
    `raw_bytes_equal: false` with `varying_zip_fields: ["date_time"]`, i.e. python-pptx DOES stamp
    `time.localtime()`), correct the sentence to state what was measured, name the likely cause of
    the original claim (a same-second probe — RESEARCH E2), and note that the fixtures are stable
    anyway *because* they route through `_normalize_zip`, not because `save()` is stable. If the
    measurement instead confirms the claim, say so and leave it — but say which run confirmed it.
    (b) Update the `NOTE on the DETERMINISM ASSERTION` paragraph to point at the note:
    "SUPERSEDED for the WRITER side by `.planning/notes/2026-08-29-pptx-determinism-decision.md`"
    — the parsed-`Source` scope remains correct for the *loader* side (ADAPT-06); the writer side is
    now byte-stable under a declared normalization. Do not delete the original reasoning; supersede
    it in place so the history stays legible.
    (c) State the ONE-normalizer reconciliation in both places: `_normalize_zip` here and
    `normalize_opc_zip` in `tests/fixtures/weekly/_determinism.py` are the same contract with two
    call sites; the weekly module is canonical, Phase 2 moves it to
    `src/newsletters/_pptx_writer.py` behind the `[pptx]` extra, and `_author_fixtures._normalize_zip`
    delegates to it **then, not now** (delegating now would risk a golden-corpus rebuild in a spec
    phase). Add the same reconciliation paragraph to the decision note under `## Other decisions`,
    with the `EPOCH_ZERO` argument stated: a second normalizer contract would drift exactly as a
    second epoch sentinel would.

    Add a one-line pointer in `tests/test_pptx_determinism.py`'s module docstring back to the
    decision note, so the test says which decision it enforces.
  </action>
  <acceptance_criteria>
    - `git diff --exit-code -- tests/fixtures/pptx/*.pptx` exits 0 — no golden binary was regenerated.
    - `git diff --stat -- tests/fixtures/pptx/_author_fixtures.py` shows changes confined to the module docstring: `git diff -U0 -- tests/fixtures/pptx/_author_fixtures.py | grep -c '^[+-][^+-]' ` counts only lines within the first 45-line docstring region (verify by reading the diff; no `def `, no `return`, no `import` line appears in the diff).
    - `grep -c '2026-08-29-pptx-determinism-decision' tests/fixtures/pptx/_author_fixtures.py` returns at least 1.
    - `grep -ci 'supersede' tests/fixtures/pptx/_author_fixtures.py` returns at least 1.
    - `grep -c '2026-08-29-pptx-determinism-decision' tests/test_pptx_determinism.py` returns at least 1.
    - `grep -c 'normalize_opc_zip' .planning/notes/2026-08-29-pptx-determinism-decision.md` returns at least 1 and the note names `_author_fixtures._normalize_zip` as the other call site.
    - `.venv/bin/python -m pytest tests/test_pptx_golden.py tests/test_pptx_determinism.py -x -q` exits 0 — the docstring edit broke nothing.
  </acceptance_criteria>
  <verify>
    <automated>git diff --exit-code -- tests/fixtures/pptx/ ':!tests/fixtures/pptx/_author_fixtures.py' && .venv/bin/python -m pytest tests/test_pptx_golden.py tests/test_pptx_determinism.py -x -q</automated>
  </verify>
  <done>No file in the repo contradicts the recorded decision: the golden-fixture docstring's byte-stability claim is corrected against the measurement, its parsed-`Source` scope note is explicitly superseded for the writer side, and the one-normalizer-contract reconciliation is stated in both the fixture and the note. No golden binary changed; both pptx test modules still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recorded decision → Phase 2/3/4 implementers | a spec-encoded unsafe default here becomes shipped behaviour later; nobody re-derives it |
| this note → the repo's other determinism claims | two contradicting statements in one repo means neither is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Elevation of privilege | the recorded marker/gate decision | mitigate | The note states that `cp:contentStatus = "draft"` is *written from* `surface.review.state` and that the writer never mutates it — no read-then-fix path. The read-back assertion reads the written FILE, so a writer that returns success without marking is caught. D-01's consequence (`semantic.py` byte-unchanged) is stated as a gate Phases 2-4 inherit. |
| T-01-11 | Spoofing | the generated-by marker | accept | `cp:category` is operator-editable — the marker is provenance, not an authenticity control, and claiming otherwise would be the unsafe default. The note states this explicitly so nobody later treats an unmarked deck as proof of anything. |
| T-01-12 | Repudiation | the decision note | mitigate | The note cites the committed evidence artifact and the test module by path rather than restating remembered numbers; plan 01-01's `--check` mode keeps those numbers bound to a live measurement. |
| T-01-13 | Tampering | `tests/fixtures/pptx/` golden corpus | mitigate | Task 2 edits a docstring only. `git diff --exit-code -- tests/fixtures/pptx/*.pptx` gates any accidental regeneration; `tests/test_pptx_golden.py` re-run proves the ADAPT-06 expectations still hold. |
| T-01-14 | Information disclosure | the note's example values | mitigate | Every example is fabricated/synthetic (`"generated-by:newsletters"`, `NL_*` names). No operator deck, org name, or path may appear — the abstraction guard's spirit applies to planning notes too. |
</threat_model>

<verification>
Run each gate ONCE, independently:

1. `.venv/bin/python -m pytest tests/test_pptx_golden.py tests/test_pptx_determinism.py -x -q` → passed
2. `.venv/bin/python -m pytest -q` → full suite, no regression
3. `git diff --exit-code -- tests/fixtures/pptx/*.pptx src/newsletters/ pyproject.toml` → exit 0
4. The decision-note completeness check in Task 1's `<automated>` → exit 0
5. Read the note end-to-end once as the Phase 2 implementer would: can Phase 2 be started from this note plus the roadmap, without reopening RESEARCH.md? If no, the note is incomplete.
</verification>

<success_criteria>
- ONE determinism outcome is recorded with its scope and its committed evidence (ROADMAP SC-2, second half).
- The marker mechanism, the template contract, the reserved prefix, the duplicate-name rule and the watermark are decided with stated reasons (ROADMAP SC-3).
- The read-back assertion is stated literally, reading the written file — never the writer's return value (ROADMAP SC-3).
- D-01, D-02 and D-03 each carry a testable consequence a later phase can gate on (ROADMAP SC-3).
- No other file in the repo contradicts the recorded decision; the supersession is written, not silent (CLAUDE.md: never let spec and code drift silently).
- The golden `.pptx` corpus is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/01-specify-de-risk/01-02-SUMMARY.md` when done. It MUST state the recorded
outcome in one sentence and list the decisions Phase 2 inherits (marker fields, template contract,
reserved prefix, watermark shape name), so plan 01-03 can cite the note without re-reading it.
</output>

<artifacts_this_phase_produces>
This plan's slice of the phase's artifacts (the complete phase-wide table lives in `01-03-PLAN.md`):

| Path | New/Modified | What it is |
|------|--------------|-----------|
| `.planning/notes/2026-08-29-pptx-determinism-decision.md` | NEW | The recorded decision note — the document the Phase 2 implementer reads instead of `RESEARCH.md`. |
| `tests/fixtures/pptx/_author_fixtures.py` | MODIFIED (module docstring only) | The byte-stability claim corrected against the measurement; the loader-side parsed-`Source` scope note explicitly superseded for the writer side; the one-normalizer reconciliation stated. **No code change, no golden binary regenerated.** |
| `tests/test_pptx_determinism.py` | MODIFIED (docstring pointer) | Names the decision it enforces. |

**Named decisions recorded by this plan:**

| Decision | Testable consequence |
|----------|---------------------|
| Determinism = **BYTE-STABLE via a declared post-save zip normalization**, scoped to a fixed (python-pptx, zlib) pair; the committed==fresh gate asserts the part-content digest | `tests/test_pptx_determinism.py` assertions A/B/C; Phase 4's gate uses A |
| Marker = **OPC core properties**: `cp:category` = `generated-by:newsletters`, `cp:contentStatus` = `draft`, `dcterms:created/modified` = `EPOCH_ZERO`, `cp:identifier` = the Surface id | The stated read-back assertion, run against the written FILE (never the writer's return value) |
| Template contract = **fill existing template slides** (not `add_slide`), bind over `slide.shapes`, `NL_` reserved prefix, raise on duplicate names, fail loud in both directions (**D-03**) | Phase 2 ships two failing-direction tests, each naming the offending placeholder |
| Draft watermark = a named added shape `NL_DRAFT_WATERMARK`, fixed literals only, no alpha XML | Read back by name from the written file |
| The weekly is a `Surface(REPORT)` — no new semantic kind (**D-01**) | `git diff --exit-code -- src/newsletters/semantic.py` stays empty through Phases 2-4 |
| Asset provenance minimum = folder + date + event label; deep link REQUIRED iff `stands_in_for: values` (**D-02**) | Specified verbatim as the `missing[]` routing table in `docs/weekly-spec.md` (plan 01-03) |
| The Weekly Spec is a **sibling loader** (`weeklyspec.py`), not a widened `casespec._KNOWN_KEYS` | Each format rejects the other's keys with a teaching error |
| Schema home = a new `docs/weekly-spec.md` (Q1) · Phase 4 commits the `.pptx` **and** asserts the part digest (Q3) · `cp:identifier` carries the Surface id (Q4) · the doc names the design-system classes per block (Q5) | Each closed in the note so Phases 2-4 re-litigate nothing |
| **ONE normalizer contract** — `_determinism.normalize_opc_zip` is canonical; Phase 2 moves it to `src/newsletters/_pptx_writer.py`; `_author_fixtures._normalize_zip` delegates then, not now | No second, drifting normalizer ships (the `EPOCH_ZERO` argument, applied to normalizers) |
| Recommend a floor pin `python-pptx>=1.0.2` in Phase 2 | Phase 2 verifies `test_pptx_extra_declared` still passes with a specifier (RESEARCH A7) |
</artifacts_this_phase_produces>
