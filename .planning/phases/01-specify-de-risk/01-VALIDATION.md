---
phase: 1
slug: specify-de-risk
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-29
updated: 2026-08-29
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Per-task map filled by the planner (2026-08-29) against `01-01`/`01-02`/`01-03-PLAN.md`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing suite; 639 green at v1.2 close + Case Spec additions) |
| **Config file** | `pyproject.toml` (existing) |
| **Interpreter** | `.venv/bin/python` — **the venv does not exist yet**; plan `01-01` Task 1 creates it |
| **Quick run command** | `.venv/bin/python -m pytest tests/test_pptx_determinism.py tests/test_ai_optional.py -x -q` |
| **Full suite command** | `.venv/bin/python -m pytest -q` |
| **Estimated runtime** | ~10s (quick, incl. one 3-second spike sleep) · ~60–120s (full) |

---

## Sampling Rate

- **After every task commit:** the task's own `<automated>` command (all are < 60s)
- **After every plan wave:** `.venv/bin/python -m pytest -q` + `.venv/bin/lint-imports`
- **Before verification:** full suite green · `lint-imports` KEPT · `newsletters check` over rev1/work/module · `git diff --exit-code -- src/newsletters/ pyproject.toml .github/workflows/ci.yml .importlinter tests/fixtures/pptx/*.pptx content/`
- **Max feedback latency:** 120 seconds
- Run each gate **once** per check — rapid re-runs throw transient errors in this environment.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | De-risks | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-01/T1 | 01-01 | 1 | WKLY-01 | T-01-SC, T-01-02 | No new dependency enters `pyproject.toml`; the committed template is fabricated and scrubbed of third-party core properties | integration | `.venv/bin/python -c "…from _determinism import normalize_opc_zip,part_digest; assert normalize_opc_zip(b)==b"` + `git diff --exit-code -- pyproject.toml` | ❌ Wave 0 (venv + fixtures created by this task) | ⬜ pending |
| 01-01/T2 | 01-01 | 1 | WKLY-01 | — | The determinism assertion is falsifiable (negative control) — a green that cannot fail proves nothing | integration | `.venv/bin/python -m pytest tests/test_pptx_determinism.py -x -q` | ❌ Wave 0 (this task creates it) | ⬜ pending |
| 01-01/T3 | 01-01 | 1 | WKLY-01 | T-01-03, T-01-05, T-01-09 | Evidence cannot be fabricated (`--check` re-measures); no production surface left behind | artifact + guard | `.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check && .venv/bin/python -m pytest tests/test_ai_optional.py tests/test_pptx_determinism.py -x -q && .venv/bin/lint-imports` | ✅ `tests/test_ai_optional.py`, `.importlinter` | ⬜ pending |
| 01-02/T1 | 01-02 | 2 | WKLY-01 | T-01-10, T-01-11, T-01-12 | The gate state is written from `review.state`, never mutated; the marker is provenance, not an authenticity control | artifact (doc-completeness assertion) | `.venv/bin/python -c "…assert t.count('## Decision')==1; assert not missing"` | ❌ Wave 0 (this task creates the note) | ⬜ pending |
| 01-02/T2 | 01-02 | 2 | WKLY-01 | T-01-13 | The golden `.pptx` corpus is not regenerated by a docstring edit | regression + guard | `git diff --exit-code -- tests/fixtures/pptx/ ':!tests/fixtures/pptx/_author_fixtures.py' && .venv/bin/python -m pytest tests/test_pptx_golden.py tests/test_pptx_determinism.py -x -q` | ✅ `tests/test_pptx_golden.py` | ⬜ pending |
| 01-03/T1 | 01-03 | 3 | WKLY-02 | T-01-15 | `safe_load` only is specified; the schema example is genuinely paste-able (a schema a reader cannot load is not hand-authorable) | artifact (YAML parse + key assertions) | `.venv/bin/python -c "…yaml.safe_load(blocks[0]); assert all keys present"` | ❌ Wave 0 (this task creates the doc) | ⬜ pending |
| 01-03/T2 | 01-03 | 3 | WKLY-02, WKLY-03, WKLY-04 | T-01-16, T-01-17, T-01-18, T-01-19, T-01-20 | Provenance-less placement is unrepresentable; root escape is a refusal, not a `missing[]` case; `stands_in_for` is author-declared | artifact (field/routing assertions) | `.venv/bin/python -c "…assert not [n for n in need if n not in t]"` | ❌ Wave 0 | ⬜ pending |
| 01-03/T3 | 01-03 | 3 | WKLY-02 | T-01-21 | No fifth Surface kind is invented (D-01); the block list is verifiably true against the live union | regression + artifact | `.venv/bin/python -m pytest -q && .venv/bin/lint-imports && git diff --exit-code -- src/newsletters/ pyproject.toml .github/workflows/ci.yml .importlinter && .venv/bin/python -c "…docs wiring OK"` | ✅ (git, pytest, lint-imports) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Nyquist check:** 8 tasks, 8 `<automated>` commands, no 3-task run without automated feedback. ✓

---

## Wave 0 Requirements

Covered by plan `01-01` Task 1 (which must run before anything else in the phase):

- [ ] `python -m venv .venv` + `.venv/bin/pip install -e '.[dev,test,pptx,config]'` — **the environment
      is bare**: no `.venv`, no `pytest`, no `import-linter`, no `python-pptx` (verified at plan time).
      Every other task fails at first contact without this.
      *Known friction (`RETRO.md` 2026-07-09):* on a debian-managed PyYAML, `pip install -e '.[…,config]'`
      can abort with "Cannot uninstall PyYAML 6.0.1, RECORD file not found". A fresh `venv` (no
      `--system-site-packages`) avoids it; if it recurs, the recorded workaround is
      `--ignore-installed PyYAML`.
- [ ] `tests/fixtures/weekly/_determinism.py` — the normalizer + digest contract the test and the
      evidence recorder both import (01-01 T1)
- [ ] `tests/fixtures/weekly/template.pptx` — the fabricated, core-property-scrubbed template
      every render assertion writes through (01-01 T1)
- [ ] `tests/test_pptx_determinism.py` — the spike as a durable fixture, incl. the negative control
      and the 3-second delay (01-01 T2)
- [ ] `.planning/notes/2026-08-29-pptx-determinism-evidence.json` — the committed measurement (01-01 T3)

*Framework install: pytest is the existing framework; only the venv is missing.*

---

## Manual-Only Verifications

`workflow.human_verify_mode = end-of-phase` — these are `<verify><human-check>` items at PR review,
not blocking checkpoint tasks.

| Behavior | De-risks | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| A normalized deck opens in real PowerPoint/Impress | WKLY-01 | No `.pptx` consumer in this environment (`libreoffice-core` lacks the Impress filters — measured). All available integrity checks pass (`unzip -t`, `testzip()`, python-pptx reopen), but the definitive consumer check cannot run here | EiC opens the Phase-4 sample deck; flagged as an open item on the PR. RESEARCH A8, medium-high risk — the single unverified link in the normalization chain |
| `docs/weekly-spec.md` is hand-authorable from alone | WKLY-02 | Prose completeness is not machine-checkable; the executable proof is Phase 3 composing a hand-authored spec | EiC reads the doc cold and answers: could you author a valid Weekly Spec from this alone, without opening `casespec.py` or `RESEARCH.md`? If no, name the missing rule |
| No new package entered the project | WKLY-01 | Package-legitimacy confirmation for `python-pptx`'s `SUS` (`unknown-downloads`) verdict | Confirm `git diff -- pyproject.toml` is empty: `python-pptx` is the pre-existing `[pptx]` extra, installed into a fresh venv, not added |
| The `NL_` reserved-prefix contract survives a real operator template | WKLY-01 | No operator deck exists here (RESEARCH A4, medium risk) | Confirm at the Phase 2 review against a real template deck |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (venv, template fixture, determinism module, test module, evidence artifact)
- [x] No watch-mode flags
- [x] Feedback latency < 120s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner-filled 2026-08-29; pending execution.
