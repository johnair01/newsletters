---
phase: 01-specify-de-risk
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - docs/weekly-spec.md
  - docs/architecture.md
  - docs/case-spec.md
  - WHERE-WE-ARE.md
  - RETRO.md
autonomous: true
requirements: []
derisks: [WKLY-02, WKLY-03, WKLY-04]
user_setup: []

must_haves:
  truths:
    - "A reader can hand-author a valid Weekly Spec from docs/weekly-spec.md alone — every key annotated, every rule stated (ROADMAP SC-1)"
    - "The four new block kinds are defined field by field with their place in the discriminated Block union (ROADMAP SC-1)"
    - "The asset-evidence record shape is specified with the exact field names the composer and renderer will use, and the exact missing[] routing when a minimum is absent (ROADMAP SC-4)"
    - "docs/architecture.md's block list is honest again — the pre-existing diagram/glossary drift is fixed in the same edit that adds the four new kinds"
    - "Every new block kind renders under design-system tokens or fails loud — the contract is written down before Phase 3 writes the dispatch"
  artifacts:
    - path: "docs/weekly-spec.md"
      provides: "The hand-authorable Weekly Spec YAML schema, the four block kinds field by field, and the asset-evidence record + missing[] routing"
      contains: "NarrativeBlock"
      min_lines: 150
    - path: "docs/architecture.md"
      provides: "§1 block list corrected (diagram, glossary) and extended (narrative, recognitions, team, asset) with a pointer to the Weekly Spec"
      contains: "weekly-spec.md"
    - path: "docs/case-spec.md"
      provides: "Sibling pointer — the Weekly Spec extends this mechanism"
      contains: "weekly-spec.md"
  key_links:
    - from: "docs/architecture.md"
      to: "docs/weekly-spec.md"
      via: "§1 pointer to the Weekly Spec schema"
      pattern: "weekly-spec\\.md"
    - from: "docs/case-spec.md"
      to: "docs/weekly-spec.md"
      via: "sibling-not-widening pointer"
      pattern: "weekly-spec\\.md"
    - from: "docs/weekly-spec.md"
      to: ".planning/notes/2026-08-29-pptx-determinism-decision.md"
      via: "the recorded decisions this schema implements"
      pattern: "2026-08-29-pptx-determinism-decision"
    - from: "docs/weekly-spec.md"
      to: "src/newsletters/render.py"
      via: "the named design-system classes each new block reuses"
      pattern: "block-h|figcaption|chapter|item"
---

<objective>
Write the weekly down before it exists in `src/`. `docs/weekly-spec.md` becomes the document a
reader can hand-author a valid Weekly Spec from — schema, rules, what happens to it, the four new
block kinds field by field, and the asset-evidence record with its exact `missing[]` routing.
`docs/architecture.md` §1 stops lying about the block union (it is already missing `diagram` and
`glossary`) and gains the four new kinds plus a pointer.

The recorded decisions this plan encodes: **D-01** — the weekly is a `Surface(REPORT)`; no new
Surface kind appears anywhere in the spec text. **D-02** — asset provenance minimum is folder +
date + event label, deep link REQUIRED when the asset stands in for values, no provenance →
`missing[]`; the doc states the routing verbatim and makes `AssetBlock.asset` required so a
provenance-less placement is unrepresentable rather than merely policed. **D-03** — named
placeholders, fail-loud; the spec text never implies the renderer may invent a slot.

Purpose: ROADMAP SC-1 and SC-4. Phase 3 must have no schema discoveries left.
Output: `docs/weekly-spec.md` (new), edits to `docs/architecture.md` and `docs/case-spec.md`, and
the phase's compass + retro entries.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-specify-de-risk/01-RESEARCH.md
@.planning/phases/01-specify-de-risk/01-PATTERNS.md
@.planning/notes/2026-08-29-pptx-determinism-decision.md
@docs/case-spec.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/weekly-spec.md — the hand-authorable schema and the rules</name>
  <files>docs/weekly-spec.md</files>
  <read_first>
    - `docs/case-spec.md` (all 70 lines) — the whole document is the template. Copy four structural moves in kind: (1) the **annotated YAML block as the schema**, one inline `#` comment per key carrying that key's rule (lines 18-36) — this is what makes a doc hand-authorable from alone; (2) **"Rules the loader enforces (teaching errors, never silent drops)"** as a short bullet list (lines 38-45); (3) **"What happens to it"** naming the real function, the real normalization ("CRLF folds to LF; every span, offset, and hash addresses the same normalized text"), `Trace.from_source` as *the* pinning constructor, and the policing test by name (lines 47-66); (4) the closing determinism + extra sentence (lines 68-70).
    - `.planning/phases/01-specify-de-risk/01-RESEARCH.md` §Pattern 5 — the proposed schema (`week`, `module`, `highlights`, `lowlights`, `recognitions`, `team`, `assets`, `config`), the six rules the doc must state explicitly, and the sibling-not-widening table mapping each Case Spec mechanism to its Weekly Spec reuse.
    - `src/newsletters/casespec.py` — skim for the real mechanism names the prose must be accurate about: the `_validate` unknown-key error, `_parse_config` via `_yaml_loader.load_config` (`safe_load` only), `_SpanMinter`, root containment, `build_case_report` returning `Surface(REPORT)` with `created=EPOCH_ZERO`.
    - `.planning/notes/2026-08-29-pptx-determinism-decision.md` §Other decisions — Q1 (this file is the decided home) and the sibling-loader decision.
  </read_first>
  <action>
    Create `docs/weekly-spec.md` in `docs/case-spec.md`'s voice and structure. Sections:

    **Title + opening** — "Weekly Spec — the authored weekly, hand-written into the reviewed
    record". Two short paragraphs: what a weekly is (the recurring module deck, composed from
    authored voice plus adapter evidence into one `Surface(REPORT, Draft)` — state D-01 in prose:
    it reuses `REPORT`, the deck is an output *format*, not a new semantic kind), and that the
    Weekly Spec is a **sibling** of the Case Spec, not an extension of its schema — the *mechanism*
    is reused verbatim, the key set is separate, because widening `casespec`'s exactly-eight-key
    validator would make each format silently accept the other's fields.

    **`## Writing one`** — the annotated YAML block, every key present, every key carrying its rule
    as an inline comment. Use the RESEARCH §Pattern 5 schema exactly: `week`, `module`,
    `highlights` (list of authored lines), `lowlights` (list), `recognitions`
    (`person` / `reason` / optional `source`), `team` (`name` / `role` / `lines` / optional `photo`
    → an assets key, never a path), `assets` (a mapping of key → record; fields deferred to the
    assets section written in Task 2, but the block must show them so the schema is complete),
    `config` (org-specific slots — annotate `# NEVER rendered into claims — specifics stay config.`).

    **`## Rules the loader enforces (teaching errors, never silent drops)`** — the six rules, each
    one bullet: (1) exactly these top-level keys; an unknown key (a typo) fails loudly rather than
    dropping what you wrote; (2) narrative fields are strings — quote values YAML would type-coerce
    (`42`, `yes`, `2026-W35`); (3) `highlights` / `lowlights` items are carried **byte-verbatim** —
    the composer never summarises, never reorders by importance, never adds connective prose
    (faithful, not suggestive); (4) every absent or empty field is **disclosed** in
    `Surface.missing[]`, including "no lowlights were authored" — precisely the absence a weekly is
    tempted to hide; (5) `config:` values are bound for downstream use and never minted into a claim
    or rendered into a block; (6) a recognition with no `source` is still carried — the author's
    word is the evidence, traced to the spec file — **and** its missing evidence is disclosed.
    Add a seventh: the spec file is read **read-only**, and any path it names must resolve under the
    project root; a path that escapes raises (it is not a `missing[]` case — it is a refusal).

    **`## What happens to it`** — name the real shapes: `newsletters.weeklyspec.load_weekly_spec(path)`
    reads the file as **newline-normalized text** into a content-addressed `Source`
    (`context="weekly-spec:{rel}"`, `timestamp=EPOCH_ZERO`), mints one traced `Claim` per authored
    value via `Trace.from_source` — real character spans of your file, so the span-containment
    faithfulness gate passes strictly, not by fallback — and
    `newsletters.weeklyspec.build_weekly_report(load, author=...)` produces a `Surface(REPORT)` in
    **Draft**: no auto-publish, the `Draft › In Review › Published` gate with a recorded approval is
    the only path out. List what the reader gets: the per-lane KPI strip and claims from the
    adapters, a `NarrativeBlock` per tone, a `RecognitionsBlock`, a `TeamBlock`, an `AssetBlock` per
    placed asset, and everything left blank in `Surface.missing[]`, shown to the reviewer.
    State that PyYAML is parsed with `safe_load` only (config is data, not code).

    **Closing paragraph** — mirror `docs/case-spec.md` lines 68-70: loads and builds are
    deterministic (`EPOCH_ZERO` timestamps, file-order iteration), so the same file always produces
    the byte-identical record; PyYAML lives behind the `[config]` extra and the rest of the spine
    runs without it; and the deck the weekly renders to is byte-stable under the recorded
    determinism decision — link `.planning/notes/2026-08-29-pptx-determinism-decision.md`.

    Mark the sections Task 2 will append (`## The four block kinds`, `## Assets — the evidence
    record`) with their headings so the document's shape is settled in one pass.
  </action>
  <acceptance_criteria>
    - `test -f docs/weekly-spec.md` succeeds.
    - Every top-level key is documented: `.venv/bin/python -c "import pathlib;t=pathlib.Path('docs/weekly-spec.md').read_text();ks=['week:','module:','highlights:','lowlights:','recognitions:','team:','assets:','config:'];m=[k for k in ks if k not in t];assert not m, m;print('schema keys OK')"` exits 0.
    - The YAML example parses as YAML: extract the first fenced ```yaml block and `.venv/bin/python -c "import yaml,sys;yaml.safe_load(sys.stdin.read())"` exits 0 (placeholder values must be quoted or comment-annotated so the block is genuinely loadable — a schema a reader cannot paste is not hand-authorable).
    - The verbatim rule is stated: `grep -ci 'byte-verbatim' docs/weekly-spec.md` returns at least 1.
    - The disclosure rule is stated: `grep -c 'missing\[\]' docs/weekly-spec.md` returns at least 2.
    - `grep -c 'safe_load' docs/weekly-spec.md` returns at least 1.
    - `grep -c 'EPOCH_ZERO' docs/weekly-spec.md` returns at least 1 and `grep -c 'Trace.from_source' docs/weekly-spec.md` returns at least 1.
    - No new Surface kind is invented (D-01): `grep -ci 'new surface kind\|kind: weekly\|WEEKLY = ' docs/weekly-spec.md` returns 0 matches, and `grep -c 'Surface(REPORT' docs/weekly-spec.md` returns at least 1.
    - The two Task-2 headings exist as placeholders: `grep -c '^## The four block kinds' docs/weekly-spec.md` returns 1 and `grep -c '^## Assets' docs/weekly-spec.md` returns 1.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "
import pathlib,re,yaml
t=pathlib.Path('docs/weekly-spec.md').read_text()
blocks=re.findall(r'\`\`\`yaml\n(.*?)\`\`\`', t, re.S)
assert blocks, 'no annotated yaml schema block found'
yaml.safe_load(blocks[0])
for k in ['week:','module:','highlights:','lowlights:','recognitions:','team:','assets:','config:','missing[]','byte-verbatim','safe_load','Surface(REPORT']:
    assert k in t, f'missing: {k}'
print('weekly-spec schema section OK', len(t.splitlines()), 'lines')"</automated>
  </verify>
  <done>`docs/weekly-spec.md` carries a paste-able, YAML-valid annotated schema for all eight top-level keys, the seven loader rules, the "what happens to it" walkthrough naming real functions and `Trace.from_source`, and the determinism/extra closing — enough for a reader to hand-author a valid spec without opening any other file.</done>
</task>

<task type="auto">
  <name>Task 2: docs/weekly-spec.md — the four block kinds and the asset-evidence record</name>
  <files>docs/weekly-spec.md</files>
  <read_first>
    - `src/newsletters/semantic.py` lines 400-464 — the live union idiom the spec must describe exactly: `kind: Literal["<name>"] = "<name>"` on every member, optional `heading`/`title`, `Field(default_factory=list)` for collections, and the `Block = Annotated[Union[...], Field(discriminator="kind")]` declaration listing eleven members today. Lines 421-434 (`GlossaryTerm.definition: Claim`) — the "faithfulness enforced *by the type*" precedent `AssetBlock.asset` must cite.
    - `src/newsletters/render.py` lines 545-620 — the live dispatch and the class vocabulary to name per new block: `block`, `block-h`, `chapter` (`t` / `ti` / `bo`), `item` (`sg-tag cat` / `ti` / `bo`), `figure.diagram` + `dh` + `figcaption`, `sg-quote`, `sg-kpi`. NOTE the true state: all eleven current union members DO have a branch (`GlossaryBlock` at line 553), so the `return ""` at line 620 is **unreachable today** — adding four kinds without four branches is exactly what makes it reachable and silent. State it that precisely; do not claim a live drop that does not exist.
    - `.planning/phases/01-specify-de-risk/01-RESEARCH.md` §Pattern 6 (the four block shapes, field by field), §Pattern 7 (the asset record — the record text IS the `Source`, the image is content-addressed inside it, because `Source.transcript` is a `str` and an image can never be a `Source`), and the §Pattern 7 routing table (four conditions, four disclosure-string shapes).
    - `docs/design-system.md` — confirm the token/class names used below exist; the visual contract is non-negotiable (`--radius: 0`).
  </read_first>
  <action>
    Append two sections to `docs/weekly-spec.md`, filling the headings placed in Task 1.

    **`## The four block kinds`** — one subsection per kind, each giving the fields with types and
    defaults, in the live union idiom (`kind: Literal[...]` discriminator, optional heading,
    `default_factory=list` collections):
    - `NarrativeBlock` — `kind`, `heading`, `tone: Literal["highlight","lowlight"]`,
      `items: list[NarrativeItem]` where `NarrativeItem` is `text: str` (byte-verbatim authored
      line) + `claim: Optional[Claim]` (the traced claim carrying that text).
    - `RecognitionsBlock` — `kind`, `heading`, `recognitions: list[Recognition]` where
      `Recognition` is `person: str`, `reason: str`, `evidence: list[Trace]` (empty ⇒ the missing
      evidence is disclosed, the recognition is still carried — rule 6).
    - `TeamBlock` — `kind`, `heading`, `members: list[TeamMember]` where `TeamMember` is
      `name: str`, `role: str = ""`, `lines: list[str]`, `photo: Optional[str]` — an `assets:` key,
      **not** a path.
    - `AssetBlock` — `kind`, `heading`, `asset: AssetRecord` (**required, not optional**),
      `caption`, `evidence: list[Trace]` (non-empty by construction). State the invariant and cite
      the precedent: this makes "an asset without provenance reached a Surface" *unrepresentable*
      rather than merely policed — the same move `GlossaryTerm.definition: Claim` already makes
      (D-02).

    Then two short paragraphs:
    - **Their place in the union** — the four join `Block = Annotated[Union[...], Field(discriminator="kind")]`
      in `semantic.py`, taking the member count from eleven to fifteen; the discriminator values are
      `"narrative"`, `"recognitions"`, `"team"`, `"asset"`.
    - **The dispatch contract** — every block kind renders under `docs/design-system.md` tokens **or
      the dispatch fails loud**. `render.py`'s `_block_html` ends in a bare `return ""`; today every
      union member has a branch so it is unreachable, and Phase 3 must keep it that way by adding
      four branches **and** converting the fall-through into a teaching `raise` naming the unhandled
      `block.kind`. A new kind that renders as the empty string is the exact failure mode this
      product exists to prevent.

    Name the classes each new block reuses, so Phase 3 has no visual discretion:
    `NarrativeBlock` → `div.block` + `h3.block-h` + one `div.item` per line, the tone label in
    `span.sg-tag.cat` and the verbatim text in `div.bo`; `RecognitionsBlock` → `div.block` +
    `h3.block-h` + one `div.item` per recognition (`div.ti` person, `div.bo` reason);
    `TeamBlock` → `div.block` + `h3.block-h` + one `div.chapter` per member (`div.t` role,
    `div.ti` name, `div.bo` the short lines); `AssetBlock` → `div.block` +
    `figure.diagram` with `div.dh` heading and `<figcaption>` caption.

    **`## Assets — the evidence record`** — the record shape with exact field names:
    `key`, `file` (repo-relative, root-contained), `sha256` (content address of the FILE BYTES),
    `folder`, `date` (ISO `YYYY-MM-DD`), `event` — the three provenance minimums per D-02 —
    `link: Optional[str]` (**REQUIRED iff** `stands_in_for == "values"`),
    `stands_in_for: Optional[Literal["values"]]`, `caption`, `alt`.

    Explain the design constraint plainly: `Source.transcript` is a `str`, so **an image can never
    be a `Source`**. The *asset record text* is the Source; the image's identity lives inside it as
    a `sha256` hex string, which is therefore a literal substring of the record and traces verbatim
    like any other field — the span-containment gate keeps its teeth.

    State why `stands_in_for` is **author-declared and never inferred**: deciding "is this a BI
    screenshot standing in for values?" from a filename or the image itself would be the composer
    forming an opinion about content. Faithful-not-suggestive means the author declares it and the
    loader enforces the consequence.

    Then the routing table, verbatim, so Phase 3 has no discretion — four rows, each with the
    condition, the outcome, and the disclosure-string shape:
    | any of `folder` / `date` / `event` absent or empty | not placed, no `AssetBlock` minted | `asset {key!r}: provenance field {field!r} is absent — the minimum is folder + date + event label; disclosed, never placed` |
    | `stands_in_for == "values"` and `link` absent/empty | not placed | `asset {key!r}: a screenshot standing in for values requires a deep link to the report; disclosed, never placed` |
    | `file` missing on disk, or its `sha256` ≠ the recorded value | not placed | `asset {key!r}: file {file!r} does not match its recorded content address — refusing to place a file that is not the one the record describes` |
    | `file` path escapes the root | `ValueError` (fail loud, **not** `missing[]`) | mirrors `casespec.load_case_spec` root containment |
    Plus the success row: all minimums present and the hash matches ⇒ an `AssetBlock` is minted with
    ≥1 `Trace` into the record.

    Close the section with the determinism note measured in research: iterating assets in **spec
    file order** keeps `ppt/media/image1..N` stable across renders, and two byte-identical images
    produce ONE media part (python-pptx content-deduplicates).
  </action>
  <acceptance_criteria>
    - All four kinds named: `.venv/bin/python -c "import pathlib;t=pathlib.Path('docs/weekly-spec.md').read_text();[t.index(k) for k in ['NarrativeBlock','RecognitionsBlock','TeamBlock','AssetBlock','AssetRecord','NarrativeItem','Recognition','TeamMember']];print('kinds OK')"` exits 0.
    - `grep -c 'NarrativeBlock' docs/weekly-spec.md` returns at least 1 (same for the other three kind names).
    - The discriminator values are stated: `grep -c '"narrative"' docs/weekly-spec.md` ≥ 1, `grep -c '"recognitions"' docs/weekly-spec.md` ≥ 1, `grep -c '"team"' docs/weekly-spec.md` ≥ 1, `grep -c '"asset"' docs/weekly-spec.md` ≥ 1.
    - The unrepresentable invariant is stated: `grep -ci 'required, not optional\|unrepresentable' docs/weekly-spec.md` returns at least 1.
    - All nine asset field names appear: `.venv/bin/python -c "import pathlib;t=pathlib.Path('docs/weekly-spec.md').read_text();m=[f for f in ['key','file','sha256','folder','date','event','link','stands_in_for','caption','alt'] if f not in t];assert not m,m;print('asset fields OK')"` exits 0.
    - The routing table has all four failure rows plus the success row: `grep -c 'disclosed, never placed' docs/weekly-spec.md` returns at least 2, and `grep -ci 'root' docs/weekly-spec.md` returns at least 1.
    - The dispatch contract is written: `grep -c 'return ""' docs/weekly-spec.md` ≥ 1 and `grep -ci 'fails loud\|fail loud' docs/weekly-spec.md` ≥ 1.
    - The design-system class map is present: `.venv/bin/python -c "import pathlib;t=pathlib.Path('docs/weekly-spec.md').read_text();m=[c for c in ['block-h','sg-tag','chapter','figcaption','figure.diagram'] if c not in t];assert not m,m;print('class map OK')"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "
import pathlib
t=pathlib.Path('docs/weekly-spec.md').read_text()
need=['NarrativeBlock','RecognitionsBlock','TeamBlock','AssetBlock','AssetRecord','NarrativeItem','TeamMember','\"narrative\"','\"recognitions\"','\"team\"','\"asset\"','stands_in_for','sha256','folder','event','disclosed, never placed','discriminator','block-h','figcaption','chapter','sg-tag','return \"\"']
m=[n for n in need if n not in t]
assert not m, f'weekly-spec.md missing: {m}'
print('block kinds + asset record sections OK', len(t.splitlines()), 'lines')"    </automated>
  </verify>
  <done>The four block kinds are defined field by field with their discriminator values and union placement; `AssetBlock.asset` is stated as required so provenance-less placement is unrepresentable; the asset record carries all nine field names and the four-condition `missing[]` routing table verbatim; each new block names the design-system classes it reuses; the dispatch contract ("renders or fails loud") is written down.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the architecture drift, wire the pointers, close the phase honestly</name>
  <files>docs/architecture.md, docs/case-spec.md, WHERE-WE-ARE.md, RETRO.md</files>
  <read_first>
    - `docs/architecture.md` lines 178-197 — the §7 bullet list. Line 186-187 is the exact sentence to edit: "**Surfaces compose from typed content `blocks`** (prose, claims, kpi, quote, chapters, items, prompt, fanout, rationale). The blocks *are* the slots." It is **already stale**: `diagram` and `glossary` are live union members (`semantic.py:449-464`) and are missing from this list. Match the bullet voice of its neighbours — bold lead-in, em-dash clause, file names in backticks.
    - `docs/architecture.md` lines 9-40 — §1 "The typed semantic model" and its `Supporting types` list, where the pointer to the Weekly Spec belongs; note the existing 2026-06-27 drift note as the house precedent for "recorded here so code and spec don't drift silently".
    - `docs/case-spec.md` lines 1-16 — where the one-line sibling pointer goes.
    - `WHERE-WE-ARE.md` lines 1-40 — newest-on-top structure, the "Where we are right now" block and the decisions-and-why log.
    - `RETRO.md` lines 1-25 — newest on top; the entry shape is `## <date> — <one-line friction>` then `**Friction observed**` then `**Rule hardened**`.
    - `src/newsletters/semantic.py` lines 449-464 — the authoritative union membership, to make the corrected list verifiably true.
  </read_first>
  <action>
    Edit `docs/architecture.md`:
    (a) Fix the §7 block-list sentence so it names **every** live union member plus the four new
    kinds: prose, claims, kpi, quote, chapters, items, prompt, fanout, rationale, **diagram,
    glossary** (the pre-existing drift) and **narrative, recognitions, team, asset** (v1.3). Add a
    short clause marking which four are v1.3 additions specified ahead of implementation, and point
    to `docs/weekly-spec.md` for their field-by-field definition.
    (b) Add a pointer in §1 (near `Supporting types`) naming the four new kinds and linking
    `docs/weekly-spec.md`, in the voice of the existing 2026-06-27 drift note — one short
    paragraph, recorded so code and spec do not drift silently.
    Do **not** invent a fifth Surface kind or alter the `kind ∈ { show, newsletter, article, report }`
    line: D-01 says the weekly is a `REPORT`.

    Edit `docs/case-spec.md`: add the one-line sibling pointer near the top — the Weekly Spec
    extends this *mechanism* (not this schema) and lives in `docs/weekly-spec.md`. One line; do not
    restructure the document.

    Update `WHERE-WE-ARE.md`: add a newest-on-top entry for Phase 1 in plain language — what is now
    settled (the determinism outcome in one sentence, the marker field, the template contract, the
    Weekly Spec schema home), where the evidence lives, and what Phase 2 may now start on. Keep the
    teaching voice; the compass is the file that survives a context reset.

    Update `RETRO.md`: add a newest-on-top entry logging Phase 1's real friction honestly. At
    minimum, name the two that occurred: (1) the environment ships **no** venv and **no**
    python-pptx, so a "just run the spike" instruction fails at first contact — the hardened rule is
    that a phase whose research finds a missing dependency must carry the install as an explicit
    first task, which this phase did; (2) a same-second double write "proves" a byte-stability that
    does not exist — the hardened rule is that a determinism spike must cross a time boundary and
    ship a negative control, or its green means nothing. Add anything else the executor actually
    hit; do not invent friction, and do not paper over any.

    Then run the phase gate set once each, independently, and record the results in the summary.
  </action>
  <acceptance_criteria>
    - The block list is complete and honest: `.venv/bin/python -c "import pathlib;t=pathlib.Path('docs/architecture.md').read_text();m=[k for k in ['diagram','glossary','narrative','recognitions','team','asset'] if k not in t];assert not m,m;print('block list OK')"` exits 0.
    - `grep -c 'weekly-spec.md' docs/architecture.md` returns at least 1 and `grep -c 'weekly-spec.md' docs/case-spec.md` returns at least 1.
    - No fifth Surface kind was invented: `grep -c 'kind ∈ { show, newsletter, article, report }' docs/architecture.md` returns 1.
    - `git diff --stat -- docs/case-spec.md` shows a small edit (fewer than 6 changed lines) — the pointer, not a restructure.
    - `grep -c '2026-08-29' WHERE-WE-ARE.md` returns at least 1 and the newest entry mentions Phase 1's determinism outcome: `grep -ci 'determinism' WHERE-WE-ARE.md` returns at least 1.
    - `head -40 RETRO.md | grep -ci 'python-pptx\|determinism\|negative control'` returns at least 1 — the new entry is on top.
    - Phase gates, each run once: `.venv/bin/python -m pytest -q` exits 0; `.venv/bin/lint-imports` exits 0 with both contracts KEPT; `git diff --exit-code -- src/newsletters/ pyproject.toml .github/workflows/ci.yml .importlinter` exits 0.
    - `.venv/bin/python -m newsletters check --corpus rev1` (and `work`, `module`) each exit 0 — or, if the CLI entrypoint differs, the equivalent shipped command from `pyproject.toml` `[project.scripts]`; all three corpora must pass.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/lint-imports && git diff --exit-code -- src/newsletters/ pyproject.toml .github/workflows/ci.yml .importlinter && .venv/bin/python -c "
import pathlib
a=pathlib.Path('docs/architecture.md').read_text(); c=pathlib.Path('docs/case-spec.md').read_text()
for k in ['diagram','glossary','narrative','recognitions','team','asset','weekly-spec.md']:
    assert k in a, f'architecture.md missing {k}'
assert 'weekly-spec.md' in c, 'case-spec.md missing sibling pointer'
assert 'kind ∈ { show, newsletter, article, report }' in a, 'D-01 violated: surface kind list changed'
print('docs wiring OK')"</automated>
  </verify>
  <done>`docs/architecture.md` names every live block kind plus the four new ones and links the Weekly Spec; `docs/case-spec.md` carries the sibling pointer; the compass and RETRO are current; the full gate set is green when re-run independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored Weekly Spec YAML → the spine | operator-authored, hand-written text is parsed and lifted into claims |
| operator asset files → a published Surface | image bytes and paths supplied by the author enter the record |
| spec text → Phase 3 implementation | an unsafe default written here ships later without re-derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Elevation of privilege | Weekly Spec YAML parsing | mitigate | The doc states `safe_load` only, via the existing `_yaml_loader.load_config` lazy `[config]` boundary — config is data, not code (CLAUDE.md hard rule). No new parser is specified. |
| T-01-16 | Tampering / information disclosure | `assets: <key>: file:` path | mitigate | Root containment is specified as a **refusal**, not a `missing[]` case: a path that escapes the project root raises `ValueError`, mirroring `casespec.load_case_spec`. Written into the routing table so Phase 3 cannot soften it. |
| T-01-17 | Tampering | substituted asset (record describes image A, image B is on disk) | mitigate | `sha256` content address is a required record field; a mismatch means the asset is **not placed** and is disclosed. Specified as a routing-table row, not left to implementer judgement. |
| T-01-18 | Elevation of privilege | provenance loophole in the spec text | mitigate | This is the phase's highest-value threat: a spec that lets an asset be placed without provenance ships that hole into Phase 3. Closed two ways — `AssetBlock.asset` is **required** (unrepresentable, per the `GlossaryTerm.definition: Claim` precedent) and the routing table enumerates every absent-minimum condition with its exact disclosure string (D-02). |
| T-01-19 | Spoofing | `stands_in_for` inferred rather than declared | mitigate | The doc states the value is **author-declared and never inferred** — inferring it from a filename would let a BI screenshot bypass its required deep link, and would also be the composer forming an opinion about content (faithful, not suggestive). |
| T-01-20 | Information disclosure | a new block kind silently rendering as `""` | mitigate | The dispatch contract is written down before the code exists: every kind renders under design-system tokens or the dispatch raises, naming the unhandled `block.kind`. Phase 3 carries it as an explicit criterion. |
| T-01-21 | Information disclosure | example content in the spec | mitigate | Every example in `docs/weekly-spec.md` is fabricated — no operator org name, person, metric, path or system name. The abstraction guard's spirit applies to `docs/` as well as `src/`. |
</threat_model>

<verification>
Run each gate ONCE, independently of any executor's "green":

1. `.venv/bin/python -m pytest -q` → full suite, no regression vs. the pre-phase baseline
2. `.venv/bin/lint-imports` → both contracts KEPT
3. `newsletters check` over ALL corpora (`rev1`, `work`, `module`) → each exit 0
4. `git diff --exit-code -- src/newsletters/ pyproject.toml .github/workflows/ci.yml .importlinter` → exit 0 (SC-5: no production surface, bare-install CI untouched)
5. `git diff --exit-code -- content/` → exit 0 and every `content/*/ids.json` unchanged (append-only ledgers — any diff on regenerate is a stop-the-line bug)
6. The Task 1/2/3 `<automated>` doc checks → each exit 0

<human-check>
At PR review (workflow.human_verify_mode = end-of-phase), the Editor-in-Chief reads
`docs/weekly-spec.md` cold and answers one question: **could you hand-author a valid Weekly Spec
from this document alone, without opening `casespec.py`, `RESEARCH.md`, or asking a question?**
That is ROADMAP SC-1's actual bar and it is not machine-checkable. If the answer is no, name the
missing rule — the fix is a doc edit, not a code change.
</human-check>
</verification>

<success_criteria>
- `docs/weekly-spec.md` defines the Weekly Spec YAML schema completely enough to hand-author from alone: every key annotated, every loader rule stated, the paste-able example is valid YAML (ROADMAP SC-1).
- The four new block kinds are defined field by field, with their discriminator values and their place in the `Block` union, and each names the design-system classes it reuses (ROADMAP SC-1).
- The asset-evidence record is specified with exact field names: provenance minimum folder + date + event label, deep link REQUIRED when standing in for values, content-addressed identity, and the exact `missing[]` routing per condition (ROADMAP SC-4).
- `docs/architecture.md`'s block list is honest — the pre-existing `diagram`/`glossary` drift is fixed in the same edit that adds the four new kinds — and both `architecture.md` and `case-spec.md` point to the Weekly Spec.
- No new Surface kind, no `semantic.py` change, no production code: `git diff --exit-code -- src/newsletters/` exits 0 (D-01, ROADMAP SC-5).
- `WHERE-WE-ARE.md` and `RETRO.md` are current, per CLAUDE.md's definition of done.
</success_criteria>

<artifacts_this_phase_produces>
Every new file path and every named decision Phase 1 leaves behind, across all three plans:

**New files**
| Path | Plan | What it is |
|------|------|-----------|
| `tests/fixtures/weekly/_determinism.py` | 01-01 | The ONE normalizer + content-digest contract (`DOS_EPOCH`, `normalize_opc_zip`, `part_digest`, `differing_parts`, `differing_zipinfo_fields`) — stdlib only |
| `tests/fixtures/weekly/_author_template.py` | 01-01 | Rerunnable provenance for the fabricated template; scrubs stock core properties |
| `tests/fixtures/weekly/template.pptx` | 01-01 | The synthetic named-placeholder template (4 × `NL_` slots + 1 unprefixed `Footer`) |
| `tests/fixtures/weekly/_record_determinism_evidence.py` | 01-01 | Evidence recorder with a `--check` drift mode |
| `tests/test_pptx_determinism.py` | 01-01 | The spike as a durable test: assertions A, B and the negative control C |
| `.planning/notes/2026-08-29-pptx-determinism-evidence.json` | 01-01 | The committed measurement (both runs' hashes, varying parts/fields, versions) |
| `.planning/notes/2026-08-29-pptx-determinism-decision.md` | 01-02 | The recorded decision note |
| `docs/weekly-spec.md` | 01-03 | The Weekly Spec schema, the four block kinds, the asset-evidence record |

**Modified files**
| Path | Plan | Change |
|------|------|--------|
| `tests/fixtures/pptx/_author_fixtures.py` | 01-02 | Module docstring only — byte-stability claim corrected, loader-side scope note superseded for the writer side, one-normalizer reconciliation stated |
| `tests/test_pptx_determinism.py` | 01-02 | Docstring pointer to the decision note |
| `docs/architecture.md` | 01-03 | §7 block list drift fixed (`diagram`, `glossary`) + four new kinds; §1 pointer to the Weekly Spec |
| `docs/case-spec.md` | 01-03 | One-line sibling pointer |
| `WHERE-WE-ARE.md` | 01-03 | Phase 1 compass entry |
| `RETRO.md` | 01-03 | Phase 1 friction + hardened rules |

**Named decisions recorded** (all in `.planning/notes/2026-08-29-pptx-determinism-decision.md` unless noted)
| Decision | Testable consequence |
|----------|---------------------|
| **Determinism = BYTE-STABLE via a declared post-save zip normalization**, scoped to a fixed (python-pptx, zlib) pair | `tests/test_pptx_determinism.py` assertions A/B/C; the Phase 4 committed==fresh gate asserts the part-content digest |
| **Marker = OPC core properties**: `cp:category` = `generated-by:newsletters`, `cp:contentStatus` = `draft`, `dcterms:created/modified` = `EPOCH_ZERO`, `cp:identifier` = the Surface id | The stated read-back assertion, run against the written FILE |
| **Template contract = fill existing template slides** (not `add_slide`), bind over `slide.shapes`, `NL_` reserved prefix, raise on duplicate names, fail loud both directions (**D-03**) | Phase 2 ships two failing-direction tests, each naming the offending placeholder |
| **Draft watermark = a named added shape** `NL_DRAFT_WATERMARK`, fixed literals only, no alpha XML | Read back by name from the written file |
| **The weekly is a `Surface(REPORT)`** — no new semantic kind (**D-01**) | `git diff --exit-code -- src/newsletters/semantic.py` stays empty through Phases 2-4 |
| **Asset provenance minimum = folder + date + event label; deep link REQUIRED iff `stands_in_for: values`** (**D-02**) | The `missing[]` routing table in `docs/weekly-spec.md`; `AssetBlock.asset` required |
| **The Weekly Spec is a sibling loader** (`weeklyspec.py`), not a widened `casespec._KNOWN_KEYS` | Each format rejects the other's keys with a teaching error |
| **Schema home = a new `docs/weekly-spec.md`** (Q1) | The doc exists and both `architecture.md` and `case-spec.md` point to it |
| **Phase 4 commits the `.pptx` AND asserts the part-content digest** (Q3) | Full-file equality is never asserted across environments |
| **ONE normalizer contract** — `_determinism.normalize_opc_zip` is canonical; Phase 2 moves it to `src/newsletters/_pptx_writer.py`; `_author_fixtures._normalize_zip` delegates then, not now | No second, drifting normalizer ships |
| **Recommend a floor pin `python-pptx>=1.0.2`** in Phase 2 | Phase 2 verifies `test_pptx_extra_declared` still passes with a specifier |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-specify-de-risk/01-03-SUMMARY.md` when done. It MUST record the results
of the independently re-run gate set (pytest, lint-imports, `newsletters check` over all three
corpora, the git-diff guards) with the actual command output, not a claim of green.
</output>
