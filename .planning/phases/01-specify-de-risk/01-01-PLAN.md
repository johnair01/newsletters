---
phase: 01-specify-de-risk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/weekly/_determinism.py
  - tests/fixtures/weekly/_author_template.py
  - tests/fixtures/weekly/template.pptx
  - tests/test_pptx_determinism.py
  - tests/fixtures/weekly/_record_determinism_evidence.py
  - .planning/notes/2026-08-29-pptx-determinism-evidence.json
autonomous: true
requirements: []
derisks: [WKLY-01]
user_setup: []

must_haves:
  truths:
    - "A REAL python-pptx double-write ran with a >2s gap between the two writes, and its measured outcome is committed as evidence (ROADMAP SC-2)"
    - "The spike is reproducible, not a one-off: it lands as a durable pytest module, not scratch code (CONTEXT specifics)"
    - "The determinism assertion can FAIL — a negative control proves the green result is attributable to the normalizer, not to two writes landing in the same wall-clock second"
    - "The spike leaves no production surface: no unguarded pptx import under src/, contracts KEPT, bare-install CI + semantic.py + pyproject.toml byte-unchanged (ROADMAP SC-5)"
    - "The committed .pptx fixture is fabricated — no operator deck, no third-party name in its core properties"
  artifacts:
    - path: "tests/fixtures/weekly/_determinism.py"
      provides: "The ONE normalizer + content-digest contract (normalize_opc_zip, part_digest, differing_parts, differing_zipinfo_fields) — stdlib only, no pptx import"
      exports: ["DOS_EPOCH", "normalize_opc_zip", "part_digest", "differing_parts", "differing_zipinfo_fields"]
      min_lines: 60
    - path: "tests/fixtures/weekly/_author_template.py"
      provides: "Rerunnable provenance for the fabricated minimal template deck; scrubs the stock template's third-party core properties"
      min_lines: 60
    - path: "tests/fixtures/weekly/template.pptx"
      provides: "The synthetic named-placeholder template the determinism test and Phase 2 render through"
    - path: "tests/test_pptx_determinism.py"
      provides: "The spike as a durable fixture: assertion A (part digest), B (byte equality after normalization), C (negative control)"
      contains: "pytestmark = pytest.mark.skipif"
      min_lines: 90
    - path: "tests/fixtures/weekly/_record_determinism_evidence.py"
      provides: "Committed, rerunnable evidence recorder with a --check mode that fails on drift"
      min_lines: 50
    - path: ".planning/notes/2026-08-29-pptx-determinism-evidence.json"
      provides: "The committed measured evidence: both runs' hashes, varying parts, varying zip fields, versions"
      contains: "part_digest_a"
  key_links:
    - from: "tests/test_pptx_determinism.py"
      to: "tests/fixtures/weekly/_determinism.py"
      via: "import of normalize_opc_zip / part_digest"
      pattern: "from fixtures.weekly._determinism import|_determinism import"
    - from: "tests/test_pptx_determinism.py"
      to: "tests/fixtures/weekly/template.pptx"
      via: "Presentation(str(TEMPLATE)) in the render helper"
      pattern: "template\\.pptx"
    - from: "tests/fixtures/weekly/_record_determinism_evidence.py"
      to: ".planning/notes/2026-08-29-pptx-determinism-evidence.json"
      via: "json.dump of the measured run + --check re-verification"
      pattern: "2026-08-29-pptx-determinism-evidence\\.json"
    - from: "tests/test_pptx_determinism.py"
      to: "src/newsletters/adapters/_timestamps.py"
      via: "EPOCH_ZERO import — one epoch sentinel, never a second one"
      pattern: "EPOCH_ZERO"
---

<objective>
Run the REAL python-pptx double-write that ROADMAP Phase 1 criterion 2 demands, and leave behind
three things: a fabricated template to write through, a durable pytest module that re-proves the
outcome on every run (including a negative control that can fail), and a committed evidence
artifact recording what was measured.

This plan produces **measurement only** — the *decision* it feeds is written in plan 01-02. Nothing
here goes into `src/newsletters/`. The recorded decisions honoured: **D-01** (reuse
`Surface(REPORT)` — `semantic.py` is not touched by any task here, and a git-diff gate proves it)
and **D-03** (named placeholders, fail-loud — the fabricated template carries `NL_`-prefixed named
shapes plus one deliberately unprefixed decorative shape so Phase 2's fail-loud contract has a
fixture to test both directions against).

Purpose: a determinism decision without evidence is a vibe. No renderer work starts until the
measurement exists and is reproducible.
Output: `tests/fixtures/weekly/` (normalizer contract, template author, template binary, evidence
recorder), `tests/test_pptx_determinism.py`, and
`.planning/notes/2026-08-29-pptx-determinism-evidence.json`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-specify-de-risk/01-RESEARCH.md
@.planning/phases/01-specify-de-risk/01-PATTERNS.md
@CLAUDE.md
</context>

<environment_note>
This environment is **bare**: there is no `.venv`, and `pytest`, `import-linter` and `python-pptx`
are all absent (verified at plan time — `python -c "import pptx"` → `ModuleNotFoundError`). Task 1
creates the venv and installs the extras. `.venv` is already gitignored (`.gitignore:132`), as is
`*.egg-info/` (`.gitignore:24`).

**No new dependency enters the project.** `python-pptx` is already declared at
`pyproject.toml:43` (`pptx = ["python-pptx"]`) and already exercised by the loader-side golden
tests and by CI. RESEARCH §Package Legitimacy Audit adjudicates its `SUS` verdict as the known
`unknown-downloads` PyPI-API false positive already recorded in `pyproject.toml`. Task 1 therefore
proves the *absence* of a supply-chain change with `git diff --exit-code -- pyproject.toml` rather
than adding a package.
</environment_note>

<tasks>

<task type="auto">
  <name>Task 1: Environment + the one normalizer contract + the fabricated template</name>
  <files>tests/fixtures/weekly/_determinism.py, tests/fixtures/weekly/_author_template.py, tests/fixtures/weekly/template.pptx</files>
  <read_first>
    - `tests/fixtures/pptx/_author_fixtures.py` — the exact analog. Lines 1-45 (docstring conventions: a `THE CORPUS` inventory, a `BYTE-REPRODUCIBILITY` section stating *why* it is stable, one ALL-CAPS gotcha heading per surprise, and a literal `Run:` line), lines 106-141 (`_normalize_zip` — the existing normalizer this must NOT be a drifting second copy of), lines 148-160 (`_new_presentation` core-property pinning, `_blank_slide`).
    - `src/newsletters/adapters/_timestamps.py` lines 1-61 — `EPOCH_ZERO` and the module-docstring house style (`WHY this exists / THE FIX / CLAIMS ARE PROVABLY UNAFFECTED`). One epoch sentinel; never mint a second.
    - `.planning/phases/01-specify-de-risk/01-RESEARCH.md` §Pattern 1 (`normalize_opc_zip`, `part_digest`, the measured variant table E15), §Pitfall 6 (the stock template ships `last_modified_by = "Steve Canny"` / `comments = "generated using python-pptx"` / `created = 2013-01-27`), E1-E2, E6.
    - `.gitignore` lines 20-30 and 128-140 — confirm `.venv` and `*.egg-info/` are ignored before installing.
  </read_first>
  <action>
    Create the venv and install: `python -m venv .venv` then
    `.venv/bin/pip install -e '.[dev,test,pptx,config]'`. Editable so later phases' `src/` edits are
    live; `[dev]` brings `import-linter` (the `.venv/bin/lint-imports` path
    `tests/test_ai_optional.py:121` looks for), `[pptx]` brings python-pptx, `[config]` brings
    PyYAML. Record the resolved `python-pptx` version — Task 3 writes it into the evidence.

    Create `tests/fixtures/weekly/_determinism.py` — **stdlib only** (`io`, `zipfile`, `hashlib`);
    it must NOT import `pptx`, so it stays importable on a bare install and needs no skip guard.
    Export exactly: `DOS_EPOCH = (1980, 1, 1, 0, 0, 0)`; `normalize_opc_zip(raw: bytes) -> bytes`
    (rewrite every entry with a fresh `ZipInfo(filename, date_time=DOS_EPOCH)`,
    `compress_type = ZIP_DEFLATED`, `create_system = 0`, preserving `external_attr` and the
    **emitted entry order** — per RESEARCH §Pattern 1 "Decision guidance", do not sort: python-pptx's
    order is already deterministic and preserving it keeps `[Content_Types].xml` first by
    construction rather than by sort luck); `part_digest(raw: bytes) -> str` (sha256 over sorted
    `(name, sha256(part bytes))` rows, joined with `\0` and `\n` — no zip metadata, no XML
    normalization); `differing_parts(a, b) -> list[str]`; and
    `differing_zipinfo_fields(a, b) -> list[str]` (compare `date_time`, `compress_type`,
    `create_system`, `external_attr`, `CRC`, `filename` order across the two infolists).
    Module docstring in the `_timestamps.py` house style, stating: WHY (python-pptx's
    `_ZipPkgWriter.write()` passes a **str** arcname to `zipfile.writestr`, so stdlib stamps
    `time.localtime()` — `pptx/opc/serialized.py:234-242`); THE FIX (rewrite with fixed metadata);
    and the SCOPE caveat (DEFLATE output is zlib-implementation-dependent, so full-byte identity is
    scoped to a fixed (python-pptx, zlib) pair — `part_digest` is the implementation-independent
    assertion). Also state the security property: the normalizer **never joins a path to the
    filesystem** (`ZipFile.read` → `ZipFile.writestr`, both in memory), which is why zip-slip in an
    operator template cannot reach disk through it (threat T-01-06).

    Create `tests/fixtures/weekly/_author_template.py` — the rerunnable provenance for the template.
    Import `pptx` at module level (this file is `_`-prefixed under `tests/fixtures/`, so pytest
    never collects it — the `_author_fixtures.py` precedent). Build a **fabricated** one-slide deck
    on the blank layout (`prs.slide_layouts[6]`) with explicitly named textboxes:
    `NL_WEEK_TITLE`, `NL_MODULE`, `NL_HIGHLIGHTS`, `NL_LOWLIGHTS`, plus **one deliberately
    unprefixed** decorative shape named `Footer` — the `NL_` reserved prefix (RESEARCH §Pattern 2)
    only earns its keep if a non-slot shape exists to prove it, and Phase 2 needs exactly this
    fixture to test the fail-loud contract in both directions (D-03). Scrub the stock template's
    third-party core properties (Pitfall 6): set `author`, `last_modified_by`, `comments`,
    `category`, `content_status` to fabricated/neutral values and `created`/`modified` to a fixed
    datetime — no `"Steve Canny"`, no `"generated using python-pptx"`, no 2013 dates may ship.
    Route the saved bytes through `normalize_opc_zip` before writing `template.pptx`, so the
    committed binary is itself byte-reproducible. Docstring carries the literal run line
    `Run:  .venv/bin/python tests/fixtures/weekly/_author_template.py`.

    Run the author script to generate `tests/fixtures/weekly/template.pptx`.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -c "import pptx; print(pptx.__version__)"` prints a version (1.0.2 expected).
    - `git diff --exit-code -- pyproject.toml` exits 0 — no new dependency entered the project.
    - `test -f tests/fixtures/weekly/template.pptx` succeeds and the file is a valid zip:
      `.venv/bin/python -c "import zipfile;print(zipfile.ZipFile('tests/fixtures/weekly/template.pptx').testzip())"` prints `None`.
    - `grep -c 'Steve Canny\|generated using python-pptx' <(.venv/bin/python -c "import zipfile,sys;sys.stdout.write(zipfile.ZipFile('tests/fixtures/weekly/template.pptx').read('docProps/core.xml').decode())")` returns 0 matches (exit 1 from grep is the pass).
    - `.venv/bin/python -c "import zipfile,io,sys;sys.path.insert(0,'tests/fixtures/weekly');from _determinism import normalize_opc_zip;b=open('tests/fixtures/weekly/template.pptx','rb').read();assert normalize_opc_zip(b)==b, 'committed template is not already normalized'"` exits 0 (idempotence of the committed artifact).
    - `grep -c '^import pptx\|^from pptx' tests/fixtures/weekly/_determinism.py` returns 0 matches — the normalizer contract is stdlib-only.
    - The five shape names are present: `.venv/bin/python -c "from pptx import Presentation;n=[s.name for s in Presentation('tests/fixtures/weekly/template.pptx').slides[0].shapes];assert sorted(n)==['Footer','NL_HIGHLIGHTS','NL_LOWLIGHTS','NL_MODULE','NL_WEEK_TITLE'], n"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -c "import sys;sys.path.insert(0,'tests/fixtures/weekly');from _determinism import normalize_opc_zip,part_digest;b=open('tests/fixtures/weekly/template.pptx','rb').read();assert normalize_opc_zip(b)==b;print('normalized+idempotent',part_digest(b)[:16])"</automated>
  </verify>
  <done>A venv exists with the `[pptx]` extra installed and `pyproject.toml` unchanged; `_determinism.py` exports the five stdlib-only names; `_author_template.py` regenerates a scrubbed, already-normalized `template.pptx` carrying four `NL_` slots and one unprefixed decorative shape.</done>
</task>

<task type="auto">
  <name>Task 2: The spike as a durable test — assertions A, B, and the negative control C</name>
  <files>tests/test_pptx_determinism.py</files>
  <read_first>
    - `tests/test_pptx_golden.py` lines 1-60 — copy the module docstring convention (numbered load-bearing invariants, each naming the criterion it discharges) and copy lines 45-58 **verbatim in kind**: the `pytestmark = pytest.mark.skipif(importlib.util.find_spec("pptx") is None, ...)` guard with the `# noqa: E402` post-guard imports. This is the ONLY sanctioned way for `tests/` to touch `pptx` and it is what keeps the bare-install gate green.
    - `tests/test_pptx_golden.py` lines 131-141 (the corpus-is-exactly-N guard) and lines 216-229 (the determinism test idiom — the assertion message states what the failure *means*).
    - `tests/fixtures/weekly/_determinism.py` (written in Task 1) — the four helpers this module imports.
    - `src/newsletters/adapters/_timestamps.py` lines 39-61 — `EPOCH_ZERO` is tz-aware UTC; the read-back is tz-naive.
    - `.planning/phases/01-specify-de-risk/01-RESEARCH.md` §Validation Architecture "The determinism assertions later phases derive from the recorded definition" (A/B/C), §Pattern 3 (the read-back assertion + the `cp:category` / `cp:contentStatus` fields), §Pitfall 5 (the tz-naive comparison), E2 (two writes in the SAME second are already byte-identical — this is why the 3-second sleep is load-bearing).
  </read_first>
  <action>
    Create `tests/test_pptx_determinism.py`. Module docstring enumerates the three load-bearing
    assertions and what each discharges: **(A)** part-content digest equality —
    implementation-independent, the assertion the Phase 4 committed==fresh gate will use because a
    committed artifact and a CI re-render may cross zlib implementations; **(B)** full-file byte
    equality after `normalize_opc_zip` — the recorded determinism definition, scoped to a fixed
    (python-pptx, zlib) pair; **(C)** the **negative control** — without it, `render() == render()`
    passes trivially whenever both writes land in one wall-clock second, a green test that proves
    nothing.

    Define a module-level `_render_bytes(title: str) -> bytes` helper: open
    `tests/fixtures/weekly/template.pptx`, fill the `NL_`-named shapes by name over `slide.shapes`
    (not `slide.placeholders` — operator textboxes are absent from that collection), set
    `core_properties.category = "generated-by:newsletters"`,
    `core_properties.content_status = "draft"`, and `created`/`modified` to
    `EPOCH_ZERO.replace(tzinfo=None)` (imported from `newsletters.adapters._timestamps` — one epoch,
    never a second sentinel), then `prs.save(io.BytesIO())` and return `.getvalue()`. This is the
    Phase 2 writer in miniature and it deliberately lives in `tests/`, never in `src/`.

    Tests to write:
    - `test_unnormalized_double_write_differs_only_in_zip_date_time` — the negative control (C).
      Two `_render_bytes` calls separated by `time.sleep(3)` (DOS timestamps have 2-second
      granularity, so 3 seconds guarantees a differing entry; a shorter gap is the false-green trap
      RESEARCH E2 measured). Assert the raw bytes are **not** equal, `differing_parts(...) == []`
      (all unzipped parts byte-identical), and `differing_zipinfo_fields(...) == ["date_time"]`.
      The assertion message must say what a failure means: if the raw bytes ARE equal, the control
      has stopped controlling and assertion B is no longer attributable to the normalizer.
    - `test_normalized_double_write_is_byte_identical` — assertion (B): reuse the same two
      time-separated payloads; `normalize_opc_zip(a) == normalize_opc_zip(b)`.
    - `test_part_digest_is_stable_across_time_separated_writes` — assertion (A):
      `part_digest(a) == part_digest(b)`, on the **un-normalized** bytes, proving the content
      identity holds independently of any zip metadata fix.
    - `test_normalized_archive_is_valid_and_reopens_with_marker_intact` — the read-back assertion:
      `normalize_opc_zip(normalize_opc_zip(x)) == normalize_opc_zip(x)` (idempotent);
      `zipfile.ZipFile(...).testzip() is None`; `[Content_Types].xml` is the first entry;
      then reopen the normalized bytes with `Presentation(io.BytesIO(...))` and assert from the
      **written file**, never from the writer's return value: `cp.category ==
      "generated-by:newsletters"`, `cp.content_status == "draft"`, `cp.created ==
      EPOCH_ZERO.replace(tzinfo=None)`, and the five shape names survive.
    - `test_weekly_fixture_corpus_is_exactly_the_committed_template` — mirror the golden corpus
      guard: exactly one `.pptx` under `tests/fixtures/weekly/`, named `template.pptx`.

    Share the two time-separated payloads via a module-scoped fixture so the suite sleeps 3 seconds
    once, not four times.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest tests/test_pptx_determinism.py -x -q` exits 0 with 5 tests passed and total runtime under 30 seconds.
    - `grep -c 'pytestmark = pytest.mark.skipif' tests/test_pptx_determinism.py` returns at least 1.
    - `grep -c 'time.sleep(3)' tests/test_pptx_determinism.py` returns at least 1 — the delay is present and load-bearing.
    - The negative control genuinely controls: temporarily reducing the sleep is NOT required, but `grep -v '^#' tests/test_pptx_determinism.py | grep -c 'raw_a != raw_b\|!= *raw_b\|is not byte-equal'` returns at least 1, i.e. an inequality assertion exists.
    - `.venv/bin/python -m pytest tests/test_pptx_determinism.py -q -p no:cacheprovider --collect-only 2>&1 | grep -c 'error'` returns 0 matches.
    - Bare-install parity: `grep -v '^#' tests/test_pptx_determinism.py | grep -n 'import pptx\|from pptx' | head -1` shows the import occurs AFTER the `pytestmark` skip guard line number.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pptx_determinism.py -x -q</automated>
  </verify>
  <done>Five tests pass. The measured outcome is now reproducible on demand: part digests match across time-separated writes (A), normalized bytes are identical (B), and un-normalized bytes across a 3-second boundary are proven NOT identical (C), so the green in B is attributable to the normalizer.</done>
</task>

<task type="auto">
  <name>Task 3: Record the committed evidence, then prove the spike left no production surface</name>
  <files>tests/fixtures/weekly/_record_determinism_evidence.py, .planning/notes/2026-08-29-pptx-determinism-evidence.json</files>
  <read_first>
    - `.planning/phases/01-specify-de-risk/01-RESEARCH.md` §Code Examples "The determinism spike, as it should be committed" — the `evidence` dict is the schema; §Pitfall 1 (why full-file hashes must NOT be asserted across environments); §Determinism Spike — Measured Results E1-E8 (the expected values).
    - `.planning/phases/01-specify-de-risk/01-PATTERNS.md` §No Analog Found — there is no committed-measurement artifact in this repo; the `EXPECTED`-table convention (a `note` per row) is the closest idea and should be carried into the JSON.
    - `tests/test_ai_optional.py` lines 119-146 (`test_import_linter_contract_holds` — it looks for `.venv/bin/lint-imports` first) and lines 444-460 (`test_pptx_extra_declared`, `test_pptx_loader_has_no_toplevel_pptx_import`).
    - `.planning/ROADMAP.md` Phase 1 criterion 5 — the exact five things that must be left untouched.
  </read_first>
  <action>
    Create `tests/fixtures/weekly/_record_determinism_evidence.py`. Default mode performs the real
    double write (reusing `_author_template`'s template and the same fill/marker steps as the test
    helper) with `time.sleep(3)` between runs, and writes
    `.planning/notes/2026-08-29-pptx-determinism-evidence.json` with these keys:
    `recorded` (ISO date), `python`, `python_pptx`, `zlib` (`zlib.ZLIB_RUNTIME_VERSION`),
    `platform`, `template_sha256`, `seconds_between_writes`, `raw_a_sha256`, `raw_b_sha256`,
    `raw_bytes_equal`, `varying_parts`, `varying_zip_fields`, `normalized_a_sha256`,
    `normalized_b_sha256`, `normalized_bytes_equal`, `part_digest_a`, `part_digest_b`,
    `outcome` (the one-line recorded outcome string), and a `notes` mapping carrying one short
    explanatory note per non-obvious key (the `EXPECTED`-table convention).

    Add a `--check` mode that re-runs the measurement and compares only the
    **implementation-independent** fields against the committed JSON — `raw_bytes_equal`,
    `varying_parts`, `varying_zip_fields`, `normalized_bytes_equal`, `part_digest_a`,
    `part_digest_b` — exiting 1 with a teaching message on any drift. The script docstring must
    state explicitly that `raw_*_sha256` and `normalized_*_sha256` are recorded as evidence but
    **never asserted across environments**, because DEFLATE output is zlib-implementation-dependent
    (Pitfall 1); asserting them would produce a test that is green locally and red in CI with
    identical part content. Docstring carries the literal run lines for both modes.

    Run it, then run `--check` to prove the committed artifact is bound to a live measurement
    (this is the mitigation for threat T-01-03, evidence fabrication: the numbers cannot be typed
    by hand and survive).

    Then perform the SC-5 no-production-surface sweep and fix anything it catches:
    `git diff --exit-code -- src/newsletters/semantic.py pyproject.toml .github/workflows/ci.yml .importlinter`;
    `git diff --exit-code -- tests/fixtures/pptx/` (the golden `.pptx` corpus must not have been
    regenerated); `.venv/bin/lint-imports`; `.venv/bin/python -m pytest tests/test_ai_optional.py -x -q`;
    and confirm `git status --porcelain` lists only the six intended new files — delete any scratch
    file, temp deck, or exploratory script rather than committing it.
  </action>
  <acceptance_criteria>
    - `.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check` exits 0.
    - `.venv/bin/python -c "import json;d=json.load(open('.planning/notes/2026-08-29-pptx-determinism-evidence.json'));assert d['part_digest_a']==d['part_digest_b'];assert d['normalized_bytes_equal'] is True;assert d['raw_bytes_equal'] is False;assert d['varying_parts']==[];assert d['varying_zip_fields']==['date_time'];assert d['seconds_between_writes']>=3;print('evidence OK',d['outcome'])"` exits 0.
    - The evidence file records both runs' hashes as distinct values: `.venv/bin/python -c "import json;d=json.load(open('.planning/notes/2026-08-29-pptx-determinism-evidence.json'));assert d['raw_a_sha256']!=d['raw_b_sha256'];assert d['normalized_a_sha256']==d['normalized_b_sha256'];assert d['python_pptx'] and d['zlib']"` exits 0.
    - `git diff --exit-code -- src/newsletters/semantic.py pyproject.toml .github/workflows/ci.yml .importlinter` exits 0.
    - `git diff --exit-code -- tests/fixtures/pptx/` exits 0.
    - `.venv/bin/lint-imports` exits 0 and its output contains `KEPT` for both contracts.
    - `.venv/bin/python -m pytest tests/test_ai_optional.py -x -q` exits 0.
    - `grep -rn '^import pptx\|^from pptx' src/newsletters/ | wc -l` prints `0`.
    - `git status --porcelain | grep -c '^??'` counts only the intended new paths (`tests/fixtures/weekly/`, `tests/test_pptx_determinism.py`, `.planning/notes/2026-08-29-pptx-determinism-evidence.json`) — no scratch decks, no stray `.py`.
  </acceptance_criteria>
  <verify>
    <automated>.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check && .venv/bin/python -m pytest tests/test_ai_optional.py tests/test_pptx_determinism.py -x -q && .venv/bin/lint-imports</automated>
  </verify>
  <done>The measured evidence is committed and re-verifiable by a committed script; `semantic.py`, `pyproject.toml`, the CI workflow, the import-linter config and the golden `.pptx` corpus are all byte-unchanged; `lint-imports` reports KEPT; no unguarded `pptx` import exists under `src/newsletters/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | `pip install -e '.[dev,test,pptx,config]'` pulls third-party wheels into the build environment |
| repo working tree → committed artifacts | a spike can leave scratch code, a real operator deck, or fabricated numbers behind |
| operator `.pptx` (zip) → normalizer | Phase 2 will feed operator-supplied archives through `normalize_opc_zip`; the contract is written here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `pip install -e '.[dev,test,pptx,config]'` | mitigate | No new package is added. `python-pptx` is pre-existing (`pyproject.toml:43`) and its `SUS`/`unknown-downloads` verdict is adjudicated in RESEARCH §Package Legitimacy Audit as the known PyPI-API false positive already recorded in `pyproject.toml`. Task 1 proves the absence of a supply-chain change with `git diff --exit-code -- pyproject.toml`; Task 3 re-runs `tests/test_ai_optional.py::test_pptx_extra_declared`, which asserts the extra is exactly `{python-pptx}`. Surfaced for EiC confirmation at PR review (see `<verification>` human-check). |
| T-01-03 | Repudiation | `.planning/notes/2026-08-29-pptx-determinism-evidence.json` | mitigate | Evidence fabrication is the real risk in an evidence-first phase. The artifact is produced by a committed, rerunnable recorder; `--check` re-measures and fails on drift of every implementation-independent field; `tests/test_pptx_determinism.py` independently re-derives the same invariants. Hand-typed numbers cannot survive either gate. |
| T-01-02 | Information disclosure | `tests/fixtures/weekly/template.pptx` | mitigate | The template is fabricated, never an operator deck. Stock-template core properties (`"Steve Canny"`, `"generated using python-pptx"`, 2013 dates) are scrubbed in `_author_template.py` and the absence is asserted against the written `docProps/core.xml` (Pitfall 6). |
| T-01-05 | Elevation of privilege | `src/newsletters/semantic.py` | mitigate | No auto-publish path may be created. No task touches `semantic.py`; `git diff --exit-code` proves it byte-unchanged (D-01: the weekly reuses `Surface(REPORT)`, so no kind change is needed). |
| T-01-06 | Tampering | `normalize_opc_zip` | mitigate | Zip-slip is unreachable by construction: the normalizer reads with `ZipFile.read(name)` and writes with `ZipFile.writestr(ZipInfo, data)` — no path is ever joined to the filesystem. Stated in the module docstring so Phase 2 inherits the property rather than rediscovering it. |
| T-01-07 | Information disclosure | XXE via operator OOXML | transfer | Mitigated upstream: python-pptx parses with `etree.XMLParser(remove_blank_text=True, resolve_entities=False)` (`pptx/oxml/__init__.py:21`). Recorded in plan 01-02's decision note so Phase 2 never builds a second parser without it. |
| T-01-08 | Denial of service | decompression bomb in an operator template | accept | The renderer reads a deck the operator supplies about their own machine — the same trust level as their own files. If a limit is ever wanted, `ZipInfo.file_size` is checkable before `read()`. |
| T-01-09 | Tampering | `tests/fixtures/pptx/` golden corpus | mitigate | A regenerated golden deck would silently invalidate the ADAPT-06 expectations. `git diff --exit-code -- tests/fixtures/pptx/` gates it in Task 3. |
</threat_model>

<verification>
Run each gate ONCE (rapid re-runs throw transient errors in this environment), independently of
any executor's "green":

1. `.venv/bin/python -m pytest tests/test_pptx_determinism.py -x -q` → 5 passed
2. `.venv/bin/python -m pytest tests/test_ai_optional.py -x -q` → passed
3. `.venv/bin/python -m pytest -q` → full suite, no regression vs. the pre-plan baseline
4. `.venv/bin/lint-imports` → both contracts KEPT
5. `.venv/bin/python tests/fixtures/weekly/_record_determinism_evidence.py --check` → exit 0
6. `git diff --exit-code -- src/newsletters/ pyproject.toml .github/workflows/ci.yml .importlinter tests/fixtures/pptx/` → exit 0

<human-check>
At PR review (workflow.human_verify_mode = end-of-phase): confirm that no new package entered
`pyproject.toml` — `python-pptx` is the pre-existing `[pptx]` extra, installed here for the first
time in this workspace rather than added. This is the package-legitimacy confirmation for the
`SUS` (`unknown-downloads`) verdict recorded in RESEARCH §Package Legitimacy Audit.
</human-check>
</verification>

<success_criteria>
- A real python-pptx double write ran with a ≥3-second gap and its outcome is committed as measured evidence (ROADMAP SC-2, first half).
- The spike is reproducible: `tests/test_pptx_determinism.py` re-proves assertions A, B and the negative control C on every run.
- The negative control exists and asserts inequality — the determinism assertion is falsifiable.
- Zero production surface: `src/newsletters/`, `pyproject.toml`, `.github/workflows/ci.yml`, `.importlinter` and `tests/fixtures/pptx/` are byte-unchanged; `lint-imports` KEPT; no column-0 `pptx` import under `src/` (ROADMAP SC-5).
- No scratch code survives — every spike artifact landed as a test fixture or a committed evidence record (CONTEXT specifics).
</success_criteria>

<output>
Create `.planning/phases/01-specify-de-risk/01-01-SUMMARY.md` when done. It MUST quote the measured
outcome verbatim (`raw_bytes_equal`, `varying_zip_fields`, `normalized_bytes_equal`, both part
digests, the `python_pptx` and `zlib` versions) — plan 01-02 turns that measurement into the
recorded decision and must not have to re-measure it.
</output>

<artifacts_this_phase_produces>
This plan's slice of the phase's artifacts (the complete phase-wide table lives in `01-03-PLAN.md`):

| Path | New/Modified | What it is |
|------|--------------|-----------|
| `tests/fixtures/weekly/_determinism.py` | NEW | The ONE normalizer + content-digest contract: `DOS_EPOCH`, `normalize_opc_zip`, `part_digest`, `differing_parts`, `differing_zipinfo_fields`. Stdlib only — no `pptx` import, so it stays importable on a bare install. Phase 2 moves it to `src/newsletters/_pptx_writer.py` behind the `[pptx]` extra. |
| `tests/fixtures/weekly/_author_template.py` | NEW | Rerunnable provenance for the fabricated template; scrubs the stock python-pptx core properties (Pitfall 6). Run line: `.venv/bin/python tests/fixtures/weekly/_author_template.py` |
| `tests/fixtures/weekly/template.pptx` | NEW (generated) | The synthetic named-placeholder template: `NL_WEEK_TITLE`, `NL_MODULE`, `NL_HIGHLIGHTS`, `NL_LOWLIGHTS` plus one deliberately unprefixed `Footer`. Phase 2 renders through this. |
| `tests/test_pptx_determinism.py` | NEW | The spike as a durable fixture — assertion A (part-content digest), B (byte equality after normalization), C (the negative control that makes B falsifiable). |
| `tests/fixtures/weekly/_record_determinism_evidence.py` | NEW | The evidence recorder, with a `--check` mode that re-measures and fails on drift. |
| `.planning/notes/2026-08-29-pptx-determinism-evidence.json` | NEW | The committed measurement: both runs' hashes, `varying_parts`, `varying_zip_fields`, both part digests, the exercised `python-pptx` and `zlib` versions. |

**Named decisions this plan produces:** none — this plan produces *measurement*. The decisions it
feeds are recorded in `01-02-PLAN.md`. The one thing it does settle by construction is the
**shape** of the reserved-prefix template contract (D-03): four `NL_` slots plus one unprefixed
decorative shape, so Phase 2 can test the fail-loud contract in both directions against a real
fixture.
</artifacts_this_phase_produces>
