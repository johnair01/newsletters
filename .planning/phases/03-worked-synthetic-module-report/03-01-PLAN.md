---
phase: 03-worked-synthetic-module-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/module/module-a.yml
  - src/newsletters/modulesite.py
  - content/module/ids.json
  - content/module/site/
requirements: [MODA-01]
autonomous: true

must_haves:
  truths:
    - "A committed content/module/module-a.yml declares module-a (area-bem) with the five §4 lanes, fabricated naming only, and exercises the real contract (up/down/Δ==0/single-endpoint KPI movement mix, one zero-KPI lane, a traced module-owner quote)"
    - "build_module_surfaces() loads the config via load_swimlanes, composes via compose_module_report with the owner quote wired, and returns one Draft REPORT Surface"
    - "build_module_site() loads content/module/ids.json (first ref R-001), builds the Site, saves the ledger, and renders content/module/site/ reusing render.py devices + the shared self-hosted fonts"
    - "The committed content/module/site/ and content/module/ids.json equal a fresh build (committed==fresh-build norm)"
  artifacts:
    - path: "content/module/module-a.yml"
      provides: "The synthetic worked-example config (fabricated §5 scheme, five §4 lanes, KPI movement mix, zero-KPI lane, module-owner quote)"
      contains: "module-a"
    - path: "src/newsletters/modulesite.py"
      provides: "build_module_surfaces() + build_module_site() — the corpus builder mirroring worksurface.py, zero edits to existing modules"
      exports: ["build_module_surfaces", "build_module_site"]
    - path: "content/module/ids.json"
      provides: "The module corpus's own append-only ledger, first entry R-001"
      contains: "R-001"
    - path: "content/module/site/library.html"
      provides: "The rendered module Library index (committed built output)"
  key_links:
    - from: "src/newsletters/modulesite.py"
      to: "src/newsletters/swimlane.py::load_swimlanes"
      via: "load the config into a SwimlaneLoad"
      pattern: "load_swimlanes"
    - from: "src/newsletters/modulesite.py"
      to: "src/newsletters/compose.py::compose_module_report"
      via: "compose the Draft REPORT surface with quote/owner wired"
      pattern: "compose_module_report"
    - from: "src/newsletters/modulesite.py"
      to: "src/newsletters/render.py::render_surface"
      via: "render each page reusing the PROV-03 devices (claim-beside-trace + honesty panel)"
      pattern: "render_surface"
    - from: "src/newsletters/modulesite.py"
      to: "content/module/ids.json"
      via: "Ledger.load -> Site.from_surfaces -> ledger.save() (R-001)"
      pattern: "Ledger\\.load"
---

<objective>
Create the committed synthetic `module-a` config and the corpus builder that composes and renders it
end-to-end (loader → composer → ledger → render → Library) into a self-contained `content/module/`
corpus with its own R-001 ledger. This is the worked-example half of MODA-01: a gate-visible,
byte-stable example that exercises the REAL contract (traced claims beside verbatim traces, a
populated honesty panel), not a happy-path brochure.

Purpose: prove the Phase-1 loader + Phase-2 composer path works on real, committed config, producing
a rendered Library with claim-beside-trace and an honestly-populated honesty panel.

Output: `content/module/module-a.yml`, `src/newsletters/modulesite.py`
(`build_module_surfaces` + `build_module_site`), and the committed built output
(`content/module/ids.json` with R-001 + `content/module/site/`).
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-worked-synthetic-module-report/03-CONTEXT.md
@.planning/research/PITFALLS.md

# The as-built code this builder reuses (read the ones you touch):
@src/newsletters/worksurface.py
@src/newsletters/compose.py
@src/newsletters/swimlane.py
@src/newsletters/site.py
@src/newsletters/render.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the synthetic module-a config</name>
  <files>content/module/module-a.yml</files>
  <read_first>
    - src/newsletters/swimlane.py (the recognized structural keys the LOADER actually reads:
      `lanes`, `heading`, `kpis`, `label`, `value`, `values`; every other scalar is minted
      generically. Module-level keys outside `lanes` are minted into `load.claims`.)
    - .planning/phases/03-worked-synthetic-module-report/03-CONTEXT.md (the LOCKED naming scheme +
      the mandated movement mix)
    - .planning/research/PITFALLS.md (Pitfall 4 scalar-location traps: to stay verbatim-locatable,
      use UNQUOTED, single-line, STRING-typed scalar tokens — avoid YAML type coercion like
      `yes`/`007`/`1.0`/dates, avoid anchors/aliases/block scalars; those would route to
      unextracted[] and muddy the worked example. Pitfall 8 confidentiality: fabricated names only.)
  </read_first>
  <action>
    Author `content/module/module-a.yml` grounded in the LOADER's recognized keys (verified against
    swimlane.py, not assumed). Top-level: a generic `module: module-a` scalar and `area: area-bem`
    scalar (module-level, minted into load.claims), a generic module-owner quote pair using generic
    structural keys the loader reads as scalars — `quote:` (the verbatim owner sentence) and
    `owner:` (the owner id, e.g. `owner-safety`) — at MODULE level so both become module-level
    traced claims. Then `lanes:` — a list of exactly the five §4 lanes per CONTEXT, in this
    fabricated scheme: Safety & Culture (owner `owner-safety`), MA incl. MA cost (owner `owner-ma`),
    Quality (owner `owner-quality`), VF Transfer (owner `owner-vf`), MOR/IQ tools & defect projects
    (owner `owner-mor`). Each lane: a `heading:` scalar, a lane `owner:` scalar, and a `kpis:` list.
    Engineers `eng-01`…, tools `toolset-1`…, metrics fabricated-generic (e.g. `throughput-index`,
    `defect-rate`) — nothing resembling real fab/tool/metric/site/program nomenclature; NO colleague
    names.

    Design the KPI movement mix so the honesty panel is POPULATED HONESTLY (per D in CONTEXT):
    - at least ONE `values: [start, close]` KPI that moves UP (close > start),
    - at least ONE `values: [start, close]` KPI that moves DOWN,
    - at least ONE `values: [start, close]` KPI with Δ==0 (start == close — honest no-change),
    - at least ONE `values:` KPI declared with a SINGLE endpoint (a one-element list) so the
      composer discloses "declares period movement but only one endpoint is usable" in missing[],
    - at least ONE lane with NO `kpis` (or an empty kpis list) so the composer discloses
      "declares no KPIs — strip omitted".
    Keep every KPI number an unquoted single-line integer/decimal token (e.g. `values: [10, 18]`)
    that appears VERBATIM in the raw file, and matching units consistent between endpoints so
    compute_delta preserves the unit (per compose.py). Add a top-of-file comment stating the file is
    SYNTHETIC and fabricated (no real names) — but do NOT encode load-bearing meaning in comments
    (Pitfall 9: parsers drop comments).

    This implements MODA-01's "fabricated §5 scheme + five §4 lanes + KPI movement mix + zero-KPI
    lane + traced module-owner quote via a generic key the loader already reads."
  </action>
  <verify>
    <automated>python -c "from newsletters.swimlane import load_swimlanes; l=load_swimlanes('content/module/module-a.yml'); assert len(l.bindings)==5, len(l.bindings); assert l.all_claims; assert all(t.is_addressed for c in l.all_claims for t in c.evidence); print('lanes',len(l.bindings),'claims',len(l.all_claims),'unextracted',len(l.all_unextracted))"</automated>
  </verify>
  <done>
    load_swimlanes over the committed config returns 5 SectionBindings, every minted claim is
    content-addressed (is_addressed True), the read-anchored coverage identity holds (no RuntimeError
    raised), and the config carries the mandated movement mix + zero-KPI lane + a module-level quote
    scalar. All names are fabricated (no real nomenclature).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build modulesite.py — build_module_surfaces + build_module_site</name>
  <files>src/newsletters/modulesite.py</files>
  <read_first>
    - src/newsletters/worksurface.py (THE structural template: build_work_surfaces /
      build_work_site / _emit_fonts / the Ledger.load → Site.from_surfaces → ledger.save() flow →
      render_surface per page + render_library. MIRROR this exactly for the module corpus.)
    - src/newsletters/compose.py (compose_module_report signature: `load`, `author`, `quote`,
      `owner`, `ledger`, `ledger_path="content/module/ids.json"` — it does ref_for internally,
      never save; the caller owns save. The sourced-or-omit QuoteBlock renders ONLY when `quote` is
      a content-addressed Claim.)
    - src/newsletters/swimlane.py (SwimlaneLoad: `source`, `bindings`, `claims` module-level, and
      `all_claims`; the loader mints module-level `quote`/`owner` scalars into `load.claims`.)
    - src/newsletters/_yaml_loader.py (the lazy `load_config` safe_load boundary — reuse it to
      re-read the module-level quote/owner values so the builder can select the matching claim.)
    - src/newsletters/site.py (Ledger.load/ref_for/save; Site.from_surfaces(surfaces, ledger=))
  </read_first>
  <action>
    Create `src/newsletters/modulesite.py` mirroring `worksurface.py`, importing ONLY stdlib +
    the existing sibling modules (swimlane, compose, site, render, and REUSING
    `worksurface._emit_fonts` for the shared self-hosted fonts — zero-edit reuse per CLAUDE.md). Do
    NOT put the builder in compose.py (compose.py's leaf rule forbids importing render/site). Export
    `build_module_surfaces` and `build_module_site`.

    `build_module_surfaces(config_path="content/module/module-a.yml", *, root=None) -> list[Surface]`:
      1. `load = load_swimlanes(config_path, root=root)`.
      2. Select the module-owner quote: re-parse the raw config's module-level scalars via the lazy
         `_yaml_loader.load_config(load.source.transcript)` to read the generic `quote` value and
         `owner` value; then select from `load.claims` the traced Claim whose `.text` equals the
         quote value (verbatim identity — the loader already content-addressed it). If no matching
         traced claim is found, pass `quote=None` (compose then discloses the omission — never
         fabricate). This is the 02-03 hand-off: `quote=` is the selected Claim, `owner=` is the
         owner id string.
      3. `surface = compose_module_report(load, quote=<selected claim>, owner=<owner id>)`. Do NOT
         pass a ledger here (mirror build_work_surfaces which does not touch the ledger); compose
         defaults to Ledger.load("content/module/ids.json"), assigns the ref, never saves.
      4. Return `[surface]`.

    `build_module_site(out_dir="content/module/site", *, root=None, config_path="content/module/module-a.yml") -> list[Path]`:
      Mirror build_work_site EXACTLY: `out.mkdir(parents=True, exist_ok=True)`;
      `surfaces = build_module_surfaces(config_path, root=root)`;
      `ledger = Ledger.load("content/module/ids.json")`;
      `site = Site.from_surfaces(surfaces, ledger=ledger)`; `ledger.save()` (this is the SOLE ledger
      writer — first ref R-001 for the report slug); then for each `page in site.pages()` write
      `render_surface(page.surface, site=site, page=page)` to `out / page.href`; write
      `render_library(site)` to `out / "library.html"`; call `worksurface._emit_fonts(out)` so the
      module Library is self-contained (zero external call). Return the written paths.

    Determinism: rely on the loader/composer's EPOCH_ZERO + file order; no datetime.now(), no set(),
    no non-total sort (Pitfalls 5/6). The slug from compose is `report-<config-stem>`
    (e.g. `report-module-a`) and Site.from_surfaces reads the same slug — R-001 stays stable.
  </action>
  <verify>
    <automated>python -c "from newsletters.modulesite import build_module_surfaces; s=build_module_surfaces()[0]; assert s.gate.value=='draft', s.gate; assert s.missing, 'honesty panel must be populated'; from newsletters.semantic import QuoteBlock; assert any(isinstance(b,QuoteBlock) for b in s.blocks), 'owner quote must render (sourced path)'; print('draft ok, missing', len(s.missing))"</automated>
  </verify>
  <done>
    build_module_surfaces returns one Draft REPORT Surface with a rendered QuoteBlock (owner quote
    wired via the sourced path) and a populated missing[] (the single-endpoint disclosure + zero-KPI
    lane + any quote/lane gaps). modulesite.py imports no AI/yaml at module top level and edits no
    existing module.
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate + commit the built module corpus output</name>
  <files>content/module/ids.json, content/module/site/</files>
  <read_first>
    - src/newsletters/worksurface.py (the committed==fresh-build norm: content/work/ids.json +
      content/work/site/ are committed built artifacts)
  </read_first>
  <action>
    Run `build_module_site()` from the repo root to generate the committed corpus output:
    `content/module/ids.json` (append-only ledger, first ref R-001 for `report-module-a`) and
    `content/module/site/` (the rendered `report-module-a.html` + `library.html` + `fonts/`). This
    establishes the committed==fresh-build baseline that Plan 03 verifies. Commit these built
    artifacts alongside the config + builder as ONE atomic commit for this plan
    ("feat(03-01): synthetic module-a config + corpus builder + committed render"). Do NOT push.

    Verify the ledger's first entry is R-001 and that a SECOND build produces byte-identical output
    (byte-stable / deterministic) before committing — a non-deterministic committed artifact would
    fail Plan 03's committed==fresh-build check.
  </action>
  <verify>
    <automated>python -c "import json,pathlib; d=json.loads(pathlib.Path('content/module/ids.json').read_text()); refs={v['ref'] for v in d.values()}; assert 'R-001' in refs, d; assert pathlib.Path('content/module/site/library.html').exists(); assert list(pathlib.Path('content/module/site/fonts').glob('*.woff2')); print('R-001 ledger + rendered site + fonts committed-ready')"</automated>
  </verify>
  <done>
    content/module/ids.json exists with first ref R-001; content/module/site/ contains
    report-module-a.html, library.html and self-hosted fonts/; a repeat build is byte-identical.
    All artifacts committed (not pushed) in one atomic commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → loader | Untrusted YAML content is read as data (`yaml.safe_load` only, via the Phase-1 `_yaml_loader` boundary) — never executed |
| config content → rendered HTML | Config-derived claim text / owner quote / honesty-panel strings cross into HTML |
| corpus path → filesystem | `config_path` / `out_dir` path handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/Elevation | YAML parse of config | mitigate | Reuse Phase-1 `_yaml_loader.load_config` (`yaml.safe_load` only — no arbitrary object construction). No new parse path. |
| T-03-02 | Injection (XSS) | render_surface over config content | mitigate | REUSE render.py's existing `_e` escaping (honesty panel line 788/794/797, claim spans) — do NOT re-implement; verify (Plan 03) that config content is escaped in output |
| T-03-03 | Information Disclosure | committed content/module/ | mitigate | Synthetic fabricated names only (Task 1); confidentiality check lands in Plan 03 |
| T-03-04 | Tampering (path) | build_module_site out_dir / config_path | accept | Corpus paths are fixed committed defaults, not user input; loader already bounds config path via `relative_to(root)` |
| T-03-05 | DoS/Network | rendered output | mitigate | Self-hosted fonts via reused `_emit_fonts`; zero external call (no-external-call test in Plan 03) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this phase — PyYAML was added + audited in Phase 1; nothing new to vet |
</threat_model>

<verification>
- `python -c "from newsletters.swimlane import load_swimlanes; load_swimlanes('content/module/module-a.yml')"` loads clean (coverage identity holds).
- `python -c "from newsletters.modulesite import build_module_site; build_module_site('/tmp/msite')"` renders without error.
- Enforced gates (re-run independently): full `pytest`, `lint-imports`, bare-install import check
  (`python -c "import newsletters; import newsletters.modulesite"` exit 0, no yaml/AI reachable
  from core). Advisory: `black`, `isort --profile black`, `mypy` on touched files — no NEW failures
  vs the 2026-07-02 baseline.
</verification>

<success_criteria>
- content/module/module-a.yml declares module-a (area-bem) with five §4 lanes, fabricated naming,
  the mandated KPI movement mix, one zero-KPI lane, and a traced module-owner quote.
- modulesite.py exposes build_module_surfaces + build_module_site, mirroring worksurface.py, editing
  no existing module (zero-edit reuse of render.py/site.py/worksurface.py).
- content/module/ids.json (R-001) + content/module/site/ are committed and byte-stable.
- The composed surface is Draft with a rendered owner QuoteBlock and a populated honesty panel.
</success_criteria>

<output>
Create `.planning/phases/03-worked-synthetic-module-report/03-01-SUMMARY.md` when done.
</output>
