---
phase: 09-rev2-site-ia-navigation-source-links
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/newsletters/render.py
  - src/newsletters/diagrams.py
  - src/newsletters/dogfood.py
  - tests/test_render.py
autonomous: true
requirements: [SITE-05, SITE-06]
must_haves:
  truths:
    - "A cited source with a file-path locator (e.g. docs/vision.md) renders as a working link to the repo blob URL"
    - "A cited source with a session/transcript locator and no resolvable target renders as plain text — never a dead link"
    - "The fan-out diagram's four surface boxes are SVG <a> anchors (no JS) and FanoutBlock rows link to the named sibling surface"
    - "Re-rendering content/rev1/site/ is byte-identical (no datetime.now() in the HTML) and every emitted page carries the generated-by-render marker"
    - "Every nav target / Home Enter link resolves to a file that actually exists on disk — no dead link"
  artifacts:
    - path: "src/newsletters/render.py"
      provides: "source_base_url + link_for_source(); evidence chips + FanoutBlock rows linked; generated marker in _page; Home links wired"
      contains: "def link_for_source"
    - path: "src/newsletters/diagrams.py"
      provides: "fanout(links=None) wraps surface boxes in SVG <a> anchors"
      contains: "def fanout"
    - path: "tests/test_render.py"
      provides: "link_for_source rule tests + byte-stable regen + generated-marker + no-dead-link coverage tests"
      contains: "def test_"
  key_links:
    - from: "src/newsletters/render.py:link_for_source"
      to: "source_base_url (GitHub blob root) / in-site anchor / plain text"
      via: "the resolved link-target rule"
      pattern: "link_for_source"
    - from: "src/newsletters/render.py:_block_html(FanoutBlock)"
      to: "Site.by_slug(slugify(title)) → page.href"
      via: "FanoutLink.href resolution"
      pattern: "FanoutLink|by_slug"
    - from: "src/newsletters/diagrams.py:fanout"
      to: "SVG <a> anchors on the surface boxes"
      via: "the optional links map"
      pattern: "<a"
---

<objective>
Wave 3 — make the fan-out diagram and every cited source render as WORKING links (SITE-05), and
prove the whole site regenerates from `render.py` with no hand-edited HTML (SITE-06).

Introduce a single configurable `source_base_url` (default = the GitHub repo blob root that
matches Home §7's repo lockup) and a `link_for_source()` helper with one resolution rule:
file-path locator → repo blob URL; session/transcript locator → in-site anchor to the recording
surface; neither → plain text (NEVER a dead link). Populate the long-unused `FanoutLink.href`
(`semantic.py:355`), turn the `diagrams.fanout()` SVG boxes into SVG `<a>` anchors (no JS), wire
evidence chips + Home §5/§7/footer links, add a generated-by-render marker to every page, and
add the byte-stable regen + no-dead-link test.

Purpose: ROADMAP SC4 (every cited source = a working link, e.g. `vision.md` → repo file) + SC5
(all output regenerated from templates; no hand-edited HTML).

Output: `source_base_url` + `link_for_source()` in `render.py`; linked evidence chips + linked
FanoutBlock rows + SVG-anchor fan-out (`diagrams.fanout(links=...)`); wired Home links; the
generated marker in `_page`; the byte-stable regen + generated-marker + no-dead-link tests.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-CONTEXT.md
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-UI-SPEC.md
@docs/surfaces.md
@docs/design-system.md
@src/newsletters/render.py
@src/newsletters/diagrams.py
@src/newsletters/semantic.py
@src/newsletters/locators.py
@src/newsletters/dogfood.py
@src/newsletters/site.py
@tests/test_site.py
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-02-SUMMARY.md
</context>

<interface_context>
Depends on Wave 2: `_nav_targets(site)` resolves each surface-type hub href; the board + nav
chrome exist. This wave consumes those resolved hub hrefs as the in-site anchor targets and as
the §5 "Enter →" / fan-out box destinations.

The link-target rule (UI-SPEC §SITE-05 — the resolved decision):
- `source_base_url` default = `https://github.com/nneibaue/newsletters/blob/main/` (matches the
  Home §7 repo lockup `nneibaue / newsletters`, surfaces.md L68). `base_url=None` → relative
  (`../path`) for fully-offline output.
- `link_for_source(source, *, base_url)`:
  1. FILE-PATH locator — a `FreeLocator.text` / `Source.context` that looks like a repo path
     (e.g. `docs/vision.md`, `CLAUDE.md`, `semantic.py`, `src/newsletters/render.py`) →
     `{source_base_url}{path}`. The dogfood corpus carries these: `dogfood.py:216` `locator="docs/"`,
     `:219` `CLAUDE.md`, `:222` `semantic.py`, `:351` `render.py`, `:528` `context="docs/roadmap.md"`.
  2. SESSION/TRANSCRIPT locator — `SessionLocator`, or a `source_id` like `session-kickoff` /
     `doc-roadmap` with no file path → an in-site anchor to the surface that records it (resolve
     via `Site.by_slug` / a source→surface map). No in-site target → PLAIN TEXT (current
     behavior, faithful, never a 404).
  3. transcript-prefix locators do NOT suffice as link targets → plain text.
  Detect "looks like a repo path" deterministically (e.g. contains `/` or ends in a known
  source extension `.md/.py/.txt/...`); keep it stdlib-only.

Evidence chips: `_block_html` ClaimsBlock branch (`render.py:211-225`) currently renders
`{source_id}:{locator.display}` as a plain `<span class="ev-chip">`. Wrap in `<a href=...>` when
`link_for_source` yields a URL; keep the plain `<span>` otherwise. Locator types: `FreeLocator.text`
/ `.display`, `SessionLocator.source_id` / `.display` (`locators.py:33-55`). `Trace.source_id` /
`Trace.locator` (`semantic.py:95-96`); `Claim.evidence` is `list[Trace]`.

Fan-out: `FanoutLink(kind, title, href=None)` (`semantic.py:352`). `_block_html` FanoutBlock
branch (`render.py:266-273`) ignores `href` today — render the row as `<a href=...>` when an
href is present OR resolves via `Site.by_slug(slugify(title))` (the dogfood FanoutLinks name real
sibling surfaces by title: `dogfood.py:259-261, 339-342, ...`). The fan-out SVG
`diagrams.fanout()` (`diagrams.py:100-111`) draws four `_box(...)`; add an optional
`links: dict[str,str] | None = None` param (label → href) and wrap each box in an SVG `<a href=...>`
(valid SVG, no JS) when a link is present; `None` keeps the current static behavior. The
`render_library` intro fan-out (`render.py:413-417`) passes the resolved nav-target map.

Home links (Wave 1 left these as anchors/placeholders): §5 "Enter →" → each surface-type hub
(nav target); §7 "Clone the repo"/repo lockup → `https://github.com/nneibaue/newsletters`,
"Read the spec" → `{source_base_url}docs/architecture.md`; §1/§8 GitHub buttons → the repo.
Footer `<a>` slots (`render.py:300-307`) → real URLs.

SITE-06 regen (UI-SPEC §SITE-06; pattern = `tests/test_site.py` byte-stability tests):
- `_page` (`render.py:310`) → add `<!-- generated by newsletters.render; do not hand-edit -->`
  once (applies to surfaces + Home + board uniformly).
- Determinism: NO `datetime.now()` / nothing time-varying in the rendered HTML (the new Home/board
  emit nothing time-varying; if a date is needed, source it from `Page.date`, never `now()`).
- The test: render `build_site(tmp_path)` and compare a second render → byte-identical per file;
  assert every emitted `*.html` contains the marker; assert the rendered set == `{index.html,
  library.html} ∪ {one file per page.href}` and every nav target resolves to a file on disk.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: source_base_url + link_for_source() + linked evidence chips (SITE-05 part 1)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - Test: link_for_source for a Source/Trace whose locator is a file path (docs/vision.md, CLAUDE.md, semantic.py) returns "{source_base_url}docs/vision.md" etc. (the repo blob URL).
    - Test: link_for_source with base_url=None returns a relative repo path (../docs/vision.md), not an absolute URL.
    - Test: link_for_source for a SessionLocator / a session-* source_id with no file path and no in-site target returns None (→ caller renders plain text), never a fabricated/dead URL.
    - Test: link_for_source resolves a session/doc locator to an in-site anchor (a {slug}.html) when the source maps to a recording surface via Site.by_slug.
    - Test: a rendered ClaimsBlock with a file-path-traced claim contains an <a class="ev-chip" href="...github.com.../docs/..."> evidence chip; an unresolvable-locator claim keeps the plain <span class="ev-chip"> (no <a>).
  </behavior>
  <action>
    Add a module-level `source_base_url` constant (default
    `https://github.com/nneibaue/newsletters/blob/main/`) and `link_for_source(source_or_trace, *,
    base_url=source_base_url, site=None)` implementing the three-branch rule (file path → blob URL
    / `../path` when `base_url is None`; session/transcript → in-site anchor via `site.by_slug` or
    plain-text `None`; neither → `None`). Detect a file-path locator deterministically (contains
    `/` or ends in a source extension) — stdlib only, no new dependency, no AI import. Build the
    URL only from the path/slug-safe value; never interpolate raw free-text into an `href`.

    Wire the ClaimsBlock evidence chips (`render.py:211-225`): wrap the chip in `<a class="ev-chip"
    href="{_e(url)}">…</a>` when `link_for_source` yields a URL, else keep the plain `<span
    class="ev-chip">` (faithful — never a dead link). Escaping via `_e()` stays.

    VISUAL-ACCEPTANCE NOTE (for gsd-ui-checker): linked evidence chips keep the `.ev-chip` mono
    9.5px token styling (design-system.md §3) and only change from `<span>` to `<a>` — same visual,
    now clickable; underline/hover follows the existing `.signals a` rule.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>source_base_url + link_for_source exist with the three-branch rule (file→blob, session→in-site/plain, else plain); file-path evidence chips link, unresolvable ones stay plain text; rule + chip tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FanoutLink.href + SVG fan-out anchors + Home/footer link wiring (SITE-05 part 2)</name>
  <files>src/newsletters/render.py, src/newsletters/diagrams.py, src/newsletters/dogfood.py, tests/test_render.py</files>
  <behavior>
    - Test: _block_html for a FanoutBlock whose FanoutLink titles name real sibling surfaces renders each row as an <a href="{resolved page.href}"> (resolved via Site.by_slug(slugify(title)) or the link's own href); an unresolvable title stays plain text (no <a>, no dead link).
    - Test: diagrams.fanout(links={...}) wraps each surface box in an SVG <a href="..."> (the output contains "<a " inside the <svg>); diagrams.fanout() with no links keeps the current static behavior (no <a>).
    - Test: the rendered Home §5 "Enter →" rows and §7 repo/spec links are working <a> hrefs (the repo URL + the surface-type hub hrefs), not "#" placeholders; the footer links resolve to real URLs.
  </behavior>
  <action>
    Render `FanoutLink.href` in the `_block_html` FanoutBlock branch (`render.py:266-273`): when a
    link has an `href`, OR one resolves via `Site.by_slug(slugify(link.title))`, render the row as
    an `<a href=...>`; otherwise keep the plain text (faithful). Thread the `Site` into
    `_block_html`/`render_surface` as needed to resolve titles → `page.href`.

    Add an optional `links: dict[str,str] | None = None` param to `diagrams.fanout()`
    (`diagrams.py:100`): when present, wrap each `_box(...)` in an SVG `<a href="{href}">…</a>`
    (valid SVG anchor, NO JS) using the label→href map; `None` preserves the current static SVG.
    Keep `diagrams.py` stdlib-only. The `render_library` intro fan-out (`render.py:413-417`) passes
    the resolved nav-target map (from `_nav_targets`) so the board's diagram boxes navigate.

    Wire the Home links Wave 1 left as anchors/placeholders: §5 "Enter →" → the surface-type hub
    hrefs (`_nav_targets`); §7 "Clone the repo"/repo lockup/GitHub buttons → the repo URL
    (`source_base_url` minus `/blob/main/`, or a `repo_url` const), "Read the spec" →
    `{source_base_url}docs/architecture.md`; §1/§8 GitHub buttons → the repo; footer slots → real
    URLs. Per "specs source of truth": if §5/§7 link behavior diverges from `docs/surfaces.md`,
    update that section of `surfaces.md` in the same change.

    VISUAL-ACCEPTANCE NOTE (for gsd-ui-checker): the fan-out boxes and FanoutBlock rows keep their
    exact look (the 3px-accent boxes, hairline `.fan-row`) and only become clickable; the SVG
    anchor adds no visible chrome; Home links keep the `.sg-btn`/`.sg-btn.ghost` styling.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>FanoutBlock rows + the fan-out SVG boxes are working links (resolved via by_slug, plain text when unresolvable), the Home §5/§7/footer links are real URLs (no "#"); tests pass; surfaces.md updated if diverged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: generated marker + byte-stable regen + no-dead-link test (SITE-06)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - Test (byte-stable): build_site(tmp_path) twice (two separate output dirs) → every same-named file is byte-identical (no datetime.now()/nondeterminism in the rendered HTML).
    - Test (marker): every emitted *.html (index.html, library.html, every surface) contains the comment "generated by newsletters.render; do not hand-edit".
    - Test (coverage / no dead link): the rendered set == {index.html, library.html} ∪ {one file per page.href}; the four nav targets (_nav_targets) each resolve to a filename that exists in the rendered set; every internal href in the rendered HTML (the {slug}.html links) points at a file that exists.
  </behavior>
  <action>
    Add the generated-marker comment to `_page` (`render.py:310-317`) once —
    `<!-- generated by newsletters.render; do not hand-edit -->` — so it appears on every surface,
    the Home, and the board uniformly. Audit the new Home/board/breadcrumb/prevnext code for any
    `datetime.now()` / time-varying value reaching the HTML and remove it (source any needed date
    from `Page.date`, never `now()`).

    Add the SITE-06 test trio to `tests/test_render.py` (model the byte-stability technique on
    `tests/test_site.py:93,239-256`): byte-stable double-render (use two `tmp_path` dirs — NEVER
    write the committed `content/rev1/site/`); marker-present on every emitted page; coverage +
    no-dead-link (the rendered set, the four nav targets resolve to existing files, internal
    `{slug}.html` hrefs all exist). This is the SITE-06 regen gate that proves no file is
    hand-edited.

    Record in the SUMMARY: ZERO new dependency added across all three Wave-3 tasks; no human
    checkpoints; the package-legitimacy checkpoint (T-09-SC) is not applicable (no installs).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q && .venv/bin/python -c "import tempfile,pathlib; from newsletters.dogfood import build_site; a=tempfile.mkdtemp(); b=tempfile.mkdtemp(); build_site(a); build_site(b); A=sorted(pathlib.Path(a).glob('*.html')); assert all((pathlib.Path(b)/p.name).read_bytes()==p.read_bytes() for p in A), 'not byte-stable'; assert all('do not hand-edit' in p.read_text() for p in A), 'marker missing'; print('regen byte-stable + marker OK')"</automated>
  </verify>
  <done>Every page carries the generated marker; re-rendering content/rev1/site/ is byte-identical (no datetime.now() in the HTML); the coverage/no-dead-link test passes; standing invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source/Trace locator text → href attribute | locator/context text could reach an `href`; only path/slug-safe values are interpolated, via `_e()` |
| committed `content/rev1/site/` ← render.py | the single source of truth for the deployed HTML; the regen test proves no hand-edit |
| dependency install surface | none — ZERO new dependency this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | a wrong `source_base_url` → 404 on every evidence chip | mitigate | the rule degrades to PLAIN TEXT (never a dead link) when a target cannot be resolved; the GitHub default matches the Home repo lockup; the no-dead-link regen test gates internal links (UI-SPEC risk 4) |
| T-09-07 | Tampering | a `datetime.now()` reaching the HTML breaks byte-stability (SITE-06) | mitigate | new Home/board emit nothing time-varying; the byte-stable regen test is the guard (mirrors the adapters' EPOCH_ZERO discipline) |
| T-09-08 | Injection | locator/title text interpolated into an `href` | mitigate | hrefs built only from path/slug-safe values (`slugify` → `[a-z0-9-]`, closes T-08-01); free-text never enters an `href`; `_e()` on every interpolation |
| T-09-09 | Tampering | hand-edited HTML in `content/rev1/site/` drifting from render.py | mitigate | the generated-marker guard + byte-stable regen test prove every committed page is render.py output (SITE-06) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NOT APPLICABLE — zero packages added; `render.py`/`diagrams.py` stay stdlib + sibling core; `lint-imports` (1 kept/0 broken) is the standing guard |
</threat_model>

<verification>
Standing invariants this plan keeps green (DoD per CLAUDE.md — re-run independently):
- `.venv/bin/pytest -q` — full suite green (Wave-1/2 tests + the SITE-05/06 tests).
- `.venv/bin/mypy src/newsletters` — NO NEW errors beyond the 12 pre-existing (deferred-items.md).
- `.venv/bin/lint-imports` — "Contracts: 1 kept, 0 broken" (render.py + diagrams.py stay AI-free,
  stdlib-only).
- `.venv/bin/python -c "from newsletters.dogfood import build_site; build_site('content/rev1/site')"`
  then re-run → byte-identical output (the SITE-06 regen gate); every cited file-path source is a
  working link; per-surface `{slug}.html` filenames unchanged.
ZERO new dependency. No human checkpoints (autonomous).
</verification>

<success_criteria>
- SITE-05 satisfied: the fan-out diagram (SVG anchors + FanoutBlock rows) and every cited source
  with a resolvable target render as working links (e.g. `vision.md` → repo blob); unresolvable
  targets render as plain text, never a dead link (ROADMAP SC4).
- SITE-06 satisfied: every page carries the generated marker; `content/rev1/site/` regenerates
  byte-identically from `render.py` (no `datetime.now()` in HTML); no dead nav/internal link
  (ROADMAP SC5).
- Standing invariants green; filenames stable; ZERO new dependency.
</success_criteria>

<output>
Create `.planning/phases/09-rev2-site-ia-navigation-source-links/09-03-SUMMARY.md` when done.
</output>
