---
phase: 09-rev2-site-ia-navigation-source-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/newsletters/render.py
  - src/newsletters/dogfood.py
  - tests/test_render.py
autonomous: true
requirements: [SITE-02]
must_haves:
  truths:
    - "Opening content/rev1/site/index.html shows the 8-section marketing Home (Hero through Invitation), not the archive list"
    - "The Home leads with the why (Hero H1 'Turn information into conversation. Conversation into action.') and ends with the Invitation CTA"
    - "The personalization demo renders the DEFAULT persona's letter inline and reads correctly with JavaScript disabled"
    - "The Library archive is reachable as a SEPARATE page at content/rev1/site/library.html"
    - "Every Home heading/token matches design-system.md exactly (DM Serif/Instrument Sans/DM Mono, --radius:0, the per-section accents)"
  artifacts:
    - path: "src/newsletters/render.py"
      provides: "render_home(site) + 8 section helpers + Home/board layout CSS + responsive @media"
      contains: "def render_home"
    - path: "src/newsletters/dogfood.py"
      provides: "build_site writes index.html=Home and library.html=board-page"
      contains: "render_home"
    - path: "tests/test_render.py"
      provides: "Home smoke test (8 sections present, index=Home, library.html exists, no-JS default letter)"
      contains: "def test_"
  key_links:
    - from: "src/newsletters/dogfood.py:build_site"
      to: "render_home"
      via: "index.html write"
      pattern: "render_home"
    - from: "src/newsletters/render.py:render_home"
      to: "_CSS Home section rules"
      via: "class names emitted into the page body"
      pattern: "render_home"
---

<objective>
Wave 1 — build the real marketing Home (SITE-02) and split the route so the Home owns
`index.html` while the Library archive moves to `library.html`.

Recreate the approved 8-section Home from `docs/surfaces.md` §"Home — Start here (V1,
approved)" and `design-reference/Newsletters - Home.html` in the existing Python renderer
(`render.py` — hand-rolled HTML strings, ZERO new dependency). Match `docs/design-system.md`
tokens EXACTLY (DM Serif / Instrument Sans / DM Mono, `--radius:0`, the flat editorial
aesthetic, the 3px-left-accent device). The persona/theme React state cannot run statically,
so render the DEFAULT persona's letter inline (N6, no-JS faithful); the theme toggle keeps the
existing tiny `_TOGGLE_JS` (not new behavior).

Purpose: the deployed site's front door currently IS the archive list; SITE-02 makes the front
door lead with the *why* and proves it with the *wow* (the personalization demo), handing the
archive to a separate Library page.

Output: `render_home()` + 8 section helpers + new Home/board layout CSS + responsive `@media`
rules in `render.py`; `build_site` writes `index.html`=Home and `library.html`=the (still-flat,
for now) archive page; a Home smoke test in `tests/test_render.py`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-CONTEXT.md
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-UI-SPEC.md
@docs/surfaces.md
@docs/design-system.md
@design-reference/Newsletters - Home.html
@src/newsletters/render.py
@src/newsletters/site.py
@src/newsletters/dogfood.py
</context>

<interface_context>
Existing renderer primitives to REUSE (do not re-pick tokens or re-implement):
- `_CSS` (`render.py:36-154`) — all design tokens are already ported 1:1; ADD new layout
  rules using ONLY the existing custom properties. The only existing `@media` is `max-width:820px`
  (`render.py:153`); the Home needs `≤980px` (two-up + demo collapse) and `≤720px` (nav links hide).
- `_page(*, title, signal_css, body, active, theme)` (`render.py:310`) — wraps body in nav +
  chrome + footer + theme toggle. Home calls it with `active="Start here"`.
- `_nav(active, theme)` (`render.py:284`) — renders `_NAV_ITEMS` (`render.py:164`). Leave the
  three `None` hrefs alone in this wave (Wave 2 resolves them); Home only needs `active="Start here"`.
- `SectionDivider` look = `.sg-divider` + `.sg-eyebrow` (`render.py:62,65`); `.sg-eyebrow.brand`
  / `.sg-eyebrow.accent` variants are NOT yet in `_CSS` — add them (color `--color-brand-primary`
  / `--color-accent`) since several Home dividers are accent/brand.
- `_chip(name)` (`render.py:198`) = `ProfileChip`; `_gate_badge(ReviewState.IN_REVIEW)`
  (`render.py:171`) for §4's gate; `PromptBlock` rendering lives inline in `_block_html`
  (`render.py:259`) — factor the inner panel into a small helper or inline the same markup for §7.
- `_e()` (`render.py:167`) — escape EVERY interpolated string (escaping discipline, UI-SPEC L398).

Home data feed (reuse, do not retype copy):
- Canonical Home copy (eyebrows/H1/dividers) is `docs/surfaces.md` L13-76 (authoritative).
- The persona letter copy is the dogfood `READERS` (`dogfood.py:157-173`) + `_LETTER_ITEMS`
  (`dogfood.py:422`) + `_letter_for`/`_newsletter_for` (`dogfood.py:445`). The three personas
  are `jj` (Founder, brand), `nate` (Maintainer, core), `newcomer` (First week, green). The
  DEFAULT inline persona = the maintainer/first persona (UI-SPEC §SITE-02 static-render note).
  Prefer feeding the demo letter from this existing typed data over re-typing prose.

Site model: `Site.collections` / `Site.pages()` / `Site.by_slug` (`site.py:206-221`),
`get_template(kind)` for `display_name`. `render_home(site)` takes the resolved `Site` so §5's
"Enter →" rows can later (Wave 3) resolve to real hub hrefs; in this wave they may be the
`#newsletters`/`#surfaces` Home anchors or placeholder `#` — Wave 3 wires the real links.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Home section CSS + the no-JS-faithful demo scaffolding</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - Test: render_home(site) returns a string containing every section anchor — #start, #newsletters, #engine, #surfaces, #developers (5 anchored sections per surfaces.md) — exactly once each.
    - Test: the output contains the Hero H1 text "Turn information into" and the two italic emphasis nouns ("conversation", "action") wrapped in <em>.
    - Test: the personalization demo renders the DEFAULT persona's letter inline — the rendered HTML contains the default persona's name (from READERS) and at least one letter item title; no <script>-gated content other than the existing _TOGGLE_JS.
    - Test: the output contains the new eyebrow variant classes for the accent/brand dividers (e.g. "sg-eyebrow accent").
  </behavior>
  <action>
    Add the Home/board layout CSS to `_CSS` (`render.py:36-154`) using ONLY existing custom
    properties — never introduce a new hex value or round a corner (`--radius:0` everywhere).
    Add: `.sg-eyebrow.brand{color:var(--color-brand-primary)}` and `.sg-eyebrow.accent{color:var(--color-accent)}`;
    Home section wrappers (reuse `.wrap` max-width 1180px, section padding `56-78px 44px`);
    the two-up `1fr 1fr` gap-56 grid (§2), the `300px 1fr` gap-36 demo grid (§3), the 4-col
    pipeline grid (§4), the `76px 1fr 240px` surface rows (§5), the 5-col practice grid (§6),
    the two-col developer grid (§7), and the surface-low Invitation panel with 3px accent
    left-border (§8). Add the responsive `@media(max-width:980px)` (two-up + demo + practice
    grids collapse to 1 col) and `@media(max-width:720px)` (`.nl-links{display:none}`) rules
    per `surfaces.md` L75-76 and the prototype `<style>`. Keep all spacing on the ported scale
    (editorial half-steps 18.5/30/26px are authoritative per the UI-SPEC Spacing Exceptions —
    do NOT re-round them to an 8-pt grid).

    Stub `render_home(site)` enough to satisfy the behavior tests: emit the section anchors,
    the Hero H1 with `<em>` emphasis nouns, and the demo letter built from the DEFAULT persona's
    `READERS`/`_LETTER_ITEMS` data (no React state — the persona buttons render as styled
    anchors/labels; the default persona's letter is inline). This task establishes the CSS +
    demo scaffolding the next task fleshes into the full 8 sections.

    VISUAL-ACCEPTANCE NOTE (for gsd-ui-checker): the Home must match design-system.md §3 type
    scale (Hero 72px/1.03 DM Serif, lead 18.5px Instrument Sans, eyebrow 10px DM Mono 0.20em),
    §1 colors (paper 60% / white 30% / brand+per-signal accent 10%), §4 `--radius:0` + the 3px
    left-accent device, and the per-section accents named in UI-SPEC §SITE-02 (brand `#0068b5`
    / green `#2f7d4f` / accent `#d4622a`). Document any spacing half-steps used as Spacing
    Exceptions. ZERO new dependency — verify `render.py` stays stdlib + the sibling-core imports.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>render_home(site) exists, the Home CSS + responsive @media + eyebrow variants are in _CSS, the default-persona letter renders inline (no-JS faithful), and the smoke tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The full 8-section Home (Hero through Invitation)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - Test: §1 Hero contains the eyebrow "An open framework for working in the open", the primary "See it in action" button and the ghost "View on GitHub" button, and the mono line "Open source · MIT · self-hostable · human-in-the-loop by design".
    - Test: §2 contains the serif statement "relevance wins." (italic emphasis on the noun).
    - Test: §3 contains the H2 "Everyone gets the newsletter that's" and the three "sg-card" feature cards (Codify once / Tune to a corpus / Re-cut on send).
    - Test: §4 renders the 4-cell pipeline Ingest → Distill → Review → Publish AND a gate badge whose current step is In Review (reuse _gate_badge(ReviewState.IN_REVIEW)).
    - Test: §5 renders four surface rows (The Show / Newsletters / The Articles / The Report) each with an "Enter" affordance; §6 renders the 5 practices; §7 renders the repo lockup + two PromptBlock panels (pip install + the synthesize API example); §8 renders the surface-low Invitation panel with the serif italic CTA.
  </behavior>
  <action>
    Flesh out `render_home(site)` into the full 8 sections per the UI-SPEC §SITE-02 table and
    `surfaces.md` L13-76 — either one builder or 8 small helpers (`_home_hero` … `_home_invitation`,
    UI-SPEC Component Inventory). Use the canonical copy from `surfaces.md` verbatim for eyebrows/
    H1/dividers; feed the §3 demo letter from the dogfood persona data (do NOT retype the `LETTERS`
    prose — reuse `READERS`/`_LETTER_ITEMS`). §4 MUST reuse `_gate_badge(ReviewState.IN_REVIEW)`.
    §7's PromptBlocks reuse the dark `.sg-prompt` panel markup; the `synthesize.py` example is the
    API snippet from `architecture.md` §2. Every interactive affordance is an `<a>`/button styled
    per design-system; in this wave §5 "Enter →" and §7 repo/spec links may target Home anchors or
    `#` placeholders (Wave 3 wires the real URLs via `link_for_source`/nav targets). Set
    `active="Start here"` when wrapping with `_page`.

    Per the project hard rule "specs are source of truth": if any rendered section diverges from
    `docs/surfaces.md`, update that section of `surfaces.md` in the SAME change and note why.

    VISUAL-ACCEPTANCE NOTE (for gsd-ui-checker): cross-check each section against its UI-SPEC
    §SITE-02 row — section anchors, the per-section divider variant (accent vs brand vs default),
    the grid templates (`1fr 1fr`/`300px 1fr`/4-col/`76px 1fr 240px`/5-col/two-col), and the
    persona accents. Confirm faithful no-JS render (the default persona's letter is present with
    JS disabled).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>All 8 Home sections render with their canonical copy + components + tokens; the per-section behavior tests pass; surfaces.md updated if anything diverged.</done>
</task>

<task type="auto">
  <name>Task 3: Route split — index.html = Home, library.html = archive</name>
  <files>src/newsletters/dogfood.py, src/newsletters/render.py, tests/test_render.py</files>
  <action>
    Update `build_site` (`dogfood.py:651-680`) so it writes the marketing Home to `index.html`
    (via `render_home(site)`) and the Library archive to `library.html` (via the existing
    `render_library(library)` — still the flat list in THIS wave; Wave 2 turns it into the board).
    The per-surface `{slug}.html` writes stay byte-stable (Phase-8 L3 — do not change filenames).
    Update `render_library`'s `_page(active=...)` if needed for the new route (resolved default:
    no active spine link on the Library, UI-SPEC §SITE-04 active-state model). Pass the resolved
    `Site` into `render_home`.

    Add a coverage test in `tests/test_render.py`: `build_site(tmp_path)` writes BOTH `index.html`
    and `library.html`; `index.html` contains the Hero H1 (it is the Home, not the archive);
    `library.html` contains the Library intro/archive marker. Use `tmp_path` — never write the
    committed `content/rev1/site/`.

    Note the threat-model item T-09-SC below: this plan adds NO package installs, so the
    package-legitimacy checkpoint does not apply; record that explicitly in the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q && .venv/bin/python -c "import tempfile,pathlib; from newsletters.dogfood import build_site; d=tempfile.mkdtemp(); ps=build_site(d); names={p.name for p in ps}; assert 'index.html' in names and 'library.html' in names, names; assert 'Turn information into' in (pathlib.Path(d)/'index.html').read_text(); print('route split OK')"</automated>
  </verify>
  <done>build_site writes index.html=Home + library.html=archive, per-surface filenames unchanged, the coverage test + the standing invariants (full pytest, no NEW mypy errors, lint-imports 1 kept/0 broken) stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed Surface/Page data → rendered HTML attributes | interpolated titles/refs/labels cross into HTML; must pass through `_e()` |
| dependency install surface | none — ZERO new dependency this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Injection (Tampering/Info-disclosure) | `render_home` interpolated strings | mitigate | every interpolated value passes through `_e()` (`render.py:167`); no raw free-text into an `href`; reuse slug/path-safe values only |
| T-09-02 | Tampering | `index.html` repurposing breaks old bookmarks to the archive | accept | pre-1.0 deployed site; Home links to `library.html` (Home §5 + footer in Wave 2); SITE-02 explicitly makes Home the front door (UI-SPEC risk 3) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NOT APPLICABLE — this plan adds zero packages; `render.py` stays stdlib + sibling core; `lint-imports` (1 kept/0 broken) is the standing guard |
</threat_model>

<verification>
Standing invariants this plan keeps green (DoD per CLAUDE.md — re-run independently, do not
trust a subagent "green"):
- `.venv/bin/pytest -q` — full suite green (459 baseline + the new Home tests).
- `.venv/bin/mypy src/newsletters` — NO NEW errors beyond the 12 pre-existing (deferred-items.md:
  render.py:243/251/267 str-vs-list; dogfood.py Source/Trace call-arg/arg-type). Do not add more.
- `.venv/bin/lint-imports` — "Contracts: 1 kept, 0 broken" (render.py stays AI-free, stdlib-only).
- `.venv/bin/python -c "from newsletters.dogfood import build_site; build_site('/tmp/site9w1')"` —
  renders index.html=Home + library.html with no errors.
ZERO new dependency. No human checkpoints (autonomous).
</verification>

<success_criteria>
- SITE-02 satisfied: `content/rev1/site/index.html` is the 8-section marketing Home; the
  archive is a separate `library.html` page (ROADMAP SC1).
- The Home matches `docs/surfaces.md` §"Home" copy and `docs/design-system.md` tokens exactly.
- The personalization demo renders the default persona's letter inline and is faithful with
  JavaScript disabled (N6).
- Per-surface `{slug}.html` filenames unchanged; standing invariants green.
</success_criteria>

<output>
Create `.planning/phases/09-rev2-site-ia-navigation-source-links/09-01-SUMMARY.md` when done.
</output>
