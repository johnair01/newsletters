---
phase: 09-rev2-site-ia-navigation-source-links
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/newsletters/render.py
  - src/newsletters/dogfood.py
  - tests/test_render.py
autonomous: true
requirements: [SITE-03, SITE-04]
must_haves:
  truths:
    - "library.html renders a three-column status board (Draft / In Review / Published) keyed off Page.gate"
    - "The board is pure CSS (display:grid) with NO JS beyond the existing theme toggle, and an empty column still shows its header + a muted placeholder"
    - "The global nav resolves to four real destinations (Start here / Newsletters / Articles / The Show) — no None/dead hrefs"
    - "Every surface page shows a breadcrumb (Home > Collection > Page) and prev/next links WITHIN its own surface type"
    - "The Library is reachable via a footer link (it is intentionally outside the four-item nav spine, N1)"
  artifacts:
    - path: "src/newsletters/render.py"
      provides: "_board/_board_column/_board_card, _nav_targets, _breadcrumb, _prevnext, footer library link, board CSS"
      contains: "def _nav_targets"
    - path: "tests/test_render.py"
      provides: "board column grouping, empty-column placeholder, nav resolution (no None href), prev/next bounds tests"
      contains: "def test_"
  key_links:
    - from: "src/newsletters/render.py:_board"
      to: "Page.gate"
      via: "grouping site.pages() into the three ReviewState buckets"
      pattern: "gate"
    - from: "src/newsletters/render.py:_nav_targets"
      to: "Site.by_slug / Collection.pages"
      via: "first Page.href per surface-type collection"
      pattern: "by_slug|collections"
    - from: "src/newsletters/render.py:_prevnext"
      to: "Collection.pages order"
      via: "index of page within its own collection"
      pattern: "_prevnext"
---

<objective>
Wave 2 — turn the Library archive into a gate-state status board (SITE-03) and make the global
navigation real: four resolved destinations + breadcrumbs + prev/next within a surface type
(SITE-04).

Replace the flat `render_library` list with a three-column board (Draft / In Review / Published)
driven by `Page.gate` (now load-bearing — it was merely CARRIED in Phase 8), using pure CSS Grid
(NO JS). Resolve the three `None` nav hrefs to the first page of each surface-type Collection,
add a breadcrumb above every surface masthead, add prev/next at the foot of every surface (within
its own Collection order), and add a footer `library.html` link so the board is always reachable.

Purpose: ROADMAP SC2 + SC3 — the archive becomes a legible status board, and navigation stops
dead-ending (the three `None` hrefs) with no breadcrumb or in-type sequencing.

Output: `_board`/`_board_column`/`_board_card`, `_nav_targets(site)`, `_breadcrumb(site, page)`,
`_prevnext(site, page)`, a footer library link, board/breadcrumb/prevnext CSS, and the surface
renderer wired to show breadcrumb + prev/next; board + nav tests.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-CONTEXT.md
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-UI-SPEC.md
@docs/surfaces.md
@docs/design-system.md
@src/newsletters/render.py
@src/newsletters/site.py
@src/newsletters/dogfood.py
@.planning/phases/09-rev2-site-ia-navigation-source-links/09-01-SUMMARY.md
</context>

<interface_context>
Depends on Wave 1: `render_home` exists and `build_site` already writes `index.html`=Home +
`library.html`=archive. This wave changes WHAT `library.html` contains (flat list → board) and
adds nav chrome to every surface.

Site model (the board + nav plumbing):
- `Site.pages()` (`site.py:211`) — every Page flattened in collection order.
- `Site.by_slug(slug)` (`site.py:215`) — cross-link resolver → `Page | None`.
- `Site.collections` (`site.py:209`) — ordered by `template.distance` (show 0, report 1,
  article 2, newsletter 3, `site.py:264`). Each `Collection.kind` / `.display_name` / `.pages`.
- `Page.gate: ReviewState` (`site.py:190`) — the board column key (DRAFT/IN_REVIEW/PUBLISHED).
- `Page.href` / `Page.title` / `Page.ref` / `Page.signal_color` / `Page.kind` / `Page.surface`.
- `_lib_ref_label(page)` (`render.py:369`) — the stable ref label (R-001/EP01/A-001/#02) for a
  card lead (the `ref`-not-position rule, SITE-01 — never `enumerate`).

Gate-state colors (design-system.md §6, UI-SPEC L209): Draft = `--text` (default), In Review =
`--color-amber`, Published = `--color-brand-primary`. The `ReviewState` ladder is
DRAFT/IN_REVIEW/PUBLISHED (`semantic.py` ReviewState; `_gate_badge`, `render.py:171-186`).

Nav: `_NAV_ITEMS` (`render.py:164`) = `[("Start here","index.html"),("Newsletters",None),
("Articles",None),("The Show",None)]`. `_nav(active, theme)` (`render.py:284`) renders them;
`.nl-links a.active` underline already exists (`render.py:111`). `_active_for(surface)`
(`render.py:320`) maps surface.kind → spine label. Resolve each `None` to the first `Page.href`
of its Collection (newsletter→`newsletter`, article→`article`, show→`show`), falling back to
`index.html`. The Report + Library are NOT in the spine (N1) — Library reached via footer link;
Report reached via Home §5 / the board.

`render_surface` (`render.py:326`) builds the masthead + blocks. Insert `_breadcrumb(site, page)`
just below `_nav` / above the masthead, and `_prevnext(site, page)` at the foot before `_footer`.
`render_surface` currently takes a `Surface`, not a `Page`/`Site`; thread the `Site` + `Page`
through `build_site`'s render loop (`dogfood.py:670-673`) so the breadcrumb/prevnext can resolve
neighbors. Keep `render_surface(surface)` working for callers that pass only a surface (resolve a
transient single-page Site, or add an optional `site=`/`page=` param — executor's discretion,
keep the existing test_site callers green).

`_footer()` (`render.py:300`) — add a `library.html` link (the footer is the home for it,
design-system.md §5; do NOT pad the four-item nav spine).
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The gate-state status board (SITE-03)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - Test: render_library(site) groups pages into exactly three columns keyed by Page.gate in the order Draft, In Review, Published; a page with gate=PUBLISHED lands in the Published column and not elsewhere.
    - Test: a gate state with zero pages still renders its column header + a muted placeholder string (e.g. "No surfaces in this state.") — the board always shows three columns.
    - Test: the board markup uses CSS grid (contains "lib-board" with grid columns) and contains NO new <script> (only the page's existing _TOGGLE_JS); each card is an <a href="{page.href}"> carrying the stable ref label (via _lib_ref_label) and the gate status tag.
    - Test: column header counts equal the number of pages in that bucket.
  </behavior>
  <action>
    Replace the flat list in `render_library` (`render.py:387-419`) with a three-column status
    board. Add `_board(site)` (groups `site.pages()` by `page.gate` into the three `ReviewState`
    buckets in ladder order), `_board_column(state, pages)` (mono eyebrow header colored per the
    gate mapping — Draft `--text`, In Review `--color-amber`, Published `--color-brand-primary`,
    design-system.md §6 — + a count; empty → header + muted mono placeholder), and
    `_board_card(page)` (an `<a class="lib-card" href="{page.href}" style="--signal:{page.signal_color.css_var}">`
    with the 3px left signal border; lead = `_lib_ref_label(page)`; `page.title` serif; the
    `display_name` + `cadence.label` mono meta; the `_status_tag(page.surface)` pill). NEVER
    `enumerate` — the lead is the stable ref (SITE-01 preserved).

    Add the board CSS to `_CSS`: `.lib-board{display:grid;grid-template-columns:repeat(3,1fr);gap:24px}`,
    `.lib-col` (hairline header), `.lib-card` (card bg, 3px left signal border, hard corners),
    collapsing to 1-col via the existing `@media(max-width:820px)`. PURE CSS — no JS — so the
    "Renders without JavaScript · WCAG AA" footer stays honest.

    Keep the existing intro + the fan-out diagram (`render.py:406-417`) ABOVE the board (its rows
    get linked in Wave 3 — leave them as-is here). Update `render_library`'s `_page(active=...)`
    per the resolved default (no active spine link on the Library, UI-SPEC §SITE-04).

    VISUAL-ACCEPTANCE NOTE (for gsd-ui-checker): three columns in ladder order with the exact
    gate colors (design-system.md §6); cards use the 3px-left-accent device + `--radius:0`; the
    empty-column placeholder keeps the board shape legible; mono headers per design-system.md §3.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>render_library renders a 3-column gate-state board (CSS grid, no JS, empty-column placeholder, ref-led linked cards); the board tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four resolved nav destinations + footer library link (SITE-04 nav)</name>
  <files>src/newsletters/render.py, tests/test_render.py</files>
  <behavior>
    - Test: _nav_targets(site) resolves Newsletters → the first newsletter Collection page's href, Articles → the first article page's href, The Show → the first show page's href; each is a real {slug}.html that exists in site.pages(), never None and never a bare "index.html" fallback when that collection has pages.
    - Test: the rendered surface nav contains four <a> links with non-empty hrefs (no href="None"/no empty href) and the four spine labels Start here / Newsletters / Articles / The Show.
    - Test: the footer contains a link to library.html.
  </behavior>
  <action>
    Add `_nav_targets(site)` that resolves each spine label to the first `Page.href` of its
    Collection via `Site.collections`/`by_slug` (Newsletters→`newsletter`, Articles→`article`,
    The Show→`show`), falling back to `index.html` only when a collection is empty. Wire `_nav`
    (`render.py:284`) to use the resolved targets instead of the raw `None`s in `_NAV_ITEMS` —
    pass the resolved targets through (thread the `Site` to `_nav`, or precompute a
    `{label: href}` map and pass it down). Keep `Start here → index.html` and the active-state
    underline behavior. Add a `library.html` link to `_footer()` (design-system.md §5 footer
    column — do NOT add a fifth nav-spine item; N1 keeps the spine at four).

    Resolved defaults (UI-SPEC §SITE-04): Newsletters → the listed newsletter (`newsletter-jj`,
    `dogfood.py:675`) which is first in its collection; Articles → the lone article; The Show →
    `show-ep01`. The Report + Library stay OUT of the spine (Library via footer, Report via the
    board / Home §5).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q</automated>
  </verify>
  <done>_nav_targets resolves all three None hrefs to real existing pages, the nav renders four working destinations, the footer links to library.html; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Breadcrumb + prev/next within a surface type (SITE-04 nav model) + wiring</name>
  <files>src/newsletters/render.py, src/newsletters/dogfood.py, tests/test_render.py</files>
  <behavior>
    - Test: _breadcrumb(site, page) renders three segments Home > {Collection.display_name} > {Page.title}; the Home + Collection segments are <a> links (index.html + the collection hub href), the Page segment is plain text (no link).
    - Test: _prevnext(site, page) for a page in the MIDDLE of its collection renders both a prev link (← {prev.title}) and a next link ({next.title} →), both within the SAME collection (never crossing surface type).
    - Test: prev/next bounds — the FIRST page of a collection renders no prev (only next); the LAST renders no next (only prev); a single-page collection renders neither.
    - Test: a rendered surface page (via build_site into tmp_path) contains its breadcrumb above the masthead and its prev/next at the foot.
  </behavior>
  <action>
    Add `_breadcrumb(site, page)` — `Home › {Collection.display_name} › {Page.title}` (mono 11px
    `.sg-mono`, `›` separators in `--line`; Home→`index.html`, Collection→the resolved hub href
    from `_nav_targets`/the collection's first page, Page = current, no link). Add
    `_prevnext(site, page)` — resolve neighbors by the page's index within its OWN
    `Collection.pages` (find the collection by `page.kind`); render `← {prev.title}` /
    `{next.title} →` as `page.href` links; omit the missing end (first has no prev, last has no
    next; single-page collection → neither). Prev/next NEVER cross surface type (ROADMAP SC3).

    Add `.nl-crumb` and `.nl-prevnext` CSS (hairline-topped row, mono labels, serif titles; reuse
    the `.fan-row`/`.byline` rhythm). Wire `render_surface` to emit `_breadcrumb` just below `_nav`
    / above the masthead, and `_prevnext` at the foot before `_footer`. Thread the resolved `Site`
    + the `Page` through the `build_site` render loop (`dogfood.py:670-673`) so neighbors resolve.
    Keep `render_surface` callable with just a surface for existing callers (executor's discretion:
    optional `site=`/`page=` params or a transient single-page Site — keep `tests/test_site.py`
    callers green).

    VISUAL-ACCEPTANCE NOTE (for gsd-ui-checker): breadcrumb mono 11px with `›` separators in
    `--line` (matches the gate-sep glyph `&rsaquo;`); prev/next hairline-topped, mono labels +
    serif titles; both use existing tokens only, `--radius:0`.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_render.py -x -q && .venv/bin/python -c "import tempfile,pathlib; from newsletters.dogfood import build_site; d=tempfile.mkdtemp(); build_site(d); html=(pathlib.Path(d)/'show-ep01.html').read_text(); assert 'nl-crumb' in html and 'nl-prevnext' in html, 'breadcrumb/prevnext missing'; print('nav model OK')"</automated>
  </verify>
  <done>Every surface page shows a Home>Collection>Page breadcrumb + within-type prev/next with correct bounds; the nav-model tests pass; standing invariants green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed Page/Collection data → HTML attributes (hrefs, titles, refs) | interpolated into nav/breadcrumb/board markup; must pass through `_e()`; hrefs built only from slug/path-safe values |
| dependency install surface | none — ZERO new dependency this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Injection (Tampering/Info-disclosure) | breadcrumb/board/nav interpolated titles + hrefs | mitigate | every value through `_e()`; hrefs are `{slug}.html` from `slugify`-guaranteed `[a-z0-9-]` (closes T-08-01); never raw free-text into an `href` |
| T-09-04 | Denial of legibility | empty gate column would drop a column and break the board shape | mitigate | empty column renders header + muted placeholder so all three states are always visible |
| T-09-05 | Tampering | a nav target resolving to a non-existent file = dead link | mitigate | `_nav_targets` resolves to a real `Page.href` from `site.pages()`; Wave-3 regen test asserts no dead nav link |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | NOT APPLICABLE — zero packages added; `render.py` stays stdlib + sibling core; `lint-imports` (1 kept/0 broken) is the standing guard |
</threat_model>

<verification>
Standing invariants this plan keeps green (DoD per CLAUDE.md — re-run independently):
- `.venv/bin/pytest -q` — full suite green (Wave-1 tests + the new board/nav tests).
- `.venv/bin/mypy src/newsletters` — NO NEW errors beyond the 12 pre-existing (deferred-items.md).
- `.venv/bin/lint-imports` — "Contracts: 1 kept, 0 broken" (render.py stays AI-free, stdlib-only).
- `.venv/bin/python -c "from newsletters.dogfood import build_site; build_site('/tmp/site9w2')"` —
  renders the board + every surface (with breadcrumb + prev/next) with no errors; per-surface
  `{slug}.html` filenames unchanged.
ZERO new dependency. No human checkpoints (autonomous).
</verification>

<success_criteria>
- SITE-03 satisfied: `library.html` is a three-column gate-state board (Draft / In Review /
  Published), pure CSS, no JS, empty-column placeholder (ROADMAP SC2).
- SITE-04 satisfied: the nav resolves to four real destinations; every surface has a
  Home>Collection>Page breadcrumb and within-type prev/next; Library reachable via footer
  (ROADMAP SC3).
- Standing invariants green; filenames stable.
</success_criteria>

<output>
Create `.planning/phases/09-rev2-site-ia-navigation-source-links/09-02-SUMMARY.md` when done.
</output>
