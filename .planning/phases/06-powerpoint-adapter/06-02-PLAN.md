---
phase: 06-powerpoint-adapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/newsletters/adapters/_pptx_loader.py
  - tests/test_ai_optional.py
  - tests/test_pptx_loader.py
autonomous: true
requirements: [ADAPT-04]
user_setup: []
must_haves:
  truths:
    - "`pip install '.[pptx]'` installs python-pptx; bare `pip install .` does not require it."
    - "`import newsletters` and `import newsletters.adapters` succeed with python-pptx blocked (bare-install simulation)."
    - "Calling the lazy loader without python-pptx raises a teaching ImportError naming `[pptx]`."
    - "No top-level `import pptx` is reachable from `import newsletters`; lint-imports stays 1 kept / 0 broken."
    - "The graphic-frame `@uri` accessor name + lxml fallback are confirmed against installed python-pptx (risk A1)."
  artifacts:
    - path: "src/newsletters/adapters/_pptx_loader.py"
      provides: "Lazy python-pptx boundary: load_presentation(raw) + teaching ImportError"
      contains: "def load_presentation"
    - path: "pyproject.toml"
      provides: "pptx optional-dependency extra"
      contains: "pptx"
  key_links:
    - from: "src/newsletters/adapters/_pptx_loader.py"
      to: "pptx.Presentation"
      via: "lazy import inside _load_pptx()"
      pattern: "import pptx"
    - from: "tests/test_ai_optional.py"
      to: "src/newsletters/adapters/_pptx_loader.py"
      via: "bare-install + no-toplevel-import gates"
      pattern: "_pptx_loader"
---

<objective>
PACKAGING (CONTEXT decision 1 / research-locked deps): add python-pptx as a lazy optional `[pptx]`
extra and prove the bare-install / AI-isolation invariant still holds WITHOUT it. python-pptx is
pre-approved (named by ADAPT-04, MIT, the standard since 2013); its transitive C-extensions (lxml,
Pillow) are acceptable BECAUSE they are sandboxed behind `[pptx]`, never reachable from core. NO
Package-Legitimacy human checkpoint — the only SUS signal is `unknown-downloads` (PyPI hides counts),
which RESEARCH pre-cleared.

This plan: adds `pptx = ["python-pptx"]` to pyproject; creates `adapters/_pptx_loader.py` mirroring
`_openpyxl_loader.py` (NO top-level `import pptx`; import lives inside `_load_pptx()` with a teaching
ImportError -> `pip install '.[pptx]'`); installs python-pptx into the dev `.venv` as the sanctioned
pre-approved dependency; extends `tests/test_ai_optional.py` with the `[pptx]` lazy-boundary gates
(mirroring the existing `[excel]`/openpyxl gates); and adds a tiny probe test confirming the
graphic-frame `@uri` accessor name + the lxml fallback (risk A1) so 06-03 can rely on it.

Purpose: lock the lazy packaging boundary so the PPTX adapter (06-03) can lazy-import python-pptx
without breaking bare-install / lint-imports. This plan is disjoint from 06-01 (zero file overlap) and
runs in the same wave.
Output: the `[pptx]` extra, the lazy loader, the installed dev dep, extended packaging gates, the A1 probe.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-powerpoint-adapter/06-CONTEXT.md
@.planning/phases/06-powerpoint-adapter/06-RESEARCH.md

# The exact patterns to mirror — read these before writing.
@src/newsletters/adapters/_openpyxl_loader.py
@tests/test_ai_optional.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the [pptx] extra + install python-pptx + lazy loader</name>
  <files>pyproject.toml, src/newsletters/adapters/_pptx_loader.py</files>
  <action>
    1. In `pyproject.toml` `[project.optional-dependencies]`, add `pptx = ["python-pptx"]` immediately
       after the `excel = ["openpyxl"]` line, with a comment mirroring the excel one: python-pptx is
       NON-AI, MIT, spec-named (ADAPT-04), pre-approved (CONTEXT decision 1 + RESEARCH Package
       Legitimacy Audit; the only SUS signal is `unknown-downloads`); its transitive C-extensions
       (lxml, Pillow) are sandboxed behind the extra and never reachable from core. python-pptx is the
       ONLY new direct dependency this phase adds — do NOT add `types-*` stub packages (mirror the
       Excel `Any`-typing decision).
    2. Install it into the dev venv as the sanctioned pre-approved dependency:
       `.venv/bin/pip install python-pptx` (this materializes lxml/Pillow/XlsxWriter/typing_extensions
       transitively — expected and acceptable). This is the one pre-approved install; if pip wants any
       OTHER new direct dependency, STOP and flag it.
    3. Create `src/newsletters/adapters/_pptx_loader.py` mirroring `_openpyxl_loader.py` in structure
       (research-locked Pattern 1): NO top-level `import pptx` / `from pptx ...`; a module constant
       `MISSING_PPTX_MESSAGE` (teaching string -> `pip install '.[pptx]'`, stating the deterministic
       spine runs without it); `_load_pptx() -> Any` that runs
       `import pptx  # type: ignore[import-untyped]  # noqa: PLC0415` inside a try/except and re-raises
       a teaching `ImportError(MISSING_PPTX_MESSAGE) from exc`; and `load_presentation(raw: bytes) ->
       Any` returning `_load_pptx().Presentation(io.BytesIO(raw))`. Type python-pptx objects as `Any`
       (no stub dep). Export `__all__ = ["_load_pptx", "load_presentation", "MISSING_PPTX_MESSAGE"]`.
       Do NOT register an adapter here and do NOT import this loader from `adapters/__init__.py` — that
       wiring belongs to 06-03 (this plan owns only the loader + packaging, keeping files disjoint from
       the adapter plan).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from newsletters.adapters._pptx_loader import load_presentation, MISSING_PPTX_MESSAGE; assert '[pptx]' in MISSING_PPTX_MESSAGE" && .venv/bin/python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert any('python-pptx' in r for r in d['project']['optional-dependencies']['pptx']); print('ok')"</automated>
  </verify>
  <done>`[pptx]` extra present with only python-pptx; python-pptx installed in .venv; loader imports with no top-level pptx import; teaching message names `[pptx]`.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the bare-install / AI-isolation gates for [pptx] + the A1 accessor probe</name>
  <files>tests/test_ai_optional.py, tests/test_pptx_loader.py</files>
  <action>
    Extend `tests/test_ai_optional.py` mirroring the existing `[excel]`/openpyxl gates (lines ~225-358),
    adding a parallel `[pptx]` block:
    - `test_pptx_extra_declared`: pyproject declares `pptx = ["python-pptx"]` and python-pptx is the
      ONLY package in that extra; it must NOT trip the AI requirement-name gate.
    - `test_pptx_loader_has_no_toplevel_pptx_import`: read `_pptx_loader.py` source; assert ZERO
      column-0 lines starting `import pptx` / `from pptx` (the only pptx import allowed is INSIDE
      `_load_pptx()`). Use the same column-0 line check as the openpyxl test (no bare `grep -c == 0`).
    - `test_adapters_package_imports_without_pptx`: in a fresh subprocess with a `sys.meta_path` finder
      that blocks `pptx`/`pptx.*` (mirror the openpyxl `_Block` finder), assert `import newsletters`,
      `import newsletters.adapters`, and `from newsletters.adapters import _pptx_loader` all succeed and
      `'pptx' not in sys.modules` afterward. (After 06-03 registers PptxAdapter on package import, this
      gate proves registration stays lazy/extra-free — write it now so 06-03 inherits a green gate.)
    - `test_pptx_loader_raises_teaching_error_without_pptx`: same blocked-finder subprocess; call
      `_load_pptx()` and assert it raises `ImportError` whose message contains `[pptx]` and `python-pptx`.
    - `test_pptx_loader_returns_module_when_present`: with python-pptx installed (dev .venv), assert
      `_load_pptx().__name__ == 'pptx'`; skip cleanly if absent (bare env).
    Keep `PYDANTIC_DISABLE_PLUGINS=true` on every subprocess as the openpyxl gates do.

    Create `tests/test_pptx_loader.py` for the risk-A1 probe (confirm the SmartArt detection accessor
    06-03 depends on): author a minimal in-memory `.pptx` containing an XML-injected diagram
    `graphicFrame` (a `<a:graphicData uri="...drawingml/2006/diagram">`); load it via `load_presentation`;
    walk to that graphic-frame shape; assert (a) `shape.shape_type is None` for SmartArt, and (b) the
    diagram URI is reachable BOTH via the candidate accessor `getattr(shape._element, "graphicData_uri",
    None)` AND via an lxml fallback that finds the `<a:graphicData>` element and reads its `@uri`
    attribute. Assert the two agree (or, if `graphicData_uri` is absent/renamed, that the lxml fallback
    still returns the diagram URI — the worst case is "report as unknown graphic frame", never crash).
    Record the confirmed accessor name in the test docstring so 06-03 wires the verified name. Skip
    cleanly when python-pptx is absent.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_ai_optional.py tests/test_pptx_loader.py -x && .venv/bin/lint-imports</automated>
  </verify>
  <done>
    All `[pptx]` packaging gates pass; bare-install simulation imports newsletters without pptx; loader
    raises the teaching ImportError; lint-imports = 1 kept / 0 broken; the A1 probe confirms the
    graphic-frame URI accessor + lxml fallback (recorded for 06-03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| optional extra → core import graph | `[pptx]` must never become reachable from `import newsletters` |
| pip registry → dev venv | python-pptx + transitive C-extensions install from PyPI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | core import graph | mitigate | `_pptx_loader.py` has NO top-level pptx import; bare-install gate + lint-imports prove core stays extra-free (test_ai_optional.py) |
| T-06-04 | Spoofing | python-pptx supply chain | accept | Pre-approved (ADAPT-04 + CONTEXT decision 1); canonical scanny/python-pptx, MIT, 11+ yr; SUS verdict is `unknown-downloads` only — a known PyPI-API false positive, NOT a real signal |
| T-06-05 | DoS | transitive lxml/Pillow C-extensions | accept | Widely-used, no telemetry, sandboxed behind `[pptx]`; never reachable from core (CONTEXT decision 1) |
| T-06-SC | Tampering | npm/pip installs | mitigate | python-pptx is the ONLY install, pre-approved (no human checkpoint per RESEARCH); if pip pulls any OTHER new DIRECT dep, STOP and flag |
</threat_model>

<verification>
Standing invariants (run after the plan, independently):
- `.venv/bin/python -m pytest` — full suite green.
- `.venv/bin/mypy src/newsletters/adapters` — clean (`_pptx_loader.py` typed `Any`, type:ignore inline).
- `.venv/bin/lint-imports` — 1 contract kept, 0 broken.
- Bare-install AI-isolation green WITHOUT `[pptx]`: the blocked-finder subprocess imports newsletters.
</verification>

<success_criteria>
- `pptx = ["python-pptx"]` declared; python-pptx is the only package in the extra; installed in .venv.
- `_pptx_loader.py` has zero top-level pptx import; `_load_pptx()` raises a teaching `[pptx]` ImportError.
- `import newsletters` / `import newsletters.adapters` succeed with pptx blocked.
- lint-imports stays 1 kept / 0 broken; full suite + mypy green.
- The A1 probe confirms the graphic-frame URI accessor name + lxml fallback for 06-03.
</success_criteria>

<output>
Create `.planning/phases/06-powerpoint-adapter/06-02-SUMMARY.md` when done.
</output>
