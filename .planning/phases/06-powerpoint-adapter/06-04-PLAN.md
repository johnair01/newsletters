---
phase: 06-powerpoint-adapter
plan: 04
type: execute
wave: 3
depends_on: ["06-03"]
files_modified:
  - tests/fixtures/pptx/_author_fixtures.py
  - tests/fixtures/pptx/title_body.pptx
  - tests/fixtures/pptx/text_box.pptx
  - tests/fixtures/pptx/table.pptx
  - tests/fixtures/pptx/notes.pptx
  - tests/fixtures/pptx/chart.pptx
  - tests/fixtures/pptx/image.pptx
  - tests/fixtures/pptx/smartart.pptx
  - tests/fixtures/pptx/nested_group.pptx
  - tests/fixtures/pptx/empty_slide.pptx
  - tests/test_pptx_golden.py
  - tests/test_coverage_roundtrip.py
  - tests/test_timestamp_determinism.py
autonomous: true
requirements: [ADAPT-04, ADAPT-06]
user_setup: []
must_haves:
  truths:
    - "A byte-reproducible .pptx golden corpus exists including SmartArt AND grouped (nested) shapes (ROADMAP criterion 3), plus title+body, text box, table, notes, chart, image, empty slide."
    - "For every fixture, the zero-silent-drops identity holds: #claims + #unextracted == #units walked (incl. nested-group accounting)."
    - "Every claim is verbatim (claim.text == trace.span == transcript slice) and content-addressed (trace.is_addressed)."
    - "Each fixture passes assert_conforms; DistillationResult round-trips losslessly through JSON."
    - "Determinism holds on the parsed Source (identical bytes -> byte-identical model_dump_json), NOT on re-saved .pptx bytes (L5)."
    - "The pptx backend joins the round-trip coverage-parity matrix and the cross-adapter determinism test."
  artifacts:
    - path: "tests/fixtures/pptx/_author_fixtures.py"
      provides: "Byte-reproducible .pptx generator (1x1 PNG literal; author-then-group; XML-inject SmartArt; pinned core props + zip mtimes)"
      contains: "def main"
    - path: "tests/test_pptx_golden.py"
      provides: "Golden corpus assertions: zero-silent-drops, verbatim+addressed, conformance, determinism, parity"
      contains: "def test_zero_silent_drops"
    - path: "tests/fixtures/pptx/smartart.pptx"
      provides: "The SmartArt + grouped-shape fixture (ROADMAP criterion 3)"
  key_links:
    - from: "tests/test_pptx_golden.py"
      to: "src/newsletters/adapters/pptx_adapter.py"
      via: "PptxAdapter().parse + distill over committed fixtures"
      pattern: "PptxAdapter"
    - from: "tests/test_coverage_roundtrip.py"
      to: "src/newsletters/adapters/pptx_adapter.py"
      via: "ADAPTER_CASES pptx param join"
      pattern: "pptx"
    - from: "tests/test_timestamp_determinism.py"
      to: "src/newsletters/adapters/pptx_adapter.py"
      via: "PptxAdapter no-created determinism param"
      pattern: "PptxAdapter"
---

<objective>
GOLDEN CORPUS (ADAPT-06 + ROADMAP criterion 3): author a byte-reproducible `.pptx` golden corpus and
drive PptxAdapter end-to-end across it, asserting the load-bearing invariants. The corpus MUST include
SmartArt AND grouped shapes (the explicit ROADMAP criterion-3 fixture), plus title+body, text box,
table, speaker notes, chart, image, a nested group, and an empty slide. This is the phase's proof of
correctness — a silent drop is a test failure by construction.

This plan mirrors the Phase-4/5 programmatic generator (`tests/fixtures/xlsx/_author_fixtures.py`):
all authoring uses python-pptx high-level APIs except SmartArt (XML injection of a diagram
`graphicFrame`), embeds a constant 1x1 PNG byte literal for the image (Pillow-free, A2), authors
group members FIRST then `add_group_shape` (which MOVES them; nested = group an inner group, L4), and
pins `core_properties.created`/`.modified` + zip-entry mtimes for byte-reproducibility. It then asserts
zero-silent-drops (incl. nested-group accounting), verbatim+content-addressed claims, conformance +
lossless JSON round-trip, DETERMINISM on the parsed Source (L5 — NOT on re-saved bytes, risk A3), and
joins the round-trip coverage-parity matrix and the cross-adapter determinism test with the pptx param.

Purpose: deliver criterion 3 (the golden test against SmartArt + grouped shapes asserting zero silent
drops) and ADAPT-06 for PPTX. Goal-backward: the executable proof that a reviewer's view is complete —
every shape is a claim or a disclosure, across persistence.
Output: the generator, the committed corpus, the golden test, and the two matrix joins.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-powerpoint-adapter/06-CONTEXT.md
@.planning/phases/06-powerpoint-adapter/06-RESEARCH.md

# The Phase-5 byte-reproducible generator + golden test + the matrices to join (read before writing).
@tests/fixtures/xlsx/_author_fixtures.py
@tests/test_excel_golden.py
@tests/test_coverage_roundtrip.py
@tests/test_timestamp_determinism.py

# The adapter under test + its taxonomy reason strings (from 06-03).
@src/newsletters/adapters/pptx_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Byte-reproducible .pptx fixture generator + the committed corpus</name>
  <files>tests/fixtures/pptx/_author_fixtures.py, tests/fixtures/pptx/title_body.pptx, tests/fixtures/pptx/text_box.pptx, tests/fixtures/pptx/table.pptx, tests/fixtures/pptx/notes.pptx, tests/fixtures/pptx/chart.pptx, tests/fixtures/pptx/image.pptx, tests/fixtures/pptx/smartart.pptx, tests/fixtures/pptx/nested_group.pptx, tests/fixtures/pptx/empty_slide.pptx</files>
  <action>
    Create `tests/fixtures/pptx/_author_fixtures.py` mirroring `tests/fixtures/xlsx/_author_fixtures.py`
    (module docstring documents provenance + the BYTE-REPRODUCIBILITY pins + the EXPECTED routing per
    fixture). Use the python-pptx authoring APIs verified in 06-RESEARCH.md "Fixture Authoring Plan":
    - `title_body.pptx`: a slide with title + body placeholder text frames.
    - `text_box.pptx`: `slide.shapes.add_textbox(...).text_frame` with multiple paragraphs.
    - `table.pptx`: `slide.shapes.add_table(rows, cols, ...).table` with verbatim cell text.
    - `notes.pptx`: `slide.notes_slide.notes_text_frame.text = ...`.
    - `chart.pptx`: `slide.shapes.add_chart(...)` with `pptx.chart.data.CategoryChartData` -> a chart
      that routes to unextracted (chart content out of scope).
    - `image.pptx`: `slide.shapes.add_picture(io.BytesIO(PNG_1x1), ...)` using a CONSTANT 1x1 PNG byte
      literal (Pillow-free, A2) -> routes to unextracted (picture out of scope).
    - `smartart.pptx`: XML-inject a `graphicFrame` whose `a:graphicData/@uri` is the diagram namespace
      (`.../drawingml/2006/diagram`) + a minimal `dgm:dataModel`/`data1.xml` part — the MINIMUM to be
      DETECTED (shape_type None + diagram URI), not rendered (L4 / RESEARCH Fixture gotcha 3). This is
      the most involved fixture — budget the bulk of the task here.
    - `nested_group.pptx`: author members FIRST, `add_group_shape([...])` (it MOVES them), then group an
      inner group into an outer group (nested, L4); include a readable text-box member AND an unreadable
      member (e.g. a picture) so member-extraction + member-reporting + nested accounting are all exercised.
    - `empty_slide.pptx`: `prs.slides.add_slide(blank_layout)` (the empty-slide assertion).
    Byte-reproducibility (L4 / risk A3): set `prs.core_properties.created` + `.modified` to a FIXED
    datetime; if a re-save still drifts the zip bytes, freeze zip-entry mtimes via a `_normalize_zip`
    helper exactly like the xlsx generator. The DETERMINISM ASSERTION in Task 2 is on the parsed Source
    (L5), so byte-identical `.pptx` files are nice-to-have but the Source-determinism property is the
    one ADAPT-06 needs — do NOT block on re-saved-byte identity. Commit the generated `.pptx` files
    (`main()` writes them; mirror the xlsx `main()`).
  </action>
  <verify>
    <automated>.venv/bin/python tests/fixtures/pptx/_author_fixtures.py && .venv/bin/python -c "import pathlib; n=sorted(p.name for p in pathlib.Path('tests/fixtures/pptx').glob('*.pptx')); assert len(n)==9, n; print(n)"</automated>
  </verify>
  <done>
    The generator runs deterministically and writes 9 committed `.pptx` fixtures including
    smartart.pptx (SmartArt) and nested_group.pptx (grouped shapes) — the ROADMAP criterion-3 pair —
    plus title+body, text box, table, notes, chart, image, empty slide.
  </done>
</task>

<task type="auto">
  <name>Task 2: Golden test + parity-matrix join + cross-adapter determinism join</name>
  <files>tests/test_pptx_golden.py, tests/test_coverage_roundtrip.py, tests/test_timestamp_determinism.py</files>
  <action>
    Create `tests/test_pptx_golden.py` mirroring `tests/test_excel_golden.py`: a module-level skipif when
    python-pptx is absent (so the bare-install gate is unaffected); an `EXPECTED` table pinning per-fixture
    `n_claims` + ordered `unextracted_reasons` DERIVED FROM THE LIVE adapter (not assumed — drive
    PptxAdapter over each committed fixture and pin the observed counts/reasons, mirroring the xlsx golden
    methodology); a `test_corpus_is_nine_committed_fixtures` guard; and parametrized assertions per fixture:
    - `test_zero_silent_drops`: `len(claims) + len(unextracted) == total_units` AND the nested-group leaf
      accounting (assert on nested_group.pptx that member text is extracted, the unreadable member is
      reported, and the GROUP nodes contribute nothing — the L3 identity).
    - `test_claims_are_verbatim_and_content_addressed`: `claim.text == trace.span`, transcript re-slice
      reproduces it, `trace.is_addressed`.
    - `test_coverage_honesty`: `complete == (len(unextracted) == 0)`.
    - `test_conformance_and_json_roundtrip`: `assert_conforms(PptxAdapter(), [source])` passes; the
      `DistillationResult` round-trips losslessly through JSON.
    - `test_determinism`: parse the SAME fixture twice -> EQUAL DistillationResult, and assert
      determinism on the parsed Source (`s1.model_dump_json() == s2.model_dump_json()`) per L5 — NOT on
      re-saved `.pptx` bytes.
    - `test_roundtrip_coverage_parity`: persist the Source, distill on a FRESH adapter -> coverage UNCHANGED.
    - Targeted routing assertions: smartart.pptx discloses the SmartArt (shape_type None + diagram URI)
      and emits NO SmartArt text claim; the grouped-shape members' readable text IS extracted; chart and
      image are disclosed not extracted; empty_slide yields zero claims and zero drops (nothing lost).

    Join the matrices (the documented seams from 06-01/06-03):
    - `tests/test_coverage_roundtrip.py`: add a `pptx` entry to `ADAPTER_CASES` mirroring the excel
      param — a `_pptx_adapter_factory()` (lazy import) + a `_pptx_fixtures()` loader reading the
      committed `tests/fixtures/pptx/*.pptx`, guarded by `pytest.mark.skipif(find_spec("python-pptx" via
      "pptx") is None, ...)`. No test-body change is needed (the matrix is parametrized).
    - `tests/test_timestamp_determinism.py`: add the `(PptxAdapter, <.pptx with core_properties.created
      = None>)` param to the no-intrinsic-timestamp determinism test (the seam 06-01 left documented),
      asserting `timestamp == EPOCH_ZERO` and byte-identical Sources on two parses; skip cleanly without
      the extra.
    Pin the taxonomy reason strings by importing them from `pptx_adapter.py` (or copying verbatim with a
    comment, as the xlsx golden does) so a drift in either side is a failure.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pptx_golden.py tests/test_coverage_roundtrip.py tests/test_timestamp_determinism.py -x</automated>
  </verify>
  <done>
    Every fixture passes zero-silent-drops (incl. nested-group accounting), verbatim+content-addressed,
    conformance + JSON round-trip, Source-determinism (L5), and round-trip coverage parity. The pptx
    backend has joined both the parity matrix and the cross-adapter determinism test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture bytes → adapter | The fixtures stand in for untrusted real-world decks; the golden test is the executable faithfulness proof |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | silent shape drop | mitigate | The zero-silent-drops accounting identity is asserted per fixture (incl. SmartArt + nested groups); a silent drop is a test failure by construction (ADAPT-06) |
| T-06-11 | Tampering | non-deterministic fixture/Source | mitigate | Pinned core props + zip mtimes; determinism asserted on the parsed Source (L5), immune to python-pptx re-save byte drift (A3) |
| T-06-12 | DoS | the 1x1 PNG / SmartArt XML on parse | accept | The image is a constant 68-byte PNG literal; the SmartArt is a minimal dataModel — neither is read for content (only shape_type/URI), so no amplification |
| T-06-SC | Tampering | pip installs | mitigate | No installs in this plan (python-pptx installed in 06-02); if any new dep appears, STOP and flag |
</threat_model>

<verification>
Standing invariants (run after the plan, independently):
- `.venv/bin/python -m pytest` — full suite green (281 baseline + the new pptx unit + golden + matrix joins).
- `.venv/bin/mypy src/newsletters/adapters src/newsletters/distill` — clean.
- `.venv/bin/lint-imports` — 1 kept / 0 broken.
- Bare-install AI-isolation green WITHOUT `[pptx]` (the golden + matrix pptx params skip cleanly when absent).
- Regenerating the corpus is deterministic (the generator has no now()/random leak).
</verification>

<success_criteria>
- 9 committed byte-reproducible `.pptx` fixtures including SmartArt + grouped (nested) shapes (criterion 3).
- Zero-silent-drops identity holds for every fixture, including nested-group accounting.
- Claims verbatim + content-addressed; conformance + lossless JSON round-trip per fixture.
- Determinism asserted on the parsed Source (L5); round-trip coverage parity holds.
- The pptx backend has joined the parity matrix + the cross-adapter determinism test.
- Full suite + mypy + lint-imports + bare-install gate green.
</success_criteria>

<output>
Create `.planning/phases/06-powerpoint-adapter/06-04-SUMMARY.md` when done.
</output>
