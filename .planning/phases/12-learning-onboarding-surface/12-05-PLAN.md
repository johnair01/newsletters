---
phase: 12-learning-onboarding-surface
plan: 05
type: execute
wave: 4
depends_on: ["12-03", "12-04"]
files_modified:
  - src/newsletters/dogfood.py
  - content/rev1/site
  - content/rev1/ids.json
  - tests/test_learning.py
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03]
must_haves:
  truths:
    - "build_site() authors a real learning surface by re-cutting report-datamodel via learning_surface() with the existing newcomer Corpus — it joins the Library board and the Site as the 5th surface type."
    - "An OnboardingPath show-ep01 → report-datamodel → the learning re-cut is authored and rendered as its own page in content/rev1/site, with each step resolving to a real surface and prev/next within the track."
    - "The learning surface gets the ledger ref L-001 (the L-{:03d} format already reserved); content/rev1/ids.json gains the L-001 entry append-only; existing refs are untouched."
    - "content/rev1/site/ regenerates byte-stable (SITE-06): a double render is byte-identical, and every pre-existing surface's HTML bytes are unchanged except the intended additions (the new learning + path pages, and any board/Home additions that list them)."
    - "The whole site (both corpora views) loads zero external resources and the AI-optional core boundary holds with the learning surface in the build."
  artifacts:
    - path: "src/newsletters/dogfood.py"
      provides: "A learning re-cut of report-datamodel (newcomer Corpus) + the onboarding path, wired into build_surfaces/build_site (rendered via render_surface + render_path), ledger appends L-001"
      contains: "learning_surface"
    - path: "content/rev1/site"
      provides: "The regenerated self-contained site including the new learning surface page + the onboarding path page (byte-stable)"
    - path: "content/rev1/ids.json"
      provides: "The append-only ledger gaining the L-001 learning ref (existing refs immutable)"
      contains: "L-001"
    - path: "tests/test_learning.py"
      provides: "End-to-end tests: the learning surface + path build, render with provenance, byte-stable, the learning ref is L-001, no external call across the whole site"
      exports: ["test_dogfood_builds_a_learning_surface_with_ref_l001", "test_dogfood_onboarding_path_builds_and_renders_ordered", "test_learning_surface_is_in_the_site_and_board", "test_site_with_learning_is_byte_stable", "test_whole_site_makes_no_external_call"]
  key_links:
    - from: "src/newsletters/dogfood.py build_surfaces/build_site"
      to: "src/newsletters/learning.py learning_surface"
      via: "re-cut report-datamodel's distillation for the newcomer Corpus"
      pattern: "learning_surface"
    - from: "src/newsletters/dogfood.py build_site"
      to: "src/newsletters/render.py render_path"
      via: "render the authored OnboardingPath to its own page in content/rev1/site"
      pattern: "render_path"
    - from: "src/newsletters/dogfood.py build_site"
      to: "content/rev1/ids.json"
      via: "Site.from_surfaces assigns L-001; ledger.save() persists it append-only"
      pattern: "L-001|ledger"
---

<objective>
Author the REAL dogfood learning example + onboarding path and regenerate the site — the END-TO-END
half of the coverage gap (L5). Re-cut report-datamodel (the richest traced claim set) into a learning
surface via the Plan-03 preset and the existing newcomer Corpus (dogfood.py:168); author the
OnboardingPath show-ep01 → report-datamodel → the learning re-cut; wire both into build_surfaces/
build_site so the learning surface joins the Library board + the Site (ledger ref L-001) and the path
renders to its own page via render_path() (Plan 04). Regenerate content/rev1/site/ byte-stable (SITE-06).

Purpose: A typed preset and a renderer with no real content prove nothing. This makes LEARN-01/02/03
real end-to-end on actual reviewed evidence — a newcomer can read the re-cut guide, trace every
concept to its source claim, and follow the ordered "watch the build → read the decisions → read the
newcomer guide" track. The committed site is the proof artifact.

CRITICAL PROCESS LESSON (RETRO / CLAUDE.md "regenerate content FIRST, run ALL gates LAST"): author +
wire + regenerate the content FIRST, inspect the diff, and run the full DoD gate battery (full pytest,
byte-stable, mypy, lint-imports, ai-optional, no-external-call) LAST as one independent pass — do not
interleave gate runs with content edits (rapid re-runs throw transient errors; one gate run per check).

Output: the dogfood learning surface + path wired into dogfood.py, the regenerated content/rev1/site/
(+ ids.json gaining L-001), and the end-to-end tests in tests/test_learning.py. This plan owns
dogfood.py + content/rev1/ exclusively (no other plan touches them).
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-learning-onboarding-surface/12-CONTEXT.md
@.planning/phases/12-learning-onboarding-surface/12-RESEARCH.md
@.planning/phases/12-learning-onboarding-surface/12-03-SUMMARY.md
@.planning/phases/12-learning-onboarding-surface/12-04-SUMMARY.md

# The dogfood file to extend — build_surfaces (~641) / build_site (~651) + the ledger save path; the
# newcomer Corpus (READERS["newcomer"], ~168); report-datamodel construction (~298) + its WorkSession/
# decisions (the distillation source to re-cut); show-ep01 (~488) for the path's first step:
@src/newsletters/dogfood.py
# learning_surface() + OnboardingPath/OnboardingStep (Plan 03) this authors with:
@src/newsletters/learning.py
# render_path() + the GlossaryBlock render branch (Plan 04) used to render the path page + learning surface:
@src/newsletters/render.py
# Site.from_surfaces / Ledger (L-{:03d} ref) / by_slug — how the learning surface lands as the 5th type:
@src/newsletters/site.py
# How a Surface exposes the distillation/claims to re-cut (build_report → capture_session → Distillation):
@src/newsletters/capture.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the learning re-cut + onboarding path and regenerate content/rev1/site (L5 — content FIRST)</name>
  <files>src/newsletters/dogfood.py, content/rev1/site, content/rev1/ids.json</files>
  <action>
    In dogfood.py, author a REAL learning surface by re-cutting report-datamodel: obtain the
    report-datamodel Distillation (rebuild it from the same WorkSession via capture_session, or expose it
    from _sources_and_reports — do NOT hand-author new claims; the claims must be the existing traced
    datamodel decisions, dogfood.py:280-296,298-343). Call learning_surface(distillation, surface_id=
    "learning-datamodel", title=..., audience=READERS["newcomer"], glossary_terms=["Distillation",
    "Surface","Claim/Trace","the review gate","report vs article"] (RESEARCH Q-E — each MUST resolve to a
    defining traced claim or route to missing[]), prerequisites=[...], author=AUTHOR), then publish()
    through the gate (NO auto-publish — call publish like the other dogfood reports). Add it to
    build_surfaces() so it flows into Site.from_surfaces as the 5th type (it lands in its own learning
    Collection by distance=4 and the ledger assigns L-001 — do NOT hardcode the ref). Author an
    OnboardingPath(id="onboarding-newcomer", title=..., audience_label="A new contributor", steps=[
    OnboardingStep(slug="show-ep01"), OnboardingStep(slug="report-datamodel"),
    OnboardingStep(slug="learning-datamodel")]) (RESEARCH L5 — ordered ≥2-record track). In build_site:
    after building the Site, write the learning surface page through the existing per-page loop (it is a
    Surface, so render_surface handles it), and write the OnboardingPath to its own file (e.g.
    "onboarding-newcomer.html") via render.render_path(path, site=site); append it to the written list.
    Decide + implement how the path surfaces in navigation per RESEARCH Open-Q2: list it on the Library/
    Home as a "track" (NOT in the gate-state board — it has no review state); a minimal honest choice is a
    footer/Library link to the path page (reuse existing link patterns; no new external asset, no JS).
    Then REGENERATE: run `.venv/bin/python -m newsletters build` (or the build entrypoint) so
    content/rev1/site/ and content/rev1/ids.json are rewritten; commit the new learning-datamodel.html +
    onboarding-newcomer.html + the ids.json L-001 append. Do this content step FIRST; run gates in Task 2.
    Faithful: the learning surface carries ONLY existing traced datamodel claims + traced glossary defs;
    any un-glossable term goes to missing[] (visible in the honesty panel). No datetime.now() anywhere.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -c "from newsletters.dogfood import build_site; import tempfile,pathlib; d=pathlib.Path(tempfile.mkdtemp()); w=build_site(d); names={p.name for p in w}; assert 'learning-datamodel.html' in names, names; assert 'onboarding-newcomer.html' in names, names; print('ok', sorted(names))"</automated>
    <human-check>Open content/rev1/site/learning-datamodel.html and onboarding-newcomer.html in a browser: confirm the learning surface shows the three ordered sections + the in-context glossary with working source links, and the path lists the three steps in order with prev/next — visually consistent with the design system (no JS needed).</human-check>
  </verify>
  <done>build_site() authors + renders the learning surface (learning-datamodel.html) and the onboarding path (onboarding-newcomer.html); content/rev1/site + ids.json regenerate with the L-001 learning ref; the path is linked from the Library/Home (not the gate board); only existing traced claims appear on the surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end tests + the full DoD gate battery (gates LAST, one pass)</name>
  <files>tests/test_learning.py, content/rev1/site</files>
  <behavior>
    - test_dogfood_builds_a_learning_surface_with_ref_l001: build_site into a tmp dir with a tmp ledger;
      the learning surface is present, lands in a learning Collection, and gets ref "L-001".
    - test_learning_surface_is_in_the_site_and_board: the learning surface appears in site.pages() and on
      the Library board (it has a gate state → it is a Page like any surface); the OnboardingPath is
      reachable from the Library/Home as a track (a link to onboarding-newcomer.html exists), NOT a board
      column of its own.
    - test_dogfood_onboarding_path_builds_and_renders_ordered: the rendered onboarding-newcomer.html lists
      show-ep01, report-datamodel, learning-datamodel in that ORDER, each linking to its real {slug}.html,
      with prev/next within the track (first no-prev, last no-next).
    - test_site_with_learning_is_byte_stable: build_site into two tmp dirs; every file (including
      learning-datamodel.html + onboarding-newcomer.html) is byte-identical across the two renders
      (SITE-06) — extend the existing byte-stable assertion to include the new pages.
    - test_whole_site_makes_no_external_call: across EVERY rendered page (both the per-reader newsletter
      re-cuts and the learning surface + path), no auto-loading external URL (font-face src / script src /
      link href / img src that fetches off-box) appears — only self-hosted relative woff2 fonts and the
      theme-toggle script (Phase 11 WORK-01 extended to the learning pages).
  </behavior>
  <action>
    Add the end-to-end tests above to tests/test_learning.py (use tmp_path + a tmp Ledger so the committed
    ids.json is never written by tests; mirror tests/test_render.py:549 byte-stable + test_site.py ledger
    patterns). Where it is cleaner, EXTEND the existing tests/test_render.py byte-stable + no-dead-link +
    no-external assertions to cover the two new pages rather than duplicating them — but keep this plan's
    files_modified to tests/test_learning.py and content/rev1/site to preserve disjoint ownership (if a
    test_render.py edit is unavoidable, it is an additive assertion only; note it in the SUMMARY).
    THEN run the FULL DoD gate battery LAST as one independent pass (per CLAUDE.md "run ALL gates LAST",
    "the agent says green ≠ green" — re-run each gate ONCE; rapid re-runs throw transient errors): full
    pytest; the byte-stable test; mypy (no NEW errors beyond the 12 baseline); lint-imports (1 kept / 0
    broken); test_ai_optional (no AI import reachable from core with the learning surface in the build);
    the bare-install AI-isolation check; and the no-external-call test across both corpora views. If any
    gate fails, fix the CONTENT/CODE (not the test) and re-run that gate once.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && .venv/bin/python -m pytest tests/test_learning.py -x && .venv/bin/python -m pytest tests/test_render.py -k "byte_stable or no_dead_link or external" -x && .venv/bin/python -m pytest && .venv/bin/mypy src/newsletters && .venv/bin/lint-imports && .venv/bin/python -m pytest tests/test_ai_optional.py -x</automated>
    <human-check>none</human-check>
  </verify>
  <done>The end-to-end tests pass: the learning surface builds with ref L-001 and lands in the Site + board; the onboarding path renders ordered with prev/next; the site (including the new pages) is byte-stable and makes no external call; the full DoD battery (pytest, byte-stable, mypy ≤12 baseline, lint-imports 1/0, ai-optional) is green in one independent pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated dogfood content → committed site | The learning re-cut + path are authored from existing reviewed claims; the published artifact must pass the SAME gate as every surface (no auto-publish — publish() is called explicitly with a recorded reviewer). |
| build → content/rev1/ (persisted) | build_site writes HTML + appends ids.json. The append must be byte-stable and append-only (existing refs immutable); no datetime.now() may leak into the bytes. |
| rendered site → reader | The whole site (incl. the new pages) must load zero off-box resources (self-hosted fonts), Phase 11 WORK-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05a | Elevation of privilege | A learning surface reaching Published without the gate (auto-publish) | mitigate | The dogfood learning surface is published only via an explicit publish(reviewer=AUTHOR) call (CLAUDE.md hard rule "no auto-publish"); the existing publish-gate test holds; the OnboardingPath publishes nothing new (A5). |
| T-12-05b | Tampering | Non-determinism leaking into the regenerated bytes (byte-stable break) | mitigate | EPOCH_ZERO sources, no datetime.now() in the preset/path; deterministic total+stable layering (Plan 03); test_site_with_learning_is_byte_stable enforces double-render identity. |
| T-12-05c | Repudiation | A fabricated/un-traced concept slipping onto the published guide | mitigate | The re-cut carries only existing traced datamodel claims + traced glossary defs; un-glossable terms route to missing[] (honesty panel); the FAITHFUL test (Plan 04) + the no-invented-prose test (Plan 03) guard it. |
| T-12-05d | Information disclosure | An off-box auto-loaded resource on the new pages | mitigate | test_whole_site_makes_no_external_call asserts no auto-loading external URL across every page; fonts stay self-hosted relative woff2. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependency (content + tests only) — confirmed by RESEARCH Package Legitimacy Audit (N/A). No install task; no legitimacy checkpoint required. |
</threat_model>

<verification>
Run LAST, as one independent pass (do NOT trust a subagent "green"; re-run each ONCE — CLAUDE.md):
- `.venv/bin/python -m pytest tests/test_learning.py -x` (the end-to-end learning tests pass)
- `.venv/bin/python -m pytest tests/test_render.py -k "byte_stable or no_dead_link or external" -x` (the new pages are byte-stable, dead-link-free, external-call-free)
- `.venv/bin/python -m pytest` (full suite green with the learning surface + path in the build)
- `.venv/bin/mypy src/newsletters` (no NEW errors beyond the 12 pre-existing baseline)
- `.venv/bin/lint-imports` (1 contract kept / 0 broken)
- `.venv/bin/python -m pytest tests/test_ai_optional.py -x` + the bare-install AI-isolation check (no AI reachable from core)
- `git diff --stat content/rev1/` shows only the intended additions (learning-datamodel.html + onboarding-newcomer.html + the ids.json L-001 append); existing surface HTML bytes unchanged.
</verification>

<success_criteria>
- LEARN-01 (e2e): a real learning surface re-cut of report-datamodel for the newcomer Corpus is built, published through the gate, and joins the Library board + the Site as the 5th type with ref L-001.
- LEARN-02 (e2e): every concept/glossary term/step on the rendered learning surface + path links back to its source (provenance devices, Plan 04).
- LEARN-03 (e2e): the OnboardingPath show-ep01 → report-datamodel → learning-datamodel renders as an ordered track page with prev/next.
- SITE-06: content/rev1/site regenerates byte-stable; existing surfaces' bytes unchanged except intended additions; ids.json append-only.
- NO-EXTERNAL-CALL across both corpora views; AI-optional core intact; full DoD battery green in one independent pass; zero new dependency.
</success_criteria>

<output>
Create `.planning/phases/12-learning-onboarding-surface/12-05-SUMMARY.md` when done.
</output>
</content>
