---
phase: 12-learning-onboarding-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/surfaces.md
autonomous: true
requirements: [LEARN-01, LEARN-02, LEARN-03]
must_haves:
  truths:
    - "docs/surfaces.md has a Learning-surface spec section stating the faithfulness contract (select/order/link, never invent)."
    - "The spec documents the three progressive-disclosure layers, the in-context glossary (term -> traced defining claim; un-traceable -> honesty panel), prerequisite-context links, the GREEN/L-NNN/distance-4 placement, and the OnboardingPath (ordered track, prev/next, no own gate)."
  artifacts:
    - path: "docs/surfaces.md"
      provides: "Learning + OnboardingPath spec section (fills the L-NNN spec gap)"
      contains: "faithful"
  key_links:
    - from: "docs/surfaces.md learning section"
      to: "docs/surfaces.md:176 L-NNN id row"
      via: "the new section explains the already-reserved L-NNN id"
      pattern: "L-NNN|L-001"
---

<objective>
Fill the docs/surfaces.md spec gap: add the "Learning — the reviewed record, re-cut for someone
new" section and the OnboardingPath spec (L7). The `L-NNN` id row already exists at
docs/surfaces.md:176; the prose spec section is the gap. Specs are the source of truth (CLAUDE.md),
so this lands in the same phase as the code.

Purpose: This phase delivers a fifth surface and a new navigation model; the spec must describe the
faithfulness contract and the surface shape so code and spec never drift. Writing the spec in Wave 1
(in parallel with the typed foundation) makes it the contract the preset/render/dogfood plans
implement against.

Output: a new spec section in docs/surfaces.md covering the Learning surface and the OnboardingPath,
mirroring the structure of the existing per-surface spec sections.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-learning-onboarding-surface/12-CONTEXT.md
@.planning/phases/12-learning-onboarding-surface/12-RESEARCH.md

# The file to extend — read the existing per-surface spec sections + the L-NNN id row (line 176)
# to mirror their structure exactly:
@docs/surfaces.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Learning-surface + OnboardingPath spec section to docs/surfaces.md (L7)</name>
  <files>docs/surfaces.md</files>
  <action>
    Add a new spec section "Learning — the reviewed record, re-cut for someone new" that mirrors the
    structure of the existing per-surface sections. It MUST cover, per RESEARCH "docs/surfaces.md Spec
    to Add (Q-F)" and the locked decisions L1-L4:
    (a) The faithfulness contract — the learning re-cut SELECTS, ORDERS, and LINKS already-reviewed
        traced claims; it NEVER invents explanatory prose (per CONTEXT "the faithfulness crux" and
        CLAUDE.md "faithful, not suggestive"). Un-traceable material is not taught — it surfaces in the
        honesty panel.
    (b) The three progressive-disclosure layers as ORDERED DOM sections (Start here / Prerequisites /
        Going deeper), no JS, ordered deterministically by confidence + topics (per L2/A3).
    (c) The in-context glossary: each term's definition IS a traced defining Claim (a GlossaryBlock,
        not invented prose); a term with no traced defining claim is routed to surface.missing[] and
        shown in the honesty panel (per L2/A4), never fabricated.
    (d) Prerequisite-context links: links to prerequisite records/claims via the provenance device,
        not new exposition (per L2/L3).
    (e) Provenance (LEARN-02): every concept/glossary term/step links back to its source claim/record
        (reuses link_for_source + the verbatim trace span; un-traceable becomes plain text / honesty
        panel).
    (f) Placement: GREEN signal color, the `L-NNN` id (reference the existing row at line 176),
        distance-4 (most distilled re-cut), AudienceScope.INDIVIDUAL, ON_DEMAND cadence, light review.
    (g) The OnboardingPath (LEARN-03): an ORDERED track of record/surface refs for a newcomer/cohort,
        rendered as a sequenced page with prev/next within the track; it is NOT a Surface and has NO own
        review gate (it publishes nothing new — it sequences surfaces that ALREADY passed the gate), per
        L4/A5.
    Use the exact phrases "Start here", "Prerequisites", "Going deeper", "glossary", "OnboardingPath",
    "prev" and "honesty" in the section so the verify gate passes. Do NOT alter the existing L-NNN id row
    (line 176) or other sections — only ADD the new section. Keep the editorial tone and heading style
    consistent with the surrounding spec.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && for k in faithful "Start here" Prerequisites "Going deeper" glossary OnboardingPath prev honesty; do grep -qF "$k" docs/surfaces.md || { echo "MISSING $k"; exit 1; }; done && echo ok</automated>
    <human-check>Open docs/surfaces.md and confirm the new Learning section reads consistently with the existing per-surface sections and states the faithfulness contract clearly.</human-check>
  </verify>
  <done>docs/surfaces.md has a Learning + OnboardingPath spec section covering the faithfulness contract, the three layers, the traced glossary, prerequisite links, provenance, GREEN/L-NNN/distance-4 placement, and the no-gate OnboardingPath; existing rows untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change. No code, no input, no I/O, no network, no AI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02a | Tampering | Spec drifting from code | mitigate | Spec is written against locked decisions L1-L4 and lands in the same phase as the implementing code; later plans implement to this spec (specs are source of truth, CLAUDE.md). |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependency (docs-only) — confirmed by RESEARCH Package Legitimacy Audit (N/A). No install task; no legitimacy checkpoint required. |
</threat_model>

<verification>
- The grep gate above passes (all required topics present).
- `git diff docs/surfaces.md` shows only additions (the L-NNN id row at line 176 and other sections unchanged).
</verification>

<success_criteria>
- LEARN-01/02/03 (spec): docs/surfaces.md documents the Learning surface (faithfulness contract, three layers, traced glossary, prerequisite links, provenance) and the OnboardingPath (ordered track, prev/next, no own gate).
- Specs-as-source-of-truth is honored: the spec lands in the same phase as the code.
- Zero new dependency; no code touched.
</success_criteria>

<output>
Create `.planning/phases/12-learning-onboarding-surface/12-02-SUMMARY.md` when done.
</output>
