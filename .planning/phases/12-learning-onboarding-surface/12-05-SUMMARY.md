---
phase: 12-learning-onboarding-surface
plan: 05
subsystem: dogfood
tags: [dogfood, learning-surface, onboarding-path, faithfulness, byte-stable, no-external-call, honesty-panel]

# Dependency graph
requires:
  - phase: 12-learning-onboarding-surface
    plan: 03
    provides: "learning_surface() faithful preset (ClaimsBlock x3 + GlossaryBlock, un-glossable -> missing[]) + OnboardingPath/OnboardingStep models"
  - phase: 12-learning-onboarding-surface
    plan: 04
    provides: "render._block_html GlossaryBlock branch + render_path(path, *, site, theme) ordered track"
  - phase: 08
    provides: "Site/Ledger (L-{:03d} ref) / Page / by_slug; content/rev1/ids.json append-only"
  - phase: 09-11
    provides: "render_surface/render_home/render_library; the merge gate (review_blockers / newsletters check); self-hosted fonts (WORK-01)"
provides:
  - "The REAL dogfood learning surface: report-datamodel re-cut for the newcomer Corpus, joined to the Site as the 5th type with ledger ref L-001, left a Draft (honest gaps in missing[])"
  - "The dogfood OnboardingPath show-ep01 -> report-datamodel -> learning-datamodel, rendered to onboarding-newcomer.html and linked from the Library"
  - "content/rev1/site regenerated byte-stable with the two new pages + the L-001 ledger append; end-to-end tests proving LEARN-01/02/03 on real reviewed evidence"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Faithful dogfood by re-cut, not re-authoring: _datamodel_distillation lifts report-datamodel's EXISTING traced claims (with their content-addressed traces) straight off its ClaimsBlocks, so the learning glossary definitions render with the exact working provenance links the report shows"
    - "Honest gaps stay Draft: a teaching surface carrying un-glossable terms in missing[] is NOT published (a Published open-missing surface is a merge blocker) — it ships its gaps visibly in the Library Draft column and waits for traced definitions"
    - "Disjoint-ownership nav injection: the onboarding-track Library link is injected by build_site (which owns content/rev1/) at a STABLE rendered-output anchor, keeping render.py's ownership and byte-stability intact"

key-files:
  created: []
  modified:
    - src/newsletters/dogfood.py
    - content/rev1/site (learning-datamodel.html + onboarding-newcomer.html created; library.html + ids.json + every page's shared CSS updated)
    - content/rev1/ids.json
    - tests/test_learning.py
    - tests/test_site.py
    - tests/test_render.py

key-decisions:
  - "The learning surface is left a DRAFT, not published — three datamodel concepts (Distillation, Surface, the review gate) are USED but never DEFINED by the record's traced claims, so they route to missing[]; publishing with open missing[] would trip the PROV-04 merge gate and violate CLAUDE.md no-silent-unsubstantiated. Draft is the faithful outcome (gate-exempt, honest gaps shown)."
  - "Glossary terms chosen to demonstrate faithfulness BOTH ways: Report/Article/capture have genuine 'X is/are …' defining traced claims and gloss; Distillation/Surface/the review gate have no defining claim and route to the honesty panel."
  - "_datamodel_distillation rebuilds a Distillation from report-datamodel's ClaimsBlock claims (build_report returns the Surface, not its Distillation) — the SAME reviewed claims, so the re-cut is the same truth with newcomer emphasis."
  - "The onboarding track is surfaced as a Library callout ABOVE the gate-state board (a path has no review state, so it is not a board column) — RESEARCH Open-Q2."

patterns-established:
  - "An end-to-end faithfulness proof on shipped content: the dogfood test asserts every rendered claim/glossary-definition string on the learning surface is set-member of the SOURCE record's claims, and zero free ProseBlock — the re-cut cannot invent prose, proven on the real corpus."

requirements-completed: [LEARN-01, LEARN-02, LEARN-03]

# Metrics
duration: 18min
completed: 2026-06-19
---

# Phase 12 Plan 05: Dogfood learning surface + onboarding path (end-to-end) Summary

**`build_site()` now authors a REAL newcomer learning surface by re-cutting `report-datamodel`'s existing traced claims via `learning_surface()` (faithful — Report/Article/capture gloss to defining traced claims; Distillation/Surface/the review gate route honestly to the missing[] panel), left a Draft so its open gaps never publish silently; it authors the OnboardingPath `show-ep01 → report-datamodel → learning-datamodel` rendered to `onboarding-newcomer.html` and linked from the Library; `content/rev1/site` regenerates byte-stable with the learning ref `L-001`, and the whole site (both corpora views) makes zero external calls — proven end-to-end by five new dogfood tests.**

## Performance
- **Duration:** ~18 min
- **Started:** 2026-06-19 (this wave)
- **Tasks:** 2 (Task 1 content; Task 2 TDD tests + DoD gates)
- **Files modified:** 6 (0 created in src; 2 site pages created)

## Accomplishments
- **LEARN-01 (e2e):** a real learning surface re-cut of `report-datamodel` for the newcomer Corpus is built and joins the Site as the **5th type** with ledger ref **L-001** (append-only in `content/rev1/ids.json`). It lands in the Library's **Draft** column.
- **LEARN-02 (e2e):** every glossed concept on the rendered surface links back to its DEFINING traced claim (the Plan-04 provenance devices); the three un-glossable terms appear ONLY in the honesty panel, never invented.
- **LEARN-03 (e2e):** the OnboardingPath renders to `onboarding-newcomer.html` as an ordered track — 3 steps in order, each linking to its real `{slug}.html`, first-no-prev / last-no-next.
- **SITE-06:** `content/rev1/site` regenerates **byte-stable** (a fresh `newsletters build` produces zero diff against the committed output); existing surfaces' bytes are unchanged except the one-time additive Plan-04 glossary/track CSS in the shared stylesheet.
- **NO-EXTERNAL-CALL / AI-optional / zero new dependency:** the whole site loads only self-hosted relative woff2 + the single theme-toggle script; lint-imports keeps core AI-free.

## The dogfood learning surface (the proof artifact)
- **Surface:** `learning-datamodel` — "How the data model works — a newcomer's guide" — ref **L-001**, **Draft**.
- **Glossed (defining traced claim found):** `Report`, `Article`, `capture`. (e.g. "The Report **is** the investigation you approve …" defines Report.)
- **Routed to `missing[]` (honesty panel — no defining claim in the record):** `Distillation`, `Surface`, `the review gate`. These are concepts the datamodel decisions USE but never DEFINE — shown to the reviewer as gaps, never fabricated.
- **Layers (newcomer emphasis):** Start here (5 high-confidence claims) + Prerequisites (the vision-topic agent-boundary claim) + Glossary.

## The onboarding path + its rendered page
- **Path:** `onboarding-newcomer` — "Start here — a new contributor's first week", audience "A new contributor".
- **Steps (ordered):** `show-ep01` (Watch the build) → `report-datamodel` (Read the decisions) → `learning-datamodel` (Read the newcomer's guide).
- **Rendered to:** `content/rev1/site/onboarding-newcomer.html` via `render_path(path, site=site)`; each step links to its real surface with in-track prev/next; linked from the Library masthead (a track, NOT a gate-state board column).

## Byte-stable + zero external calls (confirmed)
- `newsletters build` after commit → `git status content/rev1/` is **clean** (committed == regen): byte-stable, idempotent, no `datetime.now()` leak.
- `grep -rE "fonts.googleapis|@import url\('http|src=\"http|url\(http" content/rev1/site/` → **empty**.
- `test_whole_site_makes_no_external_call` asserts zero auto-loading external URL across EVERY page (both corpora views).

## Task Commits
Each task committed atomically (commit + push after each):
1. **Task 1 — content FIRST (feat):** `c0ce2b9` — author the re-cut + path, wire into build_surfaces/build_site, regenerate `content/rev1/site` + ids.json L-001.
2. **Task 2 — end-to-end tests (test):** `f108116` — the five dogfood tests in `tests/test_learning.py`.
3. **Fix + stale-assertion correction (fix):** `be95a47` — leave the learning surface a Draft (Rule 1 bug: the initial publish() tripped the merge gate); correct three other-file tests the 5th type legitimately invalidated; regenerate content (gate badge / board column → Draft).

## Files Created/Modified
- `src/newsletters/dogfood.py` — `_datamodel_distillation`, `_learning_recut`, `_onboarding_path`, `_onboarding_callout`; wired into `build_surfaces` (5th surface) + `build_site` (track page + Library callout).
- `content/rev1/site/learning-datamodel.html` (created), `content/rev1/site/onboarding-newcomer.html` (created), `content/rev1/site/library.html` (track callout + Draft column), every other page (shared Plan-04 CSS only), `content/rev1/ids.json` (L-001 append).
- `tests/test_learning.py` — 5 end-to-end dogfood tests (this plan's owned file).
- `tests/test_site.py`, `tests/test_render.py` — corrective edits (see Deviations).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] The learning surface was published; left it Draft so its honest gaps don't trip the merge gate**
- **Found during:** Task 2 full pytest (`newsletters check` → exit 1; `test_cli_check_clean_corpus_passes` / `test_check_rev1_default_unchanged` failed).
- **Issue:** Task 1's `_learning_recut` called `surface.publish()`. The re-cut faithfully carries three un-glossable terms in `missing[]`; a **Published** surface with open `missing[]` is an `OPEN_MISSING` merge blocker (PROV-04 / `review_blockers`), and CLAUDE.md forbids publishing unsubstantiated material silently. The plan's own threat model (T-12-05a) and `<wave_state>` say the learning surface is **Draft** (Library Draft column, gate-exempt).
- **Fix:** removed the `publish()` call — the re-cut returns a Draft. The teaching surface ships its honest gaps visibly and waits for a human to add traced glossary definitions before it can publish. `newsletters check` back to exit 0.
- **Files:** `src/newsletters/dogfood.py`, regenerated `content/rev1/site/{learning-datamodel,library}.html`. **Commit:** `be95a47`.

**2. [Rule 1 - Bug] Stale hardcoded corpus-shape assertions in two other-file tests**
- **Found during:** Task 2 full pytest (`test_site.py::test_page_ids_are_content_derived`, `test_site.py::test_existing_links_do_not_rot`, `test_render.py::test_rendered_set_matches_pages_and_chrome`).
- **Issue:** these tests hardcoded the corpus shape pre-Phase-12 — the collection list ended at `newsletter`, and the rendered/href file sets did not account for the new TRACK page. The plan's own success criteria (the learning surface IS the 5th type; the track renders to its own page) legitimately invalidate those literals.
- **Fix (corrective, not behavioral):** `test_page_ids_are_content_derived` collections list now ends `…, "learning"`; `test_existing_links_do_not_rot` and `test_rendered_set_matches_pages_and_chrome` treat `onboarding-newcomer.html` as chrome (it is `render_path` output, navigation over already-gated surfaces, not a per-surface Page).
- **Files:** `tests/test_site.py`, `tests/test_render.py`. **Commit:** `be95a47`. (Plan files_modified disjoint-ownership note: these edits are corrective assertions in already-existing tests the 5th type necessarily changes — flagged here per the plan's instruction.)

## Issues Encountered
None beyond the deviations above. (My Task-2 `test_dogfood_onboarding_path_builds_and_renders_ordered` initially mis-sliced the rendered HTML — a test-only bug in my own new test, fixed by splitting on the `.track-step` row marker before the test commit; the implementation was correct.)

## Known Stubs
None. The learning surface's `missing[]` entries are NOT stubs — they are the faithful, intended honesty-panel output (terms the record genuinely never defines). A future change that adds traced glossary definitions for those terms can then publish the surface through the gate.

## Verification (re-run independently per CLAUDE.md — actual output, gates LAST as one pass)
- `pytest tests/test_learning.py -q` → **22 passed** (17 prior + 5 new end-to-end).
- `pytest tests/test_render.py -k "byte_stable or no_dead_link or external" -q` → **3 passed**.
- `pytest -q` (full) → **559 passed, 1 xfailed** (baseline 554+1; +5 new dogfood tests; **0 regressions** after the deviation fixes).
- `grep -rE "fonts.googleapis|@import url\('http|src=\"http|url\(http" content/rev1/site/` → **empty** (no external call).
- `newsletters check` → **exit 0** ("All published surfaces clean — no blockers.").
- `mypy src/newsletters` → **9 errors in 2 files** (capture.py/dogfood.py — all pre-existing baseline; **0 NEW** in the Phase-12 additions; 12-04 had reduced 12→9).
- `lint-imports` → **1 contract kept, 0 broken** (core stays AI-free with the dogfood learning import).
- `pytest tests/test_ai_optional.py -q` → **15 passed, 1 xfailed** (no AI reachable from core).
- `newsletters build` then `git status content/rev1/` → **clean** (committed == regen: byte-stable, idempotent).

## Next Phase Readiness
- LEARN-01/02/03 are now real END-TO-END on actual reviewed evidence — the committed `content/rev1/site` is the proof artifact: a newcomer can read the re-cut guide, trace every glossed concept to its source claim, see the honest gaps, and follow the ordered track. This is the LAST wave of Phase 12.
- A future follow-up (out of scope here): add traced glossary definitions for `Distillation` / `Surface` / `the review gate` so the learning surface can pass the gate and publish.

## Self-Check: PASSED
- `content/rev1/site/learning-datamodel.html` and `content/rev1/site/onboarding-newcomer.html` exist on disk; `content/rev1/ids.json` contains `L-001`.
- All task commits exist in git history and are pushed: `c0ce2b9` (feat), `f108116` (test), `be95a47` (fix).

---
*Phase: 12-learning-onboarding-surface*
*Completed: 2026-06-19*
