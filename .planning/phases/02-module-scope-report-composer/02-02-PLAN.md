---
phase: 02-module-scope-report-composer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/newsletters/compose.py
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "compute_delta(start, close) derives a signed delta + dir from two traced numeric endpoints; returns (None, None) when either endpoint is absent or non-numeric; dir=None on Δ==0 (honest no-change), never a fabricated 0 (COMP-02)"
    - "compose_module_report emits one KpiStripBlock + one ClaimsBlock per SectionBinding in config order via the kind-agnostic seam — no knowledge that lanes exist (COMP-01)"
    - "The composed Surface is template=REPORT, review Draft, created=EPOCH_ZERO explicitly (deterministic, byte-stable) with traces carrying the loader Source"
    - "Module-level missing[] = union of every lane's missing + compose-found delta gaps; an empty lane set still yields a valid Draft Surface with populated missing[] (never None)"
    - "The composer only SELECTS/ORDERS/LINKS traced Claims; connective prose carries no numerals/facts (COMP-03)"
  artifacts:
    - path: "src/newsletters/compose.py"
      provides: "pure compute_delta + compose_module_report core (per-lane strip/claims, deterministic Surface, missing union)"
      exports: ["compute_delta", "compose_module_report"]
  key_links:
    - from: "src/newsletters/compose.py compose_module_report"
      to: "KpiItem.delta / KpiItem.dir"
      via: "compute_delta over binding.kpi_endpoints paired to kpi_items"
      pattern: "compute_delta"
    - from: "src/newsletters/compose.py"
      to: "Surface.created"
      via: "explicit EPOCH_ZERO (never the now() default)"
      pattern: "EPOCH_ZERO"
---

<objective>
Create the new top-level `src/newsletters/compose.py` and its core: the pure `compute_delta(start,
close)` derivation (COMP-02) and `compose_module_report`, which assembles `SectionBinding[]` into a
deterministic `Surface(REPORT, Draft)` — one `KpiStripBlock` (with compose-time Δ) + one `ClaimsBlock`
per binding, in config order, via a kind-agnostic seam (COMP-01), routing every unprovable thing to a
module-level `missing[]` (COMP-03). Quote slot, fanout stub, and the R-NNN ledger land in Plan 02-03
(same file, next wave).

Purpose: this is where the milestone's trust risk concentrates (deltas, ordering, prose). Build it
selecting/ordering/linking ONLY — the composer authors no facts.
Output: `compose.py` exporting `compute_delta` and `compose_module_report`, AI-free and yaml-free
(loader-agnostic), stdlib + pydantic + existing package modules only.

Δ CONTRACT (02-CONTEXT.md, LOCKED; research reconciliation #2): the delta is a DERIVATION over two
independently content-addressed endpoints, never itself a Claim, never traced, never rendered as a
claim. Both endpoints must exist and be numeric BEFORE any delta is computed. Either absent/non-numeric
→ `delta=None, dir=None` + a `missing[]` note — NEVER a fabricated 0. Δ==0 is a distinct honest
"no change" (`dir=None`, delta as the computed zero form). NO `Kpi` start/baseline model field (DEF-10).
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-scope-report-composer/02-CONTEXT.md
@.planning/phases/02-module-scope-report-composer/02-PATTERNS.md

@src/newsletters/swimlane.py
@src/newsletters/semantic.py
@src/newsletters/templates.py

FORBIDDEN TO EDIT (02-CONTEXT.md / PITFALLS #11 — absolute): `semantic.py`, `models.py`,
`templates.py`, `render.py`, `site.py` (READ-ONLY reuse), `faithfulness.py`, `coverage.py`,
`conftest.py`, ANY existing test, `.importlinter`, `ci.yml`. `swimlane.py` is complete (Plan 02-01) —
do NOT edit it here. The ONLY writable target is the NEW `src/newsletters/compose.py`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module scaffold + pure compute_delta(start, close)</name>
  <files>src/newsletters/compose.py</files>
  <read_first>
    - src/newsletters/adapters/_timestamps.py — the pure-function discipline to mirror (no clock, no
      state; argument-only). Import `EPOCH_ZERO` from here.
    - src/newsletters/semantic.py — the `KpiItem(label, value, delta, dir)` shape; `dir` is
      `Literal["up","down"]|None`; `delta` is `Optional[str]`.
    - 02-PATTERNS.md "compute_delta — pure-function discipline" and "No Analog Found" (there is NO
      delta precedent in the repo — this is a genuinely new, pure derivation).
  </read_first>
  <behavior>
    - compute_delta("10", "20") -> ("+10", "up")   # signed magnitude, upward movement
    - compute_delta("20", "8")  -> ("-12", "down") # downward movement
    - compute_delta("5", "5")   -> ("0", None)     # Δ==0 honest no-change: dir=None, zero form rendered
    - compute_delta("abc", "20") -> (None, None)   # non-numeric endpoint: no fabricated delta
    - compute_delta("10", "")   -> (None, None)    # absent endpoint: no fabricated delta
    - identical inputs always return identical output (pure; no clock/state)
  </behavior>
  <action>
    Create `src/newsletters/compose.py` with a module docstring stating the four-property contract
    (pure/deterministic; AI-free; yaml-free/loader-agnostic; selects-orders-links-never-authors) and
    the import discipline (from `.adapters._timestamps import EPOCH_ZERO`; from `.semantic import`
    the block types + `KpiItem`, `Claim`, `Source`, `Surface`, `Review`; from `.swimlane import
    SwimlaneLoad, SectionBinding`; from `.templates import REPORT`; `from .site import Ledger` is
    added in Plan 02-03, not here). NO `yaml`, NO `distill`, NO `render`, NO `models` import.

    Implement `compute_delta(start, close)` as a PURE function of its two argument strings returning
    `tuple[Optional[str], Optional[Literal["up","down"]]]`:
    - Parse a numeric value out of each token (int or float). Keep it simple and deterministic; a
      token that does not parse to a number yields `(None, None)`.
    - If either token is empty/None/non-numeric → `(None, None)` (caller discloses the gap; NEVER a
      fabricated 0).
    - Otherwise diff = close - start. `dir` = "up" if diff > 0, "down" if diff < 0, `None` if diff == 0.
      `delta` = a signed string of the difference ("+10", "-12"), and for Δ==0 the computed zero form
      ("0"). Unit-preserving is discretion: if both tokens carry the SAME trailing non-numeric unit,
      you MAY append it — but keep it simple and never invent a unit.
    - No rounding inside a format string; do the arithmetic then format once. Never read a clock.
  </action>
  <verify>
    <automated>python -c "from newsletters.compose import compute_delta as d; assert d('10','20')==('+10','up'); assert d('20','8')==('-12','down'); assert d('5','5')==('0',None); assert d('abc','20')==(None,None); assert d('10','')==(None,None); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `compute_delta` matches every case in <behavior>.
    - It is a pure function (repeat calls on identical input are identical; no clock/state/import of
      `datetime.now`).
    - `compose.py` imports no `yaml`, `distill`, `render`, or `models`; `lint-imports` stays green.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compose_module_report — kind-agnostic per-lane assembly + deterministic Surface</name>
  <files>src/newsletters/compose.py</files>
  <read_first>
    - src/newsletters/worksurface.py `build_work_report` (lines ~137-227) — the composed-Surface
      DISCIPLINE to mirror (traced-or-missing routing, Draft-only, lineage/traces population). Note:
      it does NOT pass `created`, so it inherits the now() default — this plan must NOT copy that bug.
    - src/newsletters/capture.py `build_report` (lines ~81-113) — the `Surface(...)` shape:
      template=REPORT, traces=<sources>, byline, `review=Review(policy=REPORT.review_policy,
      author=...)` (Draft by default).
    - src/newsletters/semantic.py — `Surface` (`model_config = validate_assignment=True`;
      `created` default is `_utcnow` — the DETERMINISM TRAP), `KpiStripBlock`, `ClaimsBlock`,
      `ProseBlock` (connective tissue only), `KpiItem`, `Review`.
    - src/newsletters/swimlane.py — `SwimlaneLoad` (`.source`, `.bindings`, `.claims`,
      `.all_claims`), `SectionBinding` (`.heading`, `.kpi_items`, `.kpi_endpoints`, `.claims`,
      `.missing`).
    - 02-CONTEXT.md "Identity & structure" and "The Δ contract".
  </read_first>
  <behavior>
    - N bindings in -> N KpiStripBlock + N ClaimsBlock out, interleaved per lane in config order.
    - A KPI with two traced numeric endpoints -> its KpiItem.delta/dir set via compute_delta.
    - A KPI declaring a movement whose endpoints are not both computable -> delta=None + a missing[] note.
    - Two calls on the same SwimlaneLoad -> byte-identical model_dump_json (Surface.created==EPOCH_ZERO).
    - Empty bindings list -> a valid Draft Surface with a populated missing[] note (never None, never raises).
    - A zero-KPI lane -> its KpiStripBlock omitted (or empty) + disclosed in missing[]; the ClaimsBlock still emitted.
  </behavior>
  <action>
    Implement `compose_module_report(load: SwimlaneLoad, *, author: str = "operator") -> Surface`
    (final public signature — the `ledger` parameter and quote/fanout blocks are added in Plan 02-03;
    keep the function shape extensible). Per COMP-01 the composer consumes `load.bindings`
    (`SectionBinding[]`) with NO knowledge that lanes exist — iterate them in order, treating each as
    a generic section:

    For each binding, in FILE ORDER (never a set/non-total sort — Pitfall 6):
    - Build a per-lane KpiStripBlock: pair each `kpi_items[i]` with `kpi_endpoints[i]`. When that
      endpoint list has ≥2 entries whose traces `is_addressed` and whose `.text` are both numeric,
      compute `(delta, dir) = compute_delta(endpoints[0].text, endpoints[-1].text)` and emit a NEW
      `KpiItem(label=item.label, value=item.value, delta=delta, dir=dir)`. When it has ≥2 entries but
      they are not both computable, emit the KpiItem with `delta=None, dir=None` AND append a
      `missing[]` note (a declared movement that could not be computed — never a fabricated 0). A KPI
      with a single endpoint (a point-in-time `value:`) is emitted value-only with `delta=None` and
      NO note (legitimately has no movement). Set the strip `heading` to the binding's `heading`
      (a traced structural label, not composer-authored). A binding with zero `kpi_items` omits the
      strip (or emits an empty one) and discloses that in `missing[]`.
    - Build a per-lane ClaimsBlock from the binding's `claims`. The composer SELECTS traced Claims
      only: any claim lacking evidence is NOT placed on the block — route its text to `missing[]`
      (mirror worksurface's traced-or-missing policy; the composer never re-mints and never
      fabricates a trace).
    - Interleave strip then claims per lane so lane sections stay grouped and ordered.

    Assemble the Surface:
    - `Surface(id=<slug>, template=REPORT, title=<structural, numeral-free, config-derived module
      label>, eyebrow="Report · ...", blocks=<the per-lane blocks + an optional connective ProseBlock
      that carries NO numerals/facts — headings/transitions only>, traces=[load.source], byline=[author],
      review=Review(policy=REPORT.review_policy, author=author), created=EPOCH_ZERO)`. Pass `created`
      EXPLICITLY (the `now()` default is the confirmed determinism trap — 02-PATTERNS.md). `id`/`slug`
      derives from the config module identity (data, not hardcoded); keep it deterministic.
    - Module-level `Surface.missing` = union of every binding's `missing` + all compose-found delta
      gaps + any claims routed out above, in a deterministic order (file order; no set()).
    - Edge case: `load.bindings == []` -> still return a valid Draft Surface (template REPORT,
      Draft review, `created=EPOCH_ZERO`) whose `missing[]` discloses the empty lane set. NEVER None,
      NEVER raise.

    The connective ProseBlock (if any) is CONNECTIVE TISSUE ONLY — no numerals, no facts not drawn
    from a traced claim (COMP-03; Hole A). Do NOT compute counts into prose. Do NOT call `publish()`
    or advance the review gate (ships Draft only). Do NOT edit `semantic.py`/`templates.py`.
  </action>
  <verify>
    <automated>python -c "
from newsletters.semantic import Source, Claim, KpiItem, Trace
from newsletters.swimlane import SwimlaneLoad, SectionBinding
from newsletters.compose import compose_module_report
from newsletters.adapters._timestamps import EPOCH_ZERO
src=Source(id='m', context='module-config:m', transcript='10 20 finding', timestamp=EPOCH_ZERO)
c0=Claim(text='10', evidence=[Trace.from_source(src,0,2)]); c1=Claim(text='20', evidence=[Trace.from_source(src,3,5)])
cf=Claim(text='finding', evidence=[Trace.from_source(src,6,13)])
b=SectionBinding(heading='Lane', kpi_items=[KpiItem(label='M', value='20')], kpi_endpoints=[[c0,c1]], claims=[c0,c1,cf])
load=SwimlaneLoad(source=src, bindings=[b], claims=[], unextracted=[], scalars_walked=3)
s=compose_module_report(load); assert s.template.name=='report'; assert s.gate.value=='draft'; assert s.created==EPOCH_ZERO
kpis=[bl for bl in s.blocks if bl.kind=='kpi']; assert kpis and kpis[0].items[0].delta=='+10' and kpis[0].items[0].dir=='up'
assert s.model_dump_json()==compose_module_report(load).model_dump_json()
empty=compose_module_report(SwimlaneLoad(source=src, bindings=[], claims=[], unextracted=[], scalars_walked=0))
assert empty is not None and empty.gate.value=='draft' and empty.missing
print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Every case in <behavior> holds.
    - The composed Surface is `template=REPORT`, review state Draft, `created==EPOCH_ZERO`, and
      `traces==[load.source]`.
    - Two composes of the same load are byte-identical (`model_dump_json`).
    - The composer contains no `set()` in the lane/KPI path and no non-total sort (Pitfall 6); no
      `.publish(`; no numerals authored into any ProseBlock.
    - `lint-imports` green; `black`/`isort --profile black`/`mypy` on `compose.py`: zero NEW failures
      vs baseline.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SectionBinding → composed blocks | config-derived Claim.text / KpiItem values flow into surface blocks (rendered later in Phase 3) |
| derived delta → KpiStripBlock | a compose-time number that appears nowhere in the YAML (the milestone's #1 faithfulness surface — Hole A) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | compute_delta result on KpiStripBlock | mitigate | Δ derived ONLY from two traced numeric endpoints via one pure function; either absent/non-numeric → None + missing[] note (never a fabricated 0). Reproducibility is proven by the Plan 02-04 test |
| T-02-05 | Spoofing | connective ProseBlock text | mitigate | Prose is connective tissue only — no numerals/facts authored; the numeral-free-prose guard (Plan 02-04) fails on any un-sourced digit run (Hole A) |
| T-02-06 | Tampering | Surface.created / lane ordering | mitigate | `created=EPOCH_ZERO` passed explicitly; lanes/KPIs iterated in file order (no set/non-total sort) — byte-stable double render (Pitfalls 5/6) |
| T-02-07 | Elevation of privilege | review gate | mitigate | Ships Draft only; no `publish()` / gate advance in compose. Proven by the no-auto-publish test (Plan 02-04) |
| T-02-08 | Information disclosure | config strings in blocks | accept | No HTML authored here; downstream render owns HTML-escaping. Blocks carry plain strings/Claims only (invariant 3) |

No package installs in this plan (no new dependency); no network/auth surface.
</threat_model>

<verification>
- The two smoke `python -c` checks pass.
- `lint-imports` green (compose is AI-free, yaml-free, loader-agnostic).
- `black`/`isort --profile black`/`mypy` on `compose.py`: zero NEW failures vs baseline.
- Full trust-guard suite lands in Plan 02-04.
</verification>

<success_criteria>
`compose.py` exists with a pure `compute_delta` and a `compose_module_report` that builds a
deterministic Draft REPORT Surface from `SectionBinding[]` via a kind-agnostic seam, with honest
`missing[]` routing and no authored facts. COMP-01/COMP-02/COMP-03 core is implemented; quote/fanout/
ledger follow in Plan 02-03.
</success_criteria>

<output>
Create `.planning/phases/02-module-scope-report-composer/02-02-SUMMARY.md` when done.
</output>
