---
phase: 13-problem-lifecycle-entity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/newsletters/problem.py
  - src/newsletters/__init__.py
  - tests/test_problem.py
autonomous: true
requirements: [PROB-01]
user_setup: []

must_haves:
  truths:
    - "A Problem aggregates >=1 traced Source; constructing one with zero evidence is refused"
    - "A Problem carries a typed ProblemState ladder Identified -> Owned -> In Progress -> Resolved -> Verified, starting at Identified"
    - "transition(to, by, note) is the SOLE mutator: it appends a TransitionEvent and sets state, refuses an empty actor and an illegal ladder move, and never auto-advances"
    - "The ladder allows sequential forward moves plus explicit re-open (Resolved->In Progress, Verified->In Progress) and refuses skips and arbitrary backward jumps"
    - "Problem and ProblemState are importable from the newsletters package (first-class API)"
    - "A dogfood Problem aggregating a real Source runs end-to-end through a human-gated transition sequence with no rendering"
  artifacts:
    - path: "src/newsletters/problem.py"
      provides: "Problem, ProblemState, TransitionEvent, the _LADDER allow-table, transition()"
      contains: "class ProblemState"
      min_lines: 50
    - path: "tests/test_problem.py"
      provides: "entity/ladder/transition/dogfood unit tests for PROB-01"
      contains: "def test_"
    - path: "src/newsletters/__init__.py"
      provides: "Problem/ProblemState/TransitionEvent re-export"
      contains: "from .problem import"
  key_links:
    - from: "src/newsletters/problem.py"
      to: "src/newsletters/semantic.py"
      via: "from .semantic import Source, Trace (the ONLY non-stdlib/pydantic import — acyclic, AI-free)"
      pattern: "from \\.semantic import"
    - from: "src/newsletters/__init__.py"
      to: "src/newsletters/problem.py"
      via: "package re-export"
      pattern: "from \\.problem import"
---

<objective>
Build the Problem lifecycle entity — a new AI-free leaf module `src/newsletters/problem.py`
carrying `Problem`, `ProblemState`, and `TransitionEvent`, with the single human-gated
`transition()` mutator and the sequential-plus-re-open ladder. Export the entity from the
package API and prove it end-to-end with a dogfood Problem aggregating a real Source. This is
the entity half of Phase 13 (the final phase of this UAT cut).

Purpose: PROB-01 — a first-class `Problem` ABOVE `Source` that makes the scattered
problem -> owned -> solution -> verified lifecycle a legible, traceable home. The legibility
layer; problem-SOLVING stays external. (Implements locked decisions L1, L2, L3, L4, L7.)

Output: `src/newsletters/problem.py`, the `__init__.py` re-export, and `tests/test_problem.py`
covering the entity, the ladder, the human-gated transition, and the dogfood example.
</objective>

<phase_goal>
**As a** practitioner whose problems live scattered across Jira/ADO, passdowns, and people's
heads, **I want to** consolidate a problem and its real lifecycle (Identified -> Verified) into
one typed, evidence-traced record, **so that** the lifecycle becomes legible without becoming a
second tracker.

> Note: the ROADMAP Phase-13 `**Goal:**` line is outcome-shaped, not strictly user-story-shaped.
> The story above is derived faithfully from that goal + the three success criteria; decisions are
> locked (L1-L8), so this is framing for the executor, not a re-opening of scope.
</phase_goal>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-problem-lifecycle-entity/13-CONTEXT.md
@.planning/phases/13-problem-lifecycle-entity/13-RESEARCH.md

# The spine Problem sits above and must NOT mutate (Source/Trace + the human-gated idiom):
@src/newsletters/semantic.py
# The import-pattern precedent (problem -> semantic, mirrors promote -> semantic):
@src/newsletters/promote.py
# The leaf-module rule (stdlib + pydantic only, acyclic):
@src/newsletters/locators.py
# The package API to extend:
@src/newsletters/__init__.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create problem.py — Problem / ProblemState / TransitionEvent / transition()</name>
  <files>src/newsletters/problem.py, tests/test_problem.py</files>
  <behavior>
    Write tests/test_problem.py FIRST (RED), then implement problem.py to GREEN. Cover (PROB-01):
    - test_problem_requires_at_least_one_source: Problem(evidence=[]) raises ValueError; Problem
      with >=1 Trace constructs and starts at ProblemState.IDENTIFIED.
    - test_ladder_forward_and_reopen: a full IDENTIFIED->OWNED->IN_PROGRESS->RESOLVED->VERIFIED
      sequence advances state and appends one TransitionEvent per step (from_state/to_state/by
      recorded); RESOLVED->IN_PROGRESS and VERIFIED->IN_PROGRESS (re-open) are allowed and logged.
    - test_transition_refuses_illegal_move: a skip (IDENTIFIED->RESOLVED) and an arbitrary
      backward jump (VERIFIED->IDENTIFIED) each raise ValueError; state is unchanged on refusal.
    - test_transition_human_gated: transition(to=OWNED, by="") raises ValueError (no actor);
      there is no public setter or auto-advance path — transition is the only way state changes.
    - test_source_ids_property: source_ids returns the distinct, de-duplicated source_id list
      from evidence (single-entity traceability — NOT a cross-Problem query).
  </behavior>
  <action>
    Create `src/newsletters/problem.py` as a leaf module per L1 mirroring promote.py's import edge
    and locators.py's leaf rule. Module docstring states: imports ONLY stdlib + pydantic + semantic
    (`Source`, `Trace`); NEVER imports distill-AI/capture/render/site/network; acyclic
    (semantic.py never imports problem). Define a local two-line `_utcnow()` (copy semantic.py:40-41
    — do NOT import the private from semantic; keep the leaf clean).

    `ProblemState(StrEnum)` (L3): IDENTIFIED="identified", OWNED="owned", IN_PROGRESS="in_progress",
    RESOLVED="resolved", VERIFIED="verified". Member VALUES must share nothing with ReviewState's
    "draft"/"in_review"/"published" (the distinctness PROOF is Plan 02's job; just keep values
    disjoint here — note IN_PROGRESS vs IN_REVIEW are deliberately different strings).

    `_LADDER: dict[ProblemState, set[ProblemState]]` (L4, sequential + explicit re-open per the
    A1-resolved rule): IDENTIFIED->{OWNED}, OWNED->{IN_PROGRESS}, IN_PROGRESS->{RESOLVED},
    RESOLVED->{VERIFIED, IN_PROGRESS}, VERIFIED->{IN_PROGRESS}. Re-open is the ONLY backward edge.

    `TransitionEvent(BaseModel)` (L4): from_state: ProblemState, to_state: ProblemState, by: str,
    note: str = "", at: datetime = Field(default_factory=_utcnow). Immutable record of one move.

    `Problem(BaseModel)` (L2): id: str, title: str, state: ProblemState = IDENTIFIED,
    evidence: list[Trace] = Field(default_factory=list), log: list[TransitionEvent] =
    Field(default_factory=list), opened: datetime = Field(default_factory=_utcnow). Add a
    `@field_validator("evidence")` that raises a teaching ValueError when evidence is empty (>=1
    required — every Problem traces to evidence). Add a `source_ids` property returning the distinct
    source_ids from evidence (mirrors Claim.is_traced style; single-entity traceability only).

    `transition(self, to, by, note="") -> "Problem"` (L4) is the SOLE mutator: raise ValueError on
    empty `by` (human-gated, mirrors Surface.approve's "requires a reviewer" idiom, semantic.py:568-569);
    raise ValueError on `to not in _LADDER[self.state]` with a teaching message listing allowed moves;
    on success append TransitionEvent(from_state=self.state, to_state=to, by=by, note=note) to log and
    set state = to. NO setter, NO advance()/auto path. Reserved verb is `transition` (NOT promote/
    publish/advance/fan-out/derive) per the terminology guard — do NOT add any gate/fan-out/promote verb.

    SCOPE GUARD (L8): build NO list[Problem] query/aggregation/portfolio/grouping container and add NO
    node/area grouping field; touch NO render.py/site.py/templates.py. Those are Phase 14 (PROB-02/04).
    Zero new dependency; no AI; no external call.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_problem.py -x</automated>
  </verify>
  <done>
    problem.py defines Problem/ProblemState/TransitionEvent/_LADDER/transition; the evidence>=1
    validator, the human-gated empty-actor refusal, the illegal-move refusal, and the
    sequential+re-open ladder all pass in tests/test_problem.py. No Phase-14 collection/query/render
    surface exists. `.venv/bin/mypy src/newsletters/problem.py` is clean (no new errors).
  </done>
</task>

<task type="auto">
  <name>Task 2: Export Problem/ProblemState/TransitionEvent + dogfood Problem end-to-end</name>
  <files>src/newsletters/__init__.py, tests/test_problem.py</files>
  <action>
    (L1 export) Extend `src/newsletters/__init__.py`: add `from .problem import Problem, ProblemState,
    TransitionEvent` alongside the existing import blocks, and add `"Problem", "ProblemState",
    "TransitionEvent"` to `__all__` under a new `# problem lifecycle (A2)` comment grouping —
    mirroring the existing grouped-export style. This makes the entity a first-class part of the
    package API per PROB-01 ("first-class entity"). Do NOT export `_LADDER` (private).

    (L7 dogfood) Add `test_dogfood_problem_end_to_end` to tests/test_problem.py: build ONE real
    `Source` (reuse the session-rev1 shape from dogfood.py — a genuine build problem, e.g. the
    Locator circular-import fix), mint evidence via `Trace.from_source(src, start, end)` (>=1),
    construct the Problem (proving >=1 enforcement), then run a human-gated sequence
    IDENTIFIED->OWNED->IN_PROGRESS->RESOLVED->VERIFIED with explicit actors (a peer actor on the
    VERIFIED step, echoing the peer-review ethos WITHOUT reusing the gate API). Assert: final state
    VERIFIED; log to_states match the sequence; source_ids == the one source id; the evidence Trace
    is_addressed and not is_stale_against(src) (drift-aware, free). NO rendering, NO HTML, NO
    list[Problem] aggregation (L8). Keep the dogfood in the TEST file, not in problem.py, so the
    module stays a pure type module.
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_problem.py -x && .venv/bin/python -c "from newsletters import Problem, ProblemState, TransitionEvent; print('exported', ProblemState.IDENTIFIED.value)"</automated>
  </verify>
  <done>
    `from newsletters import Problem, ProblemState, TransitionEvent` works and all three are in
    `__all__`; test_dogfood_problem_end_to_end passes, proving aggregation + >=1 + ladder +
    human-gated log + content-addressed traceability with zero rendering. Full
    `.venv/bin/pytest` stays green (>=560 tests).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (human) -> Problem.transition | A lifecycle state change crosses here; it MUST carry a recorded actor. The only mutation path into a Problem. |
| Problem -> Source (read-only) | Problem references Sources via Trace (source_id, content_hash); it reads for drift-checking but MUST NOT write back or mutate the spine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering / Repudiation | Problem state change | mitigate | `transition` requires a non-empty `by`; there is no setter or auto-advance; every move appends an immutable `TransitionEvent(by, at, note)` — proven by test_transition_human_gated. |
| T-13-02 | Tampering | `_LADDER` legal-move check | mitigate | An illegal/skip/backward move raises ValueError; state unchanged on refusal — proven by test_transition_refuses_illegal_move. |
| T-13-03 | Spoofing of evidence | Problem with no traced Source | mitigate | `@field_validator("evidence")` rejects a zero-evidence Problem (>=1 required) — every Problem traces to evidence. |
| T-13-04 | Tampering (out-of-boundary write) | problem.py import surface | mitigate | Leaf-module rule: imports only stdlib + pydantic + semantic; the FORMAL no-write-back proof (import-linter contract + runtime guard + API allow-list) is Plan 02 (T-13-SC carried there). |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Phase 13 installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A); no install task exists, so no supply-chain surface this plan. |
</threat_model>

<verification>
Standing invariants this plan keeps green (re-run independently — never trust a subagent's "green"):
- `.venv/bin/pytest` — full suite green (>=560 tests; the spine + review gate unchanged).
- `.venv/bin/mypy src/newsletters` — no NEW errors beyond the ~9 pre-existing baseline; problem.py clean.
- `.venv/bin/lint-imports` — still exit 0 (Plan 02 adds the second contract; this plan must not break the first).
- Zero new dependency: no change to pyproject.toml; the AI-isolation/dependency-boundary tests
  (test_ai_optional.py) stay green.
- No Phase-14 leak: no list[Problem] query/aggregation/portfolio container/grouping field; no
  render.py/site.py/templates.py touch.
</verification>

<success_criteria>
- ROADMAP success-criterion 1 met: a `Problem` aggregates >=1 traced `Source` and carries the
  typed `Identified -> Owned -> In Progress -> Resolved -> Verified` ladder, typed end to end.
- PROB-01 satisfied: first-class `Problem` above `Source`, exported from the package, distinct verb
  (`transition`) reserved for the lifecycle axis.
- Human-gated + ladder-enforced + evidence>=1 all proven by tests; dogfood proves it end-to-end with
  no rendering.
- (The formal distinctness proof + no-write-back proof are Plan 02; this plan must not regress them.)
</success_criteria>

<output>
Create `.planning/phases/13-problem-lifecycle-entity/13-01-SUMMARY.md` when done.
</output>
