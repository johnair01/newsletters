---
phase: 13-problem-lifecycle-entity
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - .importlinter
  - tests/test_problem_boundary.py
autonomous: true
requirements: [PROB-03, PROB-01]
user_setup: []

must_haves:
  truths:
    - "No network/external-system module is reachable from problem.py — proven both statically (a new import-linter contract) and at runtime (a sys.modules subprocess guard)"
    - "The Problem public API exposes NO write-back/export/sync/push path to any external system; transition is the only mutator"
    - "The semantic.py spine is unchanged by Problem: a Source's content_hash is byte-identical across a full transition sequence, and semantic.py never imports problem"
    - "ProblemState is a distinct type from ReviewState with no shared member value or name, and the verb transition collides with no review-gate/fan-out/promote verb"
    - "lint-imports reports 2 kept, 0 broken and still exits 0"
  artifacts:
    - path: ".importlinter"
      provides: "the second forbidden contract — forbid-external-write-in-problem"
      contains: "forbid-external-write-in-problem"
    - path: "tests/test_problem_boundary.py"
      provides: "the three PROB-03 no-write-back sub-proofs + the terminology-distinctness proof"
      contains: "def test_"
  key_links:
    - from: ".importlinter"
      to: "newsletters.problem"
      via: "source_modules = newsletters.problem, forbidden_modules = socket/http/urllib/ftplib/smtplib/subprocess/requests"
      pattern: "source_modules\\s*=\\s*\\n?\\s*newsletters\\.problem"
    - from: "tests/test_problem_boundary.py"
      to: "newsletters.problem"
      via: "import newsletters.problem in a fresh subprocess; assert no forbidden module in sys.modules"
      pattern: "import newsletters.problem"
---

<objective>
Prove the PROB-03 hard constraint by test — the same way no-auto-publish is proven, not asserted.
Add the second import-linter contract (`forbid-external-write-in-problem`) and a new test file
`tests/test_problem_boundary.py` carrying the three no-write-back sub-proofs (static+runtime import
boundary, API allow-list, spine-unchanged) plus the terminology-distinctness proof. This is the
boundary half of Phase 13 — the legibility-layer-not-a-tracker guarantee made durable.

Purpose: PROB-03 (the hard constraint — legibility layer, NOT an execution tracker: no write-back
to Jira/ADO; the semantic.py spine boundary preserved) and PROB-01's terminology-distinctness
criterion. (Implements locked decisions L5 and L6.)

Output: the new `.importlinter` contract + `tests/test_problem_boundary.py` (the no-write-back +
spine-unchanged + terminology-distinctness MANDATORY gates).
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-problem-lifecycle-entity/13-CONTEXT.md
@.planning/phases/13-problem-lifecycle-entity/13-RESEARCH.md

# The proof TEMPLATE — the AI-isolation guard this phase mirrors (contract + runtime sys.modules +
# allow-list + source-text import-direction checks):
@tests/test_ai_optional.py
# The forbidden-contract shape to copy:
@.importlinter
# The spine that must stay unchanged + ReviewState for the distinctness proof:
@src/newsletters/semantic.py
# The module under proof (created in Plan 01):
@src/newsletters/problem.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the forbid-external-write-in-problem import-linter contract</name>
  <files>.importlinter</files>
  <action>
    (L5a, static layer) Append a SECOND `forbidden` contract to `.importlinter`, copying the shape of
    the existing `forbid-ai-in-core` contract (.importlinter:23-36). Section header:
    `[importlinter:contract:forbid-external-write-in-problem]`. name: "problem.py must not import any
    network/external-system package". type = forbidden. source_modules = `newsletters.problem`.
    forbidden_modules: `socket`, `http`, `urllib`, `ftplib`, `smtplib`, `subprocess`, `requests`
    (the network/external-system + would-be-tracker set per RESEARCH Q.C(a) / Assumption A3).
    `include_external_packages = True` is already set at file scope — do NOT duplicate it. Add a
    short comment explaining this is the static half of the no-write-back boundary (the runtime
    sys.modules guard in Task 2 is the complement, mirroring the AI-optional two-layer playbook).

    NOTE on the "2 kept" expectation (L5): adding this contract makes `lint-imports` report
    "Contracts: 2 kept, 0 broken". The existing `test_import_linter_contract_holds`
    (test_ai_optional.py:117-137) asserts only EXIT 0, not a literal count — VERIFIED in the live
    repo (no test asserts "1 kept"). So no test edit is required for the count; just confirm
    `.venv/bin/lint-imports` still exits 0 and now lists 2 kept.
  </action>
  <verify>
    <automated>.venv/bin/lint-imports</automated>
  </verify>
  <done>
    `.venv/bin/lint-imports` exits 0 and reports "Contracts: 2 kept, 0 broken"; the new
    `forbid-external-write-in-problem` contract is listed as KEPT. The pre-existing
    `test_import_linter_contract_holds` still passes (it asserts exit 0).
  </done>
</task>

<task type="auto">
  <name>Task 2: tests/test_problem_boundary.py — no-write-back (3 sub-proofs) + terminology-distinctness</name>
  <files>tests/test_problem_boundary.py</files>
  <action>
    Create `tests/test_problem_boundary.py` mirroring the idioms in test_ai_optional.py. These are
    MANDATORY GATES (the PROB-03 hard constraint — like no-auto-publish, proven not asserted). Define
    a `FORBIDDEN_MODULES` set = {socket, http, urllib, ftplib, smtplib, subprocess, requests} and a
    `REPO_ROOT` (mirror test_ai_optional.py).

    (L5a runtime) `test_problem_loads_no_external_module`: run a fresh subprocess
    (`PYDANTIC_DISABLE_PLUGINS=true`, cwd=REPO_ROOT — exactly as test_core_import_loads_no_ai_module,
    test_ai_optional.py:143-158) that does `import newsletters.problem` then asserts none of
    FORBIDDEN_MODULES is in sys.modules. This measures OUR import graph, not the ambient .venv.
    (The static half is the contract from Task 1, covered by the existing lint-imports test.)

    (L5b API allow-list) `test_problem_api_has_no_write_back_method`: enumerate Problem's public
    callable surface (`{n for n in dir(Problem) if not n.startswith("_")}`); assert it contains NO
    name matching any of the forbidden substrings (export, push, sync, write, send, post, upload,
    jira, ado, devops, remote, publish); assert `transition` IS present (the one sanctioned mutator).
    This is a STANDING guard: a future export_to_jira fails it loudly.

    (L5c spine unchanged — two assertions) `test_spine_unchanged_by_problem`:
      (i) No mutation by construction — build a real Source, capture `source.content_hash()`, build a
          Problem from a Trace of it, run a FULL transition sequence (IDENTIFIED..VERIFIED), then
          assert source.content_hash() is byte-identical and the Source's fields are unchanged.
      (ii) Import-direction unchanged — read src/newsletters/semantic.py as text and assert no
           `import` line references `problem` (the dependency stays one-way problem -> semantic),
           mirroring test_openpyxl_loader_has_no_toplevel_openpyxl_import (test_ai_optional.py:284-304).

    (L4 human-gated, the never-auto-mutated proof) `test_transition_human_gated_empty_actor_raises`:
    assert `Problem(...).transition(to=ProblemState.OWNED, by="")` raises ValueError, closing the
    "no code path advances state without an explicit human actor" guarantee (the direct analog of
    test_published_without_approval_is_rejected). (Plan 01 also covers the ladder side; this is the
    boundary-file's standing copy of the human-gated invariant.)

    (L6 terminology-distinctness) `test_problemstate_distinct_from_reviewstate`: assert
    `ProblemState is not ReviewState`, neither is a subclass of the other; the member-VALUE sets are
    disjoint (`{m.value for m in ProblemState}.isdisjoint({m.value for m in ReviewState})` — this is
    the load-bearing check given IN_PROGRESS="in_progress" vs IN_REVIEW="in_review" are adjacent); and
    the member-NAME sets are disjoint. Plus `test_lifecycle_verb_collides_with_no_axis_verb`: assert
    `transition` is not among the review-gate/fan-out/promote verbs {publish, approve,
    open_pull_request, promote_claim_to_kpi, promote_report_to_article, fan_out, derive}; assert
    `Surface` has no `transition` attribute; assert `Problem` has no gate verb (publish/approve/
    open_pull_request). Import ReviewState/Surface from newsletters.semantic and Problem/ProblemState
    from newsletters.problem.

    SCOPE GUARD (L8): no test references render.py/site.py/templates.py, no list[Problem]
    aggregation, no portfolio container. Zero new dependency; no AI; no external call (the subprocess
    guard imports the local module only).
  </action>
  <verify>
    <automated>.venv/bin/pytest tests/test_problem_boundary.py -x</automated>
  </verify>
  <done>
    All five+ boundary/distinctness tests pass: no forbidden module reachable at runtime; the API
    exposes no write-back path and `transition` is the sole mutator; the Source content_hash is
    byte-identical across a full transition sequence and semantic.py never imports problem; the
    empty-actor transition raises; ProblemState is provably distinct (no shared value/name) from
    ReviewState and the verb `transition` collides with no other axis. Full `.venv/bin/pytest` green;
    `.venv/bin/lint-imports` reports 2 kept, 0 broken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| problem.py -> any external system (Jira/ADO/network) | The PROB-03 hard boundary: solving is external/operator-owned. Signals models the RECORD; it must never write back. Crossing this boundary in ANY direction (import or call) is forbidden and proven absent. |
| Problem axis vs Surface review-gate axis | The terminology guard: the three state axes (review gate / fan-out / problem ladder) must share no type, member value, member name, or verb — a "promote to Published" style collision could corrupt the trust gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering (out-of-boundary write) | problem.py import + API surface | mitigate | New import-linter `forbid-external-write-in-problem` contract (static) + a runtime sys.modules subprocess guard + an API allow-list test — no network/external module reachable, no write-back/export method exists. |
| T-13-05 | Tampering / Repudiation | semantic.py spine | mitigate | Spine-unchanged test: Source.content_hash byte-identical across a full transition sequence; semantic.py never imports problem (one-way dependency). |
| T-13-06 | Tampering (axis collision corrupting the trust gate) | ProblemState vs ReviewState; verb `transition` | mitigate | Terminology-distinctness test: distinct types, disjoint member value+name sets, reserved verb collides with no gate/fan-out/promote verb. |
| T-13-01 | Tampering / Repudiation | Problem state change (human-gated copy) | mitigate | Standing empty-actor-raises test in the boundary file closes the never-auto-mutated guarantee. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Phase 13 installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A). The new `.importlinter` entry is config, not a package install; no supply-chain surface. |
</threat_model>

<verification>
Standing invariants this plan keeps green (re-run independently):
- `.venv/bin/pytest` — full suite green (>=560 tests plus the new boundary tests); spine + review
  gate unchanged.
- `.venv/bin/lint-imports` — exit 0, "Contracts: 2 kept, 0 broken".
- `.venv/bin/mypy src/newsletters` — no NEW errors beyond the ~9 pre-existing baseline.
- Zero new dependency (the `.importlinter` change is config; import-linter is already in [dev]).
- The no-write-back + human-gated + terminology-distinctness tests are MANDATORY and must be GREEN
  before the phase gate (this is the PROB-03 hard constraint, like no-auto-publish).
- No Phase-14 leak: no render/site/templates touch, no list[Problem] aggregation, no portfolio
  container.
</verification>

<success_criteria>
- ROADMAP success-criterion 2 met: the problem ladder is provably distinct in code from the surface
  review gate and the fan-out chain — no shared/overloaded term (terminology-guard enforced by test).
- ROADMAP success-criterion 3 met: transitions are human-gated and never auto-mutated; there is no
  write-back path to any external system; the semantic.py spine boundary is preserved AND proven by
  test (the three sub-proofs).
- PROB-03 (hard constraint) satisfied and durable: the no-write-back guarantee is encoded as standing
  guards (import-linter contract + runtime + API allow-list), not a vibe.
</success_criteria>

<output>
Create `.planning/phases/13-problem-lifecycle-entity/13-02-SUMMARY.md` when done.
</output>
