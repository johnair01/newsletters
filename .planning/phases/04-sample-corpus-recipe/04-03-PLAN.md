---
phase: 04-sample-corpus-recipe
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - docs/weekly.md
  - docs/architecture.md
  - docs/surfaces.md
  - docs/weekly-spec.md
  - CLAUDE.md
  - content/README.md
  - WHERE-WE-ARE.md
  - RETRO.md
  - tests/test_weeklysite.py
autonomous: true
requirements: [WKLY-06, WKLY-05]

must_haves:
  truths:
    - "An operator who is not the author can go from a workbook / template deck / .eml drop / photo folder to a reviewed weekly using only docs/weekly.md, read-only and local"
    - "Every `newsletters …` command line in docs/weekly.md names a command and options the shipped Typer app actually exposes — a renamed flag turns the suite red instead of rotting the doc"
    - "Every documented command was EXECUTED against the committed synthetic corpus during this phase, with its output recorded"
    - "docs/architecture.md, docs/surfaces.md, CLAUDE.md and content/README.md describe four corpora and the shipped renderer — no document still says three"
    - "The full carried gate set is green when re-run independently, measured against the recorded baselines"
    - "WHERE-WE-ARE.md and RETRO.md are current: the phase's decisions, the known limitations, and every friction with the rule it hardened into"
  artifacts:
    - path: "docs/weekly.md"
      provides: "the WKLY-06 operator recipe — eight steps, copy-pasteable commands, trust property per step"
      min_lines: 120
    - path: "WHERE-WE-ARE.md"
      provides: "the updated compass — shipped/in-progress/not-started, the truths, the decisions-and-why log"
    - path: "RETRO.md"
      provides: "the milestone friction log with each durable fix encoded as a rule or guard test"
  key_links:
    - from: "docs/weekly.md"
      to: "newsletters.cli"
      via: "the doc-contract test parsing every fenced `newsletters …` line against the live Typer app"
      pattern: "newsletters weekly"
    - from: "docs/weekly.md"
      to: "docs/weekly-spec.md"
      via: "link, never restate — the authoring contract has one home"
      pattern: "weekly-spec.md"
    - from: "docs/weekly.md"
      to: "content/weekly/"
      via: "the worked example an operator can read"
      pattern: "content/weekly"
---

<objective>
Make the weekly usable by someone who is not the author, and make every document that describes the
system tell the truth about what now ships.

Purpose: WKLY-06 and ROADMAP SC-4 and SC-5. A recipe whose commands were never run is a promise, not
a proof — CONTEXT is explicit that "copy-pasteable = actually pasted". And the moment a fourth corpus
landed, four documents that say "three" became wrong; specs are the source of truth here, so they get
fixed in the same milestone that made them stale, with the reason recorded.

Output: `docs/weekly.md`, the two doc-contract tests, the spec deltas across
`docs/architecture.md` / `docs/surfaces.md` / `docs/weekly-spec.md` / `CLAUDE.md` /
`content/README.md`, the compass and RETRO updates, and the independently re-run phase gate sweep.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-sample-corpus-recipe/04-RESEARCH.md
@.planning/phases/04-sample-corpus-recipe/04-01-SUMMARY.md
@.planning/phases/04-sample-corpus-recipe/04-02-SUMMARY.md
@docs/weekly-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: docs/weekly.md — the operator recipe, and the tests that keep it honest</name>
  <files>docs/weekly.md, tests/test_weeklysite.py</files>

  <read_first>
    - docs/architecture.md lines 213-252 (the WORK-01 flow — the repo's operator-recipe shape: numbered steps, each naming the exact function/command, each stating the trust property it preserves)
    - docs/case-spec.md (the authoring-doc shape and the link-don't-duplicate idiom)
    - .planning/phases/04-sample-corpus-recipe/04-RESEARCH.md §`docs/weekly.md` — Shape and Honest Commands
    - src/newsletters/cli.py (the SHIPPED command and option spellings — the doc must match these exactly)
    - tests/test_collaboration_contract.py (the presence-guard idiom for a doc-contract test)
  </read_first>

  <action>
Write `docs/weekly.md` in the repo's operator-recipe voice: numbered steps, each naming the exact
command or function, each stating the trust property it preserves. Link `docs/weekly-spec.md` for
the authoring contract — never restate its schema; that doc has one home and two copies of a schema
drift exactly as two normalizers do.

Eight headings:

1. **What you need** — a template deck (`.pptx` with `NL_`-named shapes), and optionally a workbook
   export, an `.eml` drop and a photo folder. State up front, in the WORK-01 language: *everything
   stays on your machine; nothing is transmitted; nothing you point at is committed.*
2. **Install** — `pip install '.[config,pptx]'`, with `[excel]` only if you use a workbook. Say that
   the bare install is AI-free and that none of these extras is an AI dependency.
3. **Prepare the template** — Selection-Pane names start with `NL_`; the writer fills your EXISTING
   slides and never invents layout; a placeholder with no content and content with no placeholder
   each fail loud, naming the offender. Carry the two costed warnings verbatim in substance: set
   `word_wrap=True` / `auto_size=NONE` on your text boxes or long lines overflow the slide silently
   (P-07), and slots are looked up on DOCUMENT slides — layout and master placeholders are not
   walked (IN-07).
4. **Author the Weekly Spec** — link `docs/weekly-spec.md`, list the eight keys by name only, and
   state the two rules an operator most needs: every absence is disclosed and never fabricated
   (rule 4), and a path that escapes the project root RAISES rather than landing in `missing[]`
   (rule 7) — a refusal, not an absence. Mention that `config: author:` is where your byline lives,
   or pass `--author`.
5. **Point the read-only adapters at your data** — the `.eml` drop through `EmailAdapter().parse`
   (stdlib only, no extra); the workbook through the EXISTING ADAPT-03 excel adapter after exporting
   the BI view to `.xlsx` — say plainly that there is no CSV reader and no Power BI value reader
   (WKLY-04 / ADAPT-05 scope); photos as `assets:` records carrying folder + date + event, with a
   deep link REQUIRED when a screenshot stands in for values. Repeat the trust property: read-only,
   no network, nothing committed.
6. **Compose and render** — the commands, in fenced blocks, one command per line, long invocations
   backslash-wrapped. Use the SHIPPED spellings: `newsletters build --corpus weekly --out …`,
   `newsletters check --corpus weekly`, `newsletters weekly --spec … --lanes … --template …
   --author … --out …`, `newsletters assemble --out dist/site`. Do not invent a flag; the
   doc-contract test below asserts every one against the live app.
7. **Review** — open the HTML, read the honesty panel (what the record does NOT know is the part
   worth reading), check the deck's Draft watermark. State that `Draft › In Review › Published` with
   a recorded reviewer is the only way out and that it needs a human — no command in this document
   publishes anything. Note honestly that `newsletters check` is Draft-vacuous by design (it exempts
   non-Published surfaces), so the honesty panel — not the exit code — is what an operator reads.
8. **What the sample looks like** — point at `content/weekly/` as the worked example, name its three
   planted absences, and say the deck lives at `content/weekly/deck/` and is deliberately not served
   or linked from the published page (a corpus artifact, not a download).

**Two doc-contract tests in `tests/test_weeklysite.py`** (stdlib only — the module runs in
`site-integrity`):
  - `test_recipe_commands_match_the_shipped_cli` — extract every fenced line in `docs/weekly.md`
    that starts with `newsletters `, join backslash-continued lines, and for each: assert the
    command name is registered on the Typer app and every `--option` it uses is exposed by that
    command. Drive the expected set from the live app (introspect `cli.app`, or invoke `--help`
    through `typer.testing.CliRunner` and match against its output) rather than from a hand-written
    list, so a renamed flag turns the suite red instead of rotting the doc. Add a non-vacuity
    assertion that at least four command lines were found — a regex that silently matches nothing
    would otherwise pass forever.
  - `test_recipe_carries_the_load_bearing_anchors` — assert `docs/weekly.md` contains each of: the
    read-only / stays-local statement, the no-auto-publish statement naming the gate, the
    `word_wrap` warning, the link to `docs/weekly-spec.md`, the `content/weekly` worked-example
    pointer, and the "no CSV reader / no Power BI value reader" scope statement. Assert the phrases
    by their load-bearing tokens, and say in the docstring that this test protects the TRUST claims
    of the recipe, not its prose style.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_weeklysite.py -q -k "recipe"</automated>
    <human-check>At PR review the Editor-in-Chief reads docs/weekly.md cold, as an operator who is not the author, and confirms it clears the not-the-author bar. Recorded per workflow.human_verify_mode=end-of-phase; the commands themselves are machine-proved in Task 3.</human-check>
  </verify>

  <acceptance_criteria>
    - `docs/weekly.md` exists with the eight headings, each step naming its exact command/function
      and the trust property it preserves.
    - Every fenced `newsletters …` line uses only shipped commands and options, proven by the
      doc-contract test, which itself asserts it found at least four command lines.
    - The recipe LINKS `docs/weekly-spec.md` and does not restate the eight-key schema.
    - The anchors test passes and names what it is protecting.
  </acceptance_criteria>
  <done>docs/weekly.md ships with its eight steps and two non-vacuous doc-contract tests green.</done>
</task>

<task type="auto">
  <name>Task 2: The spec deltas — every document that says "three corpora" now says four</name>
  <files>docs/architecture.md, docs/surfaces.md, docs/weekly-spec.md, CLAUDE.md, content/README.md, tests/test_weeklysite.py</files>

  <read_first>
    - docs/architecture.md lines 245-252 (the `--corpus {rev1|work}` selector subsection — already stale at TWO) and lines 256-300 (§9 "the three committed corpora")
    - docs/surfaces.md lines 290-310 (§Cross-corpus Records strip — "composes three corpora")
    - CLAUDE.md line 122 (the repo-layout table's `content/{rev1,work,module}/`)
    - content/README.md (says "Empty until then" — stale since v1.1)
  </read_first>

  <action>
Fix the documents that went stale the moment a fourth corpus landed. CLAUDE.md's rule is explicit:
if code and spec disagree, either the spec wins or you update the spec in the same change and say
why. This is the "say why" half.

  - **`docs/architecture.md`** — two edits. The `--corpus {rev1|work}` selector subsection is stale
    at TWO corpora: rewrite it for all four, naming each corpus's builder, default out dir and its
    OWN append-only ledger, and keep the load-bearing sentence that the selector routes the BUILDER
    and never forks the gate (T-11-13). §9's "composes the three committed corpora" becomes four,
    with `content/weekly/site` at `/weekly/`. Add a short paragraph to §9, or a new subsection,
    recording what does NOT publish and why: the weekly's `.pptx` deck lives at
    `content/weekly/deck/` and `assemble_site` copies `content/*/site` only, so the deck cannot
    reach the published tree structurally rather than by discipline; the `FanoutBlock` download-link
    route is recorded as a costed one-task reversal, not built.
  - **`docs/surfaces.md`** — §Cross-corpus Records strip: "three corpora" becomes four, and the
    strip's link set gains the weekly record. Keep the existing rule that the strip appears on
    chrome pages only.
  - **`docs/weekly-spec.md`** — add a pointer to `docs/weekly.md` near the top (the authoring
    contract points at the operator recipe; the recipe points back). Do not move any schema text.
  - **`CLAUDE.md`** — the repo-layout table's `content/{rev1,work,module}/` becomes
    `content/{rev1,work,module,weekly}/`. Change nothing else in that file.
  - **`content/README.md`** — replace the stale "Empty until then" with the truth: the four
    committed corpora, what each is, and the one publish channel (`publish.assemble_site` /
    `newsletters assemble` → the deploy workflow force-pushes `gh-pages` from `main` only).

**One guard test in `tests/test_weeklysite.py`**, `test_docs_describe_four_corpora` — encode this as
a TEST rather than as a one-off grep in a summary, so the next corpus cannot make these documents
stale silently. For each of `docs/architecture.md`, `docs/surfaces.md`, `CLAUDE.md` and
`content/README.md`, assert the weekly corpus is named, and assert that the specific stale phrases
this task removed ("three corpora" / "three committed corpora" / `{rev1|work}`) are gone. Filter
comment and code-fence noise out of the read text before counting so the test cannot be
self-invalidated by a document quoting the phrase it forbids — and if a document legitimately needs
to quote the old wording, scope the assertion to the section rather than loosening it.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_weeklysite.py -q -k "docs or recipe" && .venv/bin/python -m pytest tests/test_collaboration_contract.py -q</automated>
  </verify>

  <acceptance_criteria>
    - No document under `docs/`, `CLAUDE.md` or `content/README.md` still describes three corpora or
      a two-value `--corpus` selector.
    - `docs/architecture.md` §9 records the deck's non-publication as a STRUCTURAL property and names
      the recorded one-task reversal without building it.
    - `docs/weekly-spec.md` and `docs/weekly.md` point at each other.
    - `test_docs_describe_four_corpora` passes and would fail if any of the four documents regressed
      (state in the summary how you confirmed it discriminates).
  </acceptance_criteria>
  <done>No document still describes three corpora, and a guard test keeps it that way.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the recipe, sweep the full gate set independently, and bring the compass current</name>
  <files>WHERE-WE-ARE.md, RETRO.md</files>

  <read_first>
    - WHERE-WE-ARE.md (the newest-on-top state entry, the load-bearing truths, the decisions-and-why log)
    - RETRO.md (the friction-log format and the rules already hardened)
    - .planning/phases/04-sample-corpus-recipe/04-VALIDATION.md (the measured baselines: 837/0 · lint-imports 2 kept · mypy 15 in 5 files · black 69 would-reformat · isort DEF-15 set)
  </read_first>

  <action>
**(a) Execute the recipe — SC-4's proof.** Run EVERY fenced `newsletters …` command from
`docs/weekly.md` against the committed synthetic corpus, in document order, in a clean shell, and
capture each command and its output verbatim into the plan summary. Where a command writes, write to
a temporary directory so the committed corpus is untouched — except the one that regenerates the
sample deck, which must reproduce the committed bytes (assert the `part_digest` afterwards). If ANY
command needs editing to work, fix `docs/weekly.md` and re-run from the top: a recipe corrected in a
summary but not in the doc is exactly the drift the doc-contract test exists to prevent. Afterwards,
`git status --porcelain` must be clean of any unintended change.

**(b) The full carried gate set, re-run independently.** "The agent says green" is not green: re-run
each of these yourself, once each (rapid re-runs throw transient errors), and record the measured
number beside its recorded baseline.
  1. `.venv/bin/python -m pytest -q` — expect > 837 passed and **0 skipped**.
  2. `.venv/bin/lint-imports` — expect 2 contracts KEPT, 0 broken.
  3. `newsletters check --corpus rev1`, `--corpus work`, `--corpus module`, `--corpus weekly` —
     four cleans.
  4. committed == fresh for every corpus: `pytest tests/test_publish.py tests/test_worksurface.py
     tests/test_modulesite.py tests/test_weeklysite.py -q`, PLUS the deck's two tiers (the stdlib
     digest check in `test_weeklysite.py` and the `[pptx]` fresh==committed check in
     `test_weekly_golden.py`). Never a raw-byte comparison on a zip.
  5. bare-install untouched: `git diff <milestone base> -- .github/workflows/ci.yml` shows ZERO
     deleted lines and no change inside the `bare-install` job.
  6. `.venv/bin/mypy src/newsletters` (baseline 15 errors in 5 files) · `.venv/bin/black --check src
     tests` (baseline 69 would-reformat) · `.venv/bin/isort --check-only src tests` (the DEF-15
     pre-existing set) — **no NEW failure attributable to this phase**. Format only the files this
     phase created; do not reformat the repo (DEF-15 is maintainer-gated).
  7. `git diff --exit-code -- content/*/ids.json` after a rebuild — ledgers append-only.
  8. `pytest tests/test_semantic_gate_frozen.py -q` — the review gate's source-hash pins and the
     zero-deleted-lines diff (needs `origin/main` reachable).
  9. `pytest tests/test_abstraction_guard.py -q` — green with the grown denylist.
  10. Read the workflow diffs and state plainly, in the summary, that no sample, script or CI step
      publishes, advances the gate, pushes to `main`, or makes an external call.
If any gate is red, STOP and fix it in this phase; do not carry a red gate into verification.

**(c) `WHERE-WE-ARE.md`** — a new newest-on-top entry: what shipped (the fourth corpus, the builder,
the deck + digest, the `newsletters weekly` command, the recipe), what the decisions were and WHY,
in the teaching voice: why a sibling corpus rather than extending `content/module` (one builder per
byte-stable corpus, preserved at the ledger layer); why the deck is a corpus artifact and not a
download link (and the recorded one-task reversal); why the ledger restarts at `R-001`; why
`check --corpus weekly` is wired despite being Draft-vacuous, and what actually proves trust for
this corpus (the honesty panel); and the two known limitations carried openly — the weekly Surface
carries only the spec `Source` in `traces` (measured harmless for a Draft sample; Phase 3 code was
deliberately not changed), and the deck is text-only this milestone. Confirm the load-bearing truths
still hold, or say which one this work strained and why.

**`RETRO.md`** — log this milestone's friction honestly, naming mistakes rather than papering over
them, and encode each durable fix as a rule or a guard test rather than a vibe. At minimum: the
recurring W21 shape (a test suite and the job that runs it are two different artifacts) and how the
`weekly` job's fail-on-skip step is the hardened form; the `_EMAIL_RE` collision and why the
allowance is documented in a test docstring instead of being silently broad; the carried tooling
friction that `gsd-tools query state.*` has mutated STATE.md on error and reported a value it did
not write (the rule: diff the file after ANY state call, and never treat the return envelope as
evidence of what was written); and DEF-15 still taxing every plan that adds an import.

Do NOT commit — the orchestrator handles committing.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest -q && .venv/bin/lint-imports && .venv/bin/newsletters check --corpus rev1 && .venv/bin/newsletters check --corpus work && .venv/bin/newsletters check --corpus module && .venv/bin/newsletters check --corpus weekly && git diff --exit-code -- content/rev1/ids.json content/work/ids.json content/module/ids.json content/weekly/ids.json</automated>
  </verify>

  <acceptance_criteria>
    - Every fenced `newsletters …` command in `docs/weekly.md` was executed against the committed
      corpus; each command and its output is pasted into the summary; the deck-regenerating command
      reproduced the committed `part_digest`.
    - All ten gate rows are recorded with a MEASURED number beside the recorded baseline
      (837/0 · 2 kept · 15 mypy · 69 black · the DEF-15 isort set), and each is green or explicitly
      explained as pre-existing.
    - `git status --porcelain` is clean of unintended change after the recipe run.
    - `WHERE-WE-ARE.md` carries a new newest-on-top entry with the decisions-and-why log, including
      both known limitations.
    - `RETRO.md` names at least the four frictions above, each with the rule or guard test it
      hardened into.
    - No commit is made by this plan.
  </acceptance_criteria>
  <done>The recipe was executed against the committed corpus, all ten gates were re-run independently and recorded against the baselines, and the compass + RETRO are current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → an operator's real data | the recipe tells a human where to point tools at private material; wrong instructions leak private data |
| documentation → the shipped CLI | a doc that drifts from the code teaches a wrong command with confidence |
| a claimed gate result → the reviewer | a green nobody re-ran is a claim, not evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | `docs/weekly.md` instructing an operator to point tools at private material | mitigate | every ingest step states read-only / stays local / no network / nothing committed, in the WORK-01 language; the anchors test asserts those statements are present, so they cannot be edited away silently |
| T-04-15 | Tampering | doc drift from the shipped CLI | mitigate | the doc-contract test parses every fenced `newsletters …` line and validates the command and its options against the LIVE Typer app, with a non-vacuity floor so a regex matching nothing cannot pass |
| T-04-16 | Repudiation | claiming a gate is green without running it | mitigate | all ten gate rows are re-run independently and recorded as measured numbers beside the recorded baselines; a red gate stops the phase rather than being carried |
| T-04-17 | Tampering | the recipe run mutating the committed corpus | mitigate | every writing command targets a temporary directory except the deck regeneration, which must reproduce the committed `part_digest`; `git status --porcelain` is asserted clean afterwards |
| T-04-18 | Information Disclosure | a stale spec understating what publishes | mitigate | `test_docs_describe_four_corpora` fails if any of the four documents regresses to describing three corpora |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no package install in this plan (04-RESEARCH §Package Legitimacy Audit: not applicable). A proposed dependency is a scope escape — stop and re-run the legitimacy gate. |
</threat_model>

<verification>
The phase gate sweep IS this plan's Task 3(b). Beyond it:

- `.venv/bin/python -m pytest tests/test_weeklysite.py -q -k "recipe or docs"` — the doc contracts
- `.venv/bin/newsletters weekly --help` matches the options `docs/weekly.md` uses
- read `.github/workflows/deploy-pages.yml` and `ci.yml` end to end and state in the summary that no
  step publishes, advances the gate, pushes to `main`, or makes an external call
</verification>

<success_criteria>
- `docs/weekly.md` takes a not-the-author operator from real inputs through authoring, composing,
  rendering and reviewing, read-only and local, with commands that match the shipped CLI and were
  executed against the synthetic corpus.
- Two doc-contract tests keep the recipe honest as the CLI evolves; both are non-vacuous.
- Every document describing the corpora describes four, and the deck's non-publication is recorded
  as a structural property.
- The full carried gate set is green when re-run independently, measured against the recorded
  baselines, with no new mypy/black/isort failure attributable to this phase.
- `WHERE-WE-ARE.md` and `RETRO.md` are current, including the two known limitations carried openly.
</success_criteria>

<artifacts_produced>
| Artifact | Kind | Why it exists |
|----------|------|---------------|
| `docs/weekly.md` | doc | WKLY-06 — the operator recipe, proved by execution |
| `test_recipe_commands_match_the_shipped_cli` | test | the doc cannot rot past a CLI rename |
| `test_recipe_carries_the_load_bearing_anchors` | test | the recipe's trust claims cannot be edited away |
| `test_docs_describe_four_corpora` | test | the next corpus cannot make four documents stale silently |
| spec deltas (architecture · surfaces · weekly-spec · CLAUDE · content README) | doc | specs are the source of truth; they get fixed in the change that made them stale |
| `WHERE-WE-ARE.md` / `RETRO.md` | compass | SC-5 — the screens that survive a context reset |
</artifacts_produced>

<output>
Create `.planning/phases/04-sample-corpus-recipe/04-03-SUMMARY.md` when done. It must contain: the
executed recipe transcript (every command and its output), the ten-row gate table with measured
numbers beside the recorded baselines, the workflow-diff reading that supports the "nothing
publishes" claim, and the two known limitations as they were written into the compass.
</output>
