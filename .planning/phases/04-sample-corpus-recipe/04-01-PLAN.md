---
phase: 04-sample-corpus-recipe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/weekly/weekly-2374-w41.yml
  - content/weekly/inbox/berth-rota-thanks.eml
  - content/weekly/assets/ring-berth-utilisation.png
  - content/weekly/template.pptx
  - content/weekly/ids.json
  - content/weekly/site/library.html
  - content/weekly/site/weekly-2374-w41.html
  - content/weekly/site/fonts/
  - content/weekly/deck/weekly-2374-w41.pptx
  - content/weekly/deck/weekly-2374-w41.pptx.digest
  - src/newsletters/weeklysite.py
  - src/newsletters/cli.py
  - tests/_corpus_scan.py
  - tests/test_weeklysite.py
  - tests/test_modulesite.py
  - tests/test_abstraction_guard.py
autonomous: true
requirements: [WKLY-05]

must_haves:
  truths:
    - "The committed synthetic weekly composes to a Draft Surface(REPORT) at EPOCH_ZERO and takes R-001 from its OWN content/weekly/ids.json"
    - "All three planted absences (a lane with no KPIs, a recognition with no source, an asset with no provenance) appear in surface.missing[] AND html-escaped in the rendered honesty panel — none silently dropped"
    - "The committed content/weekly/site/ is byte-identical to a fresh build, and a tmp_path build mutates no committed ledger"
    - "A committed deck exists OUTSIDE site/ whose part_digest equals its committed .digest sidecar — checkable with stdlib only, on a bare install"
    - "`newsletters weekly` regenerates that deck and its digest with a caller-supplied --out"
    - "The committed weekly content carries no real-looking nomenclature; the scanner that says so is proven non-vacuous by a planted leak"
  artifacts:
    - path: "src/newsletters/weeklysite.py"
      provides: "build_weekly_surfaces / build_weekly_site / build_weekly_deck — the corpus builder seam"
      exports: ["build_weekly_surfaces", "build_weekly_site", "build_weekly_deck"]
      min_lines: 150
    - path: "content/weekly/weekly-2374-w41.yml"
      provides: "the authored synthetic Weekly Spec (all eight keys, two planted absences)"
      contains: "week:"
    - path: "content/weekly/ids.json"
      provides: "the weekly corpus's own append-only ledger (R-001)"
      contains: "R-001"
    - path: "content/weekly/deck/weekly-2374-w41.pptx.digest"
      provides: "the tier-1 tamper digest for the committed deck"
    - path: "tests/_corpus_scan.py"
      provides: "the promoted synthetic-content scanner shared by the module and weekly corpora"
      exports: ["REAL_LOOKING_LITERALS", "EMAIL_RE", "scan_real_looking"]
    - path: "tests/test_weeklysite.py"
      provides: "the stdlib-only weekly corpus suite (no [pptx] import anywhere)"
  key_links:
    - from: "src/newsletters/weeklysite.py"
      to: "content/module/*.yml"
      via: "swimlane.load_swimlanes over a sorted glob — the content/module lineage link"
      pattern: "load_swimlanes"
    - from: "src/newsletters/weeklysite.py"
      to: "newsletters.weeklyspec"
      via: "load_weekly_spec / build_weekly_report / weekly_slots"
      pattern: "build_weekly_report"
    - from: "src/newsletters/weeklysite.py"
      to: "newsletters.pptx_writer"
      via: "lazy import INSIDE build_weekly_deck only"
      pattern: "from .pptx_writer import"
    - from: "src/newsletters/cli.py"
      to: "weeklysite.build_weekly_deck"
      via: "the flat `weekly` command, lazy-imported in the body"
      pattern: "build_weekly_deck"
---

<objective>
Land the synthetic weekly corpus and the builder that makes it, so that the committed corpus and
the code that regenerates it are green **together** (research §Commit/Wave Ordering step 1 — this
step is safe alone: `assemble_site` ignores a directory it does not know about, so nothing else in
the repo moves).

Purpose: WKLY-05's first half. A fabricated weekly in the `content/module` lineage composes to a
Draft `Surface(REPORT)`, renders to HTML with a populated honesty panel carrying all three planted
absences, and renders to a committed `.pptx` deck whose integrity is checkable without the
optional extra.

Output: `content/weekly/` (spec · `.eml` · PNG · template copy · ledger · site · deck + digest),
`src/newsletters/weeklysite.py`, the `newsletters weekly` command, `tests/_corpus_scan.py`,
`tests/test_weeklysite.py`.

Recorded decisions this plan implements (04-VALIDATION §Wave 0 — implement, do not reopen):
W0-1 sibling `content/weekly/` corpus with a new `weeklysite.py`; bindings reuse the committed
`content/module/*.yml`. W0-2 deck at `content/weekly/deck/` + `.digest` sidecar, not served.
W0-4 Phase 3 `src/` untouched. W0-5 `.xlsx` stays out of the corpus build path. W0-6 the `.eml`
must not trip the `_EMAIL_RE` confidentiality scanner. W0-7 ledgers stay append-only.
Milestone D-01 (the weekly reuses `Surface(REPORT)`; the deck is a *format*) and D-02 (an asset
without provenance is unrepresentable) are inherited, not re-implemented.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sample-corpus-recipe/04-PATTERNS.md
@.planning/phases/04-sample-corpus-recipe/04-RESEARCH.md
@docs/weekly-spec.md
</context>

<interface_contracts>
The Phase 2/3 APIs this plan consumes. They are SHIPPED and FROZEN — do not modify any file under
`src/newsletters/` other than the new `weeklysite.py` and the `cli.py` addition in Task 3.

- `weeklyspec.load_weekly_spec(path, *, root=None, known_sources=()) -> WeeklySpecLoad`
- `weeklyspec.build_weekly_report(load, *, author: str, bindings=(), surface_id=None) -> Surface`
  — `author` becomes `Surface.byline[0]` and `Review.author`; it is metadata, never a Claim.
- `weeklyspec.weekly_slots(load, surface) -> dict[str, list[str]]` (four `NL_` keys, fixed order)
- `swimlane.load_swimlanes(path, *, root=None) -> SwimlaneLoad` with `.bindings: list[SectionBinding]`
- `adapters.email_adapter.EmailAdapter().parse(raw: bytes, path: str) -> (Source, list[str], list[Unextracted])`
  — `Source.id` **is** the `path` string you pass. Stdlib-only; pulls no optional extra.
- `site.Ledger.load(path)` / `site.Site.from_surfaces(surfaces, ledger=...)` / `ledger.save()`
- `render.render_surface(surface, *, site, page, home_href)` / `render.render_library(site, records=..., home_href=...)`
- `worksurface._emit_fonts(out)`
- `pptx_writer.render_surface_pptx(surface, *, template, slots, out_path) -> Path`
- `pptx_writer.render_surface_pptx_bytes(surface, *, template, slots) -> bytes`
- `pptx_writer.part_digest(raw: bytes) -> str` — **stdlib only** (`zipfile` + `hashlib`), so it
  imports and runs on a bare install.
</interface_contracts>

<tasks>

<task type="auto">
  <name>Task 1: The synthetic corpus fixtures and the promoted synthetic-content scanner</name>
  <files>content/weekly/weekly-2374-w41.yml, content/weekly/inbox/berth-rota-thanks.eml, content/weekly/assets/ring-berth-utilisation.png, content/weekly/template.pptx, tests/_corpus_scan.py, tests/test_modulesite.py, tests/test_weeklysite.py, tests/test_abstraction_guard.py</files>

  <read_first>
    - tests/fixtures/weekly/weekly-full.yml (the header-comment policy and the eight-key order to mirror)
    - docs/weekly-spec.md §"Writing one" and §"The routing" (the exact disclosure wording — never author a disclosure literal)
    - tests/test_modulesite.py lines 296-379 (the scanner being promoted, verbatim)
    - tests/test_abstraction_guard.py lines 140-190 (`_WEEKLYSPEC_FIXTURE_VALUES` and the `_DENY_LITERALS` union)
    - .planning/phases/04-sample-corpus-recipe/04-RESEARCH.md §The Honesty-Path Fixture (the three exact missing[] strings, captured by execution)
  </read_first>

  <action>
Author the committed synthetic corpus and the guard that keeps it synthetic. Nothing in this task
imports python-pptx or reads a workbook.

**(a) `content/weekly/weekly-2374-w41.yml`** — exactly ONE root-level `*.yml` under
`content/weekly/` (the builder discovers it by `sorted(glob("*.yml"))[0]`; a second root-level YAML
would be silently mis-selected). Open with the SYNTHETIC-DATA header comment in the same voice as
`tests/fixtures/weekly/weekly-full.yml`, naming the three planted absences so a reader knows they
are deliberate. Use all eight keys in schema order. Fabricated, Star-Trek-flavoured content that is
on NEITHER existing denylist: use week `"2374-W41"`, module `"Docking Ring Logistics"`, and crew
names `Jadzia Dax` / `Elim Garak` / `Ezri Tigan`. Do NOT reuse `Miles O'Brien` / `Kira Nerys` /
`Julian Bashir` / `Shuttlebay Operations` / `2374-W35` — those are the *test fixture* family and
reusing them would make the committed corpus and the test fixtures indistinguishable in a grep.

Content requirements, each load-bearing:
  - `highlights:` two or three authored lines including one YAML block scalar (the block-scalar
    path is the one that folds, and it is already a proven regression site).
  - `lowlights:` at least one authored line. The sample must NOT hide its lowlights — that is the
    absence this product exists to surface, and an empty-lowlights sample would teach the wrong
    lesson to the operator reading it.
  - `recognitions:` exactly two entries. The FIRST carries `source:` set to the POSIX
    repo-relative path of the `.eml` below (`content/weekly/inbox/berth-rota-thanks.eml`), because
    `EmailAdapter().parse(raw, path)` makes `Source.id` **that path string** and
    `_resolve_recognition_evidence` matches `source:` against known Source ids exactly. The SECOND
    entry has `person:` + `reason:` and **no** `source:` — planted absence #2.
  - `team:` two members; one carries `photo:` naming the PLACED asset key.
  - `assets:` exactly two entries. `ring-berth-utilisation` is complete (file · sha256 · folder ·
    date · event · caption · alt) and points at the committed PNG. `manifest-annex-photo`
    **omits `folder`** — planted absence #3. Provenance is checked BEFORE the file read, so this
    second entry needs no file on disk; give it a plausible `file:` path under
    `content/weekly/assets/` that does not exist.
  - `config:` an org-slot subtree that includes an `author:` key. That value becomes the weekly's
    byline (Task 2) — binding a config value as Surface metadata is what "bound, never claimed"
    means; it is not a Claim. CONSTRAINT: the `author:` value (and every other `config:` leaf) must
    not appear anywhere in `highlights` / `lowlights` / `recognitions` / `team` / `assets` text, or
    the corpus-level config-never-claimed assertion in Task 2 will (correctly) fail.

Compute the `sha256:` by hashing the committed PNG bytes — do not copy the fixture's hash blindly;
the loader re-checks the content address at placement time and will refuse a mismatch.

**(b) `content/weekly/assets/ring-berth-utilisation.png`** — byte-copy
`tests/fixtures/weekly/assets/bay-cycle-throughput.png` (75 bytes) to this new name. It is a valid
minimal PNG; nothing renders it as an image (the deck is text-only per the Phase 3 decision and the
HTML `AssetBlock` branch emits no `<img>`), so its content only has to hash stably.

**(c) `content/weekly/inbox/berth-rota-thanks.eml`** — a small synthetic RFC-5322 message that
evidences the first recognition. Every address MUST use the RFC 6761 reserved domain
`@example.invalid` (W0-6): the confidentiality scanner treats ANY email address as a real-name
*shape*, and this is the single most likely "green turns red for a surprising reason" in the phase.
Give it a fixed `Date:` header so nothing about the file depends on a clock. Keep the body short
and on-theme with the recognition's `reason:`.

**(d) `content/weekly/template.pptx`** — a byte-copy of `tests/fixtures/weekly/template.pptx`
(sha256 `4461a6329aa4e74c25e3879a5212d646d7b7816c12e772b15befa70c2e722618`). `src/` must not read
from `tests/`, and the recipe must be able to point an operator at a real template. P-06 stands on
both copies: neither is ever regenerated.

**(e) Promote the synthetic-content scanner into `tests/_corpus_scan.py`.** This repo's norm is
promotion over a second copy (`specspan.SpanMinter`, `compose.compose_kpi_item`,
`pptx_writer.normalize_opc_zip` were each promoted for exactly this reason). MOVE
`_REAL_LOOKING_LITERALS`, `_EMAIL_RE` and `_scan_real_looking` out of `tests/test_modulesite.py`
into `tests/_corpus_scan.py` as public names (`REAL_LOOKING_LITERALS`, `EMAIL_RE`,
`scan_real_looking`), carrying their explanatory comments verbatim — this is a move, not a rewrite.
Leave `_FABRICATED_MARKERS` in `test_modulesite.py`: it is module-corpus-specific. Import the
promoted names back into `test_modulesite.py`. The leading underscore keeps pytest from collecting
the helper as a test module, so no CI job list changes.

**(f) The weekly's own synthetic-content test in `tests/test_weeklysite.py`.** Create the module
with the shared-ledger-path caveat docstring (`test_modulesite.py:25-29` is the model) and this
task's three tests:
  - `test_committed_content_is_synthetic` — scan every committed weekly file (`*.yml`,
    `inbox/*.eml`, `ids.json` and `site/*.html` when they exist) through `scan_real_looking`, with
    ONE documented allowance: hits ending `@example.invalid` are subtracted, and the docstring
    states why (RFC 6761 reserved; an address shape is the scanner's proxy for a real name, and a
    reserved-domain address cannot be one). Carry the planted-leak self-check verbatim so the clean
    pass stays non-vacuous, and add a second planted arm proving a NON-reserved address
    (`ops@starfleet.int`) still trips the scanner through the allowance — otherwise the allowance
    could be silently over-broad.
  - `test_template_is_byte_copy_of_fixture` — `content/weekly/template.pptx` bytes ==
    `tests/fixtures/weekly/template.pptx` bytes, with a message naming P-06.
  - `test_spec_absences_disclosed` — load the spec with `load_weekly_spec(..., root=REPO_ROOT,
    known_sources=[eml_source])` and assert the recognition-absence and asset-provenance
    disclosures are present in `load.distillation.missing`. Locate them by the composer's OWN
    phrasing (a substring taken from `specspan.absent` / `weeklyspec._ASSET_PROVENANCE_ABSENT`),
    never by a hand-typed disclosure sentence. Assert too that the *resolvable* recognition
    produced real evidence and that no unresolvable-source disclosure appears — that contrast is
    what makes the absence row mean something.

**(g) Grow the abstraction guard** (`tests/test_abstraction_guard.py`, CONTEXT discretion, logged):
add a `_WEEKLYSITE_CORPUS_VALUES` frozenset in the existing grouped style (period label, module
name, crew names, `assets:` keys, provenance labels, `config:` leaf values) and add it to the
`_DENY_LITERALS` union. The guard scans `src/` only, so the corpus's own use of these values is
safe — the point is that a future leak of a corpus value into source fails loudly.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_weeklysite.py tests/test_modulesite.py tests/test_abstraction_guard.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `content/weekly/` contains exactly one root-level `*.yml`, one `inbox/*.eml`, one
      `assets/*.png` and `template.pptx`; no other file yet.
    - `sha256sum content/weekly/template.pptx` equals the fixture's hash (byte-copy proven by test,
      not by eye).
    - Every address in the `.eml` ends `@example.invalid`; `scan_real_looking` over the corpus
      returns empty after the documented allowance, and BOTH planted arms still trip it.
    - `tests/test_modulesite.py` passes with the SAME test count as before the promotion
      (record the before/after counts in the summary).
    - `test_spec_absences_disclosed` asserts the two loader-level absences by composer phrasing;
      no disclosure sentence is typed into the test or into the fixture.
    - `pytest tests/test_abstraction_guard.py -q` green with `_WEEKLYSITE_CORPUS_VALUES` in the union.
  </acceptance_criteria>
  <done>The synthetic corpus files exist, the scanner is promoted and shared by both corpora, and every acceptance criterion above holds.</done>
</task>

<task type="auto">
  <name>Task 2: weeklysite.py — the builder, the committed HTML corpus and its ledger</name>
  <files>src/newsletters/weeklysite.py, content/weekly/ids.json, content/weekly/site/, tests/test_weeklysite.py</files>

  <read_first>
    - src/newsletters/modulesite.py (WHOLE FILE — the shape to mirror: docstring contract, imports, corpus constants, `_discover_config`, `build_module_surfaces`, `build_module_site`)
    - tests/test_modulesite.py lines 40-280 (the six tests to mirror: traced+addressed, disclosure-visible-in-HTML, no-external-calls, byte-stable double render, R-001 stability, committed==fresh with the ledger snapshot)
    - .planning/phases/04-sample-corpus-recipe/04-PATTERNS.md §1 and §3
  </read_first>

  <action>
Write `src/newsletters/weeklysite.py` as a thin mirror of `modulesite.py` and generate the
committed HTML corpus with it.

**Module docstring** — mirror `modulesite.py`'s four-property contract in structure (READ-ONLY /
NO NETWORK · CONTENT-ADDRESSED / FAITHFUL · DETERMINISTIC / BYTE-STABLE · AI-FREE / MINIMAL-CORE)
plus the "WHY a new top-level module" paragraph: `compose.py` is a COMP-contract leaf that must not
import `render`/`site`, so the build-and-render seam is a sibling builder. Add one paragraph
recording W0-1: the bindings come from the committed `content/module/*.yml`, because this IS the
weekly for the module in `content/module` — one fabricated module, one source of truth — and the
coupling is bounded and loud (a change to the module config moves both corpora's committed HTML and
both committed==fresh gates go red in the same run). Add one paragraph recording the known
limitation research measured and this phase deliberately does NOT fix: `build_weekly_report` sets
`traces=[load.source]`, so binding- and `.eml`-contributed claims reference Sources the Surface does
not carry. Measured consequence: no rendering break and no false STALE (`semantic.py:212-216` skips
an absent `source_id`), and no `href="None"` in the rendered page. It matters only if this Draft
sample were ever advanced to Published. Phase 3 code is frozen; this is recorded, not patched (W0-4).

**Imports** — in-package only, module level stdlib-clean: `worksurface` (for `_emit_fonts`),
`render.render_library` / `render.render_surface`, `semantic.Surface`, `site.Ledger` / `site.Site`,
`swimlane.load_swimlanes`, `weeklyspec.build_weekly_report` / `load_weekly_spec`. NO `yaml`, NO
`pptx`, NO `openpyxl` anywhere at module scope. `__all__ = ["build_weekly_surfaces",
"build_weekly_site", "build_weekly_deck"]`.

**Corpus constants** — generic, never a fixture filename: `_CORPUS_DIR = "content/weekly"`,
`_LANES_DIR = "content/module"`, `_LEDGER_PATH = "content/weekly/ids.json"`,
`_SITE_DIR = "content/weekly/site"`, `_DECK_DIR = "content/weekly/deck"`,
`_TEMPLATE_NAME = "template.pptx"`.

**Two structural discovery helpers**, both `sorted(dir.resolve().glob("*.yml"))[0]` with a teaching
`FileNotFoundError` naming the unpopulated corpus (`modulesite._discover_config` is the exact
idiom): `_discover_spec(root)` over `_CORPUS_DIR` and `_discover_lanes(root)` over `_LANES_DIR`.
`Path.glob` is non-recursive, so `inbox/`, `assets/`, `site/` and `deck/` never collide with it.

**`_load_inbox_sources(root)`** — read every `sorted(glob("inbox/*.eml"))` under the corpus,
`EmailAdapter().parse(path.read_bytes(), <repo-relative POSIX path>)`, and return the list of
`Source`s in sorted file order. The path string you pass IS the `Source.id` the spec's `source:`
must match — pass the repo-relative POSIX form, resolved against `root`, never an absolute path
(an absolute path would embed the build machine's directory layout in the id and break determinism
across environments). If `inbox/` is absent, return `[]` — an inbox is optional.

**`build_weekly_surfaces(spec_path=None, *, root=None, author=None) -> list[Surface]`** — the shape
`cli check` calls, mirroring `build_module_surfaces`. Discover the spec and the lanes, load the
inbox sources, `load_weekly_spec(spec, root=root, known_sources=inbox)`, `load_swimlanes(lanes,
root=root)`, then `build_weekly_report(load, author=<resolved>, bindings=swim.bindings)` and return
`[surface]`. Resolve `author` as: the explicit argument if given, else the spec's
`config["author"]` when it is a non-blank string, else raise a teaching `ValueError` naming both the
`config: author:` key and the `--author` option. Do NOT invent a default byline — a fabricated
byline in `src/` would be both an abstraction-guard leak and an unsigned record. Never call a gate
transition here; the composer returns Draft and that is the only state this builder produces.

**`build_weekly_site(out_dir=_SITE_DIR, *, root=None, spec_path=None, author=None) -> list[Path]`**
— the `modulesite.build_module_site` sequence in this exact order: mkdir, `build_weekly_surfaces`,
`Ledger.load(_LEDGER_PATH)` **at the fixed committed path, never `out_dir`**,
`Site.from_surfaces(surfaces, ledger=ledger)`, `ledger.save()` (the SOLE ledger writer), render each
page with `home_href="../index.html"`, then the library with `records=` naming its three neighbours
assembled-tree-relative — `("The Rev1 record", "../index.html")`, `("The work record",
"../work/library.html")`, `("The module record", "../module/library.html")` — then
`worksurface._emit_fonts(out)`. Return pages-then-library. Do not re-vendor fonts.

**Generate the committed corpus**: run the builder against the default out dir so
`content/weekly/ids.json` (R-001) and `content/weekly/site/` land in the commit. Never hand-edit a
generated file; regenerate.

**Tests** (extend `tests/test_weeklysite.py`), mirroring `test_modulesite.py` module-for-module and
deriving the page name from `build_weekly_surfaces()[0].id` rather than hardcoding it:
  - `test_every_claim_traced_and_addressed` — every claim on the surface is traced and
    content-addressed; the surface is Draft at `EPOCH_ZERO`; its ref is `R-001`.
  - `test_honesty_panel_shows_all_three_planted_absences` — the SC-1 test. Collect the three
    disclosures out of `surface.missing` by composer phrasing (the KPI-less lane via
    `compose.NO_KPIS`'s wording, the source-less recognition via `specspan.absent`'s wording, the
    provenance-incomplete asset via `weeklyspec._ASSET_PROVENANCE_ABSENT`'s wording), assert each
    list is non-empty as a fixture invariant with a message saying so, build the page, and assert
    `'class="honesty"' in page`, `'class="claim-span"' in page` and `html.escape(entry) in page` for
    every one of them. Reading the strings out of the model and asserting them in the HTML is the
    whole point: "in `missing[]`" and "shown to the reviewer" are two different claims.
  - `test_config_values_never_claimed` — no `config:` leaf value appears in any claim text on the
    surface, and the subtree is non-empty (so the guard is not vacuous).
  - `test_no_external_calls` — copy `test_modulesite.py`'s `forbidden` tuple, the
    `css_url_fetch` / `link_href_http` regexes and the `fonts/*.woff2` presence check verbatim.
  - `test_byte_stable_double_render` — two builds into separate tmp dirs, same file set, same bytes.
  - `test_r001_stable_across_rebuild` — on a FRESH tmp ledger: load → `Site.from_surfaces` → save →
    reload → rebuild → same ref. Never assert the literal twice.
  - `test_committed_equals_fresh_build` — snapshot `content/weekly/ids.json` bytes first, build into
    `tmp_path`, assert the committed ledger is byte-unchanged (Pitfall 6: the builder writes the
    committed ledger even for a tmp build — harmless only while idempotent), then walk the committed
    `site/` and compare every file's bytes to the fresh build's.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_weeklysite.py -q && git diff --exit-code -- content/module/ids.json content/rev1/ids.json content/work/ids.json && .venv/bin/lint-imports</automated>
  </verify>

  <acceptance_criteria>
    - `src/newsletters/weeklysite.py` imports no `yaml`, `pptx` or `openpyxl` at module scope;
      `.venv/bin/lint-imports` reports 2 contracts KEPT, 0 broken.
    - `.venv/bin/mypy src/newsletters/weeklysite.py` reports 0 errors in that file (the measured
      baseline is 15 errors in 5 OTHER files — this one adds none).
    - `content/weekly/ids.json` exists with a single `R-001` entry; the other three ledgers are
      byte-unchanged (`git diff --exit-code`).
    - `content/weekly/site/` carries the report page, `library.html` and `fonts/` including three
      `OFL-*.txt`.
    - All three planted absences are asserted present in `surface.missing` AND `html.escape`d into
      the rendered page; each assertion locates its string by composer phrasing, not by a literal.
    - `test_committed_equals_fresh_build` passes and the committed ledger is byte-unchanged after a
      `tmp_path` build.
    - No file under `src/newsletters/` other than the new `weeklysite.py` is modified
      (`git diff --stat -- src/newsletters/` names one file).
  </acceptance_criteria>
  <done>weeklysite.py builds the committed HTML corpus at R-001; all three planted absences reach the rendered honesty panel; committed == fresh with the ledger unmutated.</done>
</task>

<task type="auto">
  <name>Task 3: The committed deck, its digest sidecar, and the `newsletters weekly` command</name>
  <files>src/newsletters/weeklysite.py, src/newsletters/cli.py, content/weekly/deck/weekly-2374-w41.pptx, content/weekly/deck/weekly-2374-w41.pptx.digest, tests/test_weeklysite.py</files>

  <read_first>
    - src/newsletters/pptx_writer.py lines 236-265 (`part_digest`) and 616-700 (`render_surface_pptx_bytes` / `render_surface_pptx`, incl. the T-02-08 caller-supplied-out_path docstring)
    - src/newsletters/cli.py lines 103-131 (`assemble` — the flat-command / lazy-import / echo idiom to copy)
    - .planning/phases/04-sample-corpus-recipe/04-PATTERNS.md §1 "Deck entry point" and §4 "The two tiers"
  </read_first>

  <action>
Add the deck half of the builder, the one command that produces it, and the tier-1 integrity gate.

**`weeklysite.build_weekly_deck(out_path, *, root=None, spec_path=None, template=None,
author=None) -> Path`** — a THIRD, separate entry point so `[pptx]` never enters
`build_weekly_site`. Rendering the deck inside the site builder would drag python-pptx into the
`site-integrity` job (which installs only `[test,config]`) and into the deploy gate; keep HTML and
deck on separate entry points. Inside the body only (lazy — the bare install stays light and the
existing teaching `ImportError` telling the operator to `pip install '.[pptx]'` is the one that
fires), import `part_digest` and `render_surface_pptx` from `.pptx_writer`. Rebuild the load exactly
as `build_weekly_surfaces` does — spec, inbox sources, lanes — because `weekly_slots(load, surface)`
needs the SAME load the surface was built from, then compose the surface, derive the slots, and
render with `template=` defaulting to `<corpus>/template.pptx` and `out_path=` **caller-supplied and
never derived from Surface content** (threat T-02-08; the property is stated in
`render_surface_pptx`'s docstring and must be preserved here). After the write, compute
`part_digest(out_path.read_bytes())` and write it plus a trailing newline to
`<out_path>.digest`, UTF-8. Return the deck path. `out_path`'s parent is created if absent; a
missing template raises a teaching `FileNotFoundError` naming the expected path (fail loud, never
partial).

Do NOT compare the produced deck to the template, in any form — not bytes, not `part_digest`, not
part order. python-pptx's load path re-serializes empty core properties and re-orders parts, so
that comparison is guaranteed-wrong for reasons that have nothing to do with this phase. The golden
deck comes from the WRITER.

**The `newsletters weekly` command** (`src/newsletters/cli.py`) — one flat `@app.command()` named
`weekly`, copying `cli.assemble`'s idiom exactly: `typer.Option` with help text for each of
`--spec`, `--lanes`, `--template`, `--author`, `--out`; lazy import of `weeklysite` inside the body;
echo the deck path and the digest path, then a summary line. Write a docstring in the voice of the
neighbouring commands stating the three properties an operator needs: the render is deterministic
(two renders of one record are the same deck under the recorded `part_digest` definition), the
surface stays **Draft** and the deck is **watermarked** until a human publishes it, and `--out` is
the operator's choice and is never derived from content. `--lanes` and `--spec` default to `None`
and fall through to the builder's structural discovery. This is the ONE command the recipe
(plan 04-03) documents and the ONE path that regenerates the committed sample — do not add a
`compose|render` sub-app.

**Generate the committed artifacts by running that command**, not by an ad-hoc script: the sample
must be produced by the same path the recipe tells an operator to run, or the two drift on day one.
Write to `content/weekly/deck/weekly-2374-w41.pptx`; the sidecar lands beside it. Paste the exact
invocation and its output into the plan summary.

**Tier-1 gate in `tests/test_weeklysite.py`** (stdlib only — this module must stay importable and
runnable with no `[pptx]` extra, because it runs in `site-integrity` and on a bare install):
  - `test_committed_deck_matches_its_digest` — `part_digest(DECK.read_bytes()) ==
    DIGEST.read_text(encoding="utf-8").strip()`, with a message explaining that a mismatch means the
    committed deck was hand-edited or substituted. Add a non-vacuity arm: mutating one byte region
    of the deck bytes in memory (e.g. digesting a truncated/re-zipped copy, or asserting the digest
    of a DIFFERENT committed `.pptx` — `content/weekly/template.pptx` — is NOT equal to the sidecar)
    proves the assertion discriminates rather than comparing a string to itself.
  - `test_deck_is_not_in_the_published_tree` — assert `content/weekly/deck/` exists, that no `*.pptx`
    exists anywhere under `content/weekly/site/`, and state in the docstring that this is the
    STRUCTURAL half of "nothing publishes": `publish.assemble_site` copies `content/*/site` only, so
    a deck under `deck/` cannot reach the published tree by discipline failure. This also keeps the
    deck out of `test_publish.py`'s `read_bytes()`-per-file committed==fresh loop, which would
    compare raw zip bytes across zlib implementations and go red on a runner for no real reason.
  - `test_weekly_command_is_registered` — via `typer.testing.CliRunner`, `--help` on the app names
    the `weekly` command and its five options. (The deck-producing round trip itself is `[pptx]`-
    gated and lands in plan 04-02's `tests/test_weekly_golden.py`; keep this module extra-free.)
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_weeklysite.py -q && .venv/bin/lint-imports && .venv/bin/python -c "import newsletters.weeklysite, newsletters.cli; import sys; assert 'pptx' not in sys.modules, sorted(m for m in sys.modules if 'pptx' in m)"</automated>
  </verify>

  <acceptance_criteria>
    - `content/weekly/deck/weekly-2374-w41.pptx` and `.pptx.digest` exist; the digest file is one
      hex line plus a trailing newline.
    - The deck was produced by `newsletters weekly` (the exact invocation and output are pasted into
      the summary), not by an ad-hoc script.
    - Importing `newsletters.weeklysite` and `newsletters.cli` leaves `pptx` absent from
      `sys.modules` (the lazy boundary is real, not documented).
    - `part_digest(deck) == sidecar` passes with stdlib only, and the non-vacuity arm shows the
      assertion can fail.
    - No `*.pptx` exists anywhere under `content/weekly/site/`.
    - `lint-imports` still 2 kept / 0 broken; `newsletters weekly --help` lists all five options.
  </acceptance_criteria>
  <done>The deck and its .digest are committed, produced by `newsletters weekly`, and verified with stdlib only on a bare install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored corpus data (`*.yml`, `*.eml`, `*.png`) → loader | untrusted-SHAPED input crosses here; committed, but the loader's refusals are the contract being demonstrated |
| operator CLI arguments (`--spec`, `--template`, `--out`) → filesystem | a path from a human reaches a read and a write |
| committed binary (`.pptx`) → the reviewed record | a file a reviewer will send, which no reader can diff by eye |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `content/weekly/*.yml` asset records | mitigate | the `sha256:` is computed from the committed PNG's bytes and re-checked at placement by the shipped `weeklyspec._place_assets`; a record describing image A while image B sits on disk is refused, not placed |
| T-04-02 | Information Disclosure | the committed corpus (`.eml` headers, people, org labels) | mitigate | `tests/_corpus_scan.py` scans every committed weekly file; the ONLY allowance is the RFC 6761 `@example.invalid` domain, documented in the test's docstring; two planted arms (a real-looking name, a non-reserved address) prove the scanner still fires |
| T-04-03 | Elevation of Privilege | YAML parsing of the corpus | mitigate | no new parse path — `weeklysite` never imports `yaml`; loading goes through the shipped `_yaml_loader.load_config` (`safe_load` only) behind the lazy `[config]` boundary |
| T-04-04 | Tampering | the committed `.pptx` deck | mitigate | the `.digest` sidecar carries `part_digest` (sorted, length-prefixed `(part name, sha256(part))` rows — WR-01); tier 1 asserts it with stdlib only, so a hand-edited or substituted deck is caught on every install including bare |
| T-04-05 | Tampering | `--out` / `--spec` / `--template` path arguments | mitigate | `out_path` is caller-supplied and never derived from Surface content (T-02-08, preserved from `render_surface_pptx`); the loaders `resolve()` then `relative_to(root)` and RAISE on escape — a refusal, never a `missing[]` entry |
| T-04-06 | Repudiation / EoP | the deck path advancing the gate | mitigate | `build_weekly_deck` calls no gate transition; the surface it renders is Draft by construction and `semantic.py` is untouched (the source-hash gate pins stay green) |
| T-04-07 | Denial of Service | a `[pptx]` import creeping into the HTML/site path | mitigate | the deck is a THIRD entry point; the import is inside its body; asserted by a `sys.modules` check after importing `weeklysite` and `cli` |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | this phase installs NO package (04-RESEARCH §Package Legitimacy Audit: not applicable — no `pip install`, no new `[project.optional-dependencies]` entry, no new third-party import). Any proposed dependency is a scope escape: stop and re-run the legitimacy gate rather than adding it. |
</threat_model>

<verification>
Run each gate ONCE (rapid re-runs throw transient errors), and re-run them independently rather
than trusting a task's reported green:

- `.venv/bin/python -m pytest tests/test_weeklysite.py tests/test_modulesite.py tests/test_abstraction_guard.py -q`
- `.venv/bin/python -m pytest -q` — must exceed the measured 837 passed with **0 skipped**
- `.venv/bin/lint-imports` — 2 contracts KEPT, 0 broken
- `.venv/bin/mypy src/newsletters` — no NEW error vs the measured 15-in-5-files baseline; 0 in `weeklysite.py`
- `git diff --exit-code -- content/module/ids.json content/rev1/ids.json content/work/ids.json` — ledgers append-only
- `git diff --stat -- src/newsletters/` — exactly two files (`weeklysite.py` new, `cli.py` +weekly command)
- `.venv/bin/python -m pytest tests/test_semantic_gate_frozen.py -q` — the gate is untouched
</verification>

<success_criteria>
- `content/weekly/` is a self-contained corpus: one spec, one `.eml`, one PNG, one template copy,
  its own `R-001` ledger, a committed `site/` and a committed `deck/` with its digest sidecar.
- `build_weekly_surfaces()` returns one Draft `Surface(REPORT)` at `EPOCH_ZERO` whose `missing[]`
  carries all three planted absences, each of which is `html.escape`d into the rendered honesty panel.
- Committed `site/` bytes == a fresh build; a `tmp_path` build leaves every committed ledger unchanged.
- `part_digest(committed deck) == committed .digest`, provable with stdlib only.
- `newsletters weekly` is the one command that regenerates the deck + digest; its `--out` is
  caller-supplied.
- `lint-imports` KEPT, no new mypy error, `semantic.py` untouched, no `src/newsletters/` file other
  than `weeklysite.py` and `cli.py` modified.
</success_criteria>

<artifacts_produced>
| Artifact | Kind | Why it exists |
|----------|------|---------------|
| `content/weekly/weekly-2374-w41.yml` | corpus data | the authored synthetic weekly; carries two of the three planted absences |
| `content/weekly/inbox/berth-rota-thanks.eml` | corpus data | the resolvable recognition source; makes the absence row non-vacuous and the recipe's `.eml` step demonstrable |
| `content/weekly/assets/ring-berth-utilisation.png` | corpus data | the one PLACED asset (content-addressed) |
| `content/weekly/template.pptx` | corpus data | the operator-supplied template, byte-copied from the fixture (P-06) |
| `content/weekly/ids.json` | ledger | the weekly's own append-only ledger, `R-001` |
| `content/weekly/site/` | generated | the corpus's published HTML — the only thing `assemble_site` will copy |
| `content/weekly/deck/*.pptx` + `.digest` | generated binary + integrity sidecar | the shipped deck and its tier-1 tamper check |
| `src/newsletters/weeklysite.py` | source | the builder seam (surfaces · site · deck) |
| `src/newsletters/cli.py` (+`weekly`) | source | the one operator command that renders a deck |
| `tests/_corpus_scan.py` | test helper | the promoted synthetic-content scanner (one scanner, two corpora) |
| `tests/test_weeklysite.py` | test | the stdlib-only weekly corpus suite |
</artifacts_produced>

<output>
Create `.planning/phases/04-sample-corpus-recipe/04-01-SUMMARY.md` when done. Record: the
before/after `test_modulesite.py` counts across the scanner promotion, the exact `newsletters
weekly` invocation and its output, the three planted-absence strings as they came OUT of
`surface.missing` (verbatim), and the measured suite/lint/mypy numbers against the recorded
baselines (837/0 · 2 kept · 15 mypy).
</output>
