---
phase: 04-sample-corpus-recipe
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/newsletters/publish.py
  - src/newsletters/cli.py
  - src/newsletters/dogfood.py
  - src/newsletters/worksurface.py
  - src/newsletters/modulesite.py
  - content/rev1/site/index.html
  - content/rev1/site/library.html
  - content/work/site/library.html
  - content/module/site/library.html
  - tests/test_publish.py
  - tests/test_render.py
  - tests/test_weeklysite.py
  - tests/test_weekly_golden.py
  - .github/workflows/ci.yml
  - .github/workflows/deploy-pages.yml
autonomous: true
requirements: [WKLY-05]

must_haves:
  truths:
    - "`newsletters build --corpus weekly` and `newsletters check --corpus weekly` both work, and check is proven to BLOCK on a planted blocker rather than being vacuously green"
    - "The assembled published tree carries /weekly/library.html and the weekly report page, every href resolves, fonts and OFL licenses travel, and every page carries the generated marker"
    - "The Records strip on all four corpora names all three of its neighbours, and the four committed chrome pages are regenerated by their builders — never hand-edited"
    - "The committed deck's part_digest equals a FRESH render's part_digest under the [pptx] extra, and that test runs in a CI job that fails on any skip"
    - "The rendered deck reads back Draft-watermarked and marked, and rendering leaves the Surface model_dump()-identical — no gate advance anywhere"
    - "No sample, script, workflow or CI step publishes, advances the gate, pushes to main, or makes an external call"
  artifacts:
    - path: "tests/test_weekly_golden.py"
      provides: "the [pptx]-gated deck suite (tier 2 + the SC-3 Draft/watermark read-back)"
      contains: "requires_pptx"
    - path: "src/newsletters/publish.py"
      provides: "_CORPUS_LAYOUT with the fourth corpus"
      contains: "content/weekly/site"
    - path: ".github/workflows/deploy-pages.yml"
      provides: "gate 1 over four corpora"
      contains: "--corpus weekly"
  key_links:
    - from: "src/newsletters/cli.py"
      to: "newsletters.weeklysite"
      via: "check --corpus weekly imports the MODULE OBJECT, not the function (the blocking proof depends on it)"
      pattern: "from \\. import weeklysite"
    - from: "src/newsletters/publish.py"
      to: "content/weekly/site"
      via: "_CORPUS_LAYOUT row → assembled /weekly/"
      pattern: "content/weekly/site"
    - from: "tests/test_weekly_golden.py"
      to: "content/weekly/deck/*.pptx"
      via: "part_digest(fresh render) == part_digest(committed deck)"
      pattern: "part_digest"
    - from: ".github/workflows/ci.yml"
      to: "tests/test_weekly_golden.py"
      via: "the weekly job's module list, under the fail-on-skip guard"
      pattern: "test_weekly_golden"
---

<objective>
Wire the fourth corpus into every place that already knows about three, and prove the two things the
corpus alone cannot: that the deck really is the deck (tier 2, `[pptx]`-gated) and that the gate
really fires (a planted blocker, not a clean-input exit code).

Purpose: WKLY-05's second half plus ROADMAP SC-2 and SC-3. The technical risk in this phase is not
"can it be built" — it is *which integration point gets missed*, and the miss surfaces late (in CI
or in the deploy workflow) rather than here. 04-RESEARCH enumerated all sixteen sites; this plan
works them as a checklist.

Output: `publish._CORPUS_LAYOUT` + the CLI corpus selector + the CI/deploy gate lines + the Records
strip on four builders + four regenerated chrome pages + `tests/test_weekly_golden.py` + the
`weekly` CI job's module list.

Recorded decisions this plan implements (04-VALIDATION §Wave 0): W0-2 the deck is a corpus artifact,
not a served page — no download link (the `FanoutBlock` route is recorded as a costed one-task
reversal for round two, NOT built). W0-3 `check --corpus weekly` is wired AND proven to BLOCK; the
planted state lives in a monkeypatch and never survives into the commit. W0-7 all sixteen integration
sites covered; the four Records-strip HTML files regenerate; ledgers stay append-only.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-sample-corpus-recipe/04-PATTERNS.md
@.planning/phases/04-sample-corpus-recipe/04-RESEARCH.md
@.planning/phases/04-sample-corpus-recipe/04-01-SUMMARY.md
</context>

<integration_checklist>
The sixteen sites from 04-RESEARCH §Integration Point Inventory, with the line numbers verified
against the live tree during planning. Rows 1–2 landed in plan 04-01. Work the rest as a checklist
and tick each one in the summary; a missed row fails **loud but late** (`assemble_site` raises
`FileNotFoundError` before any write; the fonts tuple fails on a directory that does not exist).

| # | Site | File | Task |
|---|------|------|------|
| 3 | `_CORPUS_LAYOUT` | `src/newsletters/publish.py:35-39` | 1 |
| 4 | `CorpusName` enum + docstring | `src/newsletters/cli.py:16-33` | 1 |
| 5 | `_DEFAULT_OUT` | `src/newsletters/cli.py:38-42` | 1 |
| 6 | `build` branch | `src/newsletters/cli.py:81-95` | 1 |
| 7 | `check` branch (module-object import form) | `src/newsletters/cli.py:167-178` | 1 |
| 8 | Records strip — rev1 | `src/newsletters/dogfood.py:807-810` | 2 |
| 9 | Records strip — work | `src/newsletters/worksurface.py:440-443` | 2 |
| 10 | Records strip — module | `src/newsletters/modulesite.py:203-207` | 2 |
| 11 | Records strip — weekly | `src/newsletters/weeklysite.py` | done in 04-01 |
| 12 | Regenerate the 4 chrome pages | `content/{rev1/site/index.html,rev1/site/library.html,work/site/library.html,module/site/library.html}` | 2 |
| 13 | Records-strip shape test | `tests/test_render.py:850-854` | 2 |
| 14 | Published-tree tests | `tests/test_publish.py:37-49` and `:207` | 1 |
| 15 | CI merge-block + deploy gate 1 | `.github/workflows/ci.yml:118-122`, `.github/workflows/deploy-pages.yml:68-73` | 1 |
| 16 | CI test-module lists | `ci.yml` `site-integrity` step (`:143-146`), `weekly` step (`:220-226`) | 1 (site-integrity), 3 (weekly) |
</integration_checklist>

<tasks>

<task type="auto">
  <name>Task 1: Corpus registration — publish layout, CLI selector, the gate lines, and the blocking proof</name>
  <files>src/newsletters/publish.py, src/newsletters/cli.py, tests/test_publish.py, tests/test_weeklysite.py, .github/workflows/ci.yml, .github/workflows/deploy-pages.yml</files>

  <read_first>
    - src/newsletters/publish.py lines 25-70 (`_CORPUS_LAYOUT` and the fail-before-any-write guard)
    - src/newsletters/cli.py lines 16-42 and 81-95 and 160-195 (the enum, the defaults, and both branch sites)
    - tests/test_publish.py lines 30-55 and 195-212 (the assemble assertions and the fonts tuple)
    - tests/test_module_cli.py lines 60-95 (the clean-exit-zero caveat docstring AND the planted-blocker proof — mirror both)
    - .github/workflows/ci.yml lines 100-146 and .github/workflows/deploy-pages.yml lines 63-76
  </read_first>

  <action>
Register the fourth corpus everywhere three are already named. Each edit is small; the value is in
not missing one.

**`publish.py`** — append `("content/weekly/site", "weekly")` to `_CORPUS_LAYOUT` and extend the
comment above it so the tuple still explains itself with four rows. `assemble_site` raises
`FileNotFoundError` before any write if the directory is absent, which is why this edit could only
land after 04-01 committed `content/weekly/site/`.

**`cli.py`** — four edits, in the existing idiom:
  - `CorpusName` gains `weekly = "weekly"` plus a docstring bullet in the same voice as the other
    three, naming `weeklysite.py` as its builder and the `content/module` lane config as its
    bindings source.
  - `_DEFAULT_OUT` gains `CorpusName.weekly: "content/weekly/site"`.
  - the `build` command gains `elif corpus is CorpusName.weekly:` importing `build_weekly_site` and
    setting `index_name = "library.html"`; update both `--corpus` help strings so they name four
    corpora rather than three.
  - the `check` command gains `elif corpus is CorpusName.weekly:` that imports the **module object**
    (`from . import weeklysite`) and calls `weeklysite.build_weekly_surfaces()`. This form is
    LOAD-BEARING, not stylistic: `monkeypatch.setattr(weeklysite, "build_weekly_surfaces", ...)`
    only reaches the command if the command resolves the attribute off the module at call time.
    Importing the function directly would bind it at import time and make the blocking proof below
    silently untestable — a gate that cannot be proven to fire.

**`tests/test_publish.py`** — three edits:
  - rename `test_assemble_composes_three_corpora_and_chrome` to name four corpora (the old name goes
    stale in the same commit that lands the fourth) and add `(out / "weekly" / "library.html")` and
    the weekly report page to its assertions, deriving the report page name from
    `weeklysite.build_weekly_surfaces()[0].id` rather than hardcoding it.
  - the fonts tuple at `:207` gains `out / "weekly/fonts"`. This is the row research names as the
    likeliest silent miss: it fails on a `fonts_dir` that does not exist, with a message about OFL
    files rather than about a missing corpus.
  - leave the committed==fresh loop (`:141-193`) and the link-resolution loop structurally alone.
    The weekly's own committed==fresh lives in `tests/test_weeklysite.py` (the module precedent,
    stated in the comment at `test_publish.py:137-139`), and the link resolver picks the new corpus
    up for free — its `checked > 100` floor rises on its own.

**`.github/workflows/ci.yml`** — the `merge-block` job's gate step gains a fourth
`newsletters check --corpus weekly` line, and the `site-integrity` step's module list gains
`tests/test_weeklysite.py`. That module is stdlib-only by construction (04-01), so `[test,config]`
is enough for it — do NOT put any `[pptx]`-gated test there: `site-integrity` has no `0 skipped`
assertion, so such a test would skip forever, which is the W21 shape this repo has already paid for
twice.

**`.github/workflows/deploy-pages.yml`** — gate 1 gains the same fourth `newsletters check --corpus
weekly` line. Gate 2 is untouched. Nothing else in that workflow changes: it stays `main`-only, and
this phase pushes nothing to `main`.

**The CLI gate tests in `tests/test_weeklysite.py`** (mirror `tests/test_module_cli.py:62-95`):
  - `test_check_weekly_clean_exits_zero` — asserts exit 0 and the clean message, with a docstring
    that says plainly this is DRAFT-VACUOUS by design: `review_blockers` returns `[]` for any
    non-Published surface, and the sample is Draft by design, so the exit code proves the wiring and
    nothing about trust. Name the honesty-panel test (04-01) as this corpus's real trust proof.
  - `test_check_weekly_blocks_on_planted_blocker` — monkeypatch `weeklysite.build_weekly_surfaces`
    to return ONE blocked PUBLISHED surface, invoke `check --corpus weekly`, and assert nonzero exit
    plus a report naming the offending surface, `BLOCK`, and `merge blocked`. The blocker is a TEST
    fixture and exists only inside the monkeypatch's scope: **no committed corpus file is ever put
    into a blocked or Published state**, and `git status` after the run must be clean of any
    `content/` change.
  - `test_build_weekly_smoke` — `newsletters build --corpus weekly --out <tmp>` exits 0 and writes
    the report page and `library.html` into the chosen dir.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_publish.py tests/test_weeklysite.py -q && .venv/bin/newsletters check --corpus weekly && .venv/bin/newsletters assemble --out "$(mktemp -d)/site" >/dev/null && git status --porcelain -- content/ | grep -v '^$' | wc -l | xargs -I{} test {} -eq 0</automated>
  </verify>

  <acceptance_criteria>
    - `_CORPUS_LAYOUT` has four rows; `newsletters assemble --out <tmp>` succeeds and the tree
      carries `weekly/library.html`, the weekly report page and `weekly/fonts/` with three OFL files.
    - `check --corpus weekly` exits 0 on the clean corpus AND exits nonzero with a `BLOCK` report
      under the monkeypatched blocker — both tests present, the second one the reason the first is
      allowed to be vacuous.
    - The `check` branch uses `from . import weeklysite` (module object). Deleting the monkeypatch
      arm's `setattr` target would break the test — confirm by reading the branch, not by assuming.
    - `git status --porcelain -- content/` is EMPTY after the test run: the planted blocker left no
      trace in the committed corpus.
    - Both workflow files gate four corpora; `deploy-pages.yml`'s diff is exactly one added line and
      zero deletions; `ci.yml` gains the check line and `tests/test_weeklysite.py` in site-integrity,
      and the `bare-install` job is BYTE-UNCHANGED.
  </acceptance_criteria>
  <done>The fourth corpus is registered in publish, the CLI, both workflows and the published-tree tests, and `check --corpus weekly` is proven to BLOCK.</done>
</task>

<task type="auto">
  <name>Task 2: The Records strip gains its fourth record, and the four chrome pages regenerate</name>
  <files>src/newsletters/dogfood.py, src/newsletters/worksurface.py, src/newsletters/modulesite.py, content/rev1/site/index.html, content/rev1/site/library.html, content/work/site/library.html, content/module/site/library.html, tests/test_render.py</files>

  <read_first>
    - src/newsletters/dogfood.py lines 805-812 (`_REV1_RECORDS` — root corpus, no `../`)
    - src/newsletters/worksurface.py lines 436-448 and src/newsletters/modulesite.py lines 199-211 (the inline assembled-tree-relative `records=` tuples)
    - tests/test_render.py lines 838-860 (the strip shape test — chrome pages only, never a per-surface page)
  </read_first>

  <action>
Give every corpus a way into the weekly, then regenerate the pages that carry the strip — in the
SAME commit as the constant, or committed==fresh goes red.

**Three builder constants**, each in its own corpus's relative shape:
  - `dogfood._REV1_RECORDS` (the root corpus, no `../`) gains `("The weekly record",
    "weekly/library.html")`.
  - `worksurface`'s inline `records=` tuple gains `("The weekly record", "../weekly/library.html")`.
  - `modulesite`'s inline `records=` tuple gains `("The weekly record", "../weekly/library.html")`.
  `weeklysite`'s own tuple already names its three neighbours (04-01) and does not change here.
  `render.py` stays corpus-blind by design (01-CONTEXT d3) — the builder owns its corpus's position
  in the strip. Do NOT add corpus knowledge to the renderer.

**Regenerate the four chrome pages** by running the builders, never by hand-editing HTML:
`newsletters build --corpus rev1`, `--corpus work`, `--corpus module`. Exactly four committed HTML
files carry `<section class="nl-records">` — this is MEASURED (`grep -rl '<section
class="nl-records"' content/`), not estimated. A naive `grep -rl nl-records content/` returns 17
because `.nl-records` is styled in every page's inline CSS; do not size the regen from that number.
After the regen, `git status --porcelain -- content/` must name EXACTLY those four files. If any
other committed file moved, STOP and diagnose before committing — an unexplained byte change in a
committed corpus is a stop-the-line bug, not noise.

**`tests/test_render.py`** — the strip shape test gains a third assertion,
`'href="weekly/library.html"' in text`, alongside the existing work and module assertions. The
else-branch (a per-surface page must NOT carry the strip) is unchanged and must stay green: the
strip belongs at a record's front door, not inside a report.

Re-run `tests/test_publish.py` after the regen: its assembled-tree link resolver is the resolver of
record for the new strip hrefs, and it also enforces that no page contains `href="None"`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_render.py tests/test_publish.py tests/test_worksurface.py tests/test_modulesite.py tests/test_weeklysite.py -q && git diff --exit-code -- content/rev1/ids.json content/work/ids.json content/module/ids.json content/weekly/ids.json</automated>
  </verify>

  <acceptance_criteria>
    - Three builder constants each carry four records total; `weeklysite`'s tuple is unchanged.
    - `git status --porcelain -- content/` names EXACTLY the four chrome HTML files — no fifth file,
      no ledger.
    - `grep -rl '<section class="nl-records"' content/` still returns 4 paths, and each of those four
      pages contains an href to the weekly library.
    - `tests/test_render.py`'s strip test asserts all three neighbours on the rev1 chrome pages and
      still refuses the strip on a per-surface page.
    - `tests/test_publish.py` green: every assembled href resolves and no page contains `href="None"`.
    - All four `ids.json` ledgers byte-unchanged.
  </acceptance_criteria>
  <done>All four builders name four records, exactly four chrome pages regenerated by their builders, and the strip test asserts the weekly link.</done>
</task>

<task type="auto">
  <name>Task 3: The [pptx]-gated deck proof (tier 2 + Draft/watermark read-back) and the weekly CI job</name>
  <files>tests/test_weekly_golden.py, .github/workflows/ci.yml</files>

  <read_first>
    - tests/test_weeklyspec.py lines 1445-1460 (the `requires_pptx` skipif idiom — NOT a module-level `importorskip`)
    - tests/test_pptx_golden.py (the read-the-written-file-back assertion style, incl. the watermark and core-properties marker checks)
    - src/newsletters/pptx_writer.py lines 236-265, 576-615, 616-700
    - .github/workflows/ci.yml lines 179-231 (the weekly job, its extras and its fail-on-skip guard)
  </read_first>

  <action>
Prove the committed deck against a fresh render, prove SC-3 by reading the written file back, and
put both in the one CI job that fails when a test skips.

**`tests/test_weekly_golden.py`** — a NEW module, `[pptx]`-gated with the `requires_pptx`
`pytest.mark.skipif` idiom (bind `_pptx = None` in an `except ImportError`, then mark each test).
Do NOT use a module-level `pytest.importorskip`: this module must be importable everywhere so a
missing extra shows as a named skip in the one job that fails on skips, rather than as a silently
absent module. Tests:

  - `test_committed_deck_equals_fresh_render` — tier 2. Rebuild the load and the surface through
    `weeklysite.build_weekly_surfaces()` (the same entry point the corpus was built from), derive
    slots with `weekly_slots`, call `render_surface_pptx_bytes(surface, template=<corpus
    template>, slots=slots)`, and assert `part_digest(fresh) == part_digest(committed deck
    bytes)`. NEVER `read_bytes() == read_bytes()` on a zip: DEFLATE output is
    zlib-implementation-dependent (zlib vs zlib-ng), so a raw-byte assertion is green locally and
    red on a runner with identical part content. On failure, report `differing_parts(...)` in the
    assertion message so the red teaches instead of just failing.
  - `test_double_render_is_stable_across_a_real_time_boundary` — two renders of the same surface
    separated by `time.sleep(3)`, asserting equal `part_digest`. This re-proves, on the corpus's own
    inputs, the property Phase 1 recorded and Phase 2 measured.
  - `test_rendered_deck_reads_back_draft_watermarked_and_marked` — SC-3. Render to `tmp_path`,
    REOPEN the written file with python-pptx, and assert from the reopened file: the Draft watermark
    shape is present, and the generated-by marker is in the OPC core properties. Assert against the
    file, never against the writer's return value.
  - `test_render_leaves_the_surface_draft` — capture `surface.model_dump()` before the render and
    assert it is IDENTICAL after, and that `surface.review` is still Draft. The inverted form is the
    point: a writer with a gate write-path would pass a one-directional check.
  - `test_no_gate_transition_in_the_weekly_build_path` — a source-level assertion that
    `src/newsletters/weeklysite.py` contains no call to `approve(`, `open_pull_request(` or
    `publish(` (the same shape as the Phase 3 grep guard), with a message naming the first hard rule.

**`.github/workflows/ci.yml`** — add `tests/test_weekly_golden.py` to the `weekly` job's module
list. That job already installs `.[test,config,excel,pptx]`, sets `fetch-depth: 0` (three guards
resolve `git merge-base HEAD origin/main` and FAIL rather than skip without it) and fails on any
reported skip. Extend the step's comment to say why this module is here and not in `site-integrity`.
Do NOT add the module to `site-integrity` and do NOT touch `bare-install`.

Before claiming green, run the `weekly` job's EXACT command locally and record the passed/skipped
counts in the summary. A test suite and the job that runs it are two different artifacts, and only
the second one is evidence.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest tests/test_weekly_golden.py -q && .venv/bin/python -m pytest tests/test_weeklyspec.py tests/test_weekly_blocks.py tests/test_weekly_values.py tests/test_semantic_gate_frozen.py tests/test_casespec.py tests/test_compose.py tests/test_swimlane.py tests/test_abstraction_guard.py tests/test_excel_adapter.py tests/test_weekly_golden.py -q</automated>
  </verify>

  <acceptance_criteria>
    - `tests/test_weekly_golden.py` uses the `requires_pptx` skipif idiom, never a module-level
      `importorskip`, and contains no assertion comparing the deck to the TEMPLATE.
    - Tier 2 passes: `part_digest(fresh) == part_digest(committed)`; the failure message would name
      `differing_parts`.
    - The 3-second double render gives equal digests on the corpus's own inputs.
    - The reopened written deck carries the Draft watermark and the core-properties marker; the
      Surface's `model_dump()` is identical before and after the render.
    - The `weekly` job's exact command runs locally with **0 skipped** (record the numbers).
    - `git diff` on `.github/workflows/` shows ZERO deleted lines and no change to `bare-install`.
  </acceptance_criteria>
  <done>Tier 2 passes, the deck reads back Draft-watermarked with the Surface untouched, and the weekly CI job runs the new module with 0 skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed corpus → assembled published tree | `assemble_site` byte-copies what a human merged; whatever crosses here is what the world sees |
| CI / deploy workflow → `gh-pages` | the one publish channel; `main`-only, human-gated by PR |
| test process → committed corpus | a test that mutates committed state turns a proof into a liability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | the deck reaching the published tree | mitigate | the deck lives under `content/weekly/deck/` and `assemble_site` copies `content/*/site` ONLY, so "nothing publishes" holds structurally, not by discipline; asserted by `test_deck_is_not_in_the_published_tree` (04-01) |
| T-04-09 | Elevation of Privilege | `check --corpus weekly` being a gate that cannot fire | mitigate | the branch imports the MODULE OBJECT so a monkeypatched Published surface reaches it; the blocking direction is asserted (nonzero exit + `BLOCK` + `merge blocked`), and the clean-exit test's docstring states it is Draft-vacuous |
| T-04-10 | Tampering | a blocked/Published state leaking into the committed corpus | mitigate | the blocker exists only inside `monkeypatch` scope; `git status --porcelain -- content/` is asserted empty after the run |
| T-04-11 | Repudiation | a CI green that means "not run" (W21, third occurrence) | mitigate | the `[pptx]`-gated module goes ONLY in the `weekly` job, whose step greps `[0-9]+ skipped` and fails; `site-integrity` receives only the stdlib-only module |
| T-04-12 | Tampering | the publish/deploy channel | mitigate | `deploy-pages.yml` gains exactly one `check --corpus weekly` line and nothing else; it stays `if: github.ref == 'refs/heads/main'`; this phase pushes nothing to `main`; proven by a zero-deletion workflow diff |
| T-04-13 | Information Disclosure | a dead or wrong cross-corpus link in the published tree | mitigate | `test_assembled_internal_links_resolve` is the resolver of record for the new strip hrefs and also forbids `href="None"`; the four chrome pages are regenerated by their builders, never hand-edited |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | no package install in this plan (04-RESEARCH §Package Legitimacy Audit: not applicable). A proposed dependency is a scope escape — stop and re-run the legitimacy gate. |
</threat_model>

<verification>
Re-run independently, once each:

- `.venv/bin/python -m pytest -q` — > 837 passed, **0 skipped**
- `.venv/bin/newsletters check --corpus rev1|work|module|weekly` — four cleans
- `.venv/bin/newsletters assemble --out <fresh tmp dir>` then `.venv/bin/python -m pytest tests/test_publish.py -q`
- the `weekly` CI job's exact command, locally, with 0 skipped
- `.venv/bin/lint-imports` — 2 kept
- `git diff --exit-code -- content/*/ids.json`
- `git diff <milestone base> -- .github/workflows/ci.yml | grep -c '^-[^-]'` → 0 deletions; `bare-install` byte-unchanged
</verification>

<success_criteria>
- All sixteen integration sites are ticked; `newsletters assemble` produces a four-corpus tree in
  which every href resolves, every page carries the generated marker, and every corpus's fonts and
  OFL licenses are present.
- `check --corpus weekly` is wired AND proven to block; nothing about that proof survives in the
  committed corpus.
- The committed deck equals a fresh render under `part_digest`, and the deck reads back
  Draft-watermarked with the Surface untouched.
- Both workflows gate four corpora; `bare-install` is byte-untouched; the workflow diffs contain
  zero deleted lines.
</success_criteria>

<artifacts_produced>
| Artifact | Kind | Why it exists |
|----------|------|---------------|
| `tests/test_weekly_golden.py` | test (`[pptx]`) | tier-2 committed==fresh for the deck, plus the SC-3 Draft/watermark read-back |
| `_CORPUS_LAYOUT` fourth row | source | the weekly reaches the published tree |
| `CorpusName.weekly` + branches | source | `build`/`check --corpus weekly`, the selector routing the builder and never forking the gate |
| four regenerated chrome pages | generated | the Records strip names four records; regenerated by builders, never hand-edited |
| `ci.yml` / `deploy-pages.yml` gate lines | CI config | one definition of "publishable", now over four corpora |
</artifacts_produced>

<output>
Create `.planning/phases/04-sample-corpus-recipe/04-02-SUMMARY.md` when done. Record: the
integration checklist with every row ticked and its diff evidence, the exact `weekly` job command
output (passed / skipped), the workflow diff line counts (insertions and the zero deletions), and
`git status --porcelain -- content/` after the blocking-proof test run.
</output>
