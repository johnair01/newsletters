---
phase: 4
slug: sample-corpus-recipe
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-08-29
updated: 2026-08-29
plans: 3
waves: 3
---

# Phase 4 — Validation Strategy

> Per-task map filled by the planner from 04-RESEARCH.md's 17-row requirements→test map.
> Plans: `04-01-PLAN.md` (wave 1) · `04-02-PLAN.md` (wave 2) · `04-03-PLAN.md` (wave 3).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (.venv; baseline 837 passed / 0 skipped) |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `.venv/bin/python -m pytest tests/test_weeklysite.py tests/test_publish.py -x -q` |
| **Full suite command** | `.venv/bin/python -m pytest -q` |
| **Measured baselines (no-NEW-failures reference, this branch, 2026-08-29)** | 837/0 · lint-imports 2 kept · mypy 15 errors in 5 files · black 69 would-reformat · isort DEF-15 set |

**Two test modules, split by extra requirement** — the W21 lesson, third occurrence:

| Module | Extras it needs | CI job that runs it | Why the split |
|--------|-----------------|---------------------|---------------|
| `tests/test_weeklysite.py` | none beyond `[test,config]` (stdlib `part_digest`) | `site-integrity` | that job has **no `0 skipped` assertion**, so nothing extra-gated may live here |
| `tests/test_weekly_golden.py` | `[pptx]` via the `requires_pptx` skipif idiom | `weekly` | that job greps `[0-9]+ skipped` and fails — the only place an extra-gated deck test is evidence |

---

## Sampling Rate

- **After every task commit:** the task's own `<automated>` command
- **After every plan wave:** full suite + `lint-imports`
- **Before verification:** the FULL carried gate set — pytest · lint-imports · `newsletters check`
  over ALL FOUR corpora · committed==fresh for all four (incl. the committed deck via the
  stdlib digest tier + the `[pptx]` fresh==committed tier) · bare-install untouched ·
  mypy/black/isort no-NEW-failures vs the measured baselines above
- Run each gate once per check

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 04-01-T1 | 04-01 | 1 | WKLY-05 | T-04-01, T-04-02, T-04-03 | committed corpus is synthetic (`@example.invalid` allowance documented, planted-leak self-check non-vacuous); asset content address computed from the committed bytes; no new YAML parse path | unit | `.venv/bin/python -m pytest tests/test_weeklysite.py tests/test_modulesite.py tests/test_abstraction_guard.py -q` | ❌ created by this task | ⬜ pending |
| 04-01-T2 | 04-01 | 1 | WKLY-05 | T-04-05, T-04-06 | the builder never advances the gate; root containment raises on escape; ledger written only at the committed path | integration | `.venv/bin/python -m pytest tests/test_weeklysite.py -q && git diff --exit-code -- content/module/ids.json content/rev1/ids.json content/work/ids.json && .venv/bin/lint-imports` | ❌ created by this task | ⬜ pending |
| 04-01-T3 | 04-01 | 1 | WKLY-05 | T-04-04, T-04-05, T-04-07 | tier-1 `part_digest` tamper check (stdlib, every install); `--out` caller-supplied, never derived from content; `[pptx]` stays lazy | unit | `.venv/bin/python -m pytest tests/test_weeklysite.py -q && .venv/bin/lint-imports && .venv/bin/python -c "import newsletters.weeklysite, newsletters.cli; import sys; assert 'pptx' not in sys.modules"` | ❌ created by this task | ⬜ pending |
| 04-02-T1 | 04-02 | 2 | WKLY-05 | T-04-09, T-04-10, T-04-12 | `check --corpus weekly` proven to BLOCK via a monkeypatched Published surface; the planted state never reaches `content/`; deploy gate stays main-only | unit + integration | `.venv/bin/python -m pytest tests/test_publish.py tests/test_weeklysite.py -q && .venv/bin/newsletters check --corpus weekly && .venv/bin/newsletters assemble --out "$(mktemp -d)/site"` | ✅ extend (`test_publish.py`) / ❌ new arms | ⬜ pending |
| 04-02-T2 | 04-02 | 2 | WKLY-05 | T-04-13 | no dead cross-corpus link; no `href="None"`; chrome regenerated by builders, never hand-edited | unit + integration | `.venv/bin/python -m pytest tests/test_render.py tests/test_publish.py tests/test_worksurface.py tests/test_modulesite.py tests/test_weeklysite.py -q && git diff --exit-code -- content/*/ids.json` | ✅ extend (`test_render.py`) | ⬜ pending |
| 04-02-T3 | 04-02 | 2 | WKLY-05 | T-04-08, T-04-11 | tier-2 fresh==committed by `part_digest` (never raw zip bytes); deck reads back Draft-watermarked + marked; Surface `model_dump()` identical; no gate call in the build path | integration (`[pptx]`) | `.venv/bin/python -m pytest tests/test_weekly_golden.py -q` then the `weekly` job's exact module list with 0 skipped | ❌ created by this task | ⬜ pending |
| 04-03-T1 | 04-03 | 3 | WKLY-06 | T-04-14, T-04-15 | the recipe's read-only / stays-local / no-auto-publish anchors are asserted present; every fenced command is validated against the live Typer app (with a non-vacuity floor) | unit | `.venv/bin/python -m pytest tests/test_weeklysite.py -q -k "recipe"` | ❌ created by this task | ⬜ pending |
| 04-03-T2 | 04-03 | 3 | WKLY-06, WKLY-05 | T-04-18 | no document understates what publishes; a guard test fails if any of the four regresses to "three corpora" | unit | `.venv/bin/python -m pytest tests/test_weeklysite.py -q -k "docs or recipe" && .venv/bin/python -m pytest tests/test_collaboration_contract.py -q` | ❌ created by this task | ⬜ pending |
| 04-03-T3 | 04-03 | 3 | WKLY-05, WKLY-06 | T-04-16, T-04-17 | every gate re-run independently and recorded as a measured number; the recipe run leaves `git status` clean | integration (phase sweep) | `.venv/bin/python -m pytest -q && .venv/bin/lint-imports && .venv/bin/newsletters check --corpus rev1 && … --corpus work && … --corpus module && … --corpus weekly && git diff --exit-code -- content/*/ids.json` | ✅ existing gates | ⬜ pending |

### Research 17-row map → task coverage (all rows claimed)

| Research row | Task |
|--------------|------|
| composes to Draft `Surface(REPORT)` at `EPOCH_ZERO`, traced + addressed | 04-01-T2 |
| honesty path — 3 planted absences in `missing[]` AND `html.escape`d in the panel | 04-01-T2 (loader half in 04-01-T1) |
| committed `site/` == fresh, ledger unchanged | 04-01-T2 |
| byte-stable double render (HTML) | 04-01-T2 |
| `R-001` stable across rebuild | 04-01-T2 |
| committed content is synthetic (denylist + planted leak + `.eml` addresses) | 04-01-T1 |
| zero external calls (self-hosted fonts) | 04-01-T2 |
| deck tier 1 — committed digest == sidecar (stdlib) | 04-01-T3 |
| deck tier 2 — fresh render digest == committed digest (`[pptx]`) | 04-02-T3 |
| deck reads back Draft-watermarked + marked; render leaves the Surface Draft | 04-02-T3 |
| `content/weekly/template.pptx` == the fixture template (P-06) | 04-01-T1 |
| assembled tree carries `/weekly/`; links resolve; fonts + OFL; marker | 04-02-T1 |
| Records strip on rev1 chrome names the weekly record | 04-02-T2 |
| `check --corpus weekly` exits 0 clean AND blocks on a planted blocker | 04-02-T1 |
| every `newsletters …` line in `docs/weekly.md` names a live command/option | 04-03-T1 |
| `docs/weekly.md` carries the load-bearing anchors | 04-03-T1 |
| *(added by the planner)* the four documents describe four corpora | 04-03-T2 |

---

## Wave 0 Requirements

**Recorded discretionary decisions (implement, don't reopen) — and where each is implemented:**

| # | Recorded decision | Implemented in |
|---|-------------------|----------------|
| W0-1 | Sibling corpus `content/weekly/` with new `weeklysite.py`; bindings reuse the committed `content/module/*.yml` (the "lineage"; supplies the KPI-less lane). | 04-01-T1, 04-01-T2 |
| W0-2 | Deck at `content/weekly/deck/` + `.digest` sidecar, NOT served by `assemble_site` (structural no-publish). No download link — the costed `FanoutBlock` reversal is RECORDED for round two, not built. | 04-01-T3 (artifact + structural test), 04-03-T2 (recorded in `docs/architecture.md` §9) |
| W0-3 | `check --corpus weekly` wired AND proven to BLOCK on a planted blocker (never vacuously green). | 04-02-T1 |
| W0-4 | Phase 3 `src/` untouched (`build_weekly_report`'s `traces` behaviour measured harmless; recorded, not patched). | 04-01-T2 (docstring + `git diff --stat -- src/newsletters/`), 04-03-T3 (compass) |
| W0-5 | `.xlsx` values stay OUT of the corpus build path; the values proof lives in the `weekly` CI job's existing tests only. | 04-01-T2 (no `[excel]` on the build path), 04-03-T1 (the recipe describes the workbook route) |
| W0-6 | The synthetic `.eml`'s addresses must not trip the `_EMAIL_RE` confidentiality scanner — RFC 6761 `@example.invalid` plus a documented allowance. | 04-01-T1 |
| W0-7 | All 16 integration sites covered; the four Records-strip HTML files regenerate; ledgers stay append-only. | 04-02-T1, 04-02-T2 (checklist ticked in `04-02-PLAN.md` §integration_checklist) |

**Wave 0 test-file gaps (each created by its owning task, so no task ships without an automated verify):**
- [ ] `tests/_corpus_scan.py` — the promoted synthetic-content scanner (04-01-T1)
- [ ] `tests/test_weeklysite.py` — the stdlib-only weekly corpus suite (04-01-T1 creates it; T2/T3, 04-02-T1, 04-03-T1/T2 extend)
- [ ] `tests/test_weekly_golden.py` — the `[pptx]`-gated deck suite, `requires_pptx` skipif (never a module-level `importorskip`) (04-02-T3)
- [ ] No framework install needed; no `conftest.py` change expected

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Deck opens in real PowerPoint | WKLY-05 | carried A8; no `.pptx` consumer in this environment | EiC at PR review (deck committed in-repo at `content/weekly/deck/`) |
| Recipe passes the not-the-author bar | WKLY-06 | prose judgement | EiC reads `docs/weekly.md` cold at PR review; the commands were machine-executed in-phase (04-03-T3) as the SC-4 proof |
| First CI green of the `pptx` / `weekly` jobs | WKLY-05 | no `gh` CLI in this environment | PR review confirmation; the exact job commands are proved locally (carried from Phases 2 and 3 — an unobserved job is not evidence) |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (all 9 tasks carry one)
- [x] No watch-mode flags
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner, 2026-08-29
