---
phase: 05-excel-adapter
plan: 04
type: tdd
wave: 3
depends_on: ["05-03"]
files_modified:
  - tests/fixtures/xlsx/_author_fixtures.py
  - tests/fixtures/xlsx/*.xlsx
  - tests/test_excel_golden.py
autonomous: true
requirements: [ADAPT-03, ADAPT-06]
user_setup: []
must_haves:
  truths:
    - "A byte-reproducible .xlsx fixture corpus is committed, authored programmatically with openpyxl (mirror the eml/_author_fixtures.py pattern)"
    - "The corpus covers: formula WITH cache, formula WITHOUT cache (the natural openpyxl case -> unextracted), merged cells, multiple sheets, mixed types (date/number/bool/text), empty cells, an error cell, and a chart/image (silent-loss path)"
    - "For every fixture the zero-silent-drops accounting identity holds: #claims + #unextracted == #units walked (a silent drop is a test FAILURE)"
    - "Every claim is verbatim + content-addressed (claim.text == trace.span == transcript[start:end]; trace.is_addressed); coverage.complete == (unextracted empty); assert_conforms passes; parsing twice is deterministic"
    - "Round-trip coverage parity holds for each fixture (model_dump_json -> reload -> fresh ExcelAdapter distill() == original coverage)"
  artifacts:
    - path: "tests/fixtures/xlsx/_author_fixtures.py"
      provides: "byte-reproducible programmatic .xlsx fixture generator"
      contains: "load_workbook"
    - path: "tests/test_excel_golden.py"
      provides: "golden corpus test asserting the full ADAPT-03/06 invariant matrix"
      contains: "zero_silent_drops"
  key_links:
    - from: "tests/test_excel_golden.py"
      to: "ExcelAdapter"
      via: "drives the registered adapter end-to-end over each committed .xlsx"
      pattern: "ExcelAdapter"
    - from: "tests/test_excel_golden.py"
      to: "assert_conforms"
      via: "span-containment + JSON round-trip per fixture"
      pattern: "assert_conforms"
---

<objective>
Prove the Excel adapter with a committed, byte-reproducible golden corpus (ADAPT-06; ROADMAP Phase 5
success criterion 3). Author `.xlsx` fixtures programmatically with openpyxl (mirror
`tests/fixtures/eml/_author_fixtures.py`), covering the full faithfulness matrix from CONTEXT
decision 6 — formula WITH cache, formula WITHOUT cache (the natural openpyxl no-cache case routed to
`unextracted[]`), merged cells, multiple sheets, mixed types (date/number/bool/text), empty cells, an
error cell, and a chart/image (the silent-loss path). For every fixture assert the load-bearing
invariants: the zero-silent-drops accounting identity, verbatim + content-addressed claims, coverage
honesty, conformance + JSON round-trip, determinism, AND the round-trip coverage parity.

Purpose: ADAPT-06 makes a silent drop a TEST FAILURE by construction — the executable contract that
the Excel adapter's extract-vs-disclose fork holds across the whole matrix, including the
formula-cache crux and the chart/image disclosure.

Output: the fixture generator, the committed `.xlsx` corpus, and `tests/test_excel_golden.py`.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-excel-adapter/05-CONTEXT.md
@.planning/phases/05-excel-adapter/05-RESEARCH.md

# the golden-corpus pattern to mirror + the adapter under test:
@tests/test_email_golden.py
@tests/fixtures/eml/_author_fixtures.py
@src/newsletters/adapters/excel_adapter.py
@src/newsletters/distill/conformance.py
# Plan 03 added: ExcelAdapter (registered "excel"); 05-03-SUMMARY records the separator + value table.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the byte-reproducible .xlsx golden corpus</name>
  <files>tests/fixtures/xlsx/_author_fixtures.py, tests/fixtures/xlsx/*.xlsx</files>
  <behavior>
    - Running the generator with the installed openpyxl writes the committed .xlsx files; re-running reproduces them byte-for-byte (deterministic — no now()/random; set workbook properties.created/modified to a fixed datetime so the OOXML core.xml does not embed a wall-clock that breaks byte-reproducibility).
    - The corpus covers each required case as a distinct fixture: formula_with_cache, formula_no_cache (formula written WITHOUT a cached value — the natural openpyxl output), merged_cells, multi_sheet, mixed_types (date/number/bool/text), empty_cells, error_cell, and chart_or_image (the silent-loss disclosure path).
    - The formula_no_cache fixture is the faithfulness crux: an openpyxl-authored formula cell carries NO cache, so data_only=True yields None -> the adapter must route it to unextracted[], never emit 0/empty.
  </behavior>
  <action>
    Create `tests/fixtures/xlsx/_author_fixtures.py` mirroring `tests/fixtures/eml/_author_fixtures.py`:
    a `main()` that builds each workbook with `openpyxl` (via the adapter's `_load_openpyxl()` or a
    direct import in this test-only generator) and writes it to `tests/fixtures/xlsx/<name>.xlsx`.
    Each fixture is tiny and uses EXPLICIT values/types so the adapter's fork is exercised, not
    incidental. For byte-reproducibility, set `wb.properties.created` / `wb.properties.modified` to a
    FIXED `datetime` (otherwise openpyxl stamps the current time into `docProps/core.xml` and the
    bytes drift run-to-run). Document each fixture inline with its EXPECTED routing (claims vs
    unextracted), the way the eml generator does.

    Fixtures (at least these, one concern each):
    - formula_with_cache.xlsx — a formula whose cached value IS present (author it so data_only=True
      returns the cache; if openpyxl cannot write a cache directly, document the technique used).
    - formula_no_cache.xlsx — a formula cell with NO cache (the natural openpyxl output) -> the
      uncomputed-formula -> unextracted[] crux.
    - merged_cells.xlsx — a merged range (e.g. A1:B2) with a top-left anchor value.
    - multi_sheet.xlsx — two sheets, values on each, to pin workbook-order traversal.
    - mixed_types.xlsx — date, number (int + float), bool, text cells (value->string rule coverage).
    - empty_cells.xlsx — sheet with sparse cells + genuinely blank cells (blank != formula-no-cache).
    - error_cell.xlsx — a cell whose data_type is 'e' (an error like #REF!/#DIV/0!) -> unextracted[].
    - chart_or_image.xlsx — a sheet carrying a chart (and/or image) -> the silent-loss disclosure,
      using the R4-confirmed attribute (per 05-03-SUMMARY). Extract NO chart content (out of scope).

    Run the generator and commit the .xlsx outputs. The generator script is the documented, rerunnable
    provenance of the committed corpus.
  </action>
  <verify>
    <automated>.venv/bin/python tests/fixtures/xlsx/_author_fixtures.py && ls tests/fixtures/xlsx/*.xlsx | wc -l</automated>
    <automated>.venv/bin/python -c "import hashlib, glob, subprocess; pre={p:hashlib.sha256(open(p,'rb').read()).hexdigest() for p in glob.glob('tests/fixtures/xlsx/*.xlsx')}; subprocess.run(['.venv/bin/python','tests/fixtures/xlsx/_author_fixtures.py'],check=True); post={p:hashlib.sha256(open(p,'rb').read()).hexdigest() for p in glob.glob('tests/fixtures/xlsx/*.xlsx')}; assert pre==post, 'fixtures are NOT byte-reproducible'; print('byte-reproducible:', len(post))"</automated>
  </verify>
  <done>
    The corpus is committed; every required case has a distinct fixture; re-running the generator
    reproduces identical bytes (no wall-clock drift).
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Golden test — zero-silent-drops, verbatim, conformance, determinism, round-trip parity</name>
  <files>tests/test_excel_golden.py</files>
  <behavior>
    - The corpus on disk equals the expected fixture set (a missing/extra .xlsx is a failure).
    - Zero silent drops (the headline, ADAPT-06): for each fixture, #claims + #unextracted == #units walked, exactly, with the pinned ordered unextracted reasons matching the per-fixture contract derived by driving the LIVE adapter.
    - Faithful + content-addressed: claim.text == trace.span; transcript[trace.start:trace.end] == claim.text; trace.is_addressed for every claim.
    - Coverage honesty: coverage.complete == (len(coverage.unextracted) == 0) for each fixture.
    - Conformance + JSON round-trip: assert_conforms(ExcelAdapter(), [source]) passes per fixture; DistillationResult round-trips losslessly.
    - Determinism: parsing the same fixture twice yields an EQUAL DistillationResult.
    - Round-trip coverage parity: parse on adapter A -> serialize+reload the Source -> distill on a FRESH adapter B -> coverage equals A's original (the Plan-01 hardening, proven on Excel here too).
    - Targeted routing: formula_no_cache routes its formula cell to unextracted[] (NO claim with text "0" or ""); error_cell routes to unextracted[]; chart_or_image emits a chart/image disclosure; merged_cells emits the anchor exactly once (no phantom blanks).
  </behavior>
  <action>
    Create `tests/test_excel_golden.py` mirroring `tests/test_email_golden.py`'s structure: an
    `Expected`/`EXPECTED` table pinning, per fixture, `n_claims` and the EXACT ordered
    `unextracted[].reason` strings (derive these by driving the LIVE ExcelAdapter — do NOT assume
    them), a `_distill(name)` helper that parses with a FRESH adapter and distills, and the
    parametrized invariant tests: corpus-membership, zero-silent-drops accounting identity, verbatim +
    content-addressed, coverage honesty, conformance + JSON round-trip, determinism. Add the
    round-trip coverage parity test (serialize the Source, reload, distill on a fresh adapter, assert
    equal coverage). Add targeted per-fixture routing assertions: formula_no_cache must NOT produce a
    claim whose text is `"0"` or `""` and MUST have its formula cell in `unextracted[]`; error_cell in
    `unextracted[]`; the chart/image disclosure present; the merged anchor emitted exactly once.

    Write the EXPECTED table + tests, run against the LIVE adapter, and if the live counts/reasons
    reveal an adapter bug, FIX the adapter (Plan 03's file) rather than loosening the assertion —
    ADAPT-06's value is that the golden corpus catches real drops. Skip cleanly if `[excel]` is
    absent (bare-install gate unaffected).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_excel_golden.py -q</automated>
    <automated>.venv/bin/python -m pytest tests/test_excel_golden.py -k "formula_no_cache or silent_drops or merged or roundtrip" -q</automated>
    <automated>.venv/bin/python -m pytest -q</automated>
    <automated>.venv/bin/mypy src/newsletters/adapters src/newsletters/distill</automated>
    <automated>.venv/bin/lint-imports</automated>
  </verify>
  <done>
    Every golden invariant passes for every fixture; formula_no_cache routes to `unextracted[]` (never
    `0`/empty); chart/image disclosed; merged anchor emitted once; round-trip coverage parity holds;
    full suite + mypy + lint-imports green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixture corpus -> adapter under test | The golden corpus is the executable contract; a silent drop in the adapter must surface as a fixture-test failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information disclosure (silent loss escaping tests) | Excel adapter extract-vs-disclose fork | mitigate | Zero-silent-drops accounting identity asserted per fixture; a drop is a test failure by construction (ADAPT-06) |
| T-05-11 | Tampering (non-reproducible fixtures) | .xlsx OOXML core.xml wall-clock | mitigate | Fixed wb.properties.created/modified so the corpus is byte-reproducible run-to-run |
| T-05-12 | Repudiation (coverage lost on persistence) | round-trip on a reloaded Source | mitigate | Round-trip coverage parity asserted on each Excel fixture (the Plan-01 hardening proven on Excel) |
| T-05-SC | Tampering | openpyxl (test-only fixture authoring) | accept | openpyxl PRE-APPROVED (CONTEXT decision 1 + RESEARCH audit); no new dependency; no checkpoint |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_excel_golden.py -q` — green for the whole corpus.
- `.venv/bin/python -m pytest -q` — full suite green.
- `.venv/bin/mypy src/newsletters/adapters src/newsletters/distill` — clean.
- `.venv/bin/lint-imports` — `Contracts: 1 kept, 0 broken`.
- Re-running the fixture generator reproduces identical bytes (byte-reproducibility check passes).
</verification>

<success_criteria>
- ROADMAP Phase 5 criterion 3: a golden-file test covers the Excel adapter against fixtures
  containing formulas and merged cells, asserting zero silent drops.
- ADAPT-06: byte-reproducible programmatic fixtures + the accounting identity + verbatim +
  content-addressed + conformance + determinism + round-trip coverage parity, all green.
- The formula-cache crux (formula_no_cache -> unextracted[], never `0`/empty) is pinned by a fixture.
- No human checkpoints; openpyxl is the only dependency in play.
</success_criteria>

<output>
Create `.planning/phases/05-excel-adapter/05-04-SUMMARY.md` when done — record the final fixture set,
the per-fixture expected counts/reasons, and confirm zero-silent-drops + round-trip parity hold across
the corpus. Mark Phase 5 ready for `/gsd-verify-work`.
</output>
