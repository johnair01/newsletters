---
phase: 05-excel-adapter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/newsletters/adapters/_openpyxl_loader.py
  - tests/test_ai_optional.py
  - tests/test_openpyxl_probe.py
autonomous: true
requirements: [ADAPT-03]
user_setup: []
must_haves:
  truths:
    - "pyproject declares excel = [\"openpyxl\"] under [project.optional-dependencies]"
    - "openpyxl is imported LAZILY (inside a helper, never at module top), so importing the adapters package does not require the extra"
    - "Using the Excel adapter WITHOUT [excel] installed raises a clear teaching ImportError pointing at pip install '.[excel]'"
    - "The bare-install AI-isolation gate stays green WITHOUT [excel] installed; lint-imports stays 1 kept / 0 broken (openpyxl is not AI, but the contract + lazy discipline must hold)"
    - "The R4 Wave-0 probe confirms the openpyxl chart/image attribute names against the installed version, recorded as a committed test"
  artifacts:
    - path: "pyproject.toml"
      provides: "excel = [\"openpyxl\"] optional dependency"
      contains: "excel = [\"openpyxl\"]"
    - path: "src/newsletters/adapters/_openpyxl_loader.py"
      provides: "lazy _load_openpyxl() with a teaching ImportError"
      exports: ["_load_openpyxl"]
    - path: "tests/test_openpyxl_probe.py"
      provides: "R4 probe pinning the chart/image attribute names (ws._charts / ws._images or the documented fallback)"
      contains: "_charts"
    - path: "tests/test_ai_optional.py"
      provides: "extended bare-install isolation: adapters package imports without openpyxl; ExcelAdapter use without it raises the teaching error"
      contains: "openpyxl"
  key_links:
    - from: "src/newsletters/adapters/_openpyxl_loader.py"
      to: "openpyxl"
      via: "lazy import inside _load_openpyxl(), not module top"
      pattern: "import openpyxl"
---

<objective>
Stand up the openpyxl optional-dependency boundary (CONTEXT decision 1, R3) and run the R4 Wave-0
probe — BEFORE the Excel adapter is written — so the adapter (Plan 03) consumes a verified lazy
loader and verified chart/image attribute names. Add `excel = ["openpyxl"]` as an optional extra,
provide the lazy `_load_openpyxl()` helper with a teaching `ImportError`, extend the AI-isolation
suite to prove the adapters package still imports (and the spine still runs) WITHOUT the extra, and
commit the R4 probe pinning `ws._charts` / `ws._images` (assumption A1) against the installed
openpyxl.

Purpose: keep the AI-optional / minimal-core invariant intact — bare `pip install .` runs the spine
with zero AI and zero openpyxl; the adapter is selectable only when the operator installs `[excel]`.
openpyxl is NOT AI, so the forbid-ai contract is unaffected, but the lazy discipline (mirror the
`[ai]` pattern) is what keeps the bare-install/import-isolation gate green.

Output: the `[excel]` extra, the lazy loader module, extended `test_ai_optional.py`, and the
committed `tests/test_openpyxl_probe.py` whose result Plan 03 relies on.
</objective>

<execution_context>
@/home/user/newsletters/.claude/gsd-core/workflows/execute-plan.md
@/home/user/newsletters/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-excel-adapter/05-CONTEXT.md
@.planning/phases/05-excel-adapter/05-RESEARCH.md

# the lazy-import discipline to mirror + the gate to keep green:
@pyproject.toml
@tests/test_ai_optional.py
@.importlinter
@src/newsletters/adapters/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the [excel] extra + the lazy _load_openpyxl() helper; extend the bare-install gate</name>
  <files>pyproject.toml, src/newsletters/adapters/_openpyxl_loader.py, tests/test_ai_optional.py</files>
  <action>
    SETUP (sanctioned, pre-approved — NOT a Package-Legitimacy checkpoint; openpyxl is spec-named
    ADAPT-03 and pre-approved in CONTEXT decision 1 + RESEARCH Package Legitimacy Audit): the
    executor installs openpyxl into the dev .venv so tests can run —
    `.venv/bin/pip install ".[excel]"` (or `.venv/bin/pip install openpyxl`). openpyxl is the ONLY
    new dependency this phase may add; if ANY other dependency appears needed, STOP and flag — do not
    add it.

    Add `excel = ["openpyxl"]` to `[project.optional-dependencies]` in `pyproject.toml` (alongside
    `ai`, `panel`, `dev`, `test`). Do NOT pin a version beyond the package name unless a pin is
    required (research confirmed 3.1.5; an unpinned name is consistent with the existing extras).

    Create `src/newsletters/adapters/_openpyxl_loader.py` with `_load_openpyxl()` that imports
    `openpyxl` INSIDE the function (mirror the `[ai]` lazy discipline and `ports._enforce`'s lazy
    import) and re-raises a teaching `ImportError` on absence: the message names the `[excel]` extra
    and `pip install '.[excel]'`, and states the deterministic spine runs without it. The module top
    MUST NOT import openpyxl — so `adapters/__init__.py` importing the adapters package (which Plan 03
    will make import the excel adapter to `register()` it) never requires the extra.

    Extend `tests/test_ai_optional.py` with: (a) a subprocess test that imports
    `newsletters.adapters` and asserts it succeeds even when openpyxl is forced unavailable
    (simulate absence by removing it from `sys.modules` and blocking the import, OR assert the
    module-top of the excel adapter / loader contains no top-level `import openpyxl` so a bare import
    cannot pull it); (b) a test that calling the loader (or `ExcelAdapter().parse(...)` once Plan 03
    lands — guard with an import-availability skip if the adapter does not yet exist) raises the
    teaching `ImportError` mentioning `[excel]` when openpyxl is unavailable. Keep `test_ai_optional`
    green WITH openpyxl installed too (openpyxl is not in AI_MODULES, so it must not trip the AI gate).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'openpyxl' in ' '.join(d['project']['optional-dependencies']['excel']); print('excel extra ok')"</automated>
    <automated>grep -v '^#' src/newsletters/adapters/_openpyxl_loader.py | grep -c "^import openpyxl\|^from openpyxl"</automated>
    <automated>.venv/bin/python -m pytest tests/test_ai_optional.py -q</automated>
    <automated>.venv/bin/lint-imports</automated>
  </verify>
  <done>
    `[excel]` extra declared; `_load_openpyxl()` imports lazily (zero top-level openpyxl imports in
    the loader, grep count 0); a clear teaching `ImportError` on absence; `test_ai_optional` green;
    lint-imports `Contracts: 1 kept, 0 broken`.
  </done>
</task>

<task type="auto">
  <name>Task 2: R4 Wave-0 probe — pin openpyxl chart/image attribute names (assumption A1)</name>
  <files>tests/test_openpyxl_probe.py</files>
  <action>
    Per CONTEXT R4 / RESEARCH assumption A1: confirm the attribute names openpyxl uses to surface
    parsed charts and images on a worksheet (research assumes `ws._charts` / `ws._images`) against
    the INSTALLED openpyxl, so Plan 03's silent-loss detection relies on a verified name, not an
    assumption. Author a tiny in-memory workbook with `_load_openpyxl()`: create a worksheet, add a
    chart (e.g. a `BarChart` with a small data ref) and an image if the installed version exposes one
    cheaply, save to a `BytesIO`, reload, and assert the worksheet exposes the charts/images via the
    expected attribute. The test PINS the actual attribute name: if `ws._charts`/`ws._images` are
    present, assert against them; if the installed version differs, the test FAILS loudly with the
    discovered attribute names so Plan 03 uses the documented fallback. Mark the test to skip cleanly
    if `[excel]` is not installed (so the bare-install gate is unaffected), but it MUST run and pass
    in the dev .venv where openpyxl is present.

    Do NOT extract chart/image CONTENT (that is a Deferred Idea — out of scope). The probe only
    confirms the DETECTION attribute so the adapter can DISCLOSE charts/images as `unextracted[]`.
    Record the confirmed attribute names in the 05-02-SUMMARY so Plan 03 cites them.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_openpyxl_probe.py -q</automated>
  </verify>
  <done>
    The probe runs against the installed openpyxl and pins the chart/image detection attribute names
    (passing with `_charts`/`_images`, or failing loudly with the real names for Plan 03 to consume);
    the names are recorded in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bare install (no extras) -> adapters package import | Importing `newsletters.adapters` must not require openpyxl; the spine must run AI-free and openpyxl-free |
| optional extra dependency surface | openpyxl is a third-party package added to the install surface (behind an extra) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of service (bare-install breakage) | adapters package top-level import pulling openpyxl | mitigate | Lazy `_load_openpyxl()`; no top-level openpyxl import; test_ai_optional proves the bare import works |
| T-05-05 | Elevation/contract drift | AI-optional / minimal-core invariant | mitigate | lint-imports `1 kept, 0 broken` (openpyxl is not AI but the contract must stay green); bare-install gate extended |
| T-05-SC | Tampering | openpyxl install | accept | openpyxl PRE-APPROVED (CONTEXT decision 1 + RESEARCH Package Legitimacy Audit: spec-named ADAPT-03, MIT, pure-Python, PyPI-verified 3.1.5). The seam's SUS verdict is a documented offline-probe false-positive; NO checkpoint required. openpyxl is the ONLY new dependency permitted this phase |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_ai_optional.py tests/test_openpyxl_probe.py -q` — green.
- `.venv/bin/lint-imports` — `Contracts: 1 kept, 0 broken`.
- `.venv/bin/python -m pytest -q` — full suite stays green.
- The loader module has zero top-level openpyxl imports (grep count 0).
</verification>

<success_criteria>
- `excel = ["openpyxl"]` declared as an optional extra; openpyxl is the ONLY new dependency.
- openpyxl is lazy-imported; a clear teaching `ImportError` guides the user to `pip install '.[excel]'`.
- The bare-install AI-isolation gate stays green WITHOUT the extra; lint-imports stays 1 kept / 0 broken.
- The R4 probe pins the chart/image detection attribute names against the installed openpyxl.
- No human checkpoints; no Package-Legitimacy checkpoint (openpyxl pre-approved).
</success_criteria>

<output>
Create `.planning/phases/05-excel-adapter/05-02-SUMMARY.md` when done — record the confirmed
chart/image attribute names (R4 probe result) and the exact teaching ImportError message, so Plan 03
consumes them.
</output>
